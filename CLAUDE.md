# TrafficCRM

Multi-tenant SaaS CRM for media buyers (traffic arbitrage), branded MWTRACK in the product UI. Spec docs (00-15) are complete; real implementation is underway following [15_PHASES.md](15_PHASES.md) phase by phase. As of 2026-07-02, Phase 1 is done including the first real production deploy (162.213.248.131, mw-track.com), Phase 2 steps 2.1-2.9 (2.7 Analytics/2.8 Lookalike Export confirmed done 2026-07-15, 2.9 = Team/roles Phase 1), 2.11 (audience overlap + Telegram client enrichment), and 2.12 (landing tracking links: pixel + Facebook/TikTok ad macros, customizable query-param names, ad/campaign breakdown) are done; Phase 3 steps 3.1 (Drip Campaigns/automation flows: trigger→delay→push→condition, linear step-list UI instead of drag-and-drop), 3.2 (A/B/n testing for landing pages: group N existing landings with per-landing traffic weights, card-click multi-select UI, no-cookie random split per request, comparison stats; extended 2026-07-17 so a domain/path binds to either one landing directly or the whole test group, not implicitly via landing membership), 3.4 (Smart Push Timing: CTR-by-hour analysis, first click-tracking in the project via a public push-button redirect endpoint) are done as of 2026-07-15, and 3.6 (Team Analytics: buyer attribution via a hidden query-param on the tracking link rather than landing ownership, "БЕЗ БАЕРА" bucket for unattributed clients, company-wide project comparison) done 2026-07-17 (3.3 paused, 3.5 cancelled, 3.7 obsolete per user scope decisions — Phase 3 otherwise complete); and Phase 4 step 4.1 (Telegram MTProto personal account, client dialogue tracking) shipped ahead of schedule on explicit user request; on 2026-07-22, bot scenarios were merged conceptually with 3.1's automation flows (shared step-chain idea — DELAY/SEND_MESSAGE/CONDITION, new `BotScenarioStep`/`BotScenarioRun` — but deliberately separate tables/engine, since scenario runs must be able to re-fire per client while automation enrollments are one-per-client-forever) and gained a step-chain editor + ≤10-item media gallery with video-note support at `/projects/[id]/scenarios`, while automation flows (3.1) were temporarily hidden from the UI (code/data untouched) per explicit user request; on 2026-07-25, the welcome message moved out of channel settings into scenarios entirely (new `SUBSCRIBE` trigger, replacing `Channel.tgWelcomeMessage*`/`sendWelcomeMessage`), the video-note pipeline was rewritten to always re-encode via ffmpeg's own runtime filter expressions instead of a skip-if-already-square fast path (the real cause of frequent kruzhok failures across formats/resolutions), and the scenario editor was redesigned around "elements" (one content type + optional button each, delay embedded per-element instead of a separate step type, condition/standalone-delay hidden from the UI for now); also on 2026-07-25, A/B testing shipped for bot scenarios (mirrors landing A/B groups — `ScenarioAbTestGroup`, weighted random variant pick per trigger firing with no client stickiness, 6 client-snapshot conversion metrics, synthetic `command` values to sidestep `BotScenario`'s unique constraint), including a same-day `endAbTest` lifecycle bug fix (member scenarios' `abTestGroupId` wasn't cleared, blocking a second test) plus a retroactive backfill for the one real production test already corrupted by it; and on 2026-07-27, per-event-type switches were added to gate whether tracking events get forwarded to Facebook/TikTok (`Project.disabledTrackingEvents`, gated in `TrackingService.recordEvent` alongside the existing ad-attribution check — the CRM's own `TrackingEvent` write is never affected), with a visible on/off indicator row on the project overview page and a new "События" settings tab; narrowed the same day to just the 3 event types the backend fires on its own initiative (Subscribe/Dialogue/Purchase — PageView/Lead/InitiateCheckout/Click are explicit SDK calls the tenant already controls in their own landing code, so a CRM switch for those is redundant), and made the switch gate only the automatic firing path via a new `RecordEventDto.forceSend` escape hatch — manual staff actions (the "Зарегистрировать диалог" button, the "Добавить покупку" form) keep forwarding to ad platforms even while the switch is off; and the same day, message personalization placeholders (`{first_name}`/`{last_name}`/`{full_name}`/`{username}`) shipped for both pushes and bot scenarios via one shared `renderMessagePlaceholders` helper, wired into `ChannelsService.sendMessage` (pushes + automations) and `BotScenarioEngineService.sendStepMessage` (scenarios, with its own Client lookup since scenarios must fire for cold contacts with no Client row); and on 2026-07-28, the Team/roles granular-permissions system (Phase 2) was redesigned from flat/global-per-user to per-project — a Buyer/Operator can now hold different `Permission` grants on different projects (`UserPermission` gained a `projectId` column, `@RequirePermission`/`PermissionsGuard` were retired in favor of a 5th `requiredPermissions?` param on `ProjectsService.assertAccess`, DB-backed for an immediate effect instead of waiting on JWT refresh), except `DOMAINS_*` which stays company-wide by explicit user decision (`Domain.projectId` doesn't meaningfully scope a domain to one project); a new `PROJECTS_EDIT` permission gates `PATCH /projects/:id` (archive/token-regeneration stayed `@Roles(OWNER, ADMIN)` instead of a permission, per the same decision) — see 15_PHASES.md checkboxes for current status. On 2026-07-30, after reaching full page-for-page parity, the "Studio" redesign (`apps/web/src/app/dashboard/studio/*`) was promoted to the primary UI for every role at `mw-track.com`'s own default paths (`/`, `/projects`, etc.) — the old UI (still fully intact, unchanged) was split off onto a separate subdomain, `old.mw-track.com`, reachable via a "Старый дизайн" link in Studio's sidebar. Both domains are served by the same Next.js process; the split happens via `apps/web/src/middleware.ts`, which reads the `Host` header and, on the main domain, transparently rewrites every path (except the shared `/login`/`/register`/`/invite` pages) to Studio's physical files under `dashboard/studio/*` — the browser's address bar stays clean since it's a server-side rewrite, not a redirect. Studio's own internal links are root-relative (`/projects/:id`, not `/dashboard/studio/projects/:id`) for exactly this reason. Also on 2026-07-30: push limits were converted from monthly to daily (`Company.maxPushesPerDay`/`pushesToday`, rolling-window reset), then the plan ladder's `maxProjects` was revised (TRIAL 1/STARTER 5/GROWTH 10/SCALE 20/ENTERPRISE 50, the last no longer an unbounded sentinel) with push limits recomputed via a per-project rate except ENTERPRISE (kept flat); NOWPayments was connected as a second `PaymentGatewayProvider` alongside the still-inactive Heleket, using `/v1/payment` (direct address) instead of a hosted checkout so the existing `PaymentModal` needed no changes; and a new project-scoped `OPERATOR_ADMIN` role was added — it manages only `OPERATOR`-role team members within its own `ProjectAccess` projects (not company-wide), enforced via a new `assertCanAccessTeamManagement`/`assertOperatorAdminScope` pair in `team-role.util.ts` rather than the `@Roles()` decorator (whose rank-threshold model would have silently admitted BUYER/OPERATOR too) — alongside this, `OPERATOR`'s own UI was hard-restricted to two pages (`/my-clients`, `/my-stats`, the latter backed by a new `Purchase.registeredBy`-based endpoint) and the client detail card now hides ad-attribution fields from `OPERATOR` via a server-computed `canViewTrafficSource` flag; and the same day, the project client list gained an "also in other projects" badge (`tgUserId` match, same identity concept as the existing audience-overlap feature) gated by a new `Permission.CLIENTS_VIEW_CROSS_PROJECT` — granted on the viewer's OWN project, not the other one it reveals — with elevated roles or grant-holders seeing full detail (which project, joined-when, dialogue-when) and everyone else seeing only the bare fact plus a dialogue date, no project name; and the same day, a full re-audit of the standalone `apps/admin` platform panel found and fixed two real gaps left over from Phase 4.3D's explicitly-deferred scope — automation flows/bot scenarios now check `checkSubscriptionLimit` before sending (a suspended/blocked company could previously still have automated messages go out) — plus added invoice visibility per company, pagination/company-filter on the Errors/Actions/Companies list pages (Errors had been silently truncating to 50 of 135 real rows), and a `COMPANY_VIEWED` audit-log entry so cross-tenant reads (not just mutations) leave a trace; the SUPER_ADMIN password was also rotated; a same-day regression this surfaced (`TEAM_MANAGER_ROLES`, part of the `OPERATOR_ADMIN` work above, had forgotten `SUPER_ADMIN`, breaking the admin panel's "Команда" tab) was found and fixed. Also on 2026-07-30, `apps/admin` moved off the path-based `mw-track.com/adminmw` onto its own subdomain, `admin.mw-track.com` (DNS was already pointed there) — `next.config.mjs`'s `basePath` was dropped, the existing multi-SAN `mw-track.com` certificate was expanded via `certbot --nginx --expand` to cover it, and the old `/adminmw` path now 301-redirects to the new domain rather than being removed outright. On 2026-07-31, a full how-to-use-every-feature documentation page (8 sections, ported from an earlier standalone Claude Artifact) shipped as a real in-app page at `/docs` in both trees, linked from the sidebar — a single shared `DocumentationContent` component (`apps/web/src/components/docs/`) used identically by both page wrappers, with 12 real product screenshots as static files under `apps/web/public/docs/*.png` (the app's first use of `public/`-hosted static assets); this surfaced a real middleware bug — the Studio-domain rewrite in `apps/web/src/middleware.ts` was catching every path including static files, resolving `/docs/overview.png` to a nonexistent `/dashboard/studio/docs/overview.png` — fixed by excluding paths with a file extension from the rewrite. Also on 2026-07-31, Operator project access was decoupled from participant creation (the user found the old create-time project+permission form inconvenient since operator rosters on a project change often): creating/inviting an Operator no longer takes `projectIds` at all (the sole exception to the "Buyer/OPERATOR_ADMIN must pick a project immediately" rule) and starts with zero `ProjectAccess`; access is granted afterward either from the project side (new "Операторы" tab in project settings, `GET/POST/DELETE /projects/:id/operators` + a company-wide `.../candidates` list so a brand-new zero-project operator is discoverable — confirmed via `AskUserQuestion` that Operator-admins see all company operators here, not just ones already overlapping their own projects) or from the team side (new "Операторы" tab on `/team`, splitting Operator rows out of the main roster, with a per-operator "assign projects" dialog that reuses the existing `PATCH /team/:userId` with only `projectIds` — zero backend changes needed for that direction). Operator permissions are no longer manually configurable at all — always the fixed `DEFAULT_ROLE_PERMISSIONS.OPERATOR`, applied via the pre-existing `seedDefaultsIfEmpty`. Also fixed a real bug found in the same request: the project-picker `<SelectValue>` on `/my-clients`/`/my-stats` had no children render function, so Base UI displayed the raw project id instead of its name (the same recurring pattern documented earlier in this file); both pages also gained an always-visible "available projects" chip row. On 2026-07-31, both sidebars (`Sidebar`/`StudioSidebar`) gained a collapse-to-icons toggle (new shared `useUiStore`, zustand+persist) with `Tooltip`-on-hover nav links in collapsed state. On 2026-08-02, two archived-project bugs were fixed: `ProjectsService.archive()` now also deletes `ProjectAccess`/`UserPermission` for every user on the archived project (previously these dangled and blocked all future `/team` edits, since the tenant-scoping middleware only forces `deletedAt: null` on top-level `Project` queries, not nested includes); and `ProjectsService.assertAccess` gained an `allowArchived` option (via `$queryRaw`, the only way to bypass that same middleware) so landing view/edit/delete keeps working against an archived project's landings instead of 404ing. On 2026-08-03, landings and tracking pixels both gained creator tracking (`createdById`, backfilled to the company's Owner for pre-existing rows, shown in the UI as "Создал: ...") following the `Purchase.registeredBy` pattern; and Buyers/Operator-admins gained a new per-user `User.landingsVisibilityScope` setting (`OWN_LANDINGS`/`PROJECT_LANDINGS`/`ALL_LANDINGS`, default `PROJECT_LANDINGS` = prior behavior) governing what the company-wide `/landings` page shows them, configurable from all 6 team create/invite/edit pages in both trees — a user-reported bug that Buyers could see every company landing regardless of project access could not be reproduced on clean or real production data during investigation. The same day, a follow-up request fixed the landing detail page missing its `createdBy` display (a separate Prisma query path, `LandingsService.findOne`, hadn't been given the relation) and three grey-in-Studio-dark-theme cards (`LandingContentCard`, `StatsCard`/`DualStatsCard`, `LandingCard` — all had hardcoded shadcn `Card`, fixed with the same optional `containerClassName` pattern each time), plus a 7th recurrence of the `<SelectValue>`-without-children id-display bug (`GetLinkDialog`'s pixel picker). Also on 2026-08-03, the client filters gained real multi-select for "Баер"/"Пиксель" (new `components/ui/multi-select.tsx` on top of `DropdownMenu`/`DropdownMenuCheckboxItem`), Buyers gained a per-user `User.clientsVisibilityScope` toggle (`ALL_CLIENTS` default / `OWN_CLIENTS`) that scopes the client list and every project-page stats endpoint (`getProjectStats`, `getConversionFunnel`, `getAdBreakdown`, `getLeaderboards`) down to only their own attributed clients — this required adding a real `TrackingEvent.buyerId` column and wiring the SDK's already-flowing `buyerRef` link parameter into `TrackingService.resolveAttribution`, since PageView/Lead events happen before a `Client` row exists and previously carried no buyer attribution at all (not an architectural limit, just unwired plumbing, per user pushback that correctly identified this); and the team edit page gained full editing of a member's email/name/password (`UpdateTeamMemberDto` extended, one-time password reveal on change, same UX as the create-member flow). On 2026-08-04, three more fixes shipped: the project page's ad breakdown now groups by campaign only (`ProjectsService.getAdBreakdown` dropped its `adId` dimension entirely — one row per campaign, not per campaign+ad pair); the scenario-album and push-media uploaders (`components/scenarios/element-media-editor.tsx`, `components/pushes/push-content-step.tsx`, both shared across trees) gained real multi-file selection (`<input multiple>`, sequential upload to respect the existing 10-item cap and avoid a stale-closure item-loss bug that concurrent uploads would have caused); and Studio's `/my-clients` page gained the same lead search (name/username/user_id/etc.) that the classic tree's version already had but Studio's had been missing entirely; the same day, a layout bug reported on the Operator account's `/my-clients` (the search input visually floating toward the center instead of sitting next to the project dropdown) was root-caused to `flex justify-between` distributing gaps evenly across 3 direct children rather than hugging the two right-side controls together — fixed in both trees by wrapping the search input and project dropdown in one shared flex sub-container, the same fix later confirmed to be needed nowhere else once checked. Also on 2026-08-04, the pushes/broadcasts frontend was redesigned end-to-end: the 3-step creation wizard (Контент → Аудитория → Подтверждение, with a "Далее" click and an intermediate draft-`Push`-row PATCH cycle between each) was replaced by a single-page `PushComposer` (`apps/web/src/components/pushes/push-composer.tsx` + Studio's `apps/web/src/app/dashboard/studio/push-composer.tsx`) reusing the existing `PushContentStep`/`PushAudienceStep`/`ScheduleCalendar` sub-components unchanged — no draft `Push` row is created until the final submit, audience preview instead hits a new stateless `POST /pushes/preview-audience`; the sidebar's "Календарь рассылок" entry was renamed to "Рассылки" with a `Send` icon (was `CalendarDays`) at the same `/pushes-calendar` URL, which now shows the existing calendar unchanged plus a new detailed table of every company push below it (`GET /pushes`, company-wide, same `getAccessibleProjectIds` visibility as the pre-existing calendar endpoints) and a blue "Создать рассылку" button; and — since `Push.projectId` is a hard single-value schema column with no precedent for multi-project fan-out anywhere in the codebase — a genuinely new capability shipped: the composer can select and send to several projects at once, via a new `POST /pushes` (`CreateMultiPushDto{projectIds, sendNow, ...CreatePushDto}` → `PushesService.createForProjects`, a plain loop calling the existing single-project `create()`/`send()` per id, not a transaction, since `send()`'s BullMQ side effects can't be rolled back — a partial failure, e.g. hitting the daily push limit partway through, leaves earlier projects' pushes created/sent and reports the rest with a `sendError` per project rather than failing silently or all-or-nothing). The project-scoped `/projects/[id]/pushes/new` route and the new company-wide `/pushes/new` route both render the same composer, differing only in `initialProjectIds` (pre-checked vs. empty) — satisfying the request that arriving from a project's own pushes page preselects that project without limiting the picker to it alone. A same-day follow-up request refined both halves further: the "Рассылки" list below the calendar now defaults to pending pushes only (`GET /pushes?scope=pending`, `PushStatus` DRAFT/SCHEDULED) with a second "Уже были" tab (`scope=history` — SENDING/SENT/FAILED) that only fires its query when clicked, so a visit to the page never pulls full send history up front; and the composer's project picker was pulled out of its own `Card`/`STUDIO_CARD` wrapper into a plain vertical list (checkbox + `ChannelAvatar` + name + a live "Доступно: N" reachable-audience count per row), while Контент/Аудитория/Отправка collapsed from three separate cards into one — the per-project audience preview call (`POST /pushes/preview-audience`) now always covers every visible project, not just the checked ones, so the picker can show every row's number before it's selected; the Audience section's own total is derived client-side by summing that same response over just the checked ids, and its old per-project breakdown list was removed since the picker already shows it. A same-day bug-fix round covered 4 more reports: the `TelegramMessagePreview` chat-bubble mockup (shared by pushes/scenarios/welcome-message) was hardcoded light-only (`bg-[#e7f3ff]`/`bg-white`, no `dark:` variants) and looked like a glaring bright box in Studio's dark theme — given real Telegram dark-mode colors (`#0e1621` chat bg, `#182533` bubble) rather than just Studio's own palette, since the component's whole purpose is a faithful "how it'll actually render in Telegram" preview; `PushAudienceStep`'s summary card (По фильтру/Доступны/Недоступны) gained the established `containerClassName` override prop (same pattern as `StatsCard`/`LandingContentCard`/`LandingCard`) since a bare shadcn `Card` inside Studio's dark theme paints from the placeholder `--card` CSS var, not Studio's real `#171F2B` — Studio's composer now passes `STUDIO_CARD`; the "Отправка" section's mode toggle (two tab buttons that revealed/hid the calendar) was replaced with an always-visible calendar plus a small "Отправить сейчас" checkbox beside it, since the reveal/hide was pushing the submit button down every time schedule mode was picked; and a real backend bug was found in `ClientsService.buildPushFilterWhere` — the "principal audience" condition (`subscribedAt`/`botActivatedAt` not null, i.e. excluding cold external contacts who never subscribed or messaged the bot) had been bundled inside the same `if (reachableOnly !== false)` branch as the `isBotActive` check, so `countAudienceTotal` ("По фильтру", called with `reachableOnly:false`) skipped it entirely and counted every `Client` row for the project including pure-external ones — the two conditions are now independent, with the origin/audience-membership check applied unconditionally and `isBotActive` staying gated to the reachable-only count. The same day, the composer got a full layout redesign per explicit user sketch: the top half is the content editor (`PushContentStep`'s own internal grid already put fields left / `TelegramMessagePreview` right, so no new layout code was needed there, just giving it the full-width top row to itself); the bottom half splits into two cards — left holds a compact scrollable "Проекты" list (`max-h-64 overflow-y-auto`, same avatar+checkbox+"Доступно: N" rows as before) plus a "Расписание" card, right holds "Фильтры получателей" (`PushAudienceStep`); the submit button sits alone beneath both, full page width. Since content editing is now the *first* thing on the page — above project selection, not gated behind it — a project auto-selects on load whenever `initialProjectIds` was empty (`GET /pushes/new` company-wide entry), so `PushContentStep`'s media-upload auth always has a valid `projectId` from the first render instead of starting in a broken disabled state. A genuinely new capability shipped alongside this: an explicit send-timezone picker in the schedule card (defaults to the browser's own IANA zone via `Intl.DateTimeFormat().resolvedOptions().timeZone` — "чей часовой пояс сидит в CRM", not `Project.timezone`, a different concept), reusing the existing `TimezoneInput` component (`components/timezone-input.tsx`, previously project-settings-only) after giving it optional `label`/`hint` overrides; converting the picked wall-clock date+time in that zone to the correct UTC instant needed a small new dependency-free helper (`lib/timezone.ts`'s `zonedTimeToUtcIso`, the standard `Intl.DateTimeFormat`-diffing trick — no date-fns-tz/luxon added) since the old code just fed the raw string into `new Date()` and implicitly relied on the browser's own local zone. The redesign surfaced one real regression caught before shipping: `PushAudienceStep`'s internal `lg:grid-cols-2` (fields | totals side-by-side) was sized for the old near-full-width placement — once nested in the new half-width "Фильтры получателей" card, the viewport-based `lg:` breakpoint still fired (it doesn't know its parent shrank) and the two columns overflowed the card; fixed by dropping the internal grid to always-stacked, which is also just the better layout at that width. On 2026-08-05, media attachment was redesigned too: the old "pick a type from a dropdown, then upload" flow (`PushContentStep`) became one drag-and-drop-or-click container that classifies each file by its own MIME type (`image/*`→photo, `video/*`→video, `audio/*`→voice — auto-detected, no more manual type-select) — multiple photos/videos become an album automatically, same as before. Video note ("kruzhok") is no longer chosen before upload; instead, once a video is the sole item in the album, a checkbox appears right on its thumbnail to convert it after the fact via a new `POST .../pushes/media/:key/to-video-note` endpoint (`PushesService.convertToVideoNote` streams the already-uploaded object back out of MinIO, runs the same `ensureSquareVideoNote` crop used at upload time, stores the result under a fresh key) — both the original and converted URLs are cached client-side (`PushContentStep`'s local `videoNoteCache`) so toggling back and forth doesn't re-hit the network or re-crop after the first conversion. Voice is a genuinely new push capability, not previously wired end-to-end: `ChannelProvider.sendMessage`/`TelegramProvider` already supported a `'voice'` mediaType (used by bot scenarios), but `PushMediaDto.type` only allowed `photo`/`video`/`video_note` — extended to include `voice`, with `PushesService.assertValidMedia` treating it as the same kind of "solo, no-album" type as video_note (Bot API's `sendMediaGroup` supports neither). The upload-time `key` needed to support the after-the-fact conversion doesn't belong in the wire payload (`PushMediaDto` only knows `type`/`url`, and the global `ValidationPipe`'s `forbidNonWhitelisted` would 400 the whole request if it leaked through) — both composers now `.map(({url,type}) => ...)` before submit to strip it. On 2026-08-05, 3 more composer bugs were fixed: the "Расписание" calendar disappeared the moment a 2nd project got checked (was gated on `selectedIds.length === 1`; now shows the first checked project's calendar regardless of total count, since it was always inherently single-project anyway); the calendar itself stayed stuck at a fixed compact width even though its card had grown much wider in the new layout (`CalendarGrid`'s `compact` mode switched from a fixed `w-[19rem]` to `w-full`, and `ScheduleCalendar` switched from a side-by-side flex row with `DayScheduleList` to stacking them vertically, since side-by-side made a full-width calendar pointless); and a one-frame "Нет проектов с правом на создание рассылок" flash on every page load (the empty-state check ran against `projects` before the `GET /projects` query had actually resolved — fixed by also gating on the query's own `isLoading`). On 2026-08-04, a genuinely new parallel feature shipped: broadcasts sent through a project's personal MTProto account (not the bot) rather than another `Push` mode — audience/send-mechanism/content/pacing were different enough to warrant separate tables/engine, the same call already made for scenarios-vs-automations. New `PersonalBroadcast`/`PersonalBroadcastLog` (not in `modelsWithCompany`, manual `projectId` scoping like `Push`) and `Client.lastPurchaseAt` (deposit recency — `Purchase` has no dedicated "deposit" entity, denormalized in `PurchasesService.create` next to the existing `totalSpent`/`purchasesCount`); audience always starts from `dialogueSource: 'PERSONAL_ACCOUNT'` (the one unambiguous "real dialogue with this specific account" signal, set only by `TelegramPersonalService.handleIncomingMessage`), plus a live `client.getInputEntity(tgUserId)` recheck immediately before each individual send. New `TelegramPersonalService.sendDirectMessage`/`getDialogFilters`/`getFolderTgUserIds` follow the pre-existing `sendStory` "never throw, return `{success,error}`" pattern; Telegram-folder filtering only resolves explicit `includePeers` membership (GramJS has no API to enumerate who matches a rule-based folder), a disclosed v1 scope cut. Because one MTProto socket per channel can't take concurrent `invoke()`s (the same constraint `StoriesCron` already documents), a whole broadcast runs as ONE long-lived BullMQ job looping with a random `delayMinSeconds`–`delayMaxSeconds` sleep between sends (anti-ban pacing) rather than one job per recipient; `FloodWaitError`/`SlowModeWaitError` put the recipient back to `pending` and sleep the server-requested duration instead of counting it as a failure. Content "variants" (deliberately manual now, AI-generated later) are assigned round-robin, not randomly, for even/debuggable distribution. New `Permission.PERSONAL_BROADCASTS_VIEW/CREATE/SEND/DELETE` is explicit-grant-only (no default role gets it, like `CLIENTS_VIEW_CROSS_PROJECT`), and surfaces as a project-page button (not a sidebar item, unlike Push) since — unlike bot pushes — the feature has no company-wide/multi-project concept: each project's personal account is its own persona. Live verification used an isolated throwaway test company/project/clients (never real connected production accounts, same restriction the user set for the Stories module) driven directly through `PersonalBroadcastsService` — all 10 audience-filter-dimension checks and round-robin variant assignment came back correct, and it surfaced one real bug fixed the same day: `getDialogFilters`/`getFolderTgUserIds` weren't wrapped in try/catch, so a stale/unreachable MTProto session threw uncaught and 500'd the whole audience preview instead of degrading to an empty folder list like every other method in that service already does. Also on 2026-08-04, a public marketing homepage shipped for logged-out visitors: previously an unauthenticated hit on `/` (either domain) showed a loading skeleton and immediately redirected to `/login`, with no product-description entry point at all. New shared `MarketingHome` component (`apps/web/src/components/marketing/marketing-home.tsx`, Studio-styled with full dark-mode support) is wired into both root-level auth gates — `apps/web/src/app/dashboard/layout.tsx` (Studio/mw-track.com) and `apps/web/src/app/(dashboard)/layout.tsx` (classic/old.mw-track.com) — purely additively: the already-authenticated branch is untouched, a new branch only fires for `!accessToken && usePathname() === '/'` (confirmed via the same reasoning `StudioSidebar` already relies on for its cross-domain `classicHref` — `usePathname()` reflects the clean public URL, not the Studio-rewrite destination), every other unauthenticated route still redirects to `/login` as before. Verified live by driving a locally-cached headless Chromium against the deployed build with the `Host` header swapped between both domains — renders identically on both, no console errors, "Войти" correctly navigates to `/login`, and desktop/mobile/dark-theme screenshots all confirmed clean layout. The same day, a follow-up request pointed out that the personal-broadcast composer's media upload ("не такая как в обычной рассылке") still used the pre-redesign button-plus-file-dialog interaction; `PersonalBroadcastVariantEditor` (`apps/web/src/components/personal-broadcasts/variant-editor.tsx`) was brought in line with `PushContentStep`'s 2026-08-05 drag-and-drop redesign — same dropzone visuals/interaction, MIME-based auto-classification — while deliberately keeping the pre-existing single-file (not album) constraint, since that's an intentional property of "variants" as the content-diversity mechanism, unrelated to the upload-UX complaint. Verified live end-to-end: an isolated test company/user/project/channel, a real login through the actual `/login` form via headless Chromium, then a real file upload through the composer — thumbnail and remove control rendered correctly, no console errors, test data deleted by exact id afterward. The same day, the user called the marketing page's first version "слишком нейроночно" (too AI-generated-looking) — the sparkle-badge-over-hero + symmetric 3×2 icon-card grid was indeed the generic-SaaS-landing template. Redesigned around real product screenshots instead of abstract icons: cropped (via `sharp`, no Pillow/ImageMagick available in this environment) the same PNGs already used by `/docs` (`apps/web/public/docs/*.png`) down to `public/marketing/*.png` — trimmed empty whitespace and the test project's name out of frame — and wrapped them in a small `BrowserFrame` component (traffic-light dots + mono address bar) instead of bare `<img>`s. Structure changed from a symmetric feature grid to alternating text/screenshot sections (clients list → client detail with real UTM/fbclid attribution → team/buyer-revenue stats), a hero with a real funnel screenshot given a slight `rotate` and a soft color blob behind it rather than a perfectly rectangular layout, and every other feature folded into one compact tag row instead of forcing icon-cards onto features with no good screenshot. Deliberately did NOT use the `billing.png` screenshot for pricing — its numbers could be stale, and presenting them as current on a public page would risk being misleading. Verified live the same way as the initial ship (headless Chromium against the deployed build, `Host` header swapped, desktop/mobile/dark screenshots) — no console errors, real screenshots render crisply in both themes. On 2026-08-05, a follow-up request covered `/login`/`/register`/`/invite`: "убери эту карточку для формы, добавь что-то красивое в наших цветах, форму можно в бок для красоты и антисимметрии" — the shared `(auth)/layout.tsx` (previously one centered white card on a grey background, identical across all three pages) was rebuilt around a brand panel (`AuthBrandPanel`, blue gradient + the same funnel screenshot crop as the homepage, tilted and bleeding off its edge) occupying ~44% width on `lg+`, with the form itself rendered card-less directly on the page background. The panel's side alternates by `usePathname()` — left on `/login`, right on `/register`/`/invite` — that alternation between two related pages is the "antisymmetry" asked for. One real bug caught before shipping: the first version absolutely-positioned the screenshot inside a `justify-between` flex column, which made it overlap the tagline text at ~900px viewport height; fixed by moving it into normal flex flow (logo → centered text block → image with negative-margin bleed) instead of `absolute`. Verified live the same way as the homepage. Also worth noting: mid-session, the Read/Edit tools hit a transient host-side hook timeout while Bash kept working — the remaining doc edits for this round were applied via a Python script through Bash instead of the Edit tool. Separately on 2026-08-05, a user-reported revenue/leaderboard mismatch on the project page (main revenue card correct, but a buyer's expanded leaderboard row showing a wildly different, larger number than the summary line right next to it) turned out to be two real backend bugs, not a caching artifact: `ProjectsService.getBuyersFunnel` (the funnel that expands under a buyer's leaderboard row) filtered its raw SQL only by `buyerId`, with no `projectId` at all — a buyer working across multiple company projects got their revenue/purchase-count silently summed across ALL of those projects on every single project's page (confirmed: $701/11 purchases was exactly their total across two projects, $572+$129 and 10+1, while the real per-project number was $129/1); `getEventColumnFunnel` (shared by the pixels/campaigns leaderboard categories) had the identical gap for `campaignId` (an external ad-platform ID not guaranteed unique across a company's projects) — fixed both by threading `projectId` through and filtering on it, even though only the buyers path had a confirmed live example. Live-verified with an isolated two-project/one-shared-buyer test company (reproduced the exact cross-project leak pre-fix, confirmed it gone post-fix), then re-confirmed against the real Igor Conel/Diego Herrera case from the bug report. The same day, a follow-up request ("в главной статистике не учитывай покупки внешних клиентах") found the main project stats (`ClientsRepository.getProjectStats`'s revenue/FD/RD cards and charts, plus `getConversionFunnel`'s deposit stages) counted every `Purchase` by `projectId` alone, with no check that the purchasing `Client` was actually "ours" (`subscribedAt IS NOT NULL` — the same `OURS_ONLY` criterion every other client metric in that file already used) rather than an external/cold contact who never came through the funnel. The existing buyer-scoping subquery only applied that check indirectly and only when a specific buyer was selected; made "is this client ours" an unconditional part of the same subquery instead of something that only ever showed up alongside an optional `buyerId`. `ClientsRepository.getMyStats` (Operator's personal stats, scoped by `Purchase.registeredBy` rather than client identity) was deliberately left alone as out of scope. Verified live with an isolated test project (one real client with a $100 purchase, one external contact with a $50 purchase) called directly through the service, confirming the external purchase no longer appears in any of the affected fields. Later the same day, investigating a real user-reported bug — a bot's join requests were being approved (people got into the channel fine) but nothing registered in the CRM and no welcome message went out — traced to Telegram silently withholding webhook delivery for that one specific bot for ~20 hours (confirmed via nginx access logs: this bot's webhook path went completely quiet while every other connected bot on the same server kept receiving updates normally the whole time; likely Telegram-side anti-spam throttling, given the bot's welcome message was aggressive/ALL-CAPS and had a high block-rate). Since there was no monitoring for "webhook went silent," this went unnoticed until traffic/revenue had already been lost. Built a permanent safeguard: new `Channel.lastWebhookAt` column, stamped in `TelegramProvider.handleWebhook` (the one common entry point before grammY dispatches by update type) on every single incoming webhook regardless of type, fire-and-forget so it never adds latency to the Telegram-facing response. "Is this webhook stale" is computed lazily on read in `ProjectsService.sanitizeChannel` (same pattern as the existing `tgPersonalConnected` computed field, not a stored/cron-maintained boolean like `isActive`) — `lastWebhookAt` set and older than a deliberately generous 6-hour threshold, chosen because real per-channel webhook volume in this system spans two orders of magnitude (~15 to ~800+/day) and a tighter threshold would false-positive on quiet channels. Surfaced as a new "Нет вебхуков" badge alongside the existing bot-blocked indicator, in all 4 places that indicator already appears (project card + project page, both classic and Studio trees) — two independent signals, since a bot can be fully unblocked and healthy while Telegram simply isn't sending it anything. Confirmed via `AskUserQuestion` with the user beforehand: deliberately did NOT attempt retroactively reconciling the channel's ~12,600 real members against the ~296 CRM-registered ones to backfill/welcome-message the gap — cold-messaging thousands of people who never contacted the bot is a strong ban-risk that outweighs recovering the lost registrations; and the indicator itself is a CRM-only badge, not a separate alerting/admin-panel system. Verified live: an isolated test project with `lastWebhookAt` set 10 hours in the past rendered the badge correctly in all 4 spots (real login, both domains, headless Chromium, no console errors), and separately confirmed in production that `lastWebhookAt` is now actually being stamped in real time off real incoming Telegram webhooks. The very next day, that same monitoring effort led straight into a second, more concrete real incident for the same bot: a customer reported a client going through the correct tracked landing link, getting auto-accepted into the channel, but never registering or receiving a welcome message — this time the webhook wasn't silent at all (traffic was flowing), which pointed away from the previous root cause. Investigation found the same bot token (`@Diego_Herrera_Bot`) had been connected to three different projects over time (two now archived, one active) — Telegram only allows a bot ONE registered webhook URL at a time, and `getWebhookInfo` confirmed it was currently pointed at an **archived** project's channel, not the active one, because whichever channel's `initialize()` last called `setWebhook()` wins regardless of how many `Channel` rows in our own DB reference that same token. Real traffic was landing on the archived channel, which happened to be in `BOT_DIRECT` mode rather than `PRIVATE_CHANNEL_REQUEST` — so the guard that normally prevents `handleChatMemberUpdate` from creating a client on an untracked join didn't apply, and 80 real clients had silently accumulated in the archived, invisible project over ~40 minutes. Fixed immediately (via `NestFactory.createApplicationContext`, bypassing HTTP): re-ran `ChannelsService.reactivate()` on the correct channel to reclaim the webhook, deactivated the archived channel so it can't steal it back, and migrated all 80 misrouted clients into the active project — merging into an existing record (adopting `subscribedAt`/`isSubscribed`, filling missing name fields, soft-deleting the duplicate) for the 33 people who already existed there via the personal-account dialogue path, plain `projectId` reassignment for the other 47. Deliberately did not retroactively fire the welcome scenario for the moved clients as part of this — that's a separate, not-yet-made decision. Then built the actual prevention the user asked for: `ProjectsService.archive()` now cascades to the project's channel — `TelegramPersonalService.disconnect()` (logs out the MTProto session, clears the session columns) if a personal account was connected, then `ChannelsService.deactivate()` (calls `deleteWebhook()`, flips `isActive` false) — the exact same methods already used for manual disconnection from settings, just now automatic on archive, wrapped in try/catch so a Telegram-side failure can't block the archive itself. Required adding `TelegramPersonalService` to `ProjectsService`'s constructor; no new circular-dependency risk since `ChannelsModule` already flows into `ProjectsModule` via the existing `forwardRef` and already exports that service. Verified via an isolated fake-session test project (confirmed the cascade correctly clears `isActive`/session fields) and confirmed in production that the webhook stayed correctly pointed at the active channel even through a subsequent API restart (which re-initializes every `isActive` channel on boot). Later the same day, a different project ("Isabella Ramirez") surfaced a related-but-distinct revenue-mismatch report: the main stats card showed $99 while the "ТОП" leaderboard drill-downs summed to only $94 — same symptom family as the cross-project buyer-funnel leak fixed earlier that day, but this time the missing filter was `deletedAt`, not `projectId`. `getProjectStats`'s two `purchase.aggregate()` calls and its `purchaseBuyerFilterSql`, `getConversionFunnel`'s own separate `purchaseBuyerFilterSql`, and `getLandingsFunnel`/`getEventColumnFunnel`'s raw-SQL purchase queries all checked `Client.subscribedAt IS NOT NULL` but not `deletedAt IS NULL` — so a soft-deleted client (e.g. a duplicate merged away during that same day's Diego Herrera migration) still had its historical purchases counted in some stats but not others, while `getLeaderboards` (top-level) already filtered `deletedAt` correctly — hence the two numbers disagreeing instead of both being wrong. `getBuyersFunnel`'s purchase query got the same fix, on top of the `projectId` fix it had already received earlier that day for the unrelated cross-project leak. Separately confirmed the user's other complaint on the same project (buyer-leaderboard client/dialogue counts not summing to the page total) is not a bug: buyer rows only include clients with `buyerId IS NOT NULL`, so unattributed clients are correctly excluded — the landings breakdown, which has no such gate, summed to the total exactly. Verified live by re-running the real service methods against the actual project before/after the fix: `totalRevenue` dropped from 99 to 94, the affected buyer's funnel revenue from 15 to 10, the affected landing's funnel revenue from 99 to 94 — all now match `getLeaderboards` exactly. Later the same day, three requested pushes/broadcasts features shipped: editing an existing DRAFT/SCHEDULED push, duplicating any push, and inline cancel/edit/duplicate actions directly on the company-wide "Рассылки" (`/pushes-calendar`) list rather than only on a project's own pushes page. The backend edit (`PATCH .../pushes/:id`) and cancel (`DELETE .../pushes/:id`, sets `FAILED` — `PushStatus` has no separate `CANCELLED` value) endpoints already existed from the original build but had no UI wired to them at all; only `PushesService.duplicate` (clones content/filter into a fresh `DRAFT` via the existing `create()`, deliberately dropping `scheduledAt`/status/counters — a copy is a new send, not a resumed one) and its `POST .../pushes/:id/duplicate` route were new. `PushComposer`/`StudioPushComposer` gained optional `pushId`/`editProjectId` props that switch them into edit mode: content/filter/schedule prefill from `GET .../pushes/:id` (a new `lib/timezone.ts` `utcIsoToZonedParts` reverses the existing `zonedTimeToUtcIso`, since `Push.scheduledAt` only stores the UTC instant — the zone it was picked in is never persisted, so editing always redisplays in the browser's current zone), the project picker becomes a fixed read-only row (`Push.projectId` is single-value, multi-project doesn't apply to edit), and submit calls `PATCH` instead of `POST /pushes`. The "Отправить сейчас" checkbox is reused for edit mode but re-labeled "Без расписания" and re-wired to mean `scheduledAt: null` (revert to `DRAFT`) rather than an immediate send, since `PATCH` never sends. New edit routes `.../pushes/:id/edit` (project-scoped, both trees) render the same composer with those props; the company-wide list's rows already carried `push.project.id` from `findAllForCompany`, so no new company-wide backend route was needed — "Изменить"/"Копировать"/"Отменить" there call the same project-scoped endpoints directly. Live-verified via an isolated throwaway company/project (no real channel) driven straight through `PushesService`: create→DRAFT, edit with a date→`SCHEDULED`, edit with `scheduledAt:null`→back to `DRAFT`, duplicate→new independent `DRAFT` named "(копия)", and confirmed editing an already-cancelled (`FAILED`) push is correctly rejected by the pre-existing `assertEditable` guard — all 5 checks passed, test data deleted by exact id afterward. On 2026-08-06, the home page (both trees) gained real company-wide statistics — revenue, clients, FD/RD — plus a today/yesterday/7d/30d/custom period selector, neither of which existed there before (previously just 4 all-time counts reduced client-side from `GET /projects`). New `ProjectsService.getCompanyStats`/`GET /projects/company-stats` lives in `ProjectsService` itself rather than the more natural-looking `ClientsRepository`, specifically to dodge a circular import (`ClientsModule` already imports `ProjectsModule`); resolves period boundaries once against `'UTC'` (via the existing `resolveStatsPeriod`) rather than per-project-timezone-correct like the single-project `getProjectStats` does, since 7d/30d don't care about timezone at all and today/yesterday/custom's skew is an accepted tradeoff for a summary widget — documented inline as a deliberate simplification, not an oversight. Revenue is scoped to a narrower project set (`STATS_VIEW_REVENUE`) than client counts (`STATS_VIEW`), mirroring the existing per-project "zero money fields, keep counts" convention from `ClientsController.stats`, via the projectId-less `hasAnyPermission`/`getAccessibleProjectIds(..., permission)` pattern already established for company-wide checks. Frontend ported the classic project page's URL-persisted-period pattern to both home pages for the first time (Studio's own project page still doesn't have it) — classic reuses the shared `PeriodSelector`/`StatsCard`/`DualStatsCard`, Studio hand-rolls the same pill UI inline like its project page already does. Live-verified by calling the new service method directly against the real internal single-project demo company and cross-checking against the existing single-project stats endpoint — exact match on every field. The same day, the personal-broadcasts (MTProto) module got a significant audience-model change: "чтобы можно было пушить не только клиентов пришедших через нашу срм, но и всех остальных, даже внешних, все абсолютно чаты должны пушиться" — the audience source flipped from `Client` rows with `dialogueSource='PERSONAL_ACCOUNT'` (structurally invisible to anyone the CRM never created a row for) to a new `TelegramPersonalService.getAllDialogs` (live GramJS `client.getDialogs()`, 1:1 non-bot non-self chats), with CRM data merged in as an optional per-`tgUserId` overlay — a dialog with no matching `Client` is treated as zero/null CRM data (so `hasPurchase:true` correctly excludes it, `hasPurchase:false` correctly includes it, no special-casing). `PersonalBroadcastLog.clientId` had to become optional (new `tgUserId`/`tgFirstName`/`tgUsername` snapshot columns, since a clientId-less row can't join through `client` for delivery) — a real schema migration on a feature that had shipped literally the same day. The user also reported a real bug on the "Maria Lopez" project: the folder picker showed no folders despite folders existing in the real Telegram app. Root cause turned out to be two layers: `Api.messages.GetDialogFilters()`'s `DialogFilterChatlist` (shared/imported folder-link folders) was being silently dropped by a `className==='DialogFilter'`-only filter (fixed, both types share the same `title`/`includePeers` shape) — but the actual live incident was `401 AUTH_KEY_UNREGISTERED` (the MTProto session had been revoked on Telegram's side), completely invisible before this fix because every one of `getDialogFilters`/`getFolderTgUserIds` had a bare `catch { return [] }` with zero logging, indistinguishable from "this account genuinely has no folders." Added logging plus a new `handleDeadSession` pattern — regex-matches auth-dead error codes and calls the existing `disconnect()` method (same one the manual settings button uses) so the already-existing "not connected" UI badge picks it up automatically, no new UI needed. Live-verified the real diagnosis against the actual Maria Lopez channel (read-only calls only) and confirmed the session got correctly cleared in the DB; separately verified the new audience-merge logic via an isolated test project with monkey-patched `getAllDialogs`/`getFolderTgUserIds` (no real MTProto call) driving the real service end-to-end — all filter combinations and the clientId-null-vs-set split in created `PersonalBroadcastLog` rows came back exactly as expected. Later the same day, per the user's request to "проверять раз в некоторое время" whether a personal account is still connected, a `TelegramPersonalHealthCron` (`@Cron('*/30 * * * *')`, in `ChannelsModule`) was added — pings every channel with `tgSessionEncrypted` set via `TelegramPersonalService.checkSessionHealth` (GramJS `client.getMe()`, the cheapest authenticated call), routing any failure through the same dead-session detection already built for the folder bug. A new `Channel.tgPersonalLastError` column distinguishes "Telegram revoked the session" (set) from "never connected" or "manually disconnected" (both null) — `disconnect()` gained an optional `reason` param, passed only from the automatic dead-session path, so the existing `tgPersonalConnected` UI badge and a new line in Studio's notification bell ("Личный аккаунт проекта «X» отключён") both light up automatically with zero new plumbing beyond the one field. On 2026-08-06/07, while verifying this exact cron, a real incident occurred: the verification script called `app.get(TelegramPersonalService)` — the real singleton, not a mock — and overrode its `getLiveClient` method globally to simulate a dead session, then ran `checkAllPersonalSessions()`, which by design iterates every connected personal account in the whole database, not just the intended test one. Every real channel hit the faked error and was disconnected for real (a genuine `Api.auth.LogOut()` invoke against Telegram), because `disconnect()` reads `this.liveClients` directly (bypassing the mocked method), and that map had already been populated with real live connections by `ChannelsService.onModuleInit()`, which runs automatically on every `NestFactory.createApplicationContext(AppModule)` boot — including throwaway verification scripts. 5 real company projects lost their personal-account connection (Diego Herrera, Ricardo Ramirez, Santiago Vargas, Isabella Ramirez, Rolando Acuna). Root lesson, explained to the user in full: `NestFactory.createApplicationContext(AppModule)` is not an isolated test sandbox for this app — it boots the real lifecycle, including real parallel MTProto connections to every live session, and every Prisma write through it hits the real production database exactly like the real server would. Two forward-looking fixes shipped as a direct result of the incident: (1) a system-wide uniqueness check in `finalizeConnection` — the same Telegram account (`tgPersonalUserId`) can no longer be connected on two different `Channel` rows at once, anywhere in the system, not even across companies (same bug class as the earlier bot-token webhook hijack, just for MTProto sessions instead of webhooks — `id: {not: channel.id}` still allows reconnecting a channel to its own account); (2) an audit of every catch block in `telegram-personal.service.ts` found and fixed several that silently swallowed Telegram errors — `submitPassword`'s catch didn't even capture the error variable, `startConnect`/`submitCode` threw a user-visible exception but never logged server-side, `sendStory` returned failure silently, and `sendStory`/`sendDirectMessage` both called `getLiveClient()` *outside* their own try/catch (a connection failure there propagated fully uncaught, invisible to both the log and to dead-session handling) — every method now unconditionally logs before any conditional handling, and both calls moved inside their try blocks. `TelegramProvider` (bot side) was audited too and found already fully logged on every catch, no changes needed there. This round's verification deliberately avoided `NestFactory.createApplicationContext` entirely (the exact mechanism that caused the incident) — code review, `tsc`/`nest build` only, plus a read-only `PrismaClient` query (no AppModule) confirming every personal-account session in the company had already been cleared by the incident itself, so the final `pm2 restart trafficcrm-api` carried zero additional risk. Later the same day, a follow-up request ("везде где есть список клиентов... время час минута") found that Studio's client table (`apps/web/src/app/dashboard/studio/clients-table.tsx`, a separate re-implementation, not shared with classic's `ClientsTable`) formatted subscribed/unsubscribed dates via `toLocaleDateString('ru-RU')` — no `date-fns`, no time at all, unlike classic's equivalent columns which already had `HH:mm` from earlier work — plus a few other date-only spots (classic `ClientsTable`'s cross-project-overlap tooltip, `client-detail-drawer.tsx`'s Регистрация/Активность/purchase-history fields, both audience cross-project comparison pages). No shared date-format constant exists anywhere in `apps/web` — every `format()` call is an inline literal string — fixed each spot individually to `'d MMM yyyy, HH:mm'` rather than introducing one. Studio's external-subscribed/unsubscribed status pills also gained the `title` tooltip classic's already had (via a plain `<span title=...>` wrapper, since `StudioPill` itself has no `title` prop). On 2026-08-09, a user-reported project-page bug ("104 клиента за сегодня, а в топ баеров только 27, где остальные?" / pixel funnel showing 77 vs the leaderboard's 75 for the same pixel) turned out to be two separate things. The 77-vs-75 mismatch was a real bug: `ProjectsService.getEventColumnFunnel` (shared by the "Топ пикселей"/"Топ кампаний" funnel drill-downs) computed Subscribe/Dialogue from raw `TrackingEvent` rows instead of deduplicated `Client.subscribedAt`/`firstDialogueAt` — live-traced to a duplicate Subscribe event on one client plus a second client whose event write lagged the actual subscription moment by 3.5 hours, landing it in a different day-window than the leaderboard's own Client-based count. Fixed by switching to the same Client-table approach `getLandingsFunnel`/`getBuyersFunnel` already used correctly; `getAdBreakdown`'s `purchases` field was on the same latent pattern (TrackingEvent instead of the real `Purchase` table) and was fixed too for consistency, even though it didn't actually disagree for this project on this day. The "27 of 104" question was confirmed NOT a bug via live query — the missing clients have no buyer/pixel/campaign attribution at all (organic/unlabeled traffic), correctly excluded from a ranking that only makes sense between real entities — but the project page had no equivalent of the "БЕЗ БАЕРА" bucket that `TeamService.getBuyerAnalytics` already shows company-wide, so those clients just silently vanished from view. Added `buyersUnattributed`/`pixelsUnattributed`/`campaignsUnattributed` to `getLeaderboards`'s response (one combined query) and a matching non-ranked footer row in both trees' leaderboard card components, shown only when non-zero, gated by the same `STATS_VIEW_TEAM_LEADERBOARDS`/`STATS_VIEW_REVENUE` rules the rest of the leaderboard already uses. Live-verified via read-only queries reproducing the exact reported gap, then via direct (unmocked) service calls confirming the pixel funnel and leaderboard now agree exactly and that buyers/pixels sums plus their unattributed buckets both equal the independently-queried "today" total. On 2026-08-13, a routine "restart everything" request surfaced that `trafficcrm-admin` had gone missing from `pm2 list` after a server reboot (not crash-looping — just silently absent, because `pm2 save` had never been run after that app was added to `ecosystem.config.js`); fixed via delete+start-fresh+`pm2 save`, and a new `README.md` was added at the repo root with a practical setup-from-scratch + day-to-day restart/deploy runbook for this specific server (as opposed to `14_INFRA_AND_DEPLOY.md`'s fuller architecture spec). The same day, a user request to verify all connected personal Telegram accounts were confirmed healthy via a real, unmocked `checkSessionHealth` call per channel (deliberately avoiding any mocking of `TelegramPersonalService`, per the discipline established after the personal-session incident above) — all 6 tracked accounts, including the 5 disconnected by that incident and Maria Lopez, came back healthy, confirming they'd been manually reconnected since. That same verification then surfaced a new real bug: the personal-broadcast audience preview on the ROLANDO ACUNA project loaded for a long time and showed "—". Root cause, confirmed live: that account has 7553 Telegram dialogs, and `client.getDialogs({})` (added in the 2026-08-06 "all dialogs" redesign) with no limit hits heavy Telegram-side flood-wait for an account this size (97-133s across test runs) — well past `api.mw-track.com`'s 60s nginx `proxy_read_timeout`, and the frontend's debounced preview effect had no `.catch()`, so a failed request just left `audienceTotal` at its initial `null`, rendered identically to a genuine zero. Fixed with three changes: `TelegramPersonalService.getAllDialogs` gained an in-memory TTL cache (5 min) with in-flight-call deduplication, plus a non-blocking `prewarmDialogsCache` fired from `PersonalBroadcastsService.getFolders` (already hit on page load) so a large account's cold fetch is usually already running by the time the user reaches filters; a dedicated nginx `location` regex-matched to just `.../personal-broadcasts/(preview-audience|folders)` got a 200s timeout, leaving the global 60s untouched for every other route; and the frontend gained a distinct `calcError` state so a failed calculation no longer looks identical to a real zero-match result. Verified live (real, unmocked calls): prewarm + an explicit call correctly deduped into one live fetch (not two), a second call was an instant 0ms cache hit, and the account's session stayed fully healthy through the flood-wait-heavy testing. On 2026-08-18, a new landing template shipped from a user-supplied archive (`new_landing.7z`) — `age-gate-invite` ("Проверка возраста + приглашение"), the first template with two popups instead of one screen: popup 1 asks "18+?" (Sì/No), popup 1's "No" and popup 2's own button both always go to the real `TG_REDIRECT_URL`, popup 1's "Sì" only reveals popup 2 client-side (no URL) — all 7 texts/button-labels across both popups are independently editable `templateData` fields, wired through the same manual per-field whitelist every existing field already uses (`LandingsService.createFromTemplate`/`.update()`, plus the `TEMPLATE_IDS` `@IsIn` whitelist gotcha from 2026-07-15). Popup 1's default text is the archive's own Italian copy verbatim (explicit user instruction, "как сейчас в шаблоне" — the `lang="es"`-vs-Italian-content mismatch already in the source archive was left alone, out of scope); popup 2's default was deliberately changed away from the archive's own unrelated "earn money watching videos" copy to a generic "Подпишись на наш канал!" default matching every other template's `JOIN_BUTTON_TEXT` convention. New SDK capability (not landing-specific) — `data-auto-redirect-defer="true"` on the tracking script tag: instead of firing the existing `data-auto-redirect-url` immediately on load like every other template, the SDK exposes `window.tcrm.triggerAutoRedirect` for the template's own inline JS to call whenever it actually wants the redirect to fire (`template.html`'s popup-2-reveal handler calls it) — required moving `window.tcrm`'s object-literal assignment earlier in `browser.ts` so it could be mutated rather than clobbered by the later unconditional reassignment. Deploy had three parts, not the usual two: `nest build`/`next build` as always, plus a separate manual SDK publish (`apps/sdk`'s own `npm run build && node scripts/publish-cdn.js`, per the established CDN runbook) — confirmed `cf-cache-status: MISS` with a fresh `last-modified` on the live `cdn.mw-track.com/track.js` afterward. Live-verified via real (unmocked) `NestFactory` + direct `LandingRendererService.renderAndServe` calls against a fake Express req/res and an isolated throwaway Company/Project/Channel/Landing (15 checks: both popups' defaults render, zero leftover unsubstituted `{{VAR}}` placeholders, `TG_REDIRECT_URL` resolves identically into exactly 2 places, the defer attribute only appears when auto-redirect is actually enabled on the landing, editing via `update()` patches only the touched field) — one false-positive caught in the test script itself (an inline HTML comment in the new template happened to contain the literal attribute name as documentation text, tripping a naive substring check) fixed before re-confirming all-green; all test rows, including 2 genuinely orphaned ones from earlier failed attempts (missing required `Company.slug`/`Project.secretKey`/`Channel.name` on the first tries), deleted by exact id afterward. On 2026-08-18, the project clients page gained a traffic-source filter (Facebook/TikTok, derived from `Client.fbclid`/`ttclid` presence rather than pixel assignment — a click-id is present on every client who genuinely arrived via that platform's ad click, while a pixel isn't always assigned) and a period selector matching the project page's (today/yesterday/7d/30d/custom, filtering `Client.createdAt` — deliberately not reusing the existing-but-dead `subscribedFrom`/`subscribedTo` DTO fields, which no frontend had ever called and which would have wrongly bypassed the ours/external origin filter). The accompanying request to re-audit period handling everywhere surfaced a real, previously-unnoticed inconsistency: `resolveStatsPeriod`'s `7d`/`30d` branch was a raw non-timezone-aware rolling window (`now() - N*24h`) while `today`/`yesterday`/`custom` were all calendar-day-aligned in the project's own timezone — fixed by giving `7d`/`30d` the same `date_trunc`-based alignment, a genuine behavior change (7d/30d numbers now shift up to ~24h depending on time-of-day at request time) called out explicitly rather than silently shipped. Separately, URL-persisted period turned out to already exist independently on 3 pages (project page, both home pages) via 3 copies of the identical ~25-line block, plus 2 more pages missing it entirely (my-stats ×2) and Studio's project page missing it too — consolidated into one `apps/web/src/lib/use-period-query-state.ts` hook and rolled out to all 6 period-bearing pages, deleting every local duplicate. Live-verified via real `ClientsService.findMany`/`resolveStatsPeriod` calls against an isolated test project with clients carrying distinct `createdAt`/`fbclid`/`ttclid` — calendar alignment, period-window filtering, and traffic-source filtering all confirmed correct (one false-positive caught and diagnosed in the test script itself, not the product).

The same day, a follow-up round covered 4 more requests on the clients page. Full URL persistence (page/origin/search/all 16 filter fields, not just period) — rather than a second independent `router.replace` racing against `usePeriodQueryState`'s own, the clients page moved off the shared hook onto one combined `useEffect` serializing everything together; new `readClientsFiltersFromSearchParams`/`writeClientsFiltersToSearchParams` helpers in `clients-filter.tsx` share the 16-field mapping between both trees. A traffic-source column (abbreviated "FB"/"TT") was added to all 8 real `<ClientsTable>`/`<StudioClientsTable>` render sites (both dedicated clients pages, both `/my-clients`, both landing-detail pages, both project-overview "recent clients" mini-tables), gated by `user.role !== 'OPERATOR'` — implementing it surfaced a real pre-existing gap: `ClientsService.findMany`'s unrestricted Prisma query already returned `fbclid`/`ttclid` raw to every role including OPERATOR (who's explicitly denied the same data on the single-client detail endpoint via `canViewTrafficSource`), invisible only because no list column had ever read those fields before — fixed by threading the same `canViewTrafficSource` check into the list endpoint and nulling both fields server-side when false, deliberately not also touching the wider (and still-open) buyerId/pixelId/campaign/utm exposure since nothing displays those in list form today. A one-line Studio-only bug was found and fixed — the dialogue-column `<td>` in `dashboard/studio/clients-table.tsx` was missing `whitespace-nowrap` (every sibling date cell already had it; classic's table was unaffected since its shared `TableCell` primitive hardcodes the class), causing rows to grow tall instead of wide when the dialogue text was long. Finally, period URLs now always carry concrete `from`/`to` dates for every preset, not just `custom` — new `computePeriodDates` (browser-local approximation, calendar-aligned to match the prior round's backend fix) makes bookmarked period links stable across days, while the actual API calls still send the `period` enum for the backend's real timezone-correct resolution.

The same day, a design-only follow-up covered 5 more reports on the clients pages, all confirmed live via a real headless-Chromium login+screenshot pass (found a cached Playwright install left from an earlier session, reused it rather than installing anything new). Root cause: Studio's clients page had reused the plain shared `PeriodSelector`/`ClientsFilter`/`Input` components as-is — styled for the classic tree (`border-border`/`bg-blue-600`/`bg-card`/`bg-transparent`), the exact same "bare shadcn component paints wrong in Studio's dark theme" class of bug already fixed repeatedly elsewhere (`LandingContentCard`/`StatsCard`/`PushAudienceStep`). Fixed with a new `StudioClientsPeriodPicker` (`app/dashboard/studio/ui.tsx`, copies the already-correct pill markup from Studio's project page rather than risking a shared-component refactor of an already-working page), `ClientsFilter` gaining the standard `containerClassName` override, and the search `Input` given explicit Studio classes plus a height fix (`h-9` correctly overrides the base `h-8` since `cn()` uses `tailwind-merge`). Origin toggle and period picker moved onto one row with period right-aligned (both trees, pure layout). The period default changed from `'today'` to an explicit "all time" state — rather than adding a 6th nullable state to the existing `PeriodValue` type (which 6 other pages already use non-nullably), built two new `PeriodValue | null`-aware pickers with an explicit "Все" option representing "no date filter at all." Checking "apply this to `/my-clients` too" surfaced that page had never received the *entire* prior round's feature, not just today's polish — no period picker at all, and its query params only forwarded 6 of 16 `ClientsFilterState` fields even though the shared filter UI already rendered all of them (a real silent gap: visible filters that never actually filtered). Brought to full parity in the same pass, including URL persistence extended to cover `projectId` (since this page picks its project from a dropdown, not a route param). Verified end-to-end with an isolated throwaway company/user/project/3 clients created via `AuthService.register` (real password hashing), a real login through the actual `/login` form, dark mode toggled via the real UI control, and screenshots of both `/projects/:id/clients` and `/my-clients` confirming all 5 fixes — zero console errors, test data deleted by exact id afterward.

The same day, a third round covered 3 more unrelated reports. Project creation's "Способ вступления" (tgMode) dropdown defaulted to `BOT_DIRECT`, changed to `PRIVATE_CHANNEL_REQUEST` (`EMPTY_CHANNEL_FORM` in `channel-fields-editor.tsx`, confirmed used only by the two creation pages, never by channel-editing) — and its long option labels were clipped by the default `w-(--anchor-width)` `SelectContent`, the same recurring bug already fixed ~4 times elsewhere in this codebase (`create-landing-dialog.tsx`/`landing-content-card.tsx`/`get-link-dialog.tsx`/`multi-select.tsx`) but never applied to this one Select. A new "Топ источников" (Facebook/TikTok) leaderboard category was added to `ProjectsService.getLeaderboards`/`getLeaderboardFunnel` and both project pages' tab lists — the first leaderboard category grouped by a derived SQL expression (`CASE WHEN fbclid...THEN 'FACEBOOK'...END`) rather than a real column or entity table, which surfaced a genuine, previously-untested gap: `GROUP BY <alias>` referencing that CASE expression threw a real Postgres `42803` error at runtime (invisible to `tsc`), caught by the live verification pass and fixed by repeating the full expression in `GROUP BY` in all 3 query occurrences instead of the alias. `getSourcesFunnel` mirrors `getBuyersFunnel`'s simpler shape (no pageViews/leads — `TrackingEvent` has no `fbclid`/`ttclid` column) and is revenue-gated like pixels/campaigns, not buyer-identity-gated. Finally, the clients-page search input height fix from two rounds prior (`h-9`) was still off by 2px in the user's own re-measurement (36 vs 38) — root cause: `StudioLinkButton` has no fixed height at all, it's whatever `text-sm px-4 py-2`(`+dark:border`) naturally computes to, so pinning the input to a guessed number could never track it exactly; fixed by giving the input the identical box-model recipe (`h-auto` + same padding/border) instead of another guessed pixel value, verified via real `getBoundingClientRect()` measurement (36px = 36px) rather than eyeballing a screenshot, applied to both Studio clients pages including `/my-clients` again per the same "don't forget the operator page" reminder as the prior two rounds.

On 2026-08-14, the OPERATOR role gained access to personal-account (MTProto) broadcasts, project-scoped as with every other Operator-visible resource. Investigation first established that no route-level guard exists anywhere blocking OPERATOR from `/projects/[id]/personal-broadcasts` directly — the two sidebars' hardcoded 2-item `OPERATOR_NAV_ITEMS` is a pure navigation-link restriction, not an access boundary; the real gaps were (1) `DEFAULT_ROLE_PERMISSIONS.OPERATOR` never included any `PERSONAL_BROADCASTS_*` permission, and (2) no entry point existed that kept Operators inside their deliberately-sandboxed few-page UI rather than sending them into the full project page. Added `PERSONAL_BROADCASTS_VIEW/CREATE/DELETE` to the OPERATOR default template (no `SEND` — nothing in the UI has a standalone send button, sending is always via `create()`'s `sendNow`) plus a one-time live backfill (`userPermission.createMany` + `skipDuplicates`) for the 11 already-existing (Operator, Project) pairs, since `seedDefaultsIfEmpty` only seeds a pair with zero existing rows and does not retroactively apply new defaults to already-provisioned operators. Frontend: extracted the project-scoped list and composer into `projectId`-parameterized shared components (`components/personal-broadcasts/broadcast-list.tsx`/`broadcast-composer.tsx` classic, `dashboard/studio/personal-broadcasts-list.tsx`/`-composer.tsx` Studio — same `initialProjectIds`-prop precedent already used by `PushComposer`), reused by both the original project-scoped routes (now thin wrappers) and new company-wide `/my-personal-broadcasts` + `/my-personal-broadcasts/new` pages, which mirror the existing `/my-clients`/`/my-stats` picker+chip-row pattern exactly (including the established `<SelectValue>` children-render-function fix) and reuse the same unfiltered `GET /projects` those pages already call — no new backend endpoint needed since "has ProjectAccess" and "has PERSONAL_BROADCASTS_VIEW" are now the same set for every Operator by design. Added a 3rd `OPERATOR_NAV_ITEMS` sidebar entry in both trees. Live-verified: real backfill against production data, real unmocked `assertAccess` positive+negative control against a real backfilled operator, and a full personal-account health re-check (all 6 real connected accounts) after the verification's `NestFactory.createApplicationContext` boot, per the standing caution established after the earlier personal-session incident. Mid-deploy, a second bare `npm run build` (run only to grep its own warnings, not intending an actual redeploy) silently overwrote the correctly-built `apps/web` output with one missing the sourced prod env, baking `localhost:3001` back into the bundle — caught via the routine pre-restart grep before it reached users, fixed with a third correctly-sourced build.

On 2026-08-18, a follow-up request asked to fix "все попапы и дропдауны чёрно-серого цвета" — a design-consistency sweep the user scoped explicitly beyond the ~13 named locations (landing-creation popup, landing-settings popups, scenario steps/stats pages, the scenario-stats status dropdown, the domain-path popup, team-page popups, the billing popup, the docs table of contents, the client card, the client card on the audience-overlap page) to "and all others too." Rather than continuing the established per-component `containerClassName` override pattern (already used ~6 times this session — `LandingContentCard`/`StatsCard`/`PushAudienceStep`/`ClientsFilter`/etc. — but too labor-intensive to scale to dozens of popups), this round fixed the whole class at once: every shared shadcn/base-ui primitive (`Dialog`, `Select`, `DropdownMenu`, `Drawer`, `Card`, `Accordion`, `Sheet`) already styles itself purely through semantic Tailwind tokens (`bg-popover`/`bg-card`/`bg-muted`/`bg-primary`/etc.), and `globals.css`'s `.dark` block backing those tokens was a neutral achromatic `oklch` grey that had simply never been tuned to Studio's real palette (`#0F1620` page / `#171F2B` card / `#7BA9EE` accent, established in `dashboard/studio/ui.tsx`'s `STUDIO_CARD`/`StudioLinkButton`). Added one new `.studio.dark` CSS-variable override block (`--background`/`--card`/`--popover`/`--primary`/`--secondary`/`--muted`/`--accent`/`--border`/`--input`/`--ring`, all mapped onto the real Studio hex values) that every current and future component on those tokens picks up automatically, with zero further per-file edits needed. The one non-obvious wrinkle: `Dialog`/`Select`/`DropdownMenu`/`Drawer` content all render via `*.Primitive.Portal` straight to `document.body`, outside Studio's own layout subtree — so the scoping class (`studio`) has to live on `<html>` itself, added via a `useEffect` in `dashboard/studio/layout.tsx` (`document.documentElement.classList.add('studio')` on mount, removed on unmount) rather than anywhere in the nested JSX, since `<html>` is shared between both domains/trees and can't be branded server-side by a nested layout. Separately, the one genuinely-broken (not just miscolored) popup named in the request — the landing-creation dialog's project picker (`components/create-landing-dialog.tsx`) — got the same established `w-auto min-w-(--anchor-width)` `SelectContent` fix already applied ~4 times elsewhere in the codebase but missed on this one Select. Live-verified via real headless-Chromium screenshots (an isolated test company/project/client, real login, dark mode forced via `localStorage`) confirming `<html class="studio dark">` correctly cascades through Portal-rendered content: the client-detail drawer, the landing-creation dialog, the billing top-up dialog, the "add domain" dialog, and the team role `DropdownMenu` (including its hover/selected-row highlight) all render Studio's real navy instead of the old grey-black. `tsc --noEmit` and `next build` both clean, no `localhost:3001` leak, test data (company/project/client) deleted by exact id afterward.

On 2026-08-18, two small requests shipped together. First, a content-preview popup for bot pushes ("показывать мини превью рассылки если её открыть... можно смотреть любые — новые, старые, отменённые, в драфте, запланированные") — new shared `PushPreviewDialog` (`components/pushes/push-preview-dialog.tsx`, one component for all 4 lists: project-scoped + company-wide, both trees) reuses the existing `TelegramMessagePreview` component against `GET /projects/:id/pushes/:id` (already returned full `messageText`/`messageMedia`/`buttons`, just had no UI reading it), plus an audience/sent/errors/date summary row and the send-log table below. This replaces the old click-to-open behavior on the two project-scoped pages, which previously opened a logs-only `PushLogsDialog` (now deleted, duplicated per tree) — the new dialog is a strict superset, working identically for a never-sent DRAFT (logs table just shows "Логов пока нет") through SENT/CANCELLED. The two company-wide "Рассылки" list pages (`/pushes-calendar`, both trees) had no row-click at all before this — added the same dialog there too, with `e.stopPropagation()` on the existing project-name link and action buttons so those keep working independently of the new row click. Second, a temporary favicon ("хотя бы букву М, чтобы хоть что-то было") — the site had shipped with Next.js's untouched default `favicon.ico` since Phase 1 with nobody having replaced it. Used the App Router `icon.tsx` metadata-file convention (`next/og`'s `ImageResponse`, a blue `#1F4E9C` rounded square with a white "M", matching the sidebar logo's brand blue) instead of hand-crafting a raw `.ico` (sharp, the only image library available in this environment, can't encode ICO), and deleted the stale `favicon.ico` so the two don't conflict. This surfaced a real, exact repeat of the 2026-07-31 `/docs/*.png` middleware bug: `icon.tsx` compiles to the route `/icon` — no file extension — so `middleware.ts`'s Studio-domain rewrite (which already excludes paths with a dot via regex) didn't catch it, and silently rewrote it to a nonexistent `/dashboard/studio/icon`, 404ing the favicon on the main domain (old.mw-track.com was unaffected, it has no rewrite at all). Fixed by adding an explicit `METADATA_PATHS` exclusion list (`/icon`, plus `/apple-icon`/`/opengraph-image`/`/twitter-image` preemptively, for whenever those get added) alongside the existing extension-regex check — worth remembering that any *future* Next.js special metadata file (these all compile to extension-less routes) will hit this exact gap again unless added to that list. Live-verified: real headless-Chromium login against an isolated test company/project/DRAFT-push (content, buttons, audience/date fields, empty-logs state all correct, including in dark mode on the company-wide list), plus `curl -I` confirming `/icon` returns `200 image/png` on both domains post-fix (was `404` with `x-middleware-rewrite: /dashboard/studio/icon` before). `tsc --noEmit`/`next build` clean, no `localhost:3001` leak, test company deleted by exact id afterward.

The same day, a follow-up request compacted the day-schedule list next to the calendar on the company-wide "Рассылки" page (`/pushes-calendar`, both trees) — user-provided sketch: "проект (время1) (время2) (время3), в ряд, и когда нажимаешь на время открывается превью". `DayScheduleList` (`components/pushes/day-schedule-list.tsx`, shared by both `pushes-calendar` pages via `showProject=true` and the push composer's own schedule calendar via `showProject=false`) previously rendered one full row per push (name + project link + time), which grew tall fast on a day with several pushes in the same project. Redesigned to group items by `projectId` first: one flex-wrap row per project, project name on the left, every push's time as a small clickable pill to its right — the push's name is no longer shown inline at all, since clicking a time pill now opens the same `PushPreviewDialog` from earlier today, which already shows the name/content/stats. For the composer's single-project usage (`showProject=false`) grouping collapses to one implicit group, so it just renders a flat pill row with no project label. Live-verified with an isolated test company (2 projects, 5+2 scheduled pushes on the same day) — confirmed the exact `Project (10:00) (12:00) (14:00)...` layout on both domains, and that clicking a time pill opens the correct push's preview (content, scheduled time, empty logs) without triggering the project-name link's own click handler.

The same day, a bug report on this new preview popup — "первые секунды показывает содержимое старого пуша, потом обновляется" — traced to the global `QueryClient` default (`app/providers.tsx`, `staleTime: 30_000`, tuned for list/table pages) making `PushPreviewDialog` serve a previously-cached push's content instantly on open before a background refetch quietly corrected it, since nothing invalidated that query key on edit and 30s of staleness is invisible on a list but reads as a real bug on a rarely-opened single-entity modal. Fixed by overriding `staleTime: 0, gcTime: 0` locally on both of the dialog's queries (content + logs) so every open is a guaranteed-clean fetch, plus `key={pushId}` at all 5 render sites as a defensive belt-and-suspenders against any prop-only-update path. Live-verified by sampling the DOM at 0–900ms after switching between two pushes with distinct marker text — the wrong push's content never appeared at any sampled instant.

On 2026-08-19, a user-reported pixel-numbers mismatch ("60 логов на подписку, но 87 клиентов в топе пикселей... до фейсбука дошло только 50... у части клиентов баер БЕЗ БАЕРА или чужой, хотя пиксель использовал только один баер") turned out to be two completely separate, both-confirmed-real issues on the same project/landing. First, not a bug at all: the gap between 94 real Subscribe `TrackingEvent`s and only 60 `TrackingEventDelivery` rows was a clean, exact-timestamp-aligned cutoff at `project.updatedAt` (2026-08-18 16:45 UTC) — someone had switched OFF the "Подписка" toggle in this project's Settings → События tab; every Subscribe event before that instant was delivered, every one after was correctly, silently skipped by design (`TrackingService.recordEvent`'s `isEventTypeDisabled` gate) — re-enabling the switch is the fix, not a code change. Second, a real, now-fixed bug: several clients on the same landing had `buyerId: null` or a wrong buyer, and a couple had literal unresolved `{{campaign.id}}`-style Facebook macros — traced to `landing-visit-attribution:<landingId>` (`LandingRendererService.injectTrackingScripts` / `TelegramProvider.getCachedLandingAttribution`, the PRIVATE_CHANNEL_REQUEST-only attribution bridge since that flow has no per-visitor `start:<code>` deep link) being a single Redis key written with plain `SET` and read with `GET` — a "last write wins" design that the code's own 2026-07-24 comment had already flagged as a known, accepted risk ("если на лендинг почти одновременно зайдёт два разных человека, может подставиться не тот — согласовано") for the low-stakes country-guess field, but the SAME mechanism was extended in 2026-07-23 to carry the full buyer/campaign/pixel attribution block too — turning an accepted approximation into real, money-relevant misattribution once a landing gets simultaneous traffic from more than one buyer or ad. Fixed by turning the single key into a Redis LIST (`RPUSH`/`LTRIM`/`EXPIRE` on write, `LPOP` on read) — each visit queues instead of overwriting, each join request consumes the oldest unmatched visit (FIFO) instead of reading one shared value, so two visitors to the same landing no longer contaminate each other's attribution; the one remaining approximation is visit/join *order* within a pair, not full data loss. The fbp-patch-after-render path (`TrackingController.tgRedirect`, needs the browser-set `_fbp` cookie added after the fact) was updated in tandem to patch the list's *tail* (`LINDEX`/`LSET` at -1) instead of GET/SET. Deploying this surfaced a real, self-inflicted transitional risk caught by live testing before it could hit production traffic: leftover pre-deploy Redis keys were still the old STRING type (up to their remaining ~30min TTL), and `RPUSH`/`LPOP`/`LINDEX` against a STRING key throw `WRONGTYPE` — uncaught, that would have crashed the hottest real-traffic paths in the whole tracking system (every ad-click redirect, every Telegram join request) for up to half an hour post-deploy. Fixed with self-healing try/catch-and-delete-then-retry at all three call sites, and the 7 real leftover legacy keys found in production Redis were deleted proactively before re-deploying. Separately confirmed live: the one client attributed to a specific named wrong-seeming buyer was NOT conclusively an instance of this bug (a single Redis write can't merge two different visitors' fields — pixelId and buyerId in that row belonged together to one real click) — more likely that buyer genuinely generated their own tracking link for the same landing using the same shared, project-level (not buyer-exclusive) pixel, which the system has always allowed; flagged to the user as something to verify on their side rather than claimed as fixed. The literal unresolved `{{campaign.id}}`/`{{ad.id}}` macros seen on a couple of clients are a Facebook-side ad-configuration issue (that ad's destination URL isn't wired for FB's dynamic URL parameters), not a CRM bug. Verified live end-to-end against the real deployed code (not a mock): `LandingRendererService.renderAndServe` called twice with distinct fake visitor data confirmed both entries queue in order with correct per-visitor fields, cleaned up by exact list-entry match afterward (never touching real backlog via `LPOP`).

The same day, two direct follow-ups shipped. First, the user asked to actually send the missed Facebook events for the window the "Подписка" toggle was accidentally off — found the exact re-enable timestamp (`project.updatedAt` moved to 2026-08-18T23:25:23Z / 02:25 MSK, matching the user's own report almost to the second) and backfilled the 75 real Subscribe events (project-wide, all with genuine ad attribution) created during the true 16:45–23:25 UTC disabled window by re-enqueuing the exact same `send-to-platforms` BullMQ job real-time events use — not a bespoke sender, so `event_time` stayed the true original moment and dedup/routing behaved identically; all 75 delivered successfully, zero errors. Second, even after the attribution race-condition explanation above, the user asked to slow the landing auto-redirect down to a flat 2 seconds regardless — `apps/sdk/src/browser.ts`'s `fireAutoRedirect()` (previously redirected immediately once the `_fbp` cookie appeared, or polled up to 3s otherwise) was simplified to one unconditional `setTimeout(..., 2000)`, rebuilt and republished to the CDN (the publish script hit a transient MinIO `AccessDenied` on the bucket-exists check on the first attempt — a bare retry succeeded, the bucket already existed).

On 2026-08-19, three bundled requests shipped for the project page's "Топ" leaderboard section. First, each of the 5 categories (buyers/pixels/landings/campaigns/sources) was hard-capped at top-5 with no way to see more — new `GET /projects/:id/leaderboards/:category/full` (`ProjectsService.getLeaderboardFull`, same 5 query shapes as the top-5 endpoint minus `LIMIT 5`, capped at 200 as a safety net not a real limit) backs new `/projects/[id]/leaderboards/[category]/page.tsx` pages in both trees, with the same period selector as the project page itself and a "Сравнить все →" link added to each leaderboard card carrying the current period through. Second, a real bug: some buyers showed a literal "null" after their name everywhere they appeared — three independent backend call sites built `` `${firstName} ${lastName}`.trim() ``, and `.trim()` doesn't remove the literal string "null" that a null `lastName` produces inside a template literal (a common case, not an edge case) — fixed via a new shared `common/user-name.util.ts` `formatUserName()` used in `ProjectsService.getLeaderboards`, `TeamService.getBuyerAnalytics`, and `ClientsService.getClientDetail`. Third, confirmed-correct-but-confusing behavior: the project page's Топ баеров deliberately doesn't filter out deleted buyers (their historical revenue should stay visible), but gave no visual signal they were gone — added an `isDeleted` flag rendering the name in red with a "Удалённый пользователь" tooltip, both on the top-5 card and the new full-list page. Verified live on both domains with a real deleted buyer (via the actual `TeamService.remove()` path) and a real buyer with no `lastName` — no literal "null" text anywhere, correct red+tooltip rendering, full-list pages working with period persistence.

The same day, a follow-up caught a real gap in the new "Сравнить все" pages: they only showed rank/name/clients/revenue, missing the per-item conversion funnel (PageView→Lead→Subscribe→Dialogue→Purchase) that the project page's own top-5 ТОП cards already show. Fixed by raising the existing `/leaderboards/:category/funnel` endpoint's `ids` cap from 10 (tuned for top-5) to 200 (the funnel queries already batch `WHERE id IN (...ids)` per category, not N+1, so this is free), having both full-list pages fetch that same endpoint for every row, and switching both pages from a table layout to the same per-item block layout the ТОП cards use — a multi-stage funnel widget doesn't fit inside one table cell. Verified live: the full-list page's funnel-mini matched the project page's own ТОП section exactly for the same test data.

On 2026-08-20, a follow-up bug report on the leaderboard fixes above ("всё ещё показывается Удалённый пользователь") led to a real, still-active bug: the specific buyerId values in question weren't deleted users at all — they were truncated prefixes of a real, frequently-occurring buyerId, confirmed both on the reported project and company-wide on a second buyer, with timestamps showing the corruption still happening the day before the investigation. Root cause: `buildTrackedLink()` (`apps/web/src/lib/landings.ts`) placed `buyerRef` LAST in the tracking-link query string, after all the Facebook `{{macro}}` placeholders — once Facebook substitutes real (sometimes long, URL-encoded) campaign/ad names into those macros, any downstream URL-length limit truncates the tail, and buyerRef, being last, is the first and most-truncated casualty, by a different amount on every click depending on how long that click's macros happened to be. Fixed by moving `buyerRef`/`pixelId` to the FRONT of the parameter list — fixes new links going forward, but buyers with an already-affected running link need to regenerate it. Separately, the "buyer not found" fallback label was renamed from the misleading "Удалённый пользователь" to "Некорректный ID баера", since that branch can now only be reached by genuinely corrupted data (real deleted users are found and marked red per the prior fix). The same request bundled four more items: landing names in both the ТОП section and the new "Сравнить все" pages now link to the landing's own detail page, with the same preview eye-icon added to the comparison pages; DRAFT landings are now marked red instead of grey everywhere (list dots, badges, detail-page pills), and both landing detail pages gained the same publish/unpublish toggle the list card already had; and new landings now auto-publish immediately on creation (`LandingsService.createFromTemplate`/`createCustom` call the existing `publish()` method right after creating, rather than a bare status flip, so the PRIVATE_CHANNEL_REQUEST invite-link-creation side effect still fires correctly). Verified live end-to-end on an isolated test project plus a direct unmocked call against the real reported project.

On 2026-08-20, a user question about a real-world implausible A/B test group (one landing variant showing 6316 views vs the other's 144) led to a confirmed bug: `LandingsService.computeLandingStats` had zero date filtering, so a landing added to an A/B group with pre-existing traffic carried its full lifetime history into the group comparison — a landing joining a test 10 days after it was created showed 10 days of history alongside the other variant's few hours. The codebase already had a correctly-implemented sibling, `computeAbTestMemberSnapshot` (used only when a test ends), whose own comment named this exact limitation of `computeLandingStats`. Fixed by giving `computeLandingStats` an optional `since?: Date` param (conditional `Prisma.sql`/`Prisma.empty` fragments for raw queries, `{gte: since}` for Prisma queries), applied only in `getStats`'s member loop and `getGroupStats` — passing `group.createdAt` — while a landing's own standalone detail-page stats deliberately keep full lifetime history (no `since`), to avoid regressing that view. The same request also added an A/B test groups list to the company-wide `/landings` page (previously absent entirely) and replaced the project-scoped pages' always-visible groups section with a landings/groups pill-toggle on all 4 pages (classic/Studio × project-scoped/company-wide), with an empty-state "Создать тест" button. Company-wide test creation required extending the shared `AbTestGroupDialog` (`apps/web/src/components/landing-card.tsx`) to accept `projectId: null` and show an internal project `<Select>` — reusing the exact precedent `CreateLandingFromTemplateDialog`'s optional `fixedProjectId` already established. New `GET /ab-test-groups` route (`LandingsService.findAllGroupsForCompany`, scoped via `getAccessibleProjectIds(..., Permission.AB_TESTS_VIEW)`). Live verification (real headless-Chromium screenshots) caught the new project `<Select>` showing the raw project id instead of its name — the same recurring `<SelectValue>`-without-children-render-function Base UI bug documented ~7 times earlier in this file — fixed with the same established pattern before shipping. Verified via a direct unmocked `getGroupStats()` call with deliberately backdated test data confirming pageViews dropped from 23 (pre-fix, counting pre-group history) to the correct 3 (post-group-creation only); toggle/groups-list/create-test flow confirmed via real screenshots. Test data and scratch scripts deleted after.

The same day, a follow-up request pointed out the `since`-based fix above still didn't solve the real problem: "не нужно в ней учитывать статистику каждого лэндинга по отдельности, даже если эти лэндинги проливаются отдельно... группа лэндингов как отдельная сущность со своей статой разделенной" — group stats must count only traffic that came through the group's own split link, not any/all traffic to a member landing (which can also run its own separate ads via a direct link to the same landing). Fixed with an explicit `abTestGroupId` tag stamped only at the true group-routing point (`LandingRendererService.renderByDomain`, when `resolved.abTestGroupId` is set — i.e. the DomainPath binds to the GROUP, not to one landing directly) — threaded through `renderAndServe`/`injectTrackingScripts` into a new `data-ab-test-group-id` SDK script attribute (`apps/sdk/src/browser.ts` sends it on every tracked event, stored in `TrackingEvent.payload.abTestGroupId`, same JSON-path convention as the existing `landingId`) and into a new real column `Client.abTestGroupId` (indexed, no FK — same soft-reference convention as `buyerId`/`pixelId`), propagated through the `start:<code>` Redis bridge and the PRIVATE_CHANNEL_REQUEST `landing-visit-attribution` queue exactly like `landingId` already was. New `LandingsService.computeAbTestGroupMemberStats` replaces the `since`-based approach entirely (for both live stats and `stopAbTestGroup`'s frozen snapshot) — filters strictly on `abTestGroupId = groupId`, no time window needed since the tag itself only ever exists on genuinely group-routed traffic; `computeLandingStats`'s now-unused `since` param was removed. Not retroactive — pre-deploy traffic (including on already-running tests) never got the tag, disclosed explicitly to the user rather than silently shipped. The same request also locked A/B test membership: "после создания группы лэндингов для тестирования, уже нельзя будет их менять, так как статистика будет неверной" — `UpsertAbTestGroupDto` split into `CreateAbTestGroupDto` (name+members, create-only) and `UpdateAbTestGroupDto` (name only), `updateAbTestGroup`/`PATCH /ab-test-groups/:groupId` can no longer touch membership/weights at all (enforced by the global `ValidationPipe`'s `forbidNonWhitelisted`, not just UI convention); the shared `AbTestGroupDialog` now shows a read-only member+weight list with an explanatory note when editing an existing test, versus the full interactive picker when creating a new one. Live verification of this dialog change caught a real pre-existing bug: the member-prefill effect always re-equalized weights (`equalize(initialIds)`) regardless of create-vs-edit, invisible before because editing let you retype the wrong percentages before saving — now permanently mis-displayed once read-only — fixed by sourcing edit-mode weights from the real `Landing.abTestWeight` per member instead. Verified live: unmocked `renderAndServe`+`recordEvent` calls proved 3 group-routed PageViews vs. 5 separately-driven direct PageViews to the same landing correctly report as 3 via `getGroupStats`; real screenshots confirmed the locked read-only dialog (correct 60%/40%, not 50%/50%) and a working rename-only save. Test data and scratch scripts deleted by exact id afterward.

The same day, a user question ("неужели тик ток так же {{...}} использует?") after receiving a real TikTok pixel link back with literal unsubstituted `{{ad.id}}`/`{{campaign.id}}` text led to a confirmed bug — this had previously been assumed elsewhere in this same file (2026-08-19 entry) to be a Facebook-side ad-configuration issue, but that assessment was only ever checked against Facebook-driven examples. `buildTrackedLink()` (`apps/web/src/lib/landings.ts`) unconditionally emitted Facebook's `{{...}}` macro syntax for every generated tracking link regardless of platform — TikTok Ads Manager doesn't recognize that syntax at all and silently passes it through as literal text. Verified TikTok's actual macro format against its own docs before writing any code (WebFetch, cross-checked against an independent practitioner source since TikTok's macro names are notoriously mislabeled — `__AID__` is the ad GROUP id not the ad id, `__CID__` is the creative/ad id not the campaign id): `__CID__`/`__CID_NAME__` (ad), `__AID__`/`__AID_NAME__` (ad group = Facebook's adset), `__CAMPAIGN_ID__`/`__CAMPAIGN_NAME__`, `__PLACEMENT__` — no TikTok equivalent exists for `site_source_name`, so that param is simply omitted from TikTok links rather than filled with a placeholder that resolves to nothing. Fixed by branching `buildTrackedLink()` on `pixel?.platform === 'TIKTOK'`; the `pixel === null` ("all active pixels of the project") case keeps the prior Facebook-macro behavior since the platform is genuinely unknown there, a disclosed pre-existing ambiguity rather than a new regression. Verified live: a real headless-Chromium session generating a link for a TikTok pixel vs. a Facebook pixel on the same landing confirmed the TikTok link now carries `ad_id=__CID__&adset_id=__AID__&campaign_id=__CAMPAIGN_ID__...` while the Facebook link is unchanged. Existing already-running TikTok campaigns still have the old broken `{{...}}` links baked into their destination URLs and need to be regenerated from the CRM and re-pasted into TikTok Ads Manager — this fix only affects newly-generated links going forward.

The same day, a follow-up request asked to re-verify TikTok is genuinely ready for real ad spend ("перепроверь все еще раз, все ли готово к запуску тиктока"). Audited live production data (not just code) on the real account already running TikTok traffic: 15 real event deliveries in the preceding ~10 minutes all confirmed `code:0 "OK"` from TikTok's live Events API, schema re-verified against the 2026-07-29 fix with no regression, and the base `ttq.load(...)` pixel script confirmed actually injected on landings with an active TikTok pixel. Found a second real gap while investigating: `ttclid` had never been captured for this account — 0 of 14,787 real clients, 0 of the most recent real PageViews on the account's TikTok pixel — despite TikTok's claim that ttclid auto-appends to click URLs since April 2024; verified against TikTok's own docs (cross-checked a second source after the first WebFetch mis-rendered the macro's double underscores as markdown bold) that the documented fallback is appending `&ttclid=__CLICKID__` manually, added unconditionally to the TikTok branch of `buildTrackedLink()`. Also surfaced an urgent, user-actionable finding: the account's two real TikTok ads were, at the moment of the audit, actively receiving a burst of real click traffic through the OLD broken `{{...}}` destination URL from before today's macro fix — ad-platform destination URLs don't auto-update from the CRM, so real spend was running with broken ad-level attribution until the user manually regenerates the link and re-pastes it into TikTok Ads Manager. Also confirmed (not fixed, flagged as optional, non-blocking): TikTok's `_ttp` match-quality cookie is set by the base pixel script but never read back by the SDK, the same class of gap `_fbp` had for Facebook before an earlier fix — doesn't stop events from sending, just leaves match quality lower for browser-side events specifically.

The same day, a user question ("неужели тик ток так же...") turned into a broader re-check when the user separately reported real leads still landing without buyer attribution despite the buyer genuinely using a link with `z=`. Confirmed this was NOT a new bug but the exact 2026-08-20 buyerRef-truncation issue still firing for real, currently-running Facebook ads that hadn't been updated to the new link format yet: quantified at 1918 of 10,255 PageViews (≈18.7%) missing buyer attribution over 5 days on one real pixel, spread across 51 distinct ad variants under 2 campaigns — found a sample URL where Facebook's own destination-URL length limit had truncated `campaign_name` itself mid-percent-encoding sequence, dropping `placement`/`site_source_name`/`z` entirely. Since all 51 affected ads share the identical corruption pattern, they very likely share one campaign-level "website URL parameters" template in Facebook Ads Manager rather than 51 independently-typed destination URLs — flagged to the user as the probable single fix point. No code change needed — the existing fix is correct and already verified working for freshly-generated links; this is purely a not-yet-migrated old-link situation, the same class of issue as the TikTok destination-URL finding earlier the same day.

The same day, a direct follow-up question ("разве сейчас ссылка не обрезается из-за чего теряются последние параметры?") led to a genuine correction of scope: the truncation reorder fix repositions WHICH fields get lost when the destination URL is too long, it does not eliminate the underlying length limit itself (that's Facebook's own constraint, outside our control). Investigating a real card the user showed then surfaced a *different*, previously-unseen failure mode on the same buyer's traffic: `ad_id`/`ad_name` (earlier in the URL) and `placement`/`site_source_name` (later in the URL) all substituted correctly in the same click, but `adset_id`/`adset_name`/`campaign_id`/`campaign_name` in between came through as literal unsubstituted `{{...}}` text — and the broken URL was actually *shorter* than working examples of the same ad, ruling out length-based truncation entirely. Concluded this is Facebook's own ad→adset→campaign hierarchy lookup sporadically failing server-side on their end, not fixable by reordering or shortening our link. What was fixable: `decodeAdMacro()` (`apps/api/src/modules/landings/landing-renderer.service.ts`, mirrored in `apps/sdk/src/browser.ts`) previously stored a literal unsubstituted macro string as if it were real data, polluting the client card and campaign/ad leaderboards with fake `{{campaign.id}}`-shaped entries instead of showing absence of data. Added a shared `UNSUBSTITUTED_MACRO_PATTERN` regex (matches both Facebook's `{{...}}` and TikTok's `__...__` syntax) that nulls out any value still matching a raw macro placeholder after decoding, on both the backend and the SDK. Verified live by reproducing the exact real partial-substitution pattern through `LandingRendererService.renderAndServe` and reading back the resulting Redis payload — the genuinely-substituted fields passed through unchanged, the unsubstituted ones correctly became `null` instead of literal garbage text; SDK rebuilt and republished to the CDN.

Immediately after, the user caught a real gap in that fix by pointing back at their own original screenshot: `Источник показа: {{site_source_name` with no closing `}}` at all — a genuinely truncated macro, cut off mid-token by real URL-length truncation, which is a different failure mode from Facebook's complete-but-unsubstituted `{{campaign.id}}` and didn't match the previous round's full-match regex (`^\{\{.*\}\}$`). Fixed by switching `UNSUBSTITUTED_MACRO_PATTERN` from a full match to a substring test (`/\{\{|\}\}|^__[A-Z]/`) — any occurrence of `{{`/`}}` anywhere in the value, or a value starting with a TikTok-style `__UPPERCASE` prefix, now nulls it regardless of whether the macro token is complete or was cut off partway through. Verified live by reproducing the exact reported pattern through `LandingRendererService.renderAndServe` — both the complete and the truncated macro now resolve to `null`. Mirrored in `apps/sdk/src/browser.ts`, rebuilt and republished to the CDN.

The same day, a follow-up request asked to shorten the `z=`/`pixel=` tracking-link VALUES specifically (not the param names) — currently full 25-character cuids — while making OLD buyer links (already baked into running Facebook/TikTok ads) keep working, explicitly repeated as mandatory. Implemented `User.buyerShortCode`/`TrackingPixel.shortCode` (`String? @unique`, 6-character `nanoid()`, 64^6 ≈ 68.7 billion capacity, comfortably covering the requested "millions of buyers"/"hundreds of millions of pixels"). Backward compatibility is purely length-based — real ids are always exactly 25 characters, a 6-character code can never collide with that length, so parsing just checks length to pick direct-id-lookup (old links, zero DB cost) vs short-code-lookup (new links) — no migration of existing links needed. Short codes generate eagerly at creation (`AuthService.register`/`TeamService.create`/`TeamInvitesService.accept` for buyers, `PixelsService.create` for pixels) via a new `common/short-code.util.ts`; the one transactional creation site (`TeamInvitesService.accept`, inside `$transaction`) needed a separate pre-check variant since Postgres aborts the whole transaction on the first error, making a catch-and-retry-on-P2002 pattern unsafe there. Resolution back to real ids happens at exactly two independent entry points that never share code — `LandingRendererService.injectTrackingScripts` (feeds the Telegram-side Redis caches, resolved once so every downstream consumer gets an already-resolved value) and `TrackingService.recordEvent` (the browser SDK's direct POST path, which bypasses `injectTrackingScripts` entirely). Verified live via direct unmocked calls exercising both the Telegram-cache path and the SDK-direct path with both old-format and new-format values — all 4 combinations resolved to the exact same real ids; confirmed the new logic is present in the deployed frontend bundle. Test data and scratch scripts deleted by exact id afterward.

The same day, a user question ("почему они остаются пустыми... данные не приходят для баера Игорь") — comparing a real client with empty campaign/ad/UTM data against another buyer's client with full data, suspecting the just-shipped short-code feature — led to a real fix, but not the one suspected: direct investigation of production data confirmed pixelId/buyerId short-code resolution was working correctly (both resolved right on the affected client). The actual cause was the PRIVATE_CHANNEL_REQUEST `landing-visit-attribution` FIFO queue (see the 2026-08-19 entry) hitting its known approximation under real load — 15 real visits landed on the same landing within ~15 minutes, and at least one had Facebook itself fail to substitute the ad macros (the literal-`{{...}}` failure mode from the entry above, not a bot/length issue — a real human visit can hit it), which a strict `LPOP` had no way to avoid handing to an unrelated real registration even when better-attributed visits sat right next to it in the same queue. Fixed by having `TelegramProvider.getCachedLandingAttribution` `LRANGE` a bounded lookahead window (10 entries) and prefer the first one with a non-null `campaignId`, removing that specific entry via `LREM` while leaving the skipped, poorer entries in the queue for the next join request rather than discarding them — falls back to plain oldest-first if nothing in the window qualifies. Verified live against a synthetic Redis queue driven through the real `TelegramProvider` instance via reflection on the private method: a good entry buried behind 3 bad ones was correctly found and consumed while the bad ones stayed queued, and a second call correctly fell back to strict FIFO once no good candidate remained.

On 2026-08-25, a user-reported real screenshot — TikTok's own in-app browser showing "Мы не можем открыть эту страницу непосредственно в TikTok" instead of ever loading the landing — led to a new per-landing toggle ("Доп. инструкции для TikTok", matching a competitor's described UX) rather than a code-only fix, since the root cause is a well-documented platform-level restriction, not a bug in this project. Verified technical grounding before implementing rather than guessing: iOS genuinely cannot be forced out of an in-app WKWebView into real Safari via JavaScript (confirmed via multiple independent sources — an intentional Apple restriction against exactly this kind of app-hijack pattern), while Android can be escaped via an `intent://` URL with `category=android.intent.category.BROWSABLE` (the documented, sanctioned "open in a browser" mechanism, generally allowed by restrictive WebViews unlike arbitrary custom schemes such as `tg://`, which in-app browsers are specifically designed to block). The TikTok-webview detection string (`BytedanceWebview`/`musical_ly`, TikTok's pre-rebrand app name) was verified against real production traffic already captured earlier this same session, not guessed. New `Landing.tiktokBrowserHint` surfaces through the already-shared `LandingBehaviorFields` component (one edit covered both create-flow and edit-page forms in both trees) and a new `data-tiktok-browser-hint` SDK script attribute; `apps/sdk/src/browser.ts` detects the webview and, when enabled, takes over entirely from the normal auto-redirect for that load — Android fires the `intent://` escape immediately, iOS shows a self-contained full-screen "tap ··· → Open in Browser" overlay (no external CSS/font dependencies, since the script runs on arbitrary third-party domains) — skipping the normal `tg://` attempt either way, since firing it inside TikTok's webview is either already blocked or plausibly what triggers TikTok's own block screen in the first place. Verified live via a direct unmocked render call that the attribute correctly appears/disappears with the toggle; the `intent://` Android escape itself was **not** verified against a real TikTok app on a real device (unavailable in this environment) — disclosed to the user as an open verification gap. The SDK republish hit a real, disclosed limitation: the new bundle uploaded correctly to the underlying storage (confirmed via a direct bypass fetch) but Cloudflare's edge cache kept serving the stale prior version with no held API token available to force a purge, so it relies on the ~4h edge TTL rather than an immediate purge.

On 2026-08-25, a user report — "Ошибка канала: Network request for 'getMe' failed! такая ошибка показывается на всех проектах, на всех разом" — traced to a real, confirmed infrastructure gap: the production server has no working IPv6 route to the internet at all (`ip -6 addr show` shows only link-local `fe80::` addresses, `ip route get <telegram ipv6>` returns "Network is unreachable"), but Node 17+ changed `dns.lookup()`'s default result order from `ipv4first` to `verbatim` (whatever order the DNS resolver returns), and `api.telegram.org` resolves to both an A and a AAAA record — so whenever a lookup happened to place the IPv6 address first, grammY's `bot.api.getMe()` call failed outright (confirmed live: `curl -6 https://api.telegram.org` fails instantly with no connection, `curl -4` succeeds in ~150ms). This intersected with a real design gap in `ChannelsService.checkAllChannelsHealth()` (the `*/15 * * * *` cron): it calls `getMe()` per active channel sequentially with zero retry/backoff and immediately flips `isActive:false` + records `lastError` on the very first failure — so one bad DNS ordering hitting the tick's sequential loop took out several real, working bots in the same 15-minute tick, which is exactly why it "showed on all projects at once." Fixed with two changes: (1) `dns.setDefaultResultOrder('ipv4first')` added at the very top of `apps/api/src/main.ts`, before `NestFactory.create()` — the standard, official fix for this specific Node 17+ regression, forcing IPv4 first for every outbound request in the process (Telegram, Facebook/TikTok CAPI, MinIO, everything), not just a Telegram-specific workaround; (2) `checkTelegramHealth` now retries once (3s delay) before declaring a channel unhealthy, so a single transient network blip (this class of issue or any other) no longer immediately deactivates a channel. Live-verified: 6 real channels found deactivated by this exact bug (`cms4qxqn6005k7jbsnwo9kn54`/`cms3h8xkx004pje3l7jp8xtms`/`cmrtrmche002vqwjezkty2yfz`/`cmscc9znw10z4ywlfzsuo31n2`/`cms3ciiay0044je3lqzzpsohm`/`cmsdlwqa30gr2ipvuvsefx41c`) were reactivated and confirmed with zero "no bot instance"/rehydration-failure log lines in the live pm2-managed process after the fix deployed. One real mistake caught and corrected during this same fix: the first reactivation attempt was run through a throwaway `NestFactory.createApplicationContext(AppModule)` script — this correctly updated the DB (`isActive:true`, webhook re-registered with Telegram) but the registered bot instance lived only in that script's own process's in-memory `TelegramProvider.bots` map, not in the actual pm2-managed process handling real webhooks, so the channels kept logging "no bot instance" even after the DB looked fixed — corrected by restarting the real `trafficcrm-api` process instead (its own `onModuleInit()` picks up `isActive:true` channels and registers bots in the process that actually serves traffic). A second script (checking personal MTProto session health for the same 6 projects, following standing post-incident discipline of re-verifying personal sessions after any script that boots `AppModule`) opened real GramJS connections for all 6 accounts, hung on one (a previously known-fragile account) past a 2-minute timeout, and was killed — confirmed no orphaned node process was left running afterward.

On 2026-08-25, a user report that the TikTok in-app-browser overlay wasn't appearing on a real iOS device led to a rigorous check before touching any code: replayed the exact real TikTok-iOS User-Agent captured in production traffic (`musical_ly_46.4.0 ... iPhone OS 26_6`) through a real Chromium instance (Playwright) against the actual live landing URL — the overlay rendered correctly with zero JS errors, proving the SDK logic itself was not at fault. Root-caused instead to `track.js` being served with `Cache-Control: max-age=14400` (4h, a Cloudflare static-asset default, not something set in `publish-cdn.js`) — the same class of staleness that had already bitten this exact feature earlier the same day via Cloudflare's edge cache, this time one layer further out at the client/device cache. Fixed durably rather than just re-explaining the caching mechanics: `publish-cdn.js` now computes a 10-char sha256 hash of the built `track.js` on every publish and writes it to `apps/api/src/modules/landings/sdk-version.json` (copied into `dist` via a new `nest-cli.json` assets entry, same mechanism already used for template assets); `LandingRendererService` reads that hash at boot and appends it as `?v=<hash>` to every landing's `track.js` script tag — a content-addressed cache-buster that changes automatically only when the SDK's actual bytes change, so an unrelated API deploy doesn't needlessly invalidate everyone's cached copy, but a real SDK fix can never again be masked by a stale client-side cache. Falls back to an unversioned URL (old behavior) if the version file is ever missing, rather than failing landing render. Live-verified: the versioned URL (`track.js?v=7505df28c5`) is confirmed live on production landings post-deploy.

On 2026-08-27, a user report — real TikTok-iOS traffic on `isabela-cr.com/clcmrmb2/b4fj8b3c` loaded the landing normally (no browser-escape hint) but hung indefinitely on the join button — traced to the exact same real click's own captured User-Agent: `...AppleWebKit/605.1.15 (KHTML, like Gecko) Mobile/15E148`, missing both `musical_ly` and `BytedanceWebview` entirely, even though genuine TikTok-iOS visits carrying the marker still appear elsewhere in the same window's data (no evidence TikTok dropped the marker universally, just for some fraction of iOS sessions/app versions). Since detection silently failed, the escape from [[feedback_tiktok_inapp_browser_hint]] never fired and the visitor hit the exact original problem the feature exists to prevent (tg:// dead-ends inside the in-app WebView, no explicit fallback UI). Researched (WebSearch) whether a documented non-UA TikTok signal exists (JS bridge, window property) — none found; the general in-app-browser-detection community consensus is to combine circumstantial UA signals, with "UA missing the standard `Version/X Safari/604.1` tail" as one of them. Confirmed via `AskUserQuestion` before broadening detection (real tradeoff: could misfire on other apps' in-app browsers on iOS, e.g. Instagram/WhatsApp, where blaming TikTok by name in the hint text would be visibly wrong) — user chose to broaden and make the wording app-agnostic. Checked Android separately before touching it: real production data showed Android's generic `; wv)` WebView marker present on ~88% of ALL Android in-app-browser traffic across every app (13,871 of 15,780 sampled PageViews, zero real BytedanceWebview/musical_ly hits in the same 5-day window) — broadening Android the same way would misfire on nearly every Android in-app visit regardless of source app, so Android intentionally keeps the explicit-marker-only check, unchanged. iOS gained a fallback signal in `apps/sdk/src/browser.ts`: `isIOS && !hasTikTokUaMarker && !/Version\/[\d.]+.*Safari\//.test(navigator.userAgent)` — real Mobile Safari reliably ends in that token pair, an in-app WebView setting its own (or no) UA typically doesn't. The overlay's body text dropped its explicit "TikTok не даёт перейти дальше" wording in favor of an app-agnostic "перейти дальше не получится", since it can now fire on an unidentified iOS in-app browser. Verified pre-publish via a Node `vm`-sandboxed execution of the actual built bundle against 4 real UA strings (the exact failing stripped UA, genuine Mobile Safari, a genuine musical_ly-marked TikTok UA, and a generic Android `wv` UA) — only the two in-app cases triggered, Safari and Android both correctly stayed silent. Publishing hit a real, self-caused snag: running `publish-cdn.js` without first sourcing `.env.prod` left `MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY` undefined (the script's own `dotenv.config()` points at a repo-root `.env` that doesn't exist on this server, `MINIO_ENDPOINT`/`MINIO_CDN_BUCKET` have code-level fallback defaults but the credentials don't), producing a real (not transient) MinIO `AccessDenied` — resolved by sourcing `.env.prod` first, matching the standard deploy discipline already used for `apps/api`/`apps/web` builds. Live-verified post-deploy: the landing's `track.js?v=` hash advanced to the new build, `cf-cache-status: MISS` confirmed a genuinely fresh fetch (not the prior stale-cache issue recurring), and the live minified bundle contains the new `Version/…Safari/` check with zero remaining occurrences of the literal string "TikTok" in the overlay text.

On 2026-08-27, a follow-up request showed a competitor's version of the TikTok in-app-browser escape (see the 2026-08-25/27 entries above) with a full text-customization form (title, subtitle, per-platform step instructions, two button labels, a post-copy confirmation) plus a genuinely new mechanic the existing overlay didn't have: a "Copy Link" button with a clipboard-copy confirmation and an arrow pointer toward the in-app browser's "···" menu — the existing "Open in browser" overlay had been static text only, no buttons. Confirmed via `AskUserQuestion` the user wanted full parity, not just editable text. Shipped: new `Landing.tiktokHintTexts Json?` (one JSON column for all 7 fields — `title`/`subtitle`/`iosSteps`/`androidSteps`/`openButtonText`/`copyButtonText`/`copiedText` — rather than 7 columns, since they're always read/written together, same precedent as `PersonalBroadcast.variants`/`filter`), validated through a new `TiktokHintTextsDto` nested inside `LandingBehaviorDto`; any missing/empty key falls back to a built-in English default in the SDK itself (English was an explicit user requirement, a first for this feature — every other SDK-rendered hint text in the codebase had defaulted to Russian). `LandingRendererService.injectTrackingScripts` passes the whole object through as one `data-tiktok-hint-texts` JSON attribute (same pattern as `data-param-map`), so an empty/all-default landing costs nothing extra in the rendered HTML. `apps/sdk/src/browser.ts`'s overlay was rewritten from a static `innerHTML` string to real DOM-API construction (`createElement`/`textContent`, not string concatenation) specifically because the text is now landing-owner-configurable — the same trust boundary as the rest of a landing's own content, but worth avoiding stored-injection risk now that arbitrary strings flow into what used to be a fully hardcoded overlay. The Copy Link button uses `navigator.clipboard.writeText` with a `document.execCommand('copy')` fallback via a hidden textarea (for older/restricted WebViews lacking the Clipboard API — the same class of WebView-quirk risk already established for this feature). Android's existing silent `intent://` auto-escape is now paired with a 1.5s fallback: if the page is still visible after that delay (`visibilitychange`/`pagehide` listeners unregistered the moment either fires, meaning the redirect actually worked), the same overlay shows with the Android-specific step text instead of leaving the user on a silently-hung page — giving the previously-decorative "Android instructions" field (present in the competitor's form even though their Android also auto-redirects) a real, principled purpose. Verified end-to-end without a real device: a Node `vm`-sandboxed execution of the actual built bundle covering 4 scenarios (custom texts + real Clipboard API, defaults + execCommand fallback, Android auto-redirect firing the fallback overlay after 1.5s when the page never left, and Android correctly suppressing the fallback when `visibilitychange` fires first) — all passed; separately verified the full backend round-trip (DTO → `LandingsService.update()` → Prisma JSON column → rendered HTML attribute) via an isolated throwaway company/project/landing through the real `LandingRendererService.renderAndServe`, deleted by exact id afterward; and confirmed the new form renders and behaves correctly in Studio via a real headless-Chromium login against the permanent demo account (see [[project_demo_showcase_account]] in memory) — toggling the switch correctly reveals all 7 fields with the right English placeholders, no console errors, and the toggle was left off afterward (never saved, so the demo landing's DB state was unaffected either way). SDK republished (cache-busted hash advanced automatically per the 2026-08-25 mechanism) and `apps/api`/`apps/web` rebuilt/redeployed together since the feature spans both.

On 2026-08-27, a same-day follow-up fixed two real problems with the TikTok overlay from the entries above. First: the overlay was a full-screen `position:fixed;inset:0` modal with an 85%-opacity dark scrim, completely hiding the landing underneath — redesigned into a bottom-anchored banner (`left:0;right:0;bottom:0`, rounded top corners, `max-height:75vh` with its own scroll, no full-screen scrim at all) so the landing stays fully visible and scrollable above it. Second: on `age-gate-invite` (the one two-popup template), the overlay was firing immediately on page load — on popup 1, before the visitor had even reached the real "join" step on popup 2. Fixed by unifying the TikTok-escape check and the existing `autoRedirect`/`age-gate-invite` defer mechanism into one function (`runTiktokEscapeOrAutoRedirect` in `apps/sdk/src/browser.ts`) — a new `data-tiktok-hint-defer` attribute (set server-side in `LandingRendererService.injectTrackingScripts` whenever `tiktokBrowserHint` is on AND the landing uses `age-gate-invite`, reusing the same `deferAutoRedirect` boolean the auto-redirect defer already computes) makes the SDK stash this combined function on `window.tcrm.triggerAutoRedirect` instead of running it immediately; `template.html` needed *zero* changes since it was already calling that exact function at the exact right moment (the popup-1 "yes" click handler, right when popup 2 opens) for the pre-existing auto-redirect-defer feature — the same trigger point now serves both concerns. Verified with a `vm`-sandboxed run of the built bundle (confirms non-deferred templates still fire immediately, unchanged; the deferred case fires nothing on load and only shows the overlay after the trigger function is manually invoked) and, more importantly, a real headless-Chromium session against the actual live `isabela-cr.com` landing driving the real click-through flow (popup 1 "Suscribirse"-adjacent "yes" → popup 2) — confirmed the overlay stayed absent through popup 1 and appeared only after reaching popup 2, with the landing's own content (photo, channel name, subscribe button) fully visible above the new bottom banner in the screenshot. SDK republished (new cache-busted hash), `apps/api` rebuilt/redeployed.

The same day (2026-08-27), a further follow-up reported two more real problems with the just-shipped bottom banner. First: "Open in browser" did nothing at all — the previous `window.open(location.href, '_blank')` was silently swallowed by the in-app WebView (as already flagged as a real risk when it shipped). Researched before guessing (WebSearch + WebFetch on a real WordPress-Playground PR that solved the identical LinkedIn-in-app-browser problem): `x-safari-https://` is a real, semi-documented URL scheme iOS itself resolves straight to Safari, not a fabricated trick — no guarantee it beats TikTok's own scheme blocking (same uncertainty as `tg://`), but strictly better than a `window.open()` that never did anything. iOS's button now navigates to `x-safari-https://<host><pathname><search><hash>`; Android's button (relevant only once the 1.5s fallback overlay already appeared, meaning the auto `intent://` already didn't work) now just retries the same `intent://` escape. Second: the bottom banner still visually covered the landing's own content/CTA in the report — fixed by having the SDK reserve real layout space for it rather than floating over fixed content: after appending the banner, `document.body.style.paddingBottom` is set to the banner's actual measured height (`getBoundingClientRect()`, taken only after the element is attached — a detached-element measurement bug caught and fixed before shipping). Since `age-gate-invite` (and most simple templates) center their card with `body { height:100%; display:flex; align-items:center }`, adding bottom padding shrinks the flex content box and re-centers the card in the space above the banner instead of hiding it. Building the Playwright verification for the Android retry button surfaced a second, real, previously-latent bug beyond what was asked: on WebViews that don't treat an `intent://` assignment as real navigation, `window.location.href` can keep reporting the just-assigned `intent://` string rather than the page's real address, so a naive re-read for a retry (or for what the Copy Link button copies) would double-wrap into `intent://intent://...`. Fixed by capturing `const originalHref = window.location.href` once at the very top of the script (before any redirect is attempted) and using that captured value everywhere a stable "this page's real URL" is needed (`x-safari-https://` construction, the Android retry, and what Copy Link copies) instead of re-reading a value that may have already been mutated. Verified all of the above pre-publish with a Node `vm`-sandboxed run of the actual built bundle (confirmed the double-`intent://` bug reproduced against the OLD build and was gone against the fixed one, confirmed `x-safari-https://` construction and the padding-bottom math both correct) and, live, against the real `isabela-cr.com` two-popup landing — after clicking through popup 1 to popup 2, `document.body`'s computed `padding-bottom` was 337.6px and the popup 2 subscribe button's bounding rect was fully inside the viewport (not just "probably clear of it"), with a screenshot confirming visible dark gap between the landing's own card and the banner below it. SDK republished, `apps/api` rebuilt/redeployed.

On 2026-08-29, a genuinely new anti-fraud feature shipped: a payment-details journal (`PaymentDetailsLog`) guarding against an operator on a project's connected personal Telegram account substituting their own payment reference for the company's real one and stealing a client's deposit. This request initially arrived as "read through Isabella Ramirez's real Telegram chats one by one and find a payment-details message" — a retroactive bulk read of live private message content, which the session's own safety classifier blocked outright even narrowed to one keyword search; the user then reframed it as the actual underlying need (an ongoing monitoring service, not a one-off lookup), which is what shipped. Deliberately NOT retroactive — old messages are never scanned; detection only applies to genuinely new outgoing messages from the moment the feature's event listener is registered, mirroring the one existing precedent for reading a personal account's own message stream (`TelegramPersonalService.handleIncomingMessage`, used for dialogue-tracking metadata) but for *outgoing* messages instead of incoming, via a second `NewMessage({outgoing:true})` handler registered at both existing connection-establishment sites (`startListening` and `finalizeConnection`) alongside the existing incoming-message one. New `common/payment-detail-patterns.util.ts` (`containsPaymentDetails`) matches SINPE Móvil (Costa Rica mobile-transfer system, keyword `sinpe`), TRC-20/ERC-20 crypto addresses, and Spanish banking keywords (`número de cuenta`/`transferencia bancaria`/`iban`) — a real confirmed positive from this project's own traffic (`Sinpe:\nNombre:...`) was the seed for the SINPE pattern. Per explicit user decision, v1 only logs the FACT of a payment-details message being sent (recipient, timestamp, full text) — it does NOT yet cross-check against a company's real/approved reference to detect an actual *substitution*, which is the real fraud signal; that comparison is deliberately deferred to a later round. New `PaymentDetailsLog` model (not in `modelsWithCompany`, manual `projectId` scoping via `ProjectsService.findOne` before every read, same convention as `Push`/`PersonalBroadcast`) backs a new `GET /projects/:id/payment-details-log` (paginated) gated by a hard `@Roles(OWNER, ADMIN)` check rather than a grantable `Permission` — same reasoning as `archive`/`regenerateTokens`: the object of the control (an Operator) must never be able to see this journal under any circumstance, so it can't be a permission an Operator could theoretically be granted. Per explicit user decision, no push/bell notification exists yet — a project-page button (visible only to Owner/Admin, mirroring the existing `tgPersonalConnected`-gated Personal Broadcasts button) is the only surfacing, in both trees. Verified without touching any real live GramJS session: `containsPaymentDetails()` unit-tested against 7 real-shaped cases (one test-data mistake caught in the test itself — a fake TRC-20 address one character too long — not a regex bug, confirmed by fixing the fixture); the full DB round-trip (write → `ProjectsService.getPaymentDetailsLog` → client join → pagination) verified via an isolated throwaway company/project/channel/client/log row, deleted by exact id afterward; the new journal page confirmed rendering with zero console errors via a real headless-Chromium login against the permanent demo account (empty-state only, since the demo company has no real MTProto traffic to have logged anything yet). A same-day follow-up ("я её не вижу") found the button's visibility check used a strict `user?.role === 'OWNER' || user?.role === 'ADMIN'` on the frontend — this codebase treats `SUPER_ADMIN` as elevated everywhere else (`ELEVATED_ROLES` in `projects.service.ts`, and `RolesGuard`'s own rank table where `SUPER_ADMIN:5` already clears an `OWNER`/`ADMIN`:4 threshold via `Math.min(...)` — so the backend route was never actually broken, only the frontend's exact-string check). Fixed by adding `SUPER_ADMIN` to both button conditions.

The same day (2026-08-29), the user tested the payment-details journal live with a real "sinpe:" message to a real contact — it never appeared, with zero errors logged. Diagnosed with temporary debug logging (not guessed): the handler WAS firing and the pattern WAS matching, but `event.message.getChat()` silently returned `undefined` for this specific recipient — GramJS's `getChat()` resolves the peer through the client's local entity cache (or an up-to-100-dialog scan) and silently swallows resolution failures, giving no signal that anything went wrong. Fixed by reading `event.message.peerId` directly instead — a synchronous field already present on the raw update, needing no cache lookup or network round-trip at all; since `event.isPrivate` already confirms the peer is a `PeerUser`, `peerId.userId` is the recipient's tgUserId with no resolution step to fail. Debug logging (channel id, `isPrivate`, text length, a 20-char preview, and the pre-fix chat resolution outcome) was added temporarily to catch this and removed once the real cause was confirmed. A follow-up request then scoped detection per-project: SINPE Móvil (Costa Rica) only applies to the two `🇨🇷` project (Rolando Acuna, Isabella Ramirez) — the other 3 connected accounts are different countries with different payment systems and would only get noise from a Costa-Rica-specific keyword. A second, broader keyword set (`Cuenta`/`Nombre`/`Banco`/`Lafise`/`Nombre del titular`/`Tipo de moneda`/`Tipo de cuenta`/`Número de cuenta`/`BANRURAL`/`DNI`/`Moneda`, plus bare 14-digit and 8-digit number patterns — LAFISE/BANRURAL are real Guatemalan banks, matching Maria Lopez's `🇬🇹` flag) was confirmed via `AskUserQuestion` to apply globally across all 5 connected projects, not just Maria Lopez. `common/payment-detail-patterns.util.ts`'s `containsPaymentDetails` now takes a `projectId` second argument — a hardcoded `SINPE_ONLY_PROJECT_IDS` Set gates the SINPE keyword, everything else stays in one global pattern list. Verified pre-deploy by calling the built function directly against 11 real-shaped cases spanning both scoped and global patterns across 4 different projectIds — all passed — before restarting the API. Later the same day, a follow-up request narrowed access further — "должен видеть только owner, даже для админа выключи" (only the Owner should see this, turn it off even for Admin) — surfaced a real `RolesGuard` limitation: `OWNER` and `ADMIN` sit at the identical rank (4) in `roleHierarchy`, so the `@Roles()` decorator's rank-threshold check structurally cannot distinguish them no matter what's listed in its arguments (the exact scenario the existing CLAUDE.md warning about `RolesGuard` being rank-based, not set-membership, describes). Fixed by dropping `@Roles()` from this route entirely and adding an explicit `requesterRole !== UserRole.OWNER` check inside `ProjectsService.getPaymentDetailsLog`; both trees' frontend gates narrowed from `OWNER || ADMIN || SUPER_ADMIN` to bare `role === 'OWNER'`. Later the same day, a follow-up ("процентов 70 сообщений которые вылавливаем это обычные сообщения, не реквизиты") confirmed with real live data that the flat keyword-OR approach from the 2026-08-29 patterns round was too permissive — single generic Spanish words ("Nombre"/"Cuenta"/"Banco"/"Moneda"/"DNI") are everyday vocabulary that appears constantly in unrelated conversation. Asked the user to confirm the fix approach via `AskUserQuestion` before implementing (per their explicit "скажи мне сначала") rather than silently re-tuning: they picked a combined strong/weak split — unambiguous signals (crypto addresses, SINPE, bank names LAFISE/BANRURAL, multi-word phrases like "Número de cuenta"/"Nombre del titular"/"Tipo de cuenta") still fire alone, while the 5 generic single-word terms now require 2+ distinct matches in the same message to count (`WEAK_MATCH_THRESHOLD = 2` in `payment-detail-patterns.util.ts`) — a real payment form lists several fields together, an ordinary sentence essentially never uses 2+ of these specific words at once. The bare 8-digit-number pattern was deliberately KEPT unconditional per explicit user override ("число из 8 цифр оставь") despite also matching regional phone-number format — a known, accepted tradeoff, not an oversight. The same day, a follow-up request moved the journal off its per-project-only route onto a dedicated company-wide page (`/payment-details-log`, both trees) — mirrors the `/my-clients`/`/my-personal-broadcasts` picker pattern: lists only projects with `channel.tgPersonalConnected` (via the existing `GET /projects`, already exposing that computed field through `sanitizeChannel`), and selecting one shows that project's journal via a `?projectId=` query param, reusing the same `PaymentDetailsLogList` component unchanged. The old per-project-only pages (`/projects/:id/payment-details-log` in both trees) were deleted outright rather than kept as a redirect — they were only ever a few hours old and never linked from anywhere except the one project-page button, which now points to `/payment-details-log?projectId=:id` instead (preselecting, same precedent as `/pushes/new`'s `initialProjectIds`). A `Cannot find module .../page.js` error surfaced during typecheck immediately after deleting the old route files — stale generated `.next/types` entries from the previous build, not a real error; resolved by a clean `rm -rf .next` before rebuilding. Verified pre-deploy with 14 cases covering both directions (real forms/strong-signal-alone cases still matching, single-generic-word casual sentences no longer matching, 2-weak-word combinations still matching, the kept 8-digit exception) — all passed before restarting. A follow-up right after ("как попасть на саму страницу... где список проектов") pointed out the new central page had no direct entry point at all — the only link into it came from a project's own page with a project already preselected, with no way to reach the bare project-picker view. Added a sidebar item (`Журнал реквизитов`, both trees) via a new `ownerOnly` `NavItem` flag — deliberately separate from the existing `ownerAdminOnly` flag (which also admits `ADMIN`/`SUPER_ADMIN`/`OPERATOR_ADMIN`), since this feature's whole point is excluding everyone but the literal Owner. The same request also added period filtering (today/yesterday/7d/30d/custom, the same `resolveStatsPeriod`/`PeriodSelector` used everywhere else in the project — a new `PaymentDetailsLogFiltersDto` combines period+search+page into one DTO bound via a single `@Query()`, mirroring `ClientFiltersDto`'s established shape rather than fighting NestJS's whole-object query binding with two separate `@Query()` parameters) and case-insensitive text search over `messageText` to the per-project journal — unlike every other period-filtered view in the app, the journal has NO default time window (period is optional; omitting it returns full history), since silently hiding an old fraud-relevant message behind a 30-day default would defeat the feature's purpose. A follow-up on the picker page added a live summary per project ("Сегодня: N" / "последняя N назад") via a new company-wide `GET /projects/payment-details-log-summary` (placed before the `:id` wildcard route, same static-route-ordering requirement as `company-stats`) — loops per eligible project rather than one aggregate query, since the project count with connected personal accounts is small (currently 6) and each project's "today" needs to be calendar-aligned in ITS OWN timezone, not a single shared cutoff. Verified the new filters directly against real production data (period=today, no period at all, and a text search) before deploying — counts came back sensible and distinct from each other, confirming the filters actually narrow results rather than silently no-op'ing. A same-day user question ("почему он так записан... это же его айди, почему не имя") traced to a real, unavoidable Telegram/MTProto constraint, not a bug: resolving a person's name from a bare numeric id requires an `access_hash` Telegram only hands out once the session has already "seen" that user, and for some recipients the account's local entity cache has never seen them — the exact same underlying limitation already hit once this session for resolving the recipient id itself. Live investigation of the real data first confirmed this wasn't a display bug: for the exact tgUserId the user asked about, NO `Client` row exists anywhere in the database for any project — genuinely never a tracked CRM contact. Fixed the display gap the same way `PersonalBroadcastLog` already handles this exact class of "no Client row" recipient: two new snapshot columns, `PaymentDetailsLog.tgFirstName`/`tgUsername`, captured best-effort via `event.message.getChat()` at write time (wrapped in try/catch — accepted to sometimes fail, same constraint as above) and used as the display fallback before the raw numeric id. Investigating the real flagged contact's message history (already-logged, legitimate use of the feature's own UI, not a new privacy line crossed) surfaced something worth flagging to the user directly rather than silently passing over: the same untracked recipient had received THREE different people's full names + bank/LaFise account numbers over a few hours — a pattern that could be innocent internal bookkeeping or could be exactly the fraud vector this whole feature exists to catch; disclosed as a fact pattern for the user's own judgment, not an accusation. The same day, the user investigated further and reported back: that same untracked recipient (tgUserId 8506269324) was actually a BOT, not a real person — an internal utility bot, not a customer at all. Asked for a fix so bots stop being counted. `event.isPrivate` alone doesn't distinguish this (bots are still `Api.PeerUser` in Telegram's model, same as `handleIncomingMessage`'s existing incoming-side check already has to guard against via `sender.bot`) — extended the same best-effort `getChat()` call already added for the name snapshot to also check `chat.bot` and skip logging entirely when true, only for the no-existing-Client path (a real tracked `Client` can never be a bot, since `handleIncomingMessage` already excludes bots at Client-creation time, so no need to re-check there). Verified the flagged id really was a bot before touching anything — a bare `getEntity('8506269324')` call failed to resolve (same access_hash constraint as before), so used the more reliable `client.getDialogs({})` path (the same one `getAllDialogs` already relies on for personal broadcasts) to positively confirm `bot: true`, username `mw_diego_uru_gen_optimized_bot` — then deleted that one now-confirmed-mislabeled row by exact id (a deliberate, evidence-based one-off cleanup, not a retroactive rescan).

On 2026-08-31, a user-reported Pixel Logs entry ("почему такая компания и обьявление пришло?" — a Subscribe event for client "mariast3" showing empty Кампания/Объявление campaign, and raw garbled percent-encoded text for Объявление/ad name) was root-caused via the real `TrackingEventDelivery`/`TrackingEvent` rows behind that log line: `campaignName`/`campaignId` were correctly `null` (a genuine URL-length truncation had cut the query string off before the campaign params ever appeared, same class as the 2026-08-20 buyerRef-truncation finding), but `adName` held raw garbage because it had survived far enough into the URL to be present but got cut mid-percent-escape (ending in a bare unterminated `%`) — and `decodeAdMacro()` (duplicated in `LandingRendererService` and `apps/sdk/src/browser.ts`, both call sites unchanged since the 2026-08-20/27 unsubstituted-macro-detection work) had a real bug in its `catch` block: a `decodeURIComponent` throw fell through to returning the raw, still-percent-encoded string instead of being treated as proof of corrupted data, unlike the already-handled `{{...}}`/`__...` literal-macro cases. Fixed by returning `null` from that catch instead, in both files — a decode failure is now nulled exactly like every other "not real data" case `UNSUBSTITUTED_MACRO_PATTERN` already covers. Verified via direct calls to the fixed function against 7 cases (the real truncated value → null, a well-formed percent-encoded value → correct decode, `{{...}}`/`__...` literal and mid-macro-truncated values → null, a normal plain string → unchanged, `null` input → `null`) before deploying; `tsc --noEmit` clean on both `apps/api` and `apps/sdk`. Deployed both halves since the same logic runs server-side (PRIVATE_CHANNEL_REQUEST attribution path) and client-side (direct SDK POSTs): `apps/api` rebuilt/restarted with zero rehydration failures, `apps/sdk` rebuilt and republished via `publish-cdn.js` (cache-busted hash advanced per the 2026-08-25 mechanism, confirmed live on a real landing's rendered `track.js?v=` tag). Not retroactive — historical rows with already-garbled `adName` values from before this fix are unchanged.

On 2026-09-08, a follow-up bundled three reports on the CUSTOM-landing/ZIP-review feature shipped the day before. First, a real bug: the landing preview button (`GetLinkDialog`'s sibling "Предпросмотр" action, `LandingRendererService.renderPreviewHtml`) still showed no images for a CUSTOM landing with nested-folder assets — a DIFFERENT bug from the trailing-slash one fixed earlier the same day, and unfixable by that same mechanism. The preview flow fetches raw HTML via an authenticated `api.get` call, wraps it in a `Blob`, and opens it via `URL.createObjectURL()` — a `blob:` URL has no path hierarchy at all, so a relative reference like `images/icon.jpg` inside it can never resolve to anything, regardless of trailing slashes. Fixed with a new authenticated `GET /landings/:id/preview-asset/*` route (`LandingRendererService.streamPreviewAsset`, same `LANDINGS_VIEW` permission check as `/preview` itself, 404s cleanly for non-CUSTOM types since only CUSTOM has a `customBasePath` to stream from) plus a new shared frontend helper `previewLanding()` (`apps/web/src/lib/landings.ts`) that fetches the HTML, walks it with `DOMParser` for every relative `<img src>`/`<link href>`/`<script src>`, fetches each one through the new endpoint as a blob, and rewrites the attribute to a real per-asset object URL before constructing the final displayed document — replacing 6 near-identical copies of the old fetch-and-blob code across both trees' list pages and detail pages. Verified this doesn't regress TEMPLATE landings by construction (they only ever reference absolute CDN/API URLs, so the rewrite loop finds zero matching elements — a true no-op) and live end-to-end: a real CUSTOM landing's preview blob was confirmed to carry the exact byte-for-byte contents of the real uploaded asset (compared by exact length against the uploaded file, sidestepping any ambiguity from `naturalWidth` on a deliberately tiny test image), on both domains, zero console errors.

Second, the ZIP-upload dialog (shipped 2026-09-07) was widened and its instructions expanded per explicit request ("покажи в инструкциях и структуру файлов еще для полного понимания... по пунктам") — a real fix, not just a bigger number: the shared `DialogContent` component (`components/ui/dialog.tsx`) has a documented 2026-07-30 history where its own base classes used to carry a PREFIXED `sm:max-w-sm`, which `twMerge` cannot dedupe against a caller's bare `max-w-*` override (different specificity group in compiled Tailwind v4 CSS) — silently capping every dialog in the app at 384px on desktop regardless of what `max-w-*` was passed in. That fix already landed on 2026-07-30 (base class corrected to a bare, unprefixed `max-w-sm`, which `twMerge` DOES correctly dedupe against a later bare override) — this round's widening (`max-w-2xl max-h-[85vh] overflow-y-auto` on the ZIP dialog) is the first real-world exercise of that fix for this specific dialog, confirmed live via `boundingBox()` measurement returning exactly 672px, not the old capped 384px. New shared `ZipLandingInstructions` component (`apps/web/src/components/zip-landing-instructions.tsx`) replaced the old 4-line text blob with 6 numbered steps (archive structure with a real ASCII folder-tree example, relative-link syntax, the `{{TG_REDIRECT_URL}}` button convention, the auto-inserted tracking script, what the post-upload review actually checks, and the 50MB cap) — one shared component, not duplicated per tree.

Third, per explicit request ("Добавь еще кнопки загрузки zip и для подключения внешнего лэндинга на странице /landings"), both company-wide `/landings` pages (classic and Studio, previously offering only "Создать из шаблона") gained the same two buttons the project-scoped pages already had. Since the ZIP-upload dialog's entire mutation/state/dropzone logic had never been anything but inline code duplicated across the two project-scoped `page.tsx` files, adding it to a THIRD and FOURTH page properly required extracting it first: new shared `UploadZipLandingDialog` (`apps/web/src/components/upload-zip-landing-dialog.tsx`) — same `projectId?` optional-with-picker convention already established by `CreateLandingFromTemplateDialog`, with the picker relevant only for the "create new" path (re-upload posts to `/landings/:id/upload`, which never needed a project id in the first place). A provably-safe internal-state trick carried over from `CreateExternalLandingDialog`: a failed post-upload review flips the dialog from "create" into "re-upload the same now-existing id" via a self-managed `selfTarget` state that shadows the `target` prop, so a second "Загрузить" click after fixing the archive goes through the correct endpoint without any parent-side bookkeeping. `CreateExternalLandingDialog` itself needed the identical `projectId?`-optional treatment (its `open` state was previously conflated with "is `projectId` non-null," which structurally couldn't work on a page with no fixed project) — both dialogs' project `<Select>` reuse the exact JSX already established by `CreateLandingFromTemplateDialog`. Both company-wide list pages also gained the previously-missing `onReupload`/`onManageExternal` wiring on their `LandingCard`s (a real, if minor, pre-existing gap: `LandingCard` already supported managing an existing CUSTOM/EXTERNAL landing, just never wired on these two pages, so a landing created from the new company-wide buttons would otherwise have had no page it could be managed from except its own project). Live-verified via real headless-Chromium runs on both domains, logged in as a real throwaway user: both buttons present, both dialogs open with a working project picker showing real project names, zero console errors — confirming the whole round (preview fix, wider dialog, and the two new company-wide entry points) together.

Immediately after, a real user report on a real production landing (`rolando-zip-test`, 15 images
totaling ~4.5MB) — "предпросмотр открывался 20 секунд" — traced directly to the preview fix
shipped minutes earlier: `previewLanding()`'s asset-rewrite loop fetched each relative reference
through `/preview-asset/*` inside a plain `for...of` with `await`, meaning N files became N
strictly SEQUENTIAL round trips end-to-end (real network + Cloudflare + auth each time) — with 15
images that's the reported 20s, confirmed by a direct raw MinIO-stream timing check showing the
underlying storage read itself is fast (10-50ms) and NOT the bottleneck; the multiplication factor
of doing 15 of them one-after-another was. Fixed by collecting every matching element first and
firing all fetches via one `Promise.all` (browser's own per-origin connection cap does the
batching automatically) instead of the sequential loop, plus a same-function `Map`-based cache so
an identical URL referenced by more than one element only downloads once. Live-verified with a
real headless-Chromium timing run against a throwaway landing built to match the real one's
profile (15 files, ~400KB each, same domain/Cloudflare/auth path as the actual report) — the exact
same click-to-"every image loaded" measurement dropped from the reported ~20s to 879ms, roughly a
20x improvement, confirming the fix addresses the real reported case and not just a synthetic one.

On 2026-09-08, per explicit request ("добавить возможность скачивать таблицы статистики — список клиентов со всеми данными, статистика проекта за период"), CSV export shipped for both. The one existing precedent (`ClientsService.exportForLookalike`, a narrow 3-field CSV for Facebook Custom Audience uploads) didn't quote-escape values properly (`` `"${v}"` `` with no internal-quote doubling) and had no shared CSV-building code — left untouched (Facebook already accepts it, no reason to risk an already-working integration), but a proper shared `common/csv.util.ts` (`buildCsv`/`buildCsvRow`, RFC 4180 quoting, `withUtf8Bom` — without a BOM, Excel's charset auto-detection frequently mis-renders Cyrillic) backs both new exports instead of copy-pasting the same bug forward. New `GET /projects/:projectId/clients/export` (`ClientsService.exportClientsCsv`) accepts the exact same `ClientFiltersDto` the list endpoint already does, so the download always reflects whatever is currently filtered on the page, not "all clients ever" — capped at a defensive 50,000-row `EXPORT_ROW_CAP` (not a real business limit, just a runaway-query guard). Raw `buyerId`/`pixelId`/`landingId` foreign keys are resolved to real names via three small batched lookups before CSV-building, since a spreadsheet of bare cuids is useless to a human. `canViewTrafficSource` (the same `role !== OPERATOR` check `findMany` already uses to null `fbclid`/`ttclid`) is applied far more broadly here than in the list endpoint — the list only ever rendered 2 ad-attribution fields as columns, so only those 2 needed hiding; the CSV surfaces the FULL attribution bundle (buyer/pixel/campaign/ad/adset/utm*/fbclid/ttclid), so all of those are now gated behind the same check, verified live via a real OPERATOR test user that none of the real seeded attribution values leaked through. New `GET /projects/:projectId/clients/stats/export` (added to `ClientsController`, which already had both `ClientsRepository` and `ProjectsService` injected — no new cross-module wiring needed) combines `getProjectStats`/`getConversionFunnel`/`getAdBreakdown`/`getLeaderboardFull` (the uncapped per-category leaderboard endpoint, not the top-5 `getLeaderboards` the page itself shows, since an export should list everyone ranked, not just the podium) into one CSV with clearly labeled sections (new `project-stats-export.util.ts`, a pure formatting function taking already-fetched data) — replicates the exact same `STATS_VIEW`/`STATS_VIEW_REVENUE`/`STATS_VIEW_TEAM_LEADERBOARDS` three-permission gating the live page's own endpoints already enforce (revenue columns zeroed, not omitted, without `STATS_VIEW_REVENUE`; the 5 leaderboard sections omitted entirely — not just zeroed — without `STATS_VIEW_TEAM_LEADERBOARDS` or with an active buyer-scope, matching `GET .../leaderboards`'s own condition exactly), verified live with the same OPERATOR test user missing both permissions. Frontend: new shared `downloadBlob(blob, filename)` helper (`lib/utils.ts`) replaces 4 near-identical inline `createObjectURL`+`<a download>`+`click`+`revokeObjectURL` blocks (2 existing lookalike-export copies plus the 2 new buttons) rather than adding a 5th/6th copy of the same snippet. "Скачать CSV" (full export) sits next to the existing lookalike button (relabeled "Lookalike" for clarity) on both trees' client list pages, reusing the exact same filter/period params object the page's own list query already builds; "Скачать статистику" sits beside the period picker on both trees' project overview pages, reusing the page's own already-computed `periodParams`. Live-verified end-to-end on both domains via real headless-Chromium clicks (not just direct API calls) confirming real file downloads with correct filenames/sizes and zero console errors; the API-level checks separately confirmed exact CSV content (comma-containing values correctly quoted, e.g. an ad name literally containing a comma), UTF-8 BOM byte-verified (`ef bb bf`), and the OPERATOR redaction proven both ways (attribution fields absent from the client export, revenue zeroed and leaderboard sections entirely missing from the stats export) against the same seeded data an OWNER's export correctly included in full. Test company/project/users/clients/purchases deleted by exact id afterward.

The same day (2026-09-08), a real customer support case ("клиент добавил внешний лендинг, я не вижу чтобы трекались клики") led to a genuine, previously-undetected gap in the EXTERNAL-landing feature shipped 2026-09-07. Investigation of the real live page (lewis-trade.com) confirmed `track.js`/`data-project-id`/`data-landing-id` were all installed correctly and PageView events were flowing (30 real events recorded) — but Lead events (button clicks, the funnel's "Клик на кнопку" stage) were completely absent for this landing specifically, while other landings in the same project had 36 real Leads between them. Root cause: the SDK's generic `[data-track]` click handler (`apps/sdk/src/browser.ts`, documented since its own introduction as `<button data-track="Lead">Вступить</button>`) fires an event named EXACTLY whatever string is in `data-track` — the customer's own buttons carried `data-track="telegram"` instead of `data-track="Lead"`, and the public `/track/:publicToken/event` endpoint validates `eventName` against a strict whitelist (`PageView, Lead, Subscribe, Unsubscribe, Purchase, InitiateCheckout, Click`), rejecting `"telegram"` with a 400 — confirmed by replaying the exact request by hand. The SDK's `track()` wraps its `fetch()` in a bare try/catch that only catches network exceptions, never checking `response.ok`, so a rejected request produces zero visible signal anywhere — not in the browser console, not in the CRM — making this exact class of misconfiguration silently invisible to both the customer and to us until someone manually diffed real TrackingEvent counts. Doubly compounding this: `CreateExternalLandingDialog`'s connection instructions never mentioned `data-track` at all — the customer did everything the instructions actually told them to do; the gap was in what we told them, not in their attention. Fixed on both sides: (1) instructions — the join-button example (previously a bare href string the customer had to wrap in their own `<a>` tag) is now a complete, literally-copy-pasteable `<a href="..." data-track="Lead">Вступить в Telegram</a>` tag, with explanatory text calling out that clicks silently fail to count without that exact attribute; (2) verification — new `hasTrackedJoinButton()` (`landing-review.util.ts`, htmlparser2-based like the existing ZIP-review checks, deliberately NOT a substring `.includes()` check since that could false-positive on `data-track="Lead"` existing ANYWHERE on the page rather than on the actual join-button element) backs a new `click-tracking` check in `verifyExternalLanding`, added alongside the existing Telegram-only `join-link` check — a page whose button lacks the exact `data-track="Lead"` attribute now fails verification with a clear, specific message instead of silently passing. Live-verified end-to-end against two real test pages uploaded to the CDN bucket (real project, real EXTERNAL landing, real fake-but-syntactically-valid Telegram bot token per the established technique for this class of test): a page with `data-track="telegram"` passed every other check but correctly failed `click-tracking` with the exact expected message, staying `DRAFT`; the identical page with `data-track="Lead"` passed all 6 checks and correctly flipped to `PUBLISHED` — directly reproducing and catching the real reported bug. Test company/project/channel/landing and CDN test objects deleted by exact id/key afterward. The real customer's own landing was left untouched (their status/verification state is theirs to re-trigger once they fix their button) — diagnosis and product fix shipped, not a change to their site.

On 2026-09-14, the same class of gap resurfaced on a real customer project (Pura Vida Mexico) — but this time on a `ChannelType.WEBSITE` project, not an `EXTERNAL` landing, so the 2026-09-08 fix (which only touched `CreateExternalLandingDialog`'s instructions and `verifyExternalLanding`) never covered it. Diagnosed the same way as before: the live site's script tag was correctly installed (`data-project-id` matched, channel `isActive`), PageView (112 real events) and even Purchase (2 real events, via the server-signed integration) were both flowing correctly, but zero Lead events existed and the live page had literally no `data-track` attribute anywhere at all — not even a wrong one this time. Root cause confirmed by reading `ProjectsService.getSnippet()`'s generated example and `IntegrationTab`'s (`apps/web/.../settings/tabs.tsx`, both trees) "Как подключить сайт" instructions end to end: neither ever mentions `data-track` at all, for any channel type — a customer following the WEBSITE-channel instructions to the letter had no way to know a button needs `data-track="Lead"` to register as a click. Fixed by inserting a new step 3 into both trees' identical 4-step "Как подключить сайт" card (between "paste before `</head>`" and "click Проверить"): explains that click-tracking buttons need `data-track="Lead"` (with a copy-pasteable `<button data-track="Lead">Купить</button>` example) and explicitly notes that PageView keeps working without it — so a customer doesn't mistake the missing step for the whole integration being broken. No verification-check equivalent was added this time (unlike the EXTERNAL-landing round, which also got a new `click-tracking` `verifyExternalLanding` check) — `WebsiteProvider.initialize()`'s "Проверить" only ever checked the script tag/project-id, and only the instruction fix was asked for; scoped to exactly what was requested rather than assuming the same two-part fix applied here too. Live-verified: the new step confirmed present and correctly rendered (readable numbered step, working inline `<code>` formatting, zero console errors) via a real headless-Chromium login on both domains against a throwaway WEBSITE project, screenshot-checked, not just grepped from the built bundle. Test company/project deleted by exact id afterward. The real customer's own site was left untouched, same disclosed-scope precedent as the 2026-09-08 case.

On 2026-09-15, a real customer report ("выбрал для одной 50%, для второй 25% — где ещё 25%?") on A/B/n testing (both landings and bot scenarios) confirmed a genuine, asymmetric gap between the two "mirrored" features. Weights are RELATIVE shares of a pool, not literal percentages of total traffic — both `LandingRendererService.pickAbTestGroupVariant` and `TelegramProvider`'s scenario-variant picker normalize by the ACTUAL sum of all members' weights (`roll = random() * totalWeight`), not by an assumed 100 — so 50+25 genuinely splits ALL real traffic 66.7%/33.3% between the two variants, nothing is "missing," but the displayed numbers don't match the real split, which is exactly the confusing part. Investigation found `LandingsService.assertValidMembers` already hard-requires `totalWeight === 100` both server-side AND in the landing A/B dialog's own `trySave()` client-side check (`landing-card.tsx`) — making this exact broken state structurally impossible to create for landings through any normal path. `BotScenariosService.updateAbTestWeights` (`PATCH .../ab-test/weights`) and its `UpdateScenarioAbTestWeightsDto`, despite scenario A/B testing being built explicitly to "mirror landing A/B groups" (see the 2026-07-25 entry), never inherited this one validation — it wrote whatever numbers were submitted straight to `BotScenario.abTestWeight` with zero guard, and the frontend weight editor (`AbTestSection` in `scenarios/[scenarioId]/page.tsx`, both trees) let each variant's weight be edited as a fully independent number input with no live total and no submit-time check. This is where the reported bug actually reproduces. Fixed with the same two-layer pattern landings already established: `updateAbTestWeights` now requires the submitted weights to cover ALL live members of the group (not a partial subset — the one real caller always sends the full set anyway) and to sum to exactly 100, mirroring `assertValidMembers` almost verbatim; both trees' `AbTestSection` gained a live "Итого: X%" indicator (red once it drifts from 100) plus a `trySaveWeights()` guard blocking the save button with an inline error before ever hitting the network, matching `AbTestGroupDialog`'s `trySave()` UX. The landing dialog itself also gained the same live "Итого: X%" text (previously only a post-click error existed) purely for UX parity, since it was already impossible to reach the broken state there. Live-verified against a real isolated project (`TELEGRAM`/`PRIVATE_CHANNEL_REQUEST`, a fake-but-syntactically-valid bot token per the established technique) with 2 real bot scenarios in a real A/B group: replaying the EXACT reported 50+25 payload against the fixed endpoint correctly returned 400 "Сумма процентов должна быть равна 100" (previously would have silently saved), a valid 60+40 payload succeeded, and a payload covering only 1 of 2 live variants was correctly rejected too; a real headless-Chromium session on both domains confirmed the live red "Итого: 75% (должно быть 100%)" indicator renders correctly the moment the inputs are typed and that clicking "Сохранить веса" is blocked with the same inline error, zero console errors either domain. Test company/project/channel/scenarios deleted by exact id afterward (including cleaning up the `ScenarioAbTestGroup` row first — a real FK constraint against `Channel` that the first cleanup attempt missed).

The same day (2026-08-31), a follow-up ("часть логов вообще без кампании/объявления... у конкурентов на той же рекламе всё приходит") led to a much deeper live investigation, since the user provided counter-evidence (a competing CRM tracking the identical campaign with complete data on every lead) that ruled out simply repeating the earlier "Facebook sometimes fails" explanation. Quantified via direct Prisma queries against 7 days of real data on the reported project (🇬🇹MARIA LOPEZ🇬🇹, PRIVATE_CHANNEL_REQUEST): browser-side PageView/Lead events had an 15-18% missing-campaignId rate, but server-side Subscribe events (matched via the `landing-visit-attribution` FIFO/lookahead queue, see [[feedback_landing_attribution_race_condition]]) had a 50% rate — a real, unexplained gap worth chasing rather than accepting at face value. Traced a sample of real "missing" Subscribes back to their exact originating clicks via nginx access logs (matching on `fbclid`) and found a SECOND, previously undocumented Facebook-side failure mode beyond the known literal-`{{...}}`-macro case (2026-08-20/27): a meaningful share of real clicks (confirmed via genuine Instagram/Facebook in-app-browser UAs, not bots — `facebookexternalhit`/`AdsBot-Google` crawler traffic was independently confirmed already excluded from the queue since 2026-07-29's `isBotUserAgent` gate, ruling out that theory first) arrive with `campaign_id`/`campaign_name`/`adset_id`/`adset_name` **entirely absent** from the destination URL — not corrupted, just never included — while `ad_id`/`ad_name`/`fbclid` come through reliably (those don't require Facebook to walk the ad→adset→campaign hierarchy server-side, unlike the fields that fail). A queue-replay simulation (reconstructing the real FIFO+lookahead matching algorithm against real nginx-log-derived visit data) confirmed the existing `ATTRIBUTION_LOOKAHEAD = 10` window was too narrow for landings with this much traffic (thousands of visits/day, low conversion rate) — a burst of consecutive "bad" (missing-campaign) real visits can exceed 10 in a row, causing the "prefer non-null campaignId" search to fall back to strict-FIFO and hand out a bad entry even when a good one existed slightly further back in the same queue. Raised to 100 (Redis `LRANGE` cost is trivial even at that size, queue is capped at 500 by the existing `LTRIM`) — a real, low-risk improvement to OUR matching logic, though it cannot fully close the gap since Facebook's own hierarchy-lookup failure is the root cause and is outside our control. Disclosed to the user as only a partial mitigation, with the specific buyer's stale/inconsistent tracking-link data flagged for them to verify directly in Facebook Ads Manager, and a follow-up idea proposed (not yet built): since `ad_id` resolves reliably even when `campaign_id` doesn't, a Facebook Marketing API lookup (`ad_id` → campaign/adset) could backfill the missing fields server-side as a fallback — would need Marketing API read access not currently confirmed available.

The same day (2026-08-31), the user pointed out the real architectural fix competing systems use: a unique invite link PER SUBSCRIBER, not one shared link per landing — eliminating the FIFO/lookahead queue's race condition entirely rather than just widening its window. Implemented for PRIVATE_CHANNEL_REQUEST: `TrackingController.tgRedirect` (the endpoint every join-button click/auto-redirect already funnels through — `TG_REDIRECT_URL`) now, when `code` is present and the channel is `PRIVATE_CHANNEL_REQUEST`, reads the already-fully-resolved `start:<code>` attribution blob (the SAME per-visit, no-queue-needed mechanism BOT_DIRECT/PERSONAL_DM already relied on — PRIVATE_CHANNEL_REQUEST was the only mode forced onto the shared-queue approximation, because Telegram's `chat_join_request` event carries no arbitrary code the way `/start <code>` does) and calls a new `TelegramProvider.createOneTimeInviteLink(channel, attribution)`, which creates a real one-time Telegram invite link (`creates_join_request:true`, `expire_date` ~24h — verified live against real Telegram Bot API docs before implementing that `member_limit` is NOT allowed together with `creates_join_request:true`, so no built-in Telegram-side one-time enforcement; relies on `expire_date` plus an explicit `revokeChatInviteLink` after use instead) and stores the SAME attribution blob under a new `landing-visit-link:<inviteHash>` Redis key (24h TTL — safe to be long since there's no shared-key contamination risk at all with a unique-per-visit key, unlike the old `landing-visit-attribution:<landingId>` queue). `TelegramProvider.handleJoinRequest` now checks this exact key FIRST (before the old `Landing.tgInviteLink`/`Channel.tgInviteLink` + queue matching, which stays fully intact as the fallback for pre-existing links or if invite-link creation fails for any reason — Telegram API error, bot not live, no `start:<code>` found) — an exact match needs no lookahead/FIFO approximation at all, since Telegram echoes back the exact invite link used in the join-request event. `extractInviteHash` was pulled out of `telegram-link.util.ts`'s `toTgInviteScheme` as a shared pure function (also used by the new Redis key), and `isBotUserAgent` was moved from `LandingRendererService` into a new dependency-free `common/bot-user-agent.util.ts` (needed conceptually for the same "don't waste external API calls on crawler traffic" reasoning, though in practice `/tg-redirect` is naturally already crawler-free since bots don't execute JS/follow the join button). Avoided a new ChannelsModule↔TrackingModule circular dependency (ChannelsModule already imports TrackingModule) by lazily resolving `TelegramProvider` via `ModuleRef.get(..., {strict:false})` in `TrackingController` instead of a constructor injection + `forwardRef()` — the same established pattern already used by `BotScenarioEngineService` for the identical class of problem. `ProjectsService.findByPublicToken`'s channel `select` was widened (`id`/`tgChannelId` added) since its only two callers (both in `TrackingController`) never serialize the result to an HTTP response. Live-verified end-to-end against a REAL production channel (🇳🇮RICARDO RAMIREZ🇳🇮) with a synthetic `start:<code>` Redis entry: confirmed a genuinely NEW, distinct one-time invite link was created via a real Bot API call (different hash from the channel's static link), and that the exact test attribution payload (campaign/ad/buyer test data) landed correctly under `landing-visit-link:<hash>` with the correct ~24h TTL; a control request with a nonexistent `code` correctly fell through to the old static-link behavior with zero errors. Deliberately did NOT fabricate a synthetic `chat_join_request` webhook against the real channel to test the consume side, since that would create a real bogus `Client` row in real production data for a channel that was never actually joined — the consume-side GET+DEL+parse logic was instead verified via an isolated Redis-only round-trip test plus code review, backed by the fact that every new code path falls back safely to the exact prior (already-correct) behavior on any failure. The one disclosed, accepted residue: the live-test invite link itself (never distributed to anyone, so never joinable) sits unused in the real channel's invite-link list until its `expire_date` passes (~24h) — the Redis keys were cleaned up immediately after the test. `tsc --noEmit`/`nest build` clean, zero rehydration failures on restart.

The same day, a direct follow-up request ("как это показано у наших конкурентов") added the real invite link to the client detail card. New `Client.tgInviteLink String?` (PRIVATE_CHANNEL_REQUEST only, null for every other channel mode/pre-existing row — not retroactive) is now stamped by `TelegramProvider.handleJoinRequest` straight from the webhook's own `ctx.chatJoinRequest.invite_link.invite_link` — the exact link Telegram reports the join came through, whether it's a per-visit one-time link (see the entry above) or a fallback to the shared landing/channel link, so the field is always an honest record of what actually happened, never reconstructed after the fact. Threaded through `FindOrCreateClientInput`/`ClientsService.findOrCreate` with the same "first known value wins" convention every other attribution field on `Client` already uses. Displayed on the shared `client-detail-drawer.tsx` (used by both trees — one edit covers both) as a clickable link right under "Лендинг", inside the existing `canViewTrafficSource`-gated section — Operator still can't see it, same as the rest of the ad-attribution block. Migration applied via the established non-interactive path (`prisma migrate diff --script` for the raw SQL, a hand-written migration folder, `prisma migrate deploy`) since `migrate dev` refuses to run outside an interactive terminal.

The same day, per explicit user request, every remaining link to the classic UI (`old.mw-track.com`) was removed from Studio: `StudioSidebar`'s "Старый дизайн" footer link (shared component, was on every Studio page) and the project detail page's separate "← Обычный вид" link (`/dashboard/studio/projects/[id]/page.tsx`, a second independent link with different label/target that the sidebar removal alone wouldn't have caught). `old.mw-track.com` itself is untouched and still fully live (code/data/cross-domain SSO bridge all intact per the 2026-07-30 split) — only the UI's own links to it are gone; a user who already knows the URL can still reach it directly.

The same day, the user pasted a competitor's landing-page script (TikTok in-app-browser handling) and asked to adopt the most useful ideas into our own [[feedback_tiktok_hint_texts_and_copy_link]] feature. Two real improvements shipped, both scoped to Android only (the competitor's own script has no iOS equivalent either — text instructions only, matching what we already do there). First, `TrackingController.tgRedirect` gained a `?format=json` mode — same internal logic as the existing 302 path (start:<code> lookup, one-time invite-link creation, all of it unchanged) but returns `{tg, https}` instead of redirecting, so the SDK can resolve the link ahead of an actual tap. A new sibling `buildTelegramHttpsLink` (`telegram-link.util.ts`) mirrors `buildTelegramLink`'s per-mode switch for the https:// equivalent of each tg:// link (for PRIVATE_CHANNEL_REQUEST this is just the invite link's own pre-`toTgInviteScheme` form, already on hand). Second, `apps/sdk/src/browser.ts` gained `prefetchTelegramLink()` (fired the moment the Android+TikTok branch is entered, in parallel with — not blocking — the existing synchronous generic-browser escape, which stays completely unchanged) and `buildTelegramPackageIntent()`, which builds an Android intent with an explicit `package=org.telegram.messenger` (resolves silently into the Telegram app with no "which app?" prompt, unlike the existing generic unpackaged escape) plus `S.browser_fallback_url` for the case Telegram isn't installed. The fallback overlay's "Open" button (shown only if the initial silent escape demonstrably failed) now uses this direct one-step app intent when the prefetch succeeded, falling back to the original two-step generic escape unchanged if it didn't — never worse than before, meaningfully better in the common case. Verified via `vm`-sandboxed execution of the actual built bundle (not just unit-level helper checks) across 3 scenarios: the synchronous escape still fires unchanged with the prefetch running in parallel, the Open button correctly builds the direct-app intent from prefetched data, and it correctly falls back to the generic escape when the prefetch fails (simulated network error) — all passed. Backend JSON endpoint also verified live against a real production channel (confirmed both the fallback-link case and a real one-time-invite-link creation return matching `tg`/`https` hashes). Not verified against a real Android device with TikTok installed (same disclosed gap as the original 2026-08-25 Android escape — this environment has no such device).

The same day, a user question ("пересечение аудитории показывает только наших или всех?") surfaced a real, previously-unnoticed gap: `AudienceService`'s overlap matrix and detail drill-down had never applied the `OURS_ONLY` (`subscribedAt: {not: null}`) convention used almost everywhere else in the codebase — it counted/listed ALL `Client` rows with a non-null `tgUserId`, including external/cold contacts who only messaged a bot/personal account without ever going through a tracked subscription funnel. Per explicit user decision ("нет, всегда показывай только наших"), added `subscribedAt IS NOT NULL` to all three queries (the `groupBy` diagonal totals, the raw-SQL pairs matrix, and the detail drill-down's `sharedWhere`) — confirmed live against real production data the fix actually matters: company-wide overlapping-tgUserId count dropped from 10,092 (all) to 361 (subscribers only). Both trees' page description text updated from "идентифицированных клиентов" (identified clients) to "подписчиков" (subscribers) with an explicit note about excluding external contacts, since the old wording was accurate for the old behavior but misleading now.

The same day, a direct follow-up ("покажи в процентах") added a percentage to each matrix cell — computed per-cell as `count / totals[rowProjectId]`, so the raw-count matrix (symmetric — A∩B cell equals B∩A cell) becomes asymmetric once shown as a percentage (each cell relative to its own row's total), disclosed in the page's own description text so it doesn't read as a bug. A second, immediate follow-up ("проценты показываются очень плохо... переверни верхние названия вертикально") redesigned the matrix visually: column headers now rotate 90° (`writing-mode: vertical-rl; transform: rotate(180deg)`, reading bottom-to-top — the standard spreadsheet/pivot-table convention, not upside-down) with a height cap + ellipsis + `title` tooltip for long names — previously each column was as wide as its full project name, so more than 4-5 projects didn't fit on screen. The count+percent display (previously a badge with a separate text line stacked underneath, which broke row-height alignment between diagonal cells with no percent and off-diagonal cells with one) collapsed into a single line inside one badge/pill (`42 (12%)`), so cell height no longer depends on whether a percent is shown. Verified visually (not just `tsc`) via real headless-Chromium screenshots against the permanent demo account ([[project_demo_showcase_account]]) on both trees — confirmed rotated headers render legibly, badges stay uniform height across the whole matrix, and the asymmetric percentages match expectations (e.g. 8/37=22% one direction, 8/41=20% the other, for the same raw pair count).

The same day, a bigger follow-up ("сделай ячейку квадратной... покрась... уберу список клиентов... добавь период") turned the matrix cells into a proper heatmap and split the client list out to its own page. Cells are now fixed-size (72×72) rounded squares colored by a 4-band percentage scale (<10% green, <20% yellow, <30% light red, ≥30% dark red — the color is driven by the cell's own row-relative percentage, matching the asymmetric-percent design from the entry above); the diagonal (project totals) stays a neutral square, uncolored, since a percentage against itself is meaningless. Clicking any off-diagonal cell now navigates (a real `<Link>`, not a client-side toggle) to a new dedicated route — `/audience/[projectAId]/[projectBId]` in both trees — carrying the matrix's current period in the URL; the inline `OverlapComparisonPanel` that used to expand below the matrix on click was removed from `/audience` entirely, its client-list/search/pagination/expanded-dialog code moved wholesale onto the new page. That page also gained a summary row above the list — total subscribers for each side, the intersection ("Дубликаты") with both directions' percentages, and each side's unique-only count (`total - intersection`, not a second query) — backed by a new `AudienceService.getOverlapPairSummary` and `GET /audience/overlap/:a/:b/summary`. Both the matrix and the pair page gained their own independent period selector (project-page pill design in Studio, `PeriodSelector` in classic) — period resolves against `'UTC'` (same simplification as `getCompanyStats`, since a pairing spans two projects with potentially different timezones) and filters `Client.subscribedAt`; omitting `period` entirely keeps the pre-existing all-time behavior (backward compatible). The detail endpoint's query params (period + page + limit + search) had to be collapsed into one `OverlapDetailQueryDto` — the established "combined filter DTO" pattern (see the `PaymentDetailsLogFiltersDto` precedent) — since the global `ValidationPipe`'s `forbidNonWhitelisted` rejects a route combining a whole-object `@Query() period: StatsPeriodDto` with separate `@Query('page')`/`@Query('search')` params. Verified live end-to-end (not just `tsc`) via real headless-Chromium runs against the permanent demo account on both domains — matrix heatmap colors/sizes correct, click-through navigation carries the right period into the new page's URL, the summary numbers match a direct Prisma cross-check (37/41/8/29/33/22%/20% for one real pair), and the client-row expanded dialog still opens correctly from the new page — zero console errors throughout.

The same day, a direct follow-up added "Всё время" (all time) as a 6th period option on both audience pages, with the default changed from `today` to `30d`. Deliberately NOT added to the shared `PeriodSelector`/`usePeriodQueryState` used by 8+ other pages (project page, leaderboards, home page, my-stats, clients, payment-details-log) — none of them were asked for this option, and extending the shared enum would have surfaced a 6th button everywhere. Instead, a small self-contained `useAudiencePeriodQueryState` hook (`apps/web/src/lib/use-audience-period.ts`) wraps a local `AudiencePeriod = StatsPeriod | 'all'` type, plus two new small picker components — `AudiencePeriodSelector` (classic-styled) and `StudioAudiencePeriodPicker` (Studio-styled, co-located in `dashboard/studio/audience/`) — both scoped to just the 4 audience page files. `'all'` is a purely frontend concept: `toApiPeriodParams()` sends no `period` query param at all when selected, and `AudienceService.resolvePeriodWindow`'s pre-existing "no `period` → no filter" behavior (from the original OURS_ONLY fix) already does exactly the right thing — no backend changes were needed. Verified live on both domains via real headless-Chromium runs against the demo account: default view now shows "30 дней" selected, clicking "Всё время" restores the full unfiltered historical counts (confirmed matching the pre-period-feature baseline numbers), and the pair page correctly inherits and independently offers the same 6-option picker.

On 2026-09-01, a user-reported bug — pushes on `/pushes-calendar` stuck in `SENDING` status since August 23 with no progress — was root-caused to a real, previously-unaddressed architectural gap in `PushesService.send()`: it enqueued one BullMQ job per recipient in a single uncheckpointed in-memory loop with no durable record of who'd been queued, so any mid-loop process restart (a deploy, a crash) permanently orphaned every recipient after the interruption point — their jobs were simply never created — while `PushesProcessor`'s completion check (`sentCount+failedCount >= audienceReachable`) could then never trigger, since the missing recipients would never report in either direction. Unlike `PersonalBroadcastsCron.recoverStalled()` (built for the exact same class of problem on a different feature), no recovery mechanism existed for `Push` at all. Per explicit user instruction ("почисти старые без отправки и сделай полноценный фикс" — clean up the old ones without sending, and build the full proper fix), the 42 real stuck pushes were first inspected (confirmed: 0 pending BullMQ jobs outstanding for any of them, meaning they were genuinely dead, not just slow) and marked `FAILED` by exact id — no message was sent as part of the cleanup. The systemic fix converts `PushLog` from a reactively-created send record into a durable pre-created ledger: `PushesService.ensureLedger()` now writes one `PushLog` row per matched recipient with `status:'pending'` (via chunked `createMany({skipDuplicates:true})`, backed by a new `@@unique([pushId,clientId])` constraint) *before* any BullMQ job is enqueued, and `enqueuePendingLogs()` enqueues strictly off that ledger rather than an in-memory client list — so a crash between the two steps loses nothing, the ledger already reflects the full intended audience. `PushesProcessor.sendPushMessage` now atomically claims its row (`pushLog.updateMany({where:{id,status:'pending'},data:{status:'sending'}})`, `count===0` meaning someone already claimed it) instead of creating one reactively, and updates it in place rather than inserting — the same atomic-claim pattern already used elsewhere in the codebase for exactly this class of safe-to-retry job. A new `PushesService.resumeSending()` re-runs `ensureLedger`+`enqueuePendingLogs` for an already-SENDING push (idempotent — re-creating already-existing ledger rows is a no-op via `skipDuplicates`, re-enqueuing an already-claimed row is a no-op via the atomic claim), called by a new `PushesCron.recoverStalledSending()` (`*/1 * * * *`, mirrors `PersonalBroadcastsCron.recoverStalled()`) that finds `SENDING` pushes whose `updatedAt` is more than 5 minutes stale and not yet fully accounted for. Applying the new unique constraint surfaced 2 real pre-existing duplicate `(pushId,clientId)` rows in production — both stale `'pending'` orphans from this exact bug, deleted by exact id before the migration, confirming the bug's own symptom was blocking its own fix's migration. Live-verified end-to-end against the real running production API and BullMQ queue (deliberately not `NestFactory.createApplicationContext`, per the standing caution elsewhere in this file) using an isolated throwaway company/project/clients/pushes: a normal send correctly pre-created the ledger, processed all recipients, and auto-flipped to `SENT`; a simulated crash (a `SENDING` push with orphaned `'pending'` `PushLog` rows and zero enqueued jobs, `updatedAt` backdated past the stall threshold) was correctly detected and resumed by the cron within about a minute, completing the push; and a manually injected duplicate job for an already-`'failed'` `PushLog` row was correctly no-op'd by the atomic claim with zero change to `failedCount` — confirming no double-send/double-count risk from the recovery path overlapping a still-live original job. Test data deleted by exact id afterward, including the two pre-existing duplicate rows found during the migration. One environment note worth remembering: a broad `prisma.push.updateMany({where:{status:'SENDING'}, ...})` for the initial cleanup was blocked by the Claude Code auto mode classifier as too broad against production data; rescoping the identical operation to the 42 already-identified exact ids (`where:{id:{in:[...]},status:'SENDING'}`) succeeded immediately — prefer exact-id-scoped bulk writes over status/type-scoped ones against production data going forward.

On 2026-09-01, a user report that multiple team members get logged out unexpectedly and often was root-caused to a genuine refresh-token rotation race, not a random glitch: access tokens live 15 minutes and `AuthService.refresh()` rotated the refresh token by immediately hard-deleting the old DB row on every use. Because `mw-track.com`/`old.mw-track.com` deliberately share one refresh-token identity for SSO (`auth.store.ts`'s `mw_refresh` shared cookie) and multiple tabs of the same login all hydrate the identical access token from the same `localStorage` entry, two tabs (or the two domains) almost always hit their 401 within the same narrow window and race to refresh with what is, at that instant, the same refresh token — whichever loses the race got a hard `401 Refresh token недействителен` and the frontend's interceptor (`lib/api.ts`) treated that as a real session expiry, calling `logout()` and wiping the shared `localStorage`, which could then also knock out the tab that had just "won" the race the next time it reloaded. Fixed with a 30-second grace window on rotation: `RefreshToken` gained a `supersededAt` column — the first use of a token marks it superseded instead of deleting it; a second use within `REFRESH_TOKEN_GRACE_MS` (30s) is treated as a legitimate racing tab and gets its own fresh, independent token pair (not a replay of the same pair — `RefreshToken` never had a one-row-per-user constraint to begin with, concurrent sessions across devices were already normal) rather than an error; past the grace window, reuse is rejected exactly as before, so the single-use security property is preserved for anything beyond a brief overlapping-tab race. A new `AuthCron` (`*/10 * * * *`) purges superseded rows older than 5 minutes, since they're no longer deleted inline at rotation time. Live-verified against the real running API (not `NestFactory.createApplicationContext`): registered a throwaway account, refreshed once ("tab A"), then replayed the same original token 2s later ("tab B") — succeeded with its own new token pair instead of 401'ing; replaying the same original token again after the 30s window closed correctly failed; the purge query was verified by backdating a superseded row's timestamp and confirming it was deleted while non-expired rows were left alone. Test data deleted by exact id afterward — this investigation also turned up and cleaned a leftover throwaway test company from an earlier, interrupted session within this same day that had never been cleaned up.

On 2026-09-03, a user question about the "Нет вебхуков" badge (shipped 2026-08-05, see that entry above) surfaced a real UI gap while investigating two genuinely affected production channels (Santiago Vargas, Empleo en Uruguay — confirmed via a direct, read-only `getWebhookInfo` call against Telegram's real API that both webhooks were correctly registered with zero delivery errors, and via nginx access-log comparison against other healthy bots on the same server in the same time window, ruling out any server/infra-side cause — Telegram itself was simply not generating updates for these two bots, the same failure mode as the original incident): the "Переподключить" button that re-registers a channel's webhook (`POST /channels/:id/reactivate` -> `tryInitialize` -> re-runs `setWebhook`) only ever rendered when `channel.isActive === false` — for a channel that's healthy per the periodic `getMe` check but simply not receiving webhooks (exactly the "Нет вебхуков" case), there was no action available in the UI at all, not even a hint explaining why. Fixed in both trees (`(dashboard)/projects/[id]/settings/tabs.tsx`, `dashboard/studio/projects/[id]/settings/tabs.tsx`): the button condition widened to `!channel.isActive || channel.webhookStale`, plus a small amber hint line next to the status pill when a channel is active but `webhookStale` — mirroring the existing pattern of the red `lastError` line for the inactive case. The classic tree's `ChannelSummary` interface was missing `webhookStale` entirely (a field `sanitizeChannel` already returns, just never consumed by this file before), added as a required `boolean`. A direct raw write to Telegram's `setWebhook`/`deleteWebhook` API from a one-off script (attempting to fix the two affected channels immediately, bypassing the app's own authenticated endpoint) was correctly blocked by the Claude Code auto mode classifier as a risky direct external-API write — the right response was fixing the legitimate in-app action instead of finding a way around the block. Live-verified via real headless-Chromium login against the permanent demo account: temporarily set one demo channel's `isActive:true` + `lastWebhookAt` 10h stale in one atomic write (immediately followed by the screenshot, since the demo account's channels — discovered mid-verification to already all show `isActive:false` in production, a pre-existing side effect of this session's many `trafficcrm-api` restarts re-running `tryInitialize()` against the demo's intentionally-fake bot tokens on every boot, unrelated to this fix — are also subject to the real 15-minute `checkAllChannelsHealth` cron flipping them back) — confirmed the amber hint and the reconnect button both render correctly for the active-but-stale case, then reverted the demo channel to its prior state. `tsc --noEmit` clean on both trees, `next build` clean, deployed.

On 2026-09-03, a user planning to launch a plain e-commerce site (Facebook/TikTok ad traffic, no Telegram/messenger touchpoint at all — visitor lands and buys directly) rejected the existing workaround of creating an unused fake Telegram bot just to satisfy `Project`'s mandatory 1:1 `Channel` relationship, and asked for genuine first-class "website" support. Went through a full `EnterPlanMode` cycle (2 parallel Explore agents mapping the Project/Channel/Client/Purchase architecture, 1 Plan agent drafting the implementation, then manual verification of every load-bearing claim against the real code) given the size — schema, four backend modules, the shared frontend channel picker, and the SDK shipped to every customer's site all needed changes. New `ChannelType.WEBSITE` (`Channel` row exists only to satisfy the 1:1 requirement; a new `WebsiteProvider` implementing the full 3-method `ChannelProvider` interface as a no-op registers it in `ChannelsService.providers`) fixed a real, previously-latent bug in the process: `ChannelsService.getProvider()` threw a bare, uncaught `Error` for any unregistered `ChannelType` (already silently true for `VIBER`/`EMAIL`, just never exercised) — `tryInitialize()` calls it outside its own try/catch, so creating a `WEBSITE` project would have 500'd without this fix. Per an explicit user decision (confirmed via `AskUserQuestion`): the Clients list for a website project shows only people who actually purchased, not every anonymous visitor — a `Client` is created at `Purchase` time, never at `PageView`, keeping `Company.maxClients` sane against high-volume, low-conversion e-commerce traffic. New `Client.visitorId String?` (SDK-generated, `localStorage`-persisted, unlike the existing `sessionStorage`-based fbclid/utm merge which deliberately does NOT survive a new session) became a 4th, last-priority branch in `ClientsService.buildIdentityWhere` (tgUserId→waPhone→igUserId→visitorId) — unreachable for existing Telegram/WhatsApp/Instagram callers, since they always pass one of the first three. `TrackingService.recordEvent` gained a `resolveOrCreateWebsiteClient` path, gated strictly on `eventName==='Purchase' && visitorId && channel.type==='WEBSITE'`, that resolves-or-creates the `Client` then calls the existing `PurchasesService.create()` unchanged — deliberately reusing the mature `Client`-joined revenue pipeline (`getProjectStats`/`getConversionFunnel`/`getLeaderboards`/`getAdBreakdown`, all gated on the `OURS_ONLY` `subscribedAt IS NOT NULL` convention) rather than making `Purchase.clientId` nullable and touching every one of those queries — confirmed via research that campaign/buyer/landing leaderboards structurally need a `Client` row to hang that attribution off of anyway. A real, non-obvious bug was caught and fixed during implementation, not by the user: the first cut gated the new branch on `!clientId`, but `resolveClient`'s own new `visitorId` lookup (added a few lines earlier in the same function, so a *returning* website visitor's Purchase call resolves a real `clientId` before reaching the new branch) meant `!clientId` was false for exactly the "repeat customer" case — the branch never ran, no `Purchase` row got created for a second purchase from an already-known visitor, and the request instead fell through to the generic `TrackingEvent`-only path. Fixed by dropping the `!clientId` guard entirely (the real safety net is `channel.type==='WEBSITE'`, not the client-already-resolved check) and moving the block ahead of the generic client-resolution code. A second real bug from the same first cut: the outer `recordEvent` call kept executing after delegating to `resolveOrCreateWebsiteClient`, which internally calls `PurchasesService.create()` — a method that **already** fires its own canonical `TrackingEvent`/ad-platform-forward/automation-trigger internally — so every website purchase produced two `TrackingEvent` rows and would have double-forwarded to Facebook/TikTok CAPI for any purchase carrying real ad-click attribution; fixed with an early `return` right after the new branch succeeds. Also hit a genuine circular-file-import crash on deploy (unlike the already-safe `PurchasesService.getChannelsService()` precedent, which has no reverse edge, `tracking.service.ts` needed `ClientsService`+`PurchasesService`, and both of *those* files already import `TrackingService` directly — a plain top-level `import` closed a real two-file cycle, manifesting as `Nest can't resolve dependencies of the PurchasesService... argument at index [1]` on boot) — fixed by keeping only an `import('...').Type` type reference at the top and moving the actual class reference to a lazily-`require()`'d lookup inside each `getClientsService()`/`getPurchasesService()` method body, resolved via `moduleRef.get(..., {strict:false})` same as the existing precedent. SDK (`apps/sdk/src/browser.ts`) gained a persistent `visitorId` (`crypto.randomUUID()`, `localStorage`, wrapped in try/catch for Safari-private-mode/quota failures — degrades to no re-attribution rather than crashing tracking) sent on every event, and `tcrm.purchase()` auto-derives `idempotencyKey` from `orderId` when not explicitly passed (disclosed limitation: dedup is only as strong as the site's own `orderId` discipline, the same class of accepted risk as `TrackingEvent.eventId`'s existing fallback). `getConversionFunnel`'s "Subscribe" stage label swaps to "Стали клиентом" for `WEBSITE` projects (`'Вступили в канал'` is literally false — nobody joined anything) — the only dashboard change needed anywhere, confirming the reuse-the-pipeline bet. Frontend: `channel-fields-editor.tsx` (the one shared component behind both trees' project-creation forms and both settings channel tabs) gained the `WEBSITE` type/label/select-item/`canSubmit` branch (zero fields required); `clients-filter.tsx`/`clients-table.tsx` (both trees) gained cosmetic `WEBSITE` labels. Live-verified end-to-end against the real running API (not `NestFactory.createApplicationContext`) with an isolated throwaway company: project creation returned `isActive:true` (proving the `getProvider` crash was actually fixed, not just patched in theory); a `PageView` with only a `visitorId` wrote a `TrackingEvent` with `clientId:null` and did not touch `Company.currentClients`; a `Purchase` created exactly one real `Client`+`Purchase`; an exact-idempotencyKey retry produced the identical `eventId` with zero duplicate rows; a second, genuinely-different purchase from the same `visitorId` correctly created a second `Purchase` against the same `Client` (`totalSpent`/`purchasesCount` summed correctly) — this is the exact scenario the `!clientId` bug above would have silently dropped; `getProjectStats`/`getConversionFunnel` reflected the revenue/funnel correctly with zero query changes; and real headless-Chromium screenshots confirmed the project-creation picker, the empty-fields state, the `/clients` list (`Web` badge), and the client detail drawer (geo, attribution, finances) all render correctly with zero console errors, and that the `WEBSITE` project correctly does not appear in the Stories module's project picker. Test data (company, project, channel, clients, purchases, events, user) deleted by exact id afterward.

The same day (2026-09-03), a direct follow-up hardened the WEBSITE channel type shipped earlier: the channel had no actual site address anywhere, `isActive:true` meant nothing (the no-op provider never failed), a landing created for such a project had no working CTA destination, and the settings tabs were a verbatim copy of the Telegram layout. New `Channel.websiteUrl String?` (editable any time — `ChannelsService.update()` already re-runs `tryInitialize()` on every field change, so editing the URL automatically re-verifies, no new endpoint needed). `WebsiteProvider.initialize()` went from a bare no-op to a real check: fetch `websiteUrl`, confirm the response HTML contains both the real CDN script src and this exact project's `data-project-id` (guards against a false-positive match on someone else's install), throwing a descriptive error otherwise — reusing the channel's existing `isActive`/`lastError`/"Переподключить" (relabeled "Проверить" for WEBSITE) machinery unchanged. Because this is the first provider that fetches a URL the tenant themselves supplies (every other provider only ever talks to addresses we control), it needed a from-scratch SSRF guard (`common/ssrf-guard.util.ts`): protocol allowlist, DNS resolution checked against private/loopback/link-local ranges (including the `169.254.169.254` cloud metadata target) both before the request AND again against the post-redirect `response.url` (DNS-rebinding-via-redirect), request timeout, and a response-body size cap. Landing CTAs now reach the real site: `LandingRendererService.renderTemplate()` extends its existing `hasTelegramLink` ternary with a `hasWebsiteLink` branch that emits the literal self-referential placeholder `'{{TG_REDIRECT_URL}}'` (the exact deferred-substitution trick this file already uses for `START_CODE`, since resolving the real URL needs `req.query` which the template-rendering stage doesn't have), resolved later in `injectTrackingScripts` (which does have `req.query`) into the real `websiteUrl` with the visit's fbclid/utm/ad macros merged into its query string — no new redirect endpoint, the site opens directly and its own `track.js` picks up the forwarded attribution via `window.location.search`. The existing CUSTOM-landing/TEMPLATE auto-redirect path got the same `hasWebsiteLink` branch. Zero `template.html` files changed — same variable name reused for a different resolved value, confirmed the only one in every template. Settings tabs (`tabs.tsx`, both trees): Bot/Personal-account tabs turned out to already be hidden for non-Telegram channels (a pre-existing `channel.type==='TELEGRAM'` gate in both page.tsx files, zero code needed); `EventsTab` gained a `channelType` prop filtering `TRACKING_EVENT_TYPES` down to just `Purchase` for WEBSITE (Subscribe/Unsubscribe/Dialogue can structurally never fire without a messenger); `IntegrationTab` gained an explicit numbered how-to-connect card plus a callout on its existing (pre-dating this feature, just never highlighted) server-side signed `POST /track/server/:projectId/event` path as the recommended way to send `Purchase` specifically — not blocked by ad-blockers, and confirmed to already flow through the same `TrackingService.recordEvent`/`resolveOrCreateWebsiteClient` pipeline the browser SDK path uses, so zero backend change was needed to make it work for WEBSITE. SDK gained a public `tcrm.getVisitorId()` (returns the same persisted id `track()` already generates) so a site sending purchases from its own backend can thread the visitor's id through to the signed call and keep it attributed to the same Client as their earlier browser-side events; the three server-integration code samples (`ProjectsService.getSnippet()`) got a one-line `visitorId` addition each with the same explanation. A real gap was caught during live verification, not by the user: `ProjectsService.findOne()`'s and `findAll()`'s explicit Prisma `channel.select` lists (used to build every `/projects` and `/projects/:id` response) both predated `websiteUrl` and didn't include it — the frontend's new URL display would have silently shown nothing for every project despite the column being set correctly in the database; `GET /channels/:id` (`ChannelsService.findOne`, no `select` at all) already returned it as-is. Live-verified end-to-end against the real running API and a real public URL (not `NestFactory.createApplicationContext`): a fresh WEBSITE channel with no URL correctly showed `isActive:false, lastError:"Укажите ссылку на сайт"`; pointing it at a real public page with the correct script tag flipped it to `isActive:true` after a real HTTP fetch; pointing it at a real public page with a *different* project's `data-project-id` correctly stayed rejected (proves the match isn't just "some track.js exists"); a real SSRF probe against `169.254.169.254` was correctly blocked with `"Адрес недоступен для проверки"`; the query-merging logic was verified to produce the exact expected merged URL; real headless-Chromium screenshots on both domains confirmed the URL display, the relabeled "Проверить" button, the filtered Events tab, and the new Integration instructions, while a real Telegram project (the permanent demo account) was independently confirmed to show all 10 original tabs unchanged — zero regression. Test company/project/channel/landing and two temporary public CDN test pages deleted by exact id/key afterward.

The same day, a user-reported bug ('создал FAJAMAX, он пишет что активен, хотя ссылка даже не добавлена') traced to a real gap the WEBSITE feature above exposed rather than caused: `ChannelsService.onModuleInit()`'s rehydration loop calls the raw `provider.initialize()` directly, not the wrapping `tryInitialize()` — on failure it only logs a warning, it never persists `isActive:false`/`lastError` to the DB. For Telegram/WhatsApp/Instagram this was harmless (a rehydration failure there is a transient in-memory-state concern, not a correctness signal), but for `WEBSITE`, `initialize()` is now a real verification — FAJAMAX had been created before this session's WebsiteProvider rewrite (back when it was a no-op that always succeeded), and the subsequent deploy's restart correctly detected the missing `websiteUrl` on rehydration but silently swallowed the failure, leaving the stale `isActive:true` untouched in the database. A second, related gap: the 15-minute `checkAllChannelsHealth` cron's `checkChannelHealth()` fell through to a circular `{healthy: channel.isActive}` check for any non-Telegram type — for `WEBSITE` this never actually re-verified anything going forward either, it just echoed back whatever `isActive` already said. Fixed both: `onModuleInit()` now routes `WEBSITE` channels through the real `tryInitialize()` (persists the correct result) instead of the raw provider call, left unchanged for the other three types; `checkChannelHealth()` gained a `WEBSITE` branch calling `WebsiteProvider.initialize()` directly and translating a throw into `{healthy:false, message}`, so the same 15-minute cron that already catches a blocked Telegram bot now also catches a website that removed the script or changed domains later. Live-confirmed: immediately after deploying and restarting, FAJAMAX's real DB row flipped from the stale `isActive:true` to the correct `isActive:false, lastError:"Укажите ссылку на сайт"` purely from the rehydration pass, with no manual action needed.

Immediately after, the user pointed out the fix was correct but invisible: a small generic status pill next to the project title (showing raw `channel.type`/`isActive`) was the only signal, and most people don't notice it or understand what to click ("многие даже не понимают что это подключено"). Added a prominent amber callout banner on the project overview page (both trees) shown only when `channel.type==='WEBSITE' && !channel.isActive` — displays the real `channel.lastError` verbatim plus a "Подключить сайт" button linking straight to `/projects/:id/settings?tab=integration` (the same `?tab=` URL convention `TrackingEventsSummaryPill` already established for the Events tab); the banner disappears automatically once the channel is verified, no extra state to track. Also fixed the small pill itself to show `CHANNEL_TYPE_LABEL[type]` ("Обычный сайт") instead of the raw enum string `WEBSITE` — a pre-existing cosmetic gap for every non-Telegram channel type. Required adding `lastError`/`websiteUrl` to two frontend-only type definitions that had never needed them before (Studio's `PrototypeProject` in `lib/prototype-project-data.ts`, and classic's local `Project` interface in the project overview `page.tsx`) — the API already returned both fields correctly, the gap was purely a TypeScript-level omission. Live-verified on an isolated throwaway project on both domains: banner renders with the exact `lastError` text, the button correctly navigates to the Integration tab, zero console errors.

The same day, a user report ('для сервера и для публичного скрипта стоят разные id проекта, и на серверной не работает, показывает project not found') turned out NOT to be a backend bug: a real live test against the deployed server-side endpoint (`POST /track/server/:projectId/event`, HMAC-signed, correctly documented to take `project.id`) succeeded with the real `project.id` value, confirming `TrackingService.recordEvent`/`resolveOrCreateWebsiteClient` work exactly as shipped. The actual problem was that the Integration tab's "Ключи доступа" card showed `publicToken` (labeled "Public Token", for the browser SDK's `/track/:publicToken/event`) while `project.id` (needed for the server-signed path) only ever appeared inside the code-sample text, never as its own labeled field — two same-shaped opaque strings sitting side by side with no visual distinction, easy to swap by mistake, which is exactly what happened (the literal English "Project not found" string only exists on the *other*, `publicToken`-keyed endpoint, `TrackingController.trackEvent`, confirming the user had used a publicToken where a project id belonged). Fixed with three small, purely-UX changes rather than any backend logic change: both trees' `IntegrationTab` (`tabs.tsx`) gained an explanatory line under "Public Token" plus a new, separately labeled "Project ID" field (with its own copy button) between "Public Token" and "Secret Key," calling out explicitly that it's a different id from the one above it; and `TrackingController.trackServerEvent`'s bare `NotFoundException()` for an unresolvable project id gained a real message ("проверьте Project ID (не Public Token) во вкладке «Интеграция»") so a future repeat of the exact same mistake fails with a self-explanatory error instead of a bare 404. Live-verified against the real running API: a signed request keyed on a `publicToken` value (reproducing the user's exact mistake) now returns the new descriptive 404 instead of a bare one. Test data (5 throwaway companies accumulated across registration retries during verification) deleted by exact id afterward.

Immediately after, the user relayed real feedback from an external developer who had tried to integrate against the just-fixed Integration tab and still failed — a genuine bug beneath the UX fix above, not a repeat of it. The developer's report showed 4 tried variants (`Authorization: Bearer <secretKey>`, `X-API-Key`, secretKey as a query param, and duplicating the project id in the request body) plus 5 guessed endpoint paths (`/purchase`, `/track/purchase`, `/events`, `/server/purchase`, `/projects/:id/events`) — none matching the real contract (`POST /track/server/:projectId/event` with `X-Signature`/`X-Timestamp` HMAC headers), and the returned message was the plain English "Project not found," which only the *other*, `publicToken`-keyed endpoint (`trackEvent`) produces — proving the developer was hitting the wrong URL the whole time, and their guesses replaced the real auth scheme entirely rather than supplementing it. Root cause traced to the "Серверная интеграция" card's default-shown "Node.js (SDK)" tab: its code sample opened with `npm install @trafficcrm/sdk` and `require('@trafficcrm/sdk')` — a package that has never been published to the npm registry at all (confirmed via a real `npm view` 404) even though the `TrackClient` class it references genuinely exists and correctly implements the contract at `apps/sdk/src/index.ts` (built only into the CDN's browser bundle, never published for Node). An external developer reading only the default tab would hit that `npm install` 404 immediately, have no working reference implementation, and — exactly as this report showed — start guessing REST conventions from scratch instead of ever seeing the correct HMAC scheme sitting one click away in the PHP/Python tabs. Fixed by rewriting the Node.js example (`ProjectsService.getSnippet`) into a dependency-free, copy-paste-runnable script using only Node 18+'s built-in `fetch`/`crypto` — mirrors the PHP/Python examples' exact contract, no install step, nothing to fail. Tab label changed from "Node.js (SDK)" to "Node.js" in both trees since there is no separate SDK package involved. Live-verified by copy-pasting the exact generated example text (substituting a real project id/secretKey, unmodified otherwise) into a real Node process and running it against the real production API — HTTP 201, real `eventId` returned, and a real `Client`+`Purchase` row created end-to-end confirming the whole pipeline. Test company/project/client/purchase deleted by exact id afterward.

The same day, a follow-up asked for a genuinely new capability: since a WEBSITE project's site is already live on its own domain (unlike Telegram, which needs an intermediary CRM-rendered Landing), a buyer should be able to get a Facebook/TikTok destination-URL link pointing straight at the site — no Landing/DomainPath detour through our own domain at all. Investigation confirmed this genuinely didn't exist: `GetLinkDialog`/`buildTrackedLink()` are hard-wired to a `Landing` attached to a `DomainPath` (schema requires exactly one of `landingId`/`abTestGroupId` — no "bare project" variant), and `LandingType.EXTERNAL` exists in the enum but was never implemented. Shipped a landing-less parallel path rather than trying to force a `Landing` to represent "no landing": `buildTrackedLink()` in `apps/web/src/lib/landings.ts` was split so its buyerRef/pixelId/FB-or-TikTok-macro logic (the part that doesn't care about the destination) lives in a new private `buildTrackedLinkParams()`, reused by both the original (attaches to a Landing's domain+path) and a new `buildWebsiteTrackedLink(websiteUrl, ...)` (attaches the same params straight to `channel.websiteUrl`, appending `&` instead of `?` if the URL already has a query string) — zero behavior change for existing landing-based links, confirmed by `tsc` and by the fact the diff only moves code, changing no logic. `GetLinkDialog`'s pixel-select-and-link-textarea JSX was likewise extracted into an exported `PixelLinkFields` presentational component (along with `ALL_PIXELS` and a new `resolveLinkBuyerId()` helper, replacing 3 duplicated inline lines) so the new `GetWebsiteLinkDialog` (`components/get-website-link-dialog.tsx`) reuses it verbatim rather than re-implementing the same pixel dropdown a second time — one shared component serves both trees unchanged (already proven safe by the 2026-08-18 `.studio.dark` CSS-variable fix, which makes every Portal-rendered shadcn `Dialog`/`Select` render correctly in Studio's dark theme with zero per-component work). New "Получить ссылку" (`Link2` icon) button added to both project overview pages' action row, gated on `channel.type==='WEBSITE' && channel.isActive` — only shown once the site is actually verified, since a link to an unverified/misconfigured site would silently not track anything. Live-verified end-to-end via headless Chromium on both domains against an isolated test project (a temporary real page hosted on the production CDN bucket carrying the correct script tag, needed because the dialog only renders once `isActive` is real, not fakeable client-side): the button and dialog render correctly, the generated link correctly bases itself on the site's own URL (never a CRM domain) with buyerRef/pixelId/macro params appended, pixel selection correctly switches between "no pixel" and a specific pixel's params, and the copy button correctly writes to the clipboard, on both domains, zero console errors. Test company/project/pixel and the temporary CDN test page deleted by exact id/key afterward.

The same day, a bundled 5-question follow-up ("проверь все функционалы и убери те что не могут быть использованы... на проектах website") audited every remaining feature that assumes a bot/messenger exists, using 2 parallel Explore agents to trace the real mechanics before touching anything. (1) **Favicon avatar**: WEBSITE channels had no avatar source at all (Telegram's comes from `tgAvatarFileId` via the bot API) — new `Channel.websiteFaviconUrl String?`, populated inside `WebsiteProvider.initialize()` by regex-scanning the SAME HTML already fetched for the track.js check (no extra request) for the first `<link rel="icon"|"shortcut icon"|"apple-touch-icon">`, resolving a relative `href` against `websiteUrl` via `new URL(href, base)`; a parse/fetch failure is logged and swallowed, never blocking verification itself. New `ChannelsService.fetchChannelAvatarBuffer()` wraps the existing `fetchTelegramAvatarBuffer()` with a WEBSITE fallback that re-fetches the favicon bytes through the SAME SSRF guard as verification (the stored URL was extracted from third-party HTML once; trusting it forever without re-checking on every avatar view would reopen the exact class of risk the guard exists for) and proxies them through the existing `/channels/:id/avatar` endpoint — reused by `LandingsService.streamAvatar`'s landing-default-avatar fallback too, both callers now trusting one normalized `contentType` (Telegram's is force-set to `image/jpeg`, matching a prior live finding that Telegram's raw file server actually returns `application/octet-stream`). Frontend: new `hasChannelAvatar()` helper in `lib/landings.ts` replaced 15 separate inline `!!channel.tgAvatarFileId` checks across both trees (a mechanical Python-scripted find/replace, hand-fixed for 4 sites using optional-chaining variable expressions the regex mangled), plus widening the Settings→Каналы avatar-render gate from `TELEGRAM`-only to `TELEGRAM || WEBSITE` in both trees (it would otherwise never show regardless of `hasAvatar`). (2) **Bot Scenarios**: confirmed structurally dead for WEBSITE — every trigger path is Telegram-only, and `getChannelUserId()` (the one channel-agnostic entry point, used by FIRST_DEPOSIT/REPEAT_DEPOSIT triggers) returns `null` for WEBSITE before ever reaching a provider; even the SEND_MESSAGE step itself is hardcoded to `TelegramProvider`, not routed through `ChannelsService.getProvider()` at all. "Сценарии" button removed from both trees' project overview pages for `channel.type==='WEBSITE'`. (3) **Notification bell wording**: `Бот проекта «X» отключён` (Studio's own bell, `dashboard/studio/layout.tsx`) is pure client-side string-templating with no server-side notifications table behind it — branched on `channel.type` to say "Канал" instead of "Бот" for WEBSITE. (4) **Pushes/broadcasts**: also structurally dead — `ChannelsService.sendMessage()` calls the same `getChannelUserId()`, short-circuiting to a failure before ever reaching `WebsiteProvider.sendMessage()`'s own no-op, so every recipient of a WEBSITE-project push fails silently. "Рассылка" button removed from both project pages, and the composer's project picker (`push-composer.tsx`, both trees) now filters `channel?.type !== 'WEBSITE'` before the existing permission filter, so a WEBSITE project can no longer even be selected from the company-wide composer. Personal Broadcasts confirmed ALREADY safe with zero changes needed — gated on `tgPersonalConnected`, structurally always false for WEBSITE. (5) **Audience overlap**: confirmed dead by design — the matrix/pair-matching SQL joins strictly on `Client.tgUserId` (an explicit, intentional scope decision from 2026-07-04, not an oversight), which is always null for WEBSITE clients (only `visitorId` is ever set). `AudienceService.getOverlapMatrix()` now excludes WEBSITE-channel projects from the accessible-project set before building rows/columns, rather than showing them as a permanent all-zero row; the pair-detail endpoint (`getOverlapDetail`/`getOverlapPairSummary`) was deliberately left unguarded against a hand-typed WEBSITE project id in the URL — the matrix is the only real entry point, and a direct hit just renders an empty pair page, not a crash. Live-verified via direct API calls against real isolated test companies/projects (not `NestFactory.createApplicationContext`): a real page+favicon uploaded to the production CDN bucket confirmed `websiteFaviconUrl` extraction and the avatar proxy serving the exact bytes with the right content-type; the overlap matrix confirmed to include a same-company TELEGRAM project while excluding a WEBSITE one. A headless-Chromium pass on both domains confirmed the Сценарии/Рассылка buttons gone from a WEBSITE project's page while Получить ссылку/Лендинги/Настройки remain, the push composer's picker excluding WEBSITE projects entirely, and the Studio notification bell showing "Канал" instead of "Бот" — and caught one real gap the code review missed: the Settings→Каналы avatar rendered correctly (confirmed via network log — real `GET /channels/:id/avatar` → `200 image/png`), but the SAME favicon was invisible on the project overview page itself, because `ChannelAvatar` there was independently gated behind a hardcoded `channel.type === 'TELEGRAM'` check in `apps/web/src/app/(dashboard)/projects/[id]/layout.tsx` and `apps/web/src/app/dashboard/studio/projects/[id]/page.tsx` that `hasChannelAvatar()`'s introduction hadn't touched. Grepping for every other `ChannelAvatar` call site guarded by the same hardcoded `TELEGRAM`-only condition (not just the ones the mechanical `hasAvatar=` regex pass had already touched) turned up 4 more instances with the identical gap — the Studio home page, the Studio company-wide projects list, the classic home page, and the classic company-wide projects list — all widened to `TELEGRAM || WEBSITE` in the same pass. Test companies/projects/CDN files deleted by exact id/key afterward.

On 2026-09-07, a user question about custom (ZIP-uploaded) landings and client-hosted external landings surfaced two real gaps, both fixed after confirming zero risk to already-live landings. **Fix 1 — CUSTOM landings never got `{{TG_REDIRECT_URL}}` substitution.** TEMPLATE landings resolve this placeholder inside `renderTemplate()`; CUSTOM landings (client-uploaded ZIP) skip `renderTemplate()` entirely and only ever passed through `injectTrackingScripts()`, which had no equivalent substitution for the Telegram case (only for `WEBSITE`-channel destinations) — a client building their own landing HTML had no way to get a working join button without hand-constructing the raw internal `/tg-redirect?code=...&landingId=...` URL themselves. Fixed by extracting the existing Telegram-link-building expression (previously inlined twice, once for the unused-by-CUSTOM `renderTemplate` case and once for `autoRedirectUrl`) into one `telegramJoinUrl` computed in `injectTrackingScripts()`, then unconditionally `replaceAll`-ing `{{TG_REDIRECT_URL}}` with it. Zero regression risk for TEMPLATE landings, confirmed by construction: by the time html reaches `injectTrackingScripts()`, `renderTemplate()` has already substituted every occurrence of that placeholder (Telegram link, deferred `{{TG_REDIRECT_URL}}` for `WEBSITE`, or empty string — all three branches), so the new `replaceAll` finds zero matches there. **Fix 2 — a genuinely new capability: a landing hosted entirely on the client's own server/domain can now get full Telegram-join attribution.** Previously, `start:<code>` (the Redis key carrying fbclid/ttclid/utm/buyer/pixel attribution through to the Telegram bot) was written in exactly one place — `LandingRendererService.injectTrackingScripts`, which only ever runs when we render our own Landing. An external page had no way to seed it, meaning `PageView`/`Lead` tracking already worked there (`track.js` posts directly to `/track/:publicToken/event`, no rendering needed) but a real Subscribe/join was structurally unattributed. New `common/external-landing-attribution.util.ts` (`buildAttributionFromQuery`, `hasAnyAttributionSignal`) rebuilds the identical attribution shape `injectTrackingScripts` already produces, but from a plain request's raw query params instead of a landing-render context — deliberately a fresh, small copy of `decodeAdMacro`/`UNSUBSTITUTED_MACRO_PATTERN` rather than exporting the original from `landing-renderer.service.ts` (that function has a multi-round bug history — 2026-08-20/27/31 — refactoring it to be shared would risk the already-hardened TEMPLATE/CUSTOM path for a feature that doesn't touch it). `TrackingController.tgRedirect` now calls this when `?code=` is absent but the request itself carries real attribution signal, generating a fresh `start:<code>` on the spot (same TTL constant, mirrored not shared) before proceeding exactly as if a code had been passed in — this also transparently makes `PRIVATE_CHANNEL_REQUEST`'s one-time-invite-link path work for external landings too, since that logic already keys off whatever `effectiveCode` resolves to. Passing an explicit `?code=` (every existing CRM-rendered landing) short-circuits this entirely — confirmed live by scanning Redis before/after: a direct `/tg-redirect` hit with `code` present created zero new keys, one without `code` but with `fbclid`/`utm_*` created exactly one, correctly shaped. The client side needed a matching change: `apps/sdk/src/browser.ts`'s existing `/tg-redirect` click handler (previously only appended `?fbp=`) now also forwards fbclid/ttclid/utm_*/the project's own paramMap-configured ad-macro param names from the page's own `window.location.search` onto the link — but only when the href doesn't already have `?code=`, making it a genuine no-op for every existing CRM-rendered landing (verified via a `vm`-sandboxed run of the real built bundle: a link with `?code=` present triggered zero DOM changes and no `preventDefault`, while a bare external-page link correctly got `fbclid`/`utm_source`/`utm_medium` appended and intercepted). `getSnippet()`'s public embed tag also gained `data-param-map` (previously only present on CRM-rendered landings' own script tag) — without it, a project using non-default param names would have had its ad-macros silently invisible to the SDK on any external page using the plain snippet, WEBSITE-channel integrations included. Live-verified end-to-end against the real running API (internal `serve-landing` route hit directly from localhost, the same path Nginx uses in production, bypassing the need for a real registered domain): a CUSTOM landing's literal `{{TG_REDIRECT_URL}}` correctly resolved to a working tg-redirect URL, an existing TEMPLATE landing rendered byte-for-byte the same tg-redirect URL shape as before (zero regression), and the SDK's two click-handler scenarios both passed. SDK republished with a fresh cache-busted hash (confirmed `cf-cache-status: MISS` on the new versioned URL). Test company/project/landings/MinIO objects/Redis keys deleted by exact id/key afterward.

The same day, a direct follow-up asked for two significantly larger pieces on top of the fixes above, both planned via a full `EnterPlanMode` cycle (1 Explore + 1 Plan agent) given the size: (1) real HTML/link validation for CUSTOM (ZIP) landings, gating "Активен" status on the result rather than always auto-publishing; (2) `LandingType.EXTERNAL` — previously a dead enum value the renderer explicitly 404'd (`landing-renderer.service.ts`, comment "шаг 2.4, не реализуется здесь") — built out as a real, first-class Landing entity for a page hosted entirely on the client's own server, with per-landing attribution and a "Проверить подключение" verification flow mirroring `WebsiteProvider`. Per explicit `AskUserQuestion` decision, A/B/n testing between EXTERNAL landings was ruled OUT (a shared randomized-split link across different client domains would need a whole new redirect mechanism the user didn't want) — EXTERNAL landings are instead explicitly blocked from ever joining an `AbTestGroup`. **Schema**: `Landing` gained `externalUrl String?` and `lastError String?` (mirrors `Channel.websiteUrl`/`lastError` exactly — `DRAFT`/`PUBLISHED` already sufficed as the "unverified"/"verified" pair, no new `LandingStatus` value needed). **Shared SSRF-safe fetch, extracted not duplicated**: new `common/safe-html-fetch.util.ts` (`fetchPublicHtml`) is a straight extraction of `WebsiteProvider`'s former private `fetchHtmlSafely` (protocol/URL validation, `assertPublicHost` pre- and post-redirect, streamed-read cap) — `WebsiteProvider.initialize()` now calls the shared version with zero behavior change, and the new EXTERNAL-landing verifier reuses the exact same hardened logic rather than growing a second, independently-drifting copy of SSRF-guarded fetch code (the opposite call than the deliberately-NOT-shared `decodeAdMacro` case above — that one has a fragile bug history worth insulating from unrelated callers, this one is small/mechanical and security-sensitive, so one source of truth is the safer choice). **CUSTOM review** (`LandingsService`, new `landing-review.util.ts`): `createCustom`/`uploadCustomLanding` were merged into one `processAndReviewZip()` that, after the existing structural checks (unchanged: `.zip` extension, 50MB cap, zip-slip, `index.html`-at-root), captures the `index.html` buffer and the archive's file list *before* extraction, then runs `reviewLandingHtml()` — a new `htmlparser2`-based check (added as a direct `apps/api` dependency; it was already vendored at the needed version as an incidental transitive dependency of `telegram`/gramjs, confirmed via `node_modules` before adding, zero install cost) covering three checks: the file actually parses as HTML (≥1 tag found — `htmlparser2` is deliberately the same permissive parser cheerio is built on, so this never false-fails on merely-untidy-but-real markup), the literal `{{TG_REDIRECT_URL}}` placeholder is present (this exact substring is what `injectTrackingScripts`' `replaceAll` already looks for in production, so the check's semantics match runtime behavior exactly), and every relative `<link href>`/`<script src>`/`<img src>` resolves to a real file in the archive (absolute/protocol-relative/`{{...}}`-templated refs are skipped, not flagged). All checks pass → `publish()` (auto-publish, as before); any fail → `lastError` set to a readable summary, status stays `DRAFT` — and, deliberately, `uploadCustomLanding` (re-upload) now runs through this SAME gate every time, meaning a previously-`PUBLISHED` landing can revert to `DRAFT` if the replacement archive is broken (a real behavior change from "re-upload never touched status," a direct and correct consequence of "not active until it passes review" applying uniformly, not just on first upload). **EXTERNAL landing** (`LandingsService`): `createExternal()` creates a `DRAFT` row with no auto-publish (verification is a separate explicit action, unlike CUSTOM); `verifyExternalLanding()` fetches the page via the shared `fetchPublicHtml` and checks four things — page reachable, the tracking script tag present, `data-project-id` matches this project, and (the one check beyond what `WebsiteProvider` does for WEBSITE channels) `data-landing-id` matches *this specific* landing, not just any snippet for the project — plus a fifth `join-link` check only for `TELEGRAM`-channel projects (confirms the page actually points its button at `/tg-redirect`); all pass → `publish()`, any fail → `lastError` + stays `DRAFT`. `getExternalLandingSnippet()` returns a ready-to-copy `<script>` tag (mirrors `ProjectsService.getSnippet()`, plus `data-landing-id`) and a ready join-button href. **The attribution link-up that makes per-landing stats work for a page we never render**: `data-landing-id`/`data-ab-test-group-id` on `track.js` were already fully wired end-to-end from an earlier session round (SDK reads the attribute → sends with every event → `TrackEventDto.landingId` already whitelisted → `TrackingService.recordEvent` writes it straight into `TrackingEvent.payload.landingId`, the exact JSON path `computeAbTestGroupMemberStats` already reads) — so handing an EXTERNAL landing's own id to the client in its snippet was sufficient with zero further backend/SDK plumbing for PageView/Lead/Purchase. The one genuinely new SDK piece: `withExternalAttribution()` (added earlier today for fbclid/utm forwarding) now also forwards the already-parsed `landingId`/`abTestGroupId` onto a code-less `/tg-redirect` click, under the same "only if the link doesn't already carry `?code=`" guard — meaning `getExternalLandingSnippet()`'s join href can stay a plain, static URL with no query string at all; the script tag's `data-landing-id` is the single source of truth, stamped on at click time. **A/B block**: `assertValidMembers()` (backend) and `AbTestGroupDialog`'s candidate list (frontend, `landing-card.tsx`) both now reject `type === 'EXTERNAL'` explicitly — confirmed live via a real `POST /ab-test-groups` attempt with one EXTERNAL landing among members, correctly rejected with a named-landing error message. **Frontend**: new shared `ReviewChecklist` component (pass/fail list with detail text, used by both the ZIP-upload dialog and the new EXTERNAL flow) and new `CreateExternalLandingDialog` (one dialog, two stages driven by whether the landing exists yet: name+URL form, then live snippet/join-link textareas with copy buttons + "Проверить подключение" button + checklist) — both trees gained a new "Лендинг на вашем сервере" button next to "Загрузить ZIP", the ZIP dialog gained inline instructions text sourced from this session's earlier customer-facing artifact, and `LandingCard` gained an `onManageExternal` prop (same pattern as the existing `onReupload`) plus hid its domain-attach button/text and A/B-test button for `type==='EXTERNAL'` (showing the real `externalUrl` instead) — the standalone per-landing detail page (`landings/[landingId]/page.tsx`, both trees) got the same domain-button hiding treatment for consistency, though its own "Получить ссылку" button stays gated on the pre-existing `attachment` check rather than also branching for EXTERNAL, a deliberately narrower fix since that page is a secondary entry point and the primary list-page flow already fully supports it. `GetLinkDialog` gained an EXTERNAL branch reusing the already-built `buildWebsiteTrackedLink()` (from the earlier WEBSITE-channel "landing-less link" feature) — clean reuse, zero new link-building logic. Live-verified end-to-end against the real running API: three ZIP scenarios (valid → auto-`PUBLISHED` and confirmed actually renders via the internal `serve-landing` route; missing placeholder → `DRAFT` with correct `lastError`; broken asset ref → `DRAFT` naming the exact missing files) plus a re-upload of a fixed archive flipping `DRAFT`→`PUBLISHED`; an EXTERNAL landing created against a real page uploaded to the CDN bucket carrying the exact generated snippet → all checks passed, `PUBLISHED`; a second EXTERNAL landing pointed at a page with a *different* landing's `data-landing-id` → only that one check correctly failed; a direct `/tg-redirect` hit with `&landingId=...&fbclid=...` (simulating the SDK's click-time forwarding) produced a real `302` with `&start=<code>`, and the generated Redis blob carried the exact `landingId`/`fbclid` passed in; the A/B-block rejection. Test company/project/landings/CDN objects/Redis keys deleted by exact id/key afterward; a separate headless-Chromium Agent verified all 4 new UI elements (button placement, the two-stage dialog including its failure-checklist rendering against a deliberately wrong URL, the ZIP dialog's new instructions, zero console errors) on both domains, confirming clean — no fixes needed.

On 2026-09-07, a customer-reported bug on a real production CUSTOM landing (`manosya.click/7neyyfv7/w1cjt9cb`, an id-derived two-segment `DomainPath.path`) with a solid, self-diagnosed writeup: hitting the domain path WITHOUT a trailing slash still renders the landing (200) but every relative asset reference inside it (`images/icon.jpg`) 404s, because per RFC 3986 the browser resolves a relative URL against the current address with its LAST path segment dropped — without a trailing slash that drops `w1cjt9cb` itself, turning `images/icon.jpg` into `.../7neyyfv7/images/icon.jpg` instead of `.../7neyyfv7/w1cjt9cb/images/icon.jpg`. Root-caused to `matchDomainPath()` (`domain-path.util.ts`): both the bare path and the path+`/` resolve to the identical `subPath: ''`, so `serveCustomFile` serves the same `index.html` either way with nothing in the response distinguishing the two cases — a systemic bug hitting every CUSTOM landing with nested-folder ZIP assets (the exact pattern this session's own new upload instructions describe as supported) whenever accessed without a trailing slash, not a fault of the specific archive. Fixed with the standard directory-URL canonicalization every web server already does: `LandingRendererService.renderByDomain` now issues a 301 to the same path + `/` (query string forwarded via `req.originalUrl`, untouched — buyerRef/pixel/fbclid ordering isn't at risk since it's not reconstructed) whenever `matchDomainPath` resolves `subPath: ''` AND the original `fullPath` didn't already end in `/` — the one condition that isolates the bare-path branch from the already-correct-with-slash branch, checked BEFORE loading the `Landing` at all so it applies uniformly regardless of type (TEMPLATE never has nested relative assets so the redirect is a no-op cost there, but scoping it to CUSTOM only would have meant an extra DB read just to decide, for zero behavioral gain). Chose a redirect over the alternative fix (injecting `<base href>` into the HTML, feasible since tracking-script injection already modifies `<head>`) because it fixes every kind of relative reference — CSS `url()`, inline `fetch()`, not just tag attributes — and matches standard server behavior for directory-shaped URLs. The specific example landing (`TEST_ROLANDO_LAND`) had been independently archived/soft-deleted by the customer's own team ~4 minutes before the fix was investigated (confirmed via its `deletedAt`/`updatedAt` timestamp, project itself untouched) — unrelated to this fix, so verification used a fresh isolated CUSTOM landing with a real nested `images/` folder instead: real API upload → real ZIP-review-gate pass → a throwaway `Domain`/`DomainPath` row inserted directly (bypassing real DNS/cert verification, the established technique for this class of test) → confirmed via raw `http.request` calls (plain `fetch()` forbids overriding the `Host` header, a small trap worth remembering) that the bare path 301s to path+`/` with the query string preserved exactly, the with-slash path still serves the correct HTML unchanged, and the nested asset resolves 200 via the correct prefix. Test company/project/landing/domain/path deleted by exact id afterward; API rebuilt and `pm2 restart`ed with a clean "Nest application successfully started" and zero regression in live FB CAPI/TikTok Events traffic observed immediately post-restart.

On 2026-09-15, a user report that IVAN-team projects accumulate implausible click volume ("172613 кликов на 2022 пдп за месяц") led to a confirmed, system-wide tracking bug plus a redesign of how the Telegram transition is counted, modelled on a competitor's landing the user supplied. Root cause: `fireAutoRedirect()` (`apps/sdk/src/browser.ts`) unconditionally called `track('Lead')`, and for every template except `age-gate-invite` it ran immediately on page load — so every view of a landing with `autoRedirect=true` sent a Lead with no human action. The template's join button carries `data-track="Lead"` in parallel, so a genuine click added a second Lead, and repeat taps on a `tg://` link that silently fails inside TikTok/Instagram in-app browsers added more (real tail: 52 Leads from one ad click). Confirmed on live data rather than inferred: Lead exceeded PageView (structurally impossible in a real funnel), per-landing L/PV sat at exactly 1.00-1.07 (57/57, 180/182, 2046/2087 in one day), and the correlation was decisive — `autoRedirect=true` averaged L/PV 1.42 across 46 landings versus 0.56 across 8 with it off. Two exception classes confirmed the mechanism rather than contradicting it: `age-gate-invite` (deferred trigger, needs a real click) and landings with `tiktokBrowserHint` (the escape branch `return`s before `fireAutoRedirect`) both sat far below 1. Verified live in headless Chromium against real production landings — a bare load of `manosya.click/7neyyfv7/s8h0t5rp` fired PageView **and** Lead with nothing touched, while an `autoRedirect=false` landing fired only PageView. The damage was not just reporting: the fake Lead was delivered to ad platforms as a conversion (4082 Lead versus 20 real Subscribe to TikTok in one day), so the platform saw a conversion on nearly every click, optimised toward the cheapest possible click, and escalated junk volume — a self-reinforcing loop, which is what buyers were experiencing as "боты" and broken pixel optimisation. Separately quantified and reported but NOT changed (it is a per-landing data toggle on real customer landings, left to the user): `tiktokBrowserHint` correlates with a conversion collapse — on TikTok-dominant landings CR 0.51% with it on versus 1.82% off, and on a clean same-project control pair (identical traffic mix, one period) SURJ1 with the hint converts at 0.68% against Conley RolandoCR's 1.81%, with the overlay firing for 56.4% of real visitors. Also established that the PageView count itself is NOT inflated — 1.12-1.30 views per unique `ttclid`, i.e. views track real delivered ad clicks; the volume ramp was a genuine Facebook→TikTok switch around 2026-09-09 (TikTok went from ~40 to 7000-14500 views/day while Facebook collapsed to near zero). The fix, modelled on the competitor's landing (which sends only `fbq('track','PageView')` from the browser, no Lead at all, and counts the click server-side on its own `/ldpg/<id>/redirect` endpoint): `fireAutoRedirect()` no longer fires Lead — genuine clicks keep flowing through the existing `data-track="Lead"` button handler, which becomes the sole source of Lead; `track()` gained per-visit deduplication for Lead (`DEDUPED_EVENTS`/`firedOnce`, deliberately excluding PageView since a real reload is an honest second view) so repeat taps on a dead button can no longer multiply; and `TrackingController.tgRedirect` now writes a `TelegramRedirect` event server-side, which backs a new "Переход в Telegram" funnel stage between PageView and Lead (`ClientsRepository.getConversionFunnel`). That new event is written directly via Prisma, deliberately bypassing `TrackingService.recordEvent`, so it never reaches Facebook/TikTok — an event that fires on ~100% of visits is useless for optimisation and is exactly what broke pixel learning; it is recorded only on the real 302 branch, never on `?format=json` (that is the SDK's pre-tap link prefetch, so counting it would reintroduce the same inflation from the other side), and fire-and-forget since `/tg-redirect` is the hottest path in the system. The funnel stage is hidden for WEBSITE channels and when the count is zero, so historical data (before this date, when the event did not exist) does not render an empty stage. Two further ideas adopted from the same competitor landing: `withFbp` became `withFbCookies` and now forwards `_fbc` alongside `_fbp` (they wait for and append both; we only ever sent `_fbp`), and `?ar=0` in a landing's URL now disables auto-redirect so a landing with it enabled can actually be viewed. Verified pre-publish with a `vm`-sandboxed run of the real built bundle across 5 scenarios (bare load fires PageView only and still redirects; one genuine click fires exactly one Lead; six repeat taps still fire exactly one; `?ar=0` blocks the redirect while PageView still fires; both FB cookies appear on the redirect URL), then live after deploy: real production landings serve the new `track.js?v=06d650d167` and fire only PageView on a bare load, and real `TelegramRedirect` rows appeared from live traffic (3 recorded against 4 nginx `/tg-redirect` hits post-restart, the difference being a `format=json` prefetch). Landing HTML was confirmed `no-cache, no-store` / `cf-cache-status: DYNAMIC`, so every visitor gets the new hash immediately with no edge-cache lag. The post-deploy sample was small (late-night lull, ~1-3 redirects/minute versus 2000+/hour at the 19-21 UTC peak) — real validation lands at the next traffic peak. NOT done, flagged as a separate task: proxying `connect.facebook.net/en_US/fbevents.js` and `analytics.tiktok.com/.../events.js` through our own domain the way the competitor self-hosts its `fb.js` — ad blockers cut a share of our browser-side events today, but standing up a caching reverse proxy for third-party pixel scripts needs its own verification pass and could break the pixel for every tenant if rushed.

Immediately after, the user supplied the competitor's own settings UI for the same concern — two independent toggles, "Отправлять событие Lead при клике на Join Channel" and "Отправлять событие Lead при авто-редиректе" — and asked for the same. This is a better design than the hardcoded removal shipped hours earlier: the competitor's landing had auto-redirect Lead switched OFF (`leadTracked: false` in its own `appSettings`), but the capability exists as a per-landing choice rather than a product-wide rule. Shipped as `Landing.leadOnClick Boolean @default(true)` / `Landing.leadOnAutoRedirect Boolean @default(false)` — defaults chosen so no existing landing changes behaviour by even one event: a button click still sends Lead exactly as it always has, and auto-redirect still sends nothing, matching the state right after the earlier fix. Threaded through `LandingBehaviorDto` (both new fields optional booleans, defaults living in the schema rather than the DTO), both `LandingsService` write sites (`createFromTemplate` and `update`, the same two-place whitelist every behaviour field already uses), and `LandingRendererService.injectTrackingScripts`, which emits `data-lead-on-click="false"` / `data-lead-on-auto-redirect="true"` only when the value differs from the SDK's own default — so a landing on defaults renders a byte-identical script tag to before, and the attributes never appear at all in the common case. `apps/sdk/src/browser.ts` reads both (`!== 'false'` and `=== 'true'` respectively, so a missing attribute means the default): `fireAutoRedirect()` regained its `track('Lead')` but gated behind `leadOnAutoRedirect`, and the generic `[data-track]` click handler skips only `Lead` when `leadOnClick` is off, leaving InitiateCheckout/Purchase and any other event untouched since those have nothing to do with the join button. Frontend is one edit to the shared `LandingBehaviorFields` component (covers create-flow and edit-page forms in both trees) — a bordered "События Lead" group holding both switches, plus the two fields added to `LandingBehaviorState`/`EMPTY_LANDING_BEHAVIOR`/`behaviorStateToPayload` and to `isLandingBehaviorNonDefault` (which counts *disabling* `leadOnClick` or *enabling* `leadOnAutoRedirect` as a real change, since `leadOnClick:true` is itself the default); both landing detail pages prefill from the landing with `?? true` / `?? false` fallbacks, and `LandingItem` gained both fields. Verified with a `vm`-sandboxed run of the real built bundle across all four toggle combinations × three interaction patterns (bare load, one genuine click, five repeat taps) — every cell correct. That run also caught a real inaccuracy in the UI helper text before it shipped: it warned that enabling both toggles would produce two Leads per visit, but the per-visit Lead dedup added earlier the same day suppresses the second one, so a click yields exactly one Lead either way — the text was rewritten to name the actual risk (with auto-redirect Lead on, Lead fires on essentially every visit, so the ad platform receives a conversion per click and the event carries no optimisation signal) rather than a duplicate-count that cannot happen. Backend round-trip verified live against the real running API on an isolated throwaway company/project/channel plus three landings covering the combinations, rendered through the internal `serve-landing` route (note: that route lives under the global `/api/v1` prefix — a bare `/internal/...` path 404s): the defaults landing emitted no attributes at all, `leadOnClick:false`+`leadOnAutoRedirect:true` emitted both, and `leadOnClick:false` alone emitted just the one. Test data deleted by exact id afterward. Live post-deploy confirmation: real production landings serve `track.js?v=fd59ff4a20` and still fire only PageView on a bare load.

On 2026-09-16, a new company-level feature shipped: a Telegram bot for internal staff alerts ("вкладка настроек, где сможем добавить бота ТГ для оповещений... вписываются юзернеймы... кто и какие оповещения будет получать"). Lives as a third "Оповещения" tab on the existing `/settings` page in both trees (Owner/Admin/SUPER_ADMIN only, matching `@Roles(OWNER, ADMIN)` on the new `NotificationsController`), rendered by one shared `components/notifications/notification-settings.tsx` (Studio passes `STUDIO_CARD` via `containerClassName`; the tab widens the page from `max-w-2xl` to `max-w-5xl` for the recipient matrix). New module `apps/api/src/modules/notifications/`: `NotificationBot` (one per company, `tgBotId @unique` across the whole system, token encrypted via `EncryptionService` — unlike `Channel.tgBotToken`, which is historically plaintext — and a random `webhookSecret` checked with `timingSafeEqual` against Telegram's `X-Telegram-Bot-Api-Secret-Token` header, a protection channel bots don't have), `NotificationRecipient` (lowercased username without `@`, nullable `chatId`, `enabledTypes String[]` rather than a Prisma enum so new types don't need a migration), and `NotificationAlertState` (unique on company+type+subject). A platform constraint shaped the whole recipient flow: a Telegram bot cannot message anyone by username until that person has messaged the bot, so recipients are entered as usernames and each one must press Start; the separate `NotificationWebhookController` (`POST /webhooks/notifier/:id`, deliberately not `WebhooksController`, which serves channel bots) matches `from.username` case-insensitively against the list, stores `chatId`, and replies with the enabled types plus a summary of problems already ongoing (so someone who links later still learns about an alert that was sent before they joined); `/stop` unlinks, group chats and bots are ignored, unknown usernames get told to ask the owner. Connecting a bot calls `getMe` (checking the returned id matches the token prefix), then `setWebhook` with `secret_token`, `allowed_updates:['message']` and `drop_pending_updates`, rolling the row back if `setWebhook` fails. Six alert types, each tied to a signal the system already has: `LOW_BALANCE` (paid plan, renewal within 7 days, balance below the plan price — the case where `BillingService.runRenewals` would downgrade the company), `TRIAL_EXPIRING` (TRIAL with ≤3 days left, or already expired), `PLAN_DOWNGRADED` (a `DOWNGRADE` balance transaction in the last 7 days while still on TRIAL; subject is the transaction id so each downgrade is its own event), `CHANNEL_DISCONNECTED`, `WEBHOOK_STALE` (reads `ProjectsService.WEBHOOK_STALE_THRESHOLD_MS`, made public for this so the Telegram alert and the UI badge can't drift apart), and `PERSONAL_ACCOUNT_DISCONNECTED`. Detection is a single `NotificationsEvaluator` run by a `*/5 * * * *` cron over companies with an active bot, not events at each state-change site — channel `isActive` alone is flipped by at least four independent paths and two of the six types are time-based rather than events, so one comparing cron sees every path's outcome without touching the fragile channel/MTProto code. It computes the current problem set, creates an alert-state row per new problem (via `create` + P2002 so overlapping ticks can't double-send; there's also an in-memory re-entrancy guard) and fans out a ⚠️ message, and deletes rows for problems that disappeared, sending ✅ only when the disappearance is genuinely good news: an archived project's problems vanish silently, `LOW_BALANCE`/`TRIAL_EXPIRING`/`PLAN_DOWNGRADED` clearing while the company is still on TRIAL is silent (the downgrade message covers it, or the freshness window simply expired). The cron runs outside any HTTP request, so the tenant middleware applies no scoping — every query passes `companyId` and `deletedAt: null` explicitly. Delivery goes through a new BullMQ queue `notification-messages` (limiter 20/s, 4 attempts with exponential backoff), whose processor classifies Telegram errors: 403 or "chat not found" unlinks the recipient with a visible reason, 401 marks the whole bot inactive, only network/429/5xx errors retry. The one synchronous send is the per-recipient "Тест" button, which needs an immediate delivered/not-delivered answer. Two guards against the 2026-08-05 webhook-hijack class of bug: connecting a notifier bot is refused when any channel of a non-archived project already uses the same bot id (the other project's name is shown only if it belongs to the same company), and `TelegramProvider.initialize()` now refuses a channel token whose bot id belongs to a notifier bot — placed there rather than at channel create/update because `initialize()` runs on every path including boot rehydration, and `tryInitialize` surfaces the reason in the channel's `lastError`. Migration written by hand from `prisma migrate diff`, which also emitted an unrelated, pre-existing drift on `PersonalBroadcastLog`'s foreign key — those statements were left out of this migration. Verified without booting `AppModule`: real HTTP against the running API on an isolated throwaway company (bad token format, a well-formed token rejected by real Telegram with 401, the same-company channel conflict naming the project, recipient normalisation from `@Alice_Owner` and `https://t.me/bob_buyer`, duplicate in a different case → 409, bad username/type → 400, webhook without or with a wrong secret → 401 and nothing linked, `/start` from an unlisted username → nothing linked, `/start` from a listed username in a different case → linked, `chatId`/token/secret never returned by the API, group-chat `/start` ignored, `/stop` unlinks, a test send against a revoked token → 400 and the bot marked inactive with a reason, the reverse guard deactivating a channel given the notifier's token, and a BUYER getting 403 on all three routes tried). The evaluator was instantiated directly from the compiled class with a real Prisma client and a stub queue, run in a window between live cron ticks, across six rounds: first detection (4 problems; recipients got only their own types; an unlinked recipient got nothing; project name HTML-escaped), a repeat tick (no duplicates), recovery (✅ to exactly the subscribed recipients), archiving (silent), a downgrade (only the recipient subscribed to that type), and a trial-expiry that silently retired the downgrade. That run caught a real text bug ("через 1 дн.." from the abbreviation plus sentence period), fixed with proper Russian plurals. The first webhook run returned 500 because the test inserted a base64 ciphertext while `EncryptionService` uses hex — a test mistake, but it exposed a real gap: token decryption sat outside the handler's error handling, so a corrupted row or rotated key would have made Telegram retry the same update forever; decryption now happens inside it and marks the bot inactive with a reason. Headless Chromium on both domains (light, dark, 400px) confirmed the tab, the matrix, the pending/linked pills, the disabled Test button before Start, a checkbox toggle persisting across sessions, and the disconnect → empty state → Telegram-rejection message flow, with the only console entry being the browser's own line for that intentional 400. Not verified: a real message arriving in a real Telegram chat, since that needs a real bot token from @BotFather. Observed but not changed: Studio's sidebar doesn't collapse at phone width on any page, so content is squeezed at 400px — pre-existing and unrelated to this tab. All test rows deleted by exact id after confirming the company name.

The same day, a follow-up ("пользователь может поменять юзернейм... лучше сразу брать его user_id... и когда он поменяет юзернейм, он поменяется и в срм? Сделай так, чтобы это не использовало много ресурсов") moved recipient identity from username to Telegram user id. Checked against the Bot API types shipped with grammY before designing: `getChat` accepts `@username` only for a supergroup or channel, so a private user's id genuinely cannot be resolved at add time — and the bot couldn't message them before Start anyway, so the earliest useful moment is the `/start` update, which carries `from.id`. `NotificationRecipient` gained `tgUserId` (set on first Start and never cleared, not even by `/stop` or a block, so a renamed person can still come back by id), `tgFirstName` and `profileCheckedAt`; `username` became nullable (a person can remove their username entirely), and the unique constraint moved from `(botId, username)` to `(botId, tgUserId)` so a rename can never collide with another row at the database level — duplicate usernames on add are now rejected by the service instead, including against a linked recipient's current name. Linking order in the webhook: by `tgUserId` first, then an unlinked row with the sender's username; a row already linked to a different id is never handed over, so someone who takes over a username a recipient abandoned does not start receiving that company's alerts. Profile freshness is almost free: every incoming message from a known recipient and every `sendMessage` response (whose `chat` object carries the current username and first name) feed `NotificationsService.syncProfile`, which writes only when something changed or the last check is older than 12 hours; if the new username matches a still-unlinked row of the same bot (the owner re-added the person under their new name), that row is merged in — enabled types unioned — and deleted. A `20 */3 * * *` cron (`refreshStaleProfiles`) calls `getChat` only for linked recipients not heard from in 12 hours, oldest first, at most 200 per run, 50 ms apart, stopping the whole run on a 429 and skipping a bot after a 401 — at most two `getChat` calls a day per recipient. Migration is hand-written (drop NOT NULL, add columns, backfill `tgUserId` from `chatId` for already-linked rows, swap the indexes), and `prisma migrate diff` confirmed no remaining drift on the model. The recipient table now shows first name and Telegram ID under the username, falls back to name or ID when the username is gone, and the instructions say renames are handled automatically. Verified through the real webhook on an isolated company: first Start stores id and name; a rename appears in the CRM on the next message with the link intact; another person who takes the old username is not linked; removing the username keeps the link with the name and ID shown; `/stop` keeps the id and a later Start under a new name reconnects the same row; re-adding the current name of a linked recipient is refused; the re-added-under-future-name duplicate is merged with types unioned; and a message with an unchanged profile writes nothing. The background refresh was checked by constructing `NotificationsService` directly with the Telegram call overridden on that one instance (no `AppModule`): only stale linked recipients are queried, fresh and unlinked ones are skipped, a rename is picked up, an unchanged profile only bumps the check time, a block unlinks while keeping the id, an immediate second run makes no calls, and 429/401 stop the loop. That test surfaced that the refresh is correctly system-wide — it also selected a real recipient of a real bot the user had connected for ISABELA minutes earlier; because the call was overridden on the test instance and returned nothing, no request reached Telegram and nothing was written (confirmed read-only: the real bot's `updatedAt` still equals its `createdAt`, and the recipient's last change predates the test). The real bot is itself the first live end-to-end confirmation: token and webhook accepted, two real people linked via Start (one already through the new id-based path), and the 23:30 cron run raised two genuine `WEBHOOK_STALE` alerts for ISABELA's Empleo en Uruguay and Trabajo en Nicaragua projects with no delivery errors recorded. A misleading log line ("юзернейм perridol → perridol", fired by a first-name change) was fixed to log only real username changes. Test company deleted by exact id.

On 2026-09-17, a report that STT's clients see dialogues "not reaching Facebook" (with the user suggesting sending them as `Contact`, backed by a competitor's payload: `event_name:"Contact"`, `action_source:"chat"`) turned into three fixes plus a new alert. Diagnosis from live data first: over 14 days STT had 580 Dialogue events, 294 delivered — every one a subscriber with ad attribution — while 283 undelivered were external contacts who messaged a personal account without ever coming from an ad (no fbclid, no pixel; correctly not forwarded) and 2 were people who wrote first and subscribed a day or two later, so the event carried no attribution when it fired (left as is). The delivered payloads were good (fbc, fbp, IP, UA, hashed country, subscription_id — more than the competitor's) and Facebook answered `events_received:1`. The real gaps were the event itself: `FacebookCAPIService` sent `event_name` verbatim, so Facebook received `Dialogue`, a custom event that Meta's delivery system does not understand until an advertiser wraps it in a custom conversion — while `Contact` is the standard event Meta documents for "initiates contact via telephone, SMS, email, chat"; and it went with `action_source:"website"`, although Meta's server-event reference states `event_source_url` is required for website events and a Telegram conversation never has one (Meta documents `chat` for "conversion made via a messaging app"). TikTok's `eventMap` had no Dialogue entry either, so TikTok got a custom `Dialogue`, although `Contact` is one of TikTok's standard events and one of those required for lead-generation campaigns. Per explicit decision via `AskUserQuestion` (warned beforehand that 40 ISABELA pixels and 7 STT pixels were receiving `Dialogue`, so any custom conversion built on that name stops receiving events), Dialogue now goes out as `Contact` to both platforms for everyone, and to Facebook with `action_source:"chat"` — via two small maps at the top of `facebook-capi.service.ts` (`FACEBOOK_EVENT_NAMES`, `FACEBOOK_ACTION_SOURCES`) and one entry in TikTok's `eventMap`. The internal event name stays `Dialogue` everywhere else (funnel, logs, switches, event ids); every other event keeps `website`, per the 2026-07-30 decision, and the test-event button's explicit `forceActionSource` still takes precedence. The same investigation found pixels silently rejecting every event for weeks, with nothing in the UI showing it: 12 active pixels marked Facebook but holding TikTok-format codes (`D9QF…`, "Cannot parse access token" on every send), 7 TikTok pixels whose token has no access to the pixel (`code=40001`), Facebook pixels with invalid tokens, and 7 Facebook pixels whose stored ID had surrounding whitespace — a leading space Facebook tolerates, a trailing one it does not (one Diego Herrera pixel had 3731 rejections and zero successful sends since it was created). Fixes: new `pixels/pixel-id-format.util.ts` (`assertPixelIdMatchesPlatform`) rejects a non-numeric Facebook ID and a numeric TikTok ID with a message naming the likely right platform, applied on create, on update when the ID is sent (the platform is immutable, so it checks against the stored one), and on the unsaved-pixel test send before any request reaches the platform; pixel ID and token are trimmed on save and — so the existing broken rows start working without touching customer data — also at send time in both platform services. Both trees' pixel forms previously rendered only a successful test response, so a 400 from the test endpoint would have shown nothing; they now show the server's message. Per the same decision, a seventh notification type shipped, `PIXEL_DELIVERY_FAILING`: a pixel counts as failing when it has at least 3 deliveries in the last 24 hours and its most recent (up to 5) are all errors — judged on the latest attempts rather than a time window, so a night-time lull neither raises nor clears it. It needed a new `TrackingEventDelivery(pixelId, sentAt)` index (the table is about 1.18M rows and 2.3 GB with no usable index); it was built on production with `CREATE INDEX CONCURRENTLY` (7 s, valid) to avoid blocking delivery writes, and the checked-in migration uses `CREATE INDEX IF NOT EXISTS`, so it is a no-op here and a normal index build on a fresh database. One `LATERAL` query per company then covers all its pixels (16 ms for ISABELA's 161, index scan confirmed with `EXPLAIN`). State is kept per pixel, so "working again" is announced only when that pixel's own latest delivery succeeded (a quiet or deactivated pixel clears silently), but messages are composed per project — the evaluator gained a small grouping step (`composeMessages`) so ROLANDO's nine failing pixels arrive as one message, capped at 8 lines plus "…и ещё N" because Telegram rejects anything over 4096 characters outright. Each line carries a hint for the three causes seen in production (TikTok code under Facebook, TikTok token without pixel access, invalid token), with the repeated tail of TikTok's error text dropped. Recipients who had all six earlier types enabled (the three real ISABELA recipients) got the new type appended; nobody else was changed. Verified: the pixel-alert query and the composed messages were first previewed read-only on ISABELA's real data (three messages, longest 2009 characters); after deploy the live 08:15 cron created 11 pixel states and the queue showed all 9 resulting messages completed with none failed or waiting; the validation and trimming were exercised through the real API on a throwaway company (7 checks); and the compiled services, run against a fake pixel so no customer pixel was touched, produced `Contact`/`chat` for Dialogue to Facebook, unchanged `Subscribe`/`website`, and `Contact` with a trimmed pixel code to TikTok. Not yet observed: a real Dialogue delivery after the deploy — none arrived in the first 13 minutes (night time in the customers' timezones) — and the two trailing-space pixels had no traffic yet to show the trim working live. The broken pixels themselves still need their owners: recreate TikTok codes as TikTok pixels, reissue tokens.

The same day, the manager flow in channel bots (`Channel.tgManagerUsernames`, 2026-07-21 — most live projects are `PRIVATE_CHANNEL_REQUEST` and 6 of them have a manager configured) gained purchase registration, so managers can record deposits without leaving Telegram. Before, a manager forwarded a client's message (or typed `@username`/an ID) and got a single "✅ Да, диалог / ❌ Нет" prompt; the ID/username path already existed but was brittle: username lookup was case-sensitive, and a bare name, a `t.me` link or a mention picked from the member list were not understood. Input parsing now lives in a pure `channels/manager-input.util.ts` (`parseClientIdentifier` handles a forward, `@username`, a bare username, `t.me/…`, `tg://user?id=`, a `text_mention` entity which carries the id even for people without a username, and `ID: 123…`; `parseUsdAmount` accepts `150`, `49.99`, `49,99`, `$150`, `150 usd`, `1 250`, rejects the ambiguous `1,250` and anything above $100,000), and `ClientsService.findByUsername` became case-insensitive, preferring the most recently active row when a stale duplicate username exists (it is also used by `/purchase`). The bot now answers with a client card (name, @username, ID, whether a dialogue is already registered, purchase count and total, and a warning when the person is an external contact whose purchases stay out of project stats) and two buttons: `mgr_dlg:<clientId>` records the dialogue at once (`recordManualDialogue` now returns whether it was the first one, so the card says "already registered — not resent to pixels"; a 10-second Redis lock stops a double tap from bumping `dialogueMessageCount` twice), and the card keeps a "Покупка" button because a deposit often follows. `mgr_buy:<clientId>` sends a prompt with a cancel button and stores `mgr-purchase:<channelId>:<managerTgUserId>` in Redis for 15 minutes; the manager's next numeric message becomes the amount and goes through the unchanged `PurchasesService.create` (USD, `source: 'bot'`, `registeredBy: null` since a Telegram manager is not a CRM user), so the Purchase event, first/repeat-deposit scenarios and stats behave exactly as for the CRM form. The pending state is claimed with `DEL` and the idempotency key is tied to the amount message (`tg-manager:<channelId>:<chatId>:<messageId>`), so a redelivered update cannot create a second purchase. While an amount is awaited, a 7+ digit number, a username, a link or a forward means "next client" and cancels the prompt; `/cancel` and the cancel button (only for the current prompt) clear it; `/start` and anything unrecognised get a help text. Old cards' `mgr_dlg_yes:`/`mgr_dlg_no` buttons still work. The Managers card text in both trees and a new "Менеджеры в боте" paragraph in `/docs` describe the flow. Verified without Telegram and without `AppModule`: a separate process patched `grammy.Api.prototype` so every Bot API call was recorded or refused, built the real `TelegramProvider` with real `ClientsService`/`PurchasesService` (tracking and scenario trigger stubbed) against an isolated throwaway company, and drove `bot.handleUpdate` through 47 checks — every identifier form, a hidden forward, a client from another project, a non-manager pressing buttons, first and already-registered dialogue, double tap, invalid/too-large/ambiguous amounts, a $149.50 purchase with its tracking event and FIRST_DEPOSIT trigger, a redelivered amount update creating nothing, switching client mid-prompt, both cancel paths, and a repeat deposit — all passed; the only refused Bot API call was the pre-existing `getChat` inside `initialize()`. Parsing was also checked on 39 standalone cases. Test rows deleted by exact id. Not verified: a real manager tapping the buttons in a real Telegram chat.

On 2026-09-19, a report that a Facebook client of project ROLANDO ACUNA (`cms3h8xkv004nje3l7neyyfv7`, `PRIVATE_CHANNEL_REQUEST`, 40 s join delay) was attributed to buyer Jerry, who runs only TikTok, traced to two gaps in the per-visit invite-link flow (2026-08-31). The client carried both an `fbclid` and a `ttclid` and had two Subscribe events 31 s apart. nginx logs showed the visitor tapped the join button twice with the same `code`: the first tap got a per-visit link and a correct join (Facebook, no buyer); the second tap's `createChatInviteLink` failed with `429 Too Many Requests`, so `tgRedirect` fell back to the landing's shared link, and the second join request (sent while the first was still waiting for its delayed approval) went through the FIFO/lookahead queue and took another visitor's entry — a TikTok visit from Jerry's link. `findOrCreate`'s first-known-value-wins merge then filled the client's empty `buyerId` and `ttclid` from that visit, a second Subscribe went to the Facebook pixel, and the second delayed approval hit `USER_ALREADY_PARTICIPANT` and still sent the welcome scenario again; the other visitor's queue entry was consumed as well. The 429 is not rare: about 950 of roughly 4,560 redirects in the preceding 24 h failed to get a per-visit link (508 on this channel alone, `retry after` 3–14 s), and 20% of redirects are repeat taps with an already-seen `code`, each of which used to create another link. Three fixes: `handleJoinRequest` now treats a request from a client who is already `isSubscribed` with `subscribedAt` inside `tgJoinDelaySeconds + 10 min` as a repeat — it only approves the request, without welcome, and never touches the queue or the client's attribution (checked after the one-time-link lookup, so a repeat through the visitor's own link is handled the same way); `createOneTimeInviteLink` takes the visit `code` and returns the same link for repeat taps (`visit-invite:<channelId>:<code>`, TTL one hour shorter than the link's expiry), with the reuse key stored in the attribution payload as `visitInviteKey` and deleted when a join consumes the link, because a used link is revoked and must not be handed out again; and a 429 now sets `invite-link-cooldown:<channelId>` for `retry_after` seconds, during which the method returns null without calling Telegram, instead of every click extending the limit. Over the past week there were about 30 repeat joins within 10 minutes; only this one changed the buyer. The client row was corrected by exact id (`buyerId` and `ttclid` cleared, the other fields confirmed to come from the client's own visit by diffing the two Subscribe payloads) and `buyerId` cleared on the two affected events; the duplicate Subscribe already delivered to the Facebook pixel cannot be recalled. Verified without Telegram and without `AppModule` (patched `grammy.Api.prototype`, real `TelegramProvider`/`ClientsService`, isolated throwaway company): 21 checks covering link reuse, a new link after the used one is revoked, the 429 cooldown skipping calls, the exact live scenario (a repeat request through the landing's shared link with a seeded TikTok queue entry leaves the client's attribution unchanged, records no second Subscribe, approves without welcome and leaves the queue entry for its owner), a new person still taking the queue entry as before, and returning subscribers outside the window still going through the normal path. Still open: at peak traffic Telegram's rate limit will keep sending some first taps to the shared-link queue; a pre-created pool of per-visit links refilled at a safe rate would remove that, but was not built.

On 2026-09-23, cloaking (`Landing.cloakingEnabled`/`cloakingCountries`/`cloakingRedirectUrl` — a country allow-list with a 302 fallback for everyone else, previously a single inline check in `LandingRendererService.renderAndServe`) was extracted into an explicitly extensible subsystem per the user's own stated reasoning ("cloaking will keep growing with new options over time") — the same shape this codebase already uses for a growing family of interchangeable behaviors, `ChannelProvider`: a `CloakingHandler` interface (`serveBlockedVisitor(landing, subPath, req, res)`) with one class per `CloakingType` enum value, registered in a `CloakingService` map (`apps/api/src/modules/landings/cloaking/`) that `renderAndServe` calls into instead of branching inline — a future third cloaking type is a new class plus one registry line, no `renderAndServe`/if-else changes anywhere. The prior behavior became `RedirectCloakingHandler` unchanged (`DEFAULT_CLOAK_REDIRECT_URL` moved with it); geo-resolution (`resolveVisitorCountry`, `cf-ipcountry`/geoip-lite) moved to a shared `common/geo-country.util.ts` rather than living inside the cloaking service, since `LandingRendererService.injectTrackingScripts` also calls it independently for Facebook Advanced Matching's `countryCode` — a second real call site the initial extraction would have missed if the geo logic had been made private to `CloakingService`. The new type, `PRELANDING` (a "white page" — a static site shown on the same domain to visitors the country allow-list excludes, instead of redirecting them off-domain), reuses the ZIP-upload machinery already built for CUSTOM landings: `LandingsService.processAndReviewZip`'s structural parsing (extension/size/zip-slip/`index.html` presence) was extracted into `openLandingZip()` and is shared by both paths, but the outcome on a failed content review is deliberately stricter for the white page than the existing CUSTOM-landing precedent — explicit user requirement ("a broken one doesn't get through") — a failed `reviewLandingHtml()` (extended with an optional `requireRedirectPlaceholder` flag, since a white page has no Telegram join button to check for) means `uploadCloakingPrelanding()` writes nothing to MinIO or the DB at all and returns `{accepted:false, checks}`, unlike CUSTOM's existing behavior of uploading anyway and merely skipping auto-publish. A landing with `cloakingType=PRELANDING` but no successful upload yet (or one whose white page was removed) falls back to the `RedirectCloakingHandler` behavior instead of 404/erroring the blocked visitor — the same fail-safe precedent already used for an unset `cloakingRedirectUrl` (defaults to Wikipedia) and for a country that fails to resolve (treated as disallowed). New `Landing.cloakingType`/`cloakingPrelandingBasePath` fields (MinIO prefix `landing-cloaking-prelanding/{id}`, deliberately separate from CUSTOM content's `landings/{id}` since both can exist on the same landing independently); new `POST`/`DELETE /landings/:id/cloaking/prelanding` routes reuse the existing `Permission.LANDINGS_EDIT` (no new permission — this is editing an existing landing's behavior, not a separate resource). Both routes always return HTTP 200 with `{accepted, checks}` rather than throwing on a failed review, matching the existing `processAndReviewZip` convention — `HttpExceptionFilter` only forwards an exception's `message` to the client, silently dropping any other field such as `checks`, which would have broken the shared `ReviewChecklist` frontend component. Frontend: `LandingBehaviorFields` gained a REDIRECT/PRELANDING toggle inside the existing cloaking block; the drag-and-drop file picker was extracted out of `UploadZipLandingDialog` into a shared `ZipDropZone` (a second real consumer, `CloakingPrelandingUpload`, justified pulling it out rather than duplicating the markup); `ZipLandingInstructions` gained a `variant` prop so white-page uploads see accurate structure guidance (no join-button placeholder requirement, no tracking script) instead of the CUSTOM-landing copy. Verified end-to-end via direct construction of the real classes (deliberately not `NestFactory.createApplicationContext(AppModule)`, per the standing caution elsewhere in this file — that boots every real channel's `onModuleInit`, not just a throwaway test one) against an isolated test company/project/landing: all 4 `CloakingService.applyCloaking` branches (REDIRECT with an unset URL, an allowed country, PRELANDING with nothing uploaded falling back to redirect, PRELANDING serving a real uploaded page) and both `uploadCloakingPrelanding` outcomes (a broken archive rejected outright with nothing written, a valid one landing in MinIO and the DB) came back correct, plus a regression check that the `openLandingZip` extraction left the existing CUSTOM-landing upload flow (`processAndReviewZip`, `createCustom`) producing identical results. `tsc --noEmit` clean on both `apps/api`/`apps/web`, `nest build`/`next build` clean, deployed via `pm2 restart trafficcrm-api trafficcrm-web` with the new routes confirmed mapped and no new errors in the post-restart logs; one leftover test company from an earlier failed dry run (before the Project→Channel FK creation order was fixed in the test script itself, not the product code) was found and deleted by exact id during the same verification pass.

The same day, a follow-up moved cloaking off the shared "Опции лендинга" card onto its own tab on the landing detail page (both trees) — the user's own reasoning: the page is already large and cloaking will keep growing (more filter types beyond REDIRECT/PRELANDING). `LandingCloakingFields` was extracted out of `LandingBehaviorFields` (`apps/web/src/components/landing-behavior-fields.tsx`) into its own exported component operating on a narrower `CloakingFieldsState` (`Pick<LandingBehaviorState, 'cloakingEnabled'|'cloakingType'|'countriesText'|'redirectUrl'>`) — still used inline by `LandingBehaviorFields` itself (now via a `hideCloaking` prop) for the create-dialog flow (`UploadZipLandingDialog`), which keeps saving everything in one combined PATCH since it's a single one-shot action with no split-tab risk. Both landing detail pages now wrap "Опции"/"Клоакинг" in a `Tabs` (the same neutral shadcn primitive already proven to render correctly in Studio's dark theme without re-skinning, per the 2026-08-18 CSS-variable fix — no separate Studio markup was needed, same as `LandingBehaviorFields` itself), with `?tab=` URL persistence mirroring `/projects/[id]/settings`'s `readTabFromSearchParams` pattern. The real correctness risk here — explicitly flagged by the user ("don't break old landings with REDIRECT cloaking already on") — wasn't the migration (already handled by the schema's `@default(REDIRECT)`) but a UI hazard the split itself could introduce: if the "Опции" tab's save button PATCHed the full combined payload (as the old single-form version did), it would silently roll back whatever the "Клоакинг" tab had just saved back to whatever cloaking state was in memory when the page loaded. Fixed by giving each tab's save button a narrow, explicit payload — `optionsOnlyPayload`/`cloakingOnlyPayload`, both built from `behaviorStateToPayload` — so saving one tab can structurally never send the other tab's fields at all (omitted keys, not just resent-unchanged values); `LandingsService.update()` already treats an absent DTO field as "don't touch," so no backend change was needed. Verified by tracing both payload builders by hand against several real production landings with `cloakingEnabled:true` (confirmed the pre-existing migration default correctly carried them to `cloakingType:'REDIRECT'`) rather than a live browser pass. One real gap caught only by `next build` (not `tsc --noEmit`, which stayed clean throughout) — `optionsOnlyPayload`'s destructure-then-discard of the 4 cloaking keys tripped `@typescript-eslint/no-unused-vars`; fixed by building the trimmed object explicitly instead of destructuring-to-discard. Worth remembering: `tsc --noEmit` alone is not a sufficient pre-deploy check for `apps/web` — ESLint errors (like this one) only surface through the real `next build`.

The same day, a direct follow-up moved cloaking off that tab entirely onto its own route, `/projects/:id/landings/:landingId/cloaking` (both trees) — the user wanted a standalone page, not a tab inside "Опции лендинга", reached via a new "Клоакинг" button in the landing page's own header action row (next to "Получить ссылку"/"Предпросмотр"). "Опции лендинга" reverted to a single non-tabbed section (`hideCloaking` always on, `optionsOnlyPayload` only). The extracted `LandingCloakingFields` component and the `cloakingOnlyPayload`/`optionsOnlyPayload` payload-isolation design (see the entry above) carried over unchanged — only where the component is mounted changed, not the correctness guarantee protecting already-REDIRECT-cloaked landings from a cross-section stale overwrite. Verified with `next build` (not just `tsc --noEmit`, per the lesson just learned) before restarting `trafficcrm-web`.

On 2026-09-24, a genuinely new capability shipped for the admin panel: a button in the `apps/admin` companies list that logs the super-admin straight into the real `apps/web` dashboard as that company's actual **Owner**, for debugging — the user was explicit this is a real session login, not a read-only profile view. No such mechanism (cross-app impersonation) existed before. The core difficulty is that `admin.mw-track.com` and `mw-track.com` are different origins under a pure Bearer-JWT auth model (`app.enableCors({origin:true})` without `credentials:true`) — `localStorage` can't be shared, and unlike the existing SSO bridge between `mw-track.com`/`old.mw-track.com` (a `mw_refresh` cookie with `Domain=.mw-track.com`, see the entry above about that bridge), a brand-new cookie-based bridge was deliberately not reused here since it would also become readable by JS on `admin.mw-track.com` itself (a real subdomain of `.mw-track.com`) — a bigger blast radius than a single click-triggered action needs. Shipped instead as a one-time Redis-backed exchange code: `POST /admin/companies/:id/impersonate` (`AdminCompanyController`, `@Roles(SUPER_ADMIN)` already at the class level) calls new `AuthService.startImpersonation(companyId)`, which finds the company's active OWNER (`role:'OWNER', deletedAt:null, isActive:true`, no fallback — `BadRequestException` if none), signs an access token via a `signAccessToken` helper extracted out of the existing `issueTokens` (same secret/TTL/payload shape, just without the refresh-token/`RefreshToken`-row side of it), and stores `{accessToken, user}` under `impersonation:<uuid>` in Redis with a 60-second TTL — deliberately no refresh token and no `RefreshToken` row at all, so the debug session self-expires in 15 minutes via the existing `refreshAccessToken()`-fails-so-`logout()` path with zero new frontend expiry logic. `AdminCompanyService.impersonate` (needed `AuthModule` added to `AdminModule`'s imports — confirmed safe, `AuthModule` has zero domain-module dependencies and isn't part of the documented `ProjectsModule↔ChannelsModule↔ClientsModule` cycle elsewhere in this same admin module) then writes a **synchronous**, fail-closed `AdminActionLog{action:'IMPERSONATION_STARTED', newValue:{targetUserId, targetUserEmail, targetUserRole:'OWNER'}}` — unlike the existing fire-and-forget `COMPANY_VIEWED` log, a failed write here should fail the whole operation, not let an unaudited login through. The admin frontend's new "Войти как Owner" button opens `${NEXT_PUBLIC_WEB_URL}/impersonate?code=...` in a new tab (new env var, added to `.env.prod`/`.env.prod.example`) rather than navigating the current one, so the super-admin's own panel session is untouched; `apps/web` gained a new **top-level** `/impersonate` page (deliberately outside `(auth)`/`(dashboard)`/`dashboard` route groups, so neither existing auth-gate layout needed touching) that reads `?code=`, POSTs to a new `@Public() POST /auth/exchange-impersonation-code` (raw axios, same reasoning as `login()`/`bootstrapFromSharedCookie` for avoiding the wrapped `api` client's own interceptors), and calls `useAuthStore.setState({accessToken, refreshToken:null, user})` directly before redirecting to `/` — no shared cookie is written, so the debug session does not bridge over to `old.mw-track.com` the way a real login would. `middleware.ts`'s `SHARED_PATHS` list gained `/impersonate` alongside `/login`/`/register`/`/invite`. One real build-time catch: `next build` (not `tsc --noEmit`, which stayed clean) failed prerendering `/impersonate` with "useSearchParams() should be wrapped in a suspense boundary" — fixed with a `<Suspense>` wrapper around the component reading `useSearchParams()`, the same class of gap the project's own standing lesson about `tsc` alone being insufficient for `apps/web` already warns about. Also worth noting: an initial attempt to delegate the architecture design to a `Plan` subagent was blocked outright by the Claude Code auto-mode classifier ("Blocked by classifier") — per the tool's own instructions, no workaround was attempted; the user was asked directly and chose to have the whole feature implemented without any subagent, which is what happened. Verified live via direct construction of the real `AuthService` class (deliberately not `NestFactory.createApplicationContext`, per the standing caution elsewhere in this file) against an isolated throwaway company/Owner: one-time code creation/redemption, the 60s Redis TTL, JWT payload correctness (`sub`/`companyId`/`role`, 900s expiry), zero `RefreshToken` rows created, a second exchange of the same code correctly failing, a company with no OWNER correctly rejected, and the full `AdminActionLog` write path — 19/19 checks passed (one cleanup-ordering bug in the test script itself, deleting the Owner row before the `AdminActionLog` row that FK-references it as `adminUserId`, caused one real leftover test company/user, found and deleted by exact id afterward — not a product bug). `tsc --noEmit`/`nest build`/`next build` (both frontends) clean; deployed via `pm2 restart` for all 3 processes, both frontend rebuilds done with `.env.prod` sourced (confirmed `NEXT_PUBLIC_WEB_URL` actually baked into the compiled `apps/admin` bundle before restarting, per the standing Next.js env-build-regression lesson), and both new routes confirmed live over the public HTTPS domains post-deploy.

## Before doing anything

Read [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md) first, then the numbered doc matching the area you're touching (01-15, listed in 00's file map). Each numbered doc beyond 00 is currently a TODO stub with section headers only — fill them in as that part of the system gets built, don't let them go stale.

## Hard invariants (do not violate when writing code here)

- Every DB query that touches tenant data must be scoped by `company_id`. This is enforced at the middleware level, not by convention — if you add a new table or query path, it must go through that scoping.
- Channels (Telegram, WhatsApp, Instagram, Viber, Email) implement one `ChannelProvider` interface. Adding a channel = new class implementing that interface, never an if/else branch in business logic.
- Tracking events (PageView, Lead, Purchase) are written to an `events` table first, then pushed to Facebook/TikTok asynchronously via queue. Never call Facebook CAPI / TikTok Events API synchronously from the request path.
- All pushes go through BullMQ queues with rate limiting (30 msg/sec for Telegram). Never send a push directly.
- Every external/idempotent-sensitive request carries an `idempotency_key`.
- No hard deletes — every delete is `deleted_at` soft delete. Exceptions (documented deviations, not oversights): `Channel` has no `deletedAt` column; its "delete" is `isActive: false` (bot/webhook torn down, row kept). `TeamInvite` also has no `deletedAt` column; it's a transient token/credential (like `RefreshToken`, also hard-deleted on logout), not tenant data — revoking one is a genuine hard delete. Both are excluded from the `PrismaService` tenant-scoping middleware's `modelsWithCompany` list for the same reason — that middleware auto-injects `deletedAt: null` into every read, which would throw `Unknown argument` against any of them. `Domain` DID follow this same hard-delete-because-globally-`@unique` exception until 2026-09-02, when it was switched to a real soft delete (`deletedAt` column added, `Domain.domain`'s unique constraint became a partial index `WHERE "deletedAt" IS NULL` via a hand-written migration) per explicit user request — nginx server block/webhook still gets torn down on "delete" so the domain stops actually serving, but the row survives for history/audit and the same hostname can be re-added later. `Domain` is still excluded from `modelsWithCompany` (every read in `DomainsService`/`LandingRendererService.renderByDomain`/`AdminCompanyService` has to filter `deletedAt: null` by hand) — the exclusion reason changed from "no deletedAt column at all" to "manual scoping predates the auto-middleware and nothing has migrated it since." `NotificationBot`/`NotificationRecipient`/`NotificationAlertState` (2026-09-16) are also hard-deleted by design: the bot row holds a credential (encrypted bot token) that must actually be erased on disconnect, a recipient is an access grant in the same category as `TeamInvite`, and an alert state is transient bookkeeping that exists only while a problem is ongoing. None has a `deletedAt` column, and none is in `modelsWithCompany` — every query scopes by `companyId` explicitly (a recipient is always looked up through its bot's `companyId`).

## Shared choke points — check before changing

These are places where multiple independent consumers rely on one piece of code staying
consistent. If you're editing one, grep for the others before you ship.

- **`modelsWithCompany` array** (`apps/api/src/prisma/prisma.service.ts`) — the tenant-scoping
  middleware only auto-injects `company_id`/`deletedAt` filters for models explicitly listed
  here (`Project`, `Landing`, `Client`, `BotScenario`, `AutomationFlow`, `AbTestGroup`,
  `StoryPost`). A new table needing tenant scoping does NOT get it automatically from having a
  `companyId` column — it must be added to this array, or every read against it is an unscoped
  cross-tenant leak.
- **`checkSubscriptionLimit`** (`apps/api/src/common/guards/subscription.util.ts`) — the single
  shared function deciding whether a company is blocked from sending pushes (plan expired,
  monthly cap, and — once Phase 4.3D ships — manually blocked/suspended by an admin). Consumed
  by both `SubscriptionGuard` (manual HTTP send) and `PushesCron.fireScheduledPushes`
  (automated). Any new "is this company allowed to push" condition belongs inside this one
  function, not as a second, divergent check bolted onto only one of its two callers.
- **`apps/admin`** (standalone Next.js app, separate from the CRM's `apps/web` — planned/being
  built per [15_PHASES.md](15_PHASES.md) §4.3, not yet fully live) is a second, cross-tenant
  consumer of core CRM services: `ProjectsService`, `ClientsService`, `PushesService`,
  `LandingsService`, `DomainsService`, `TeamService`, and the `Company` model itself. It reaches
  them either via a `runAsCompany()` helper (re-enters the tenant-scoping AsyncLocalStorage
  context with an admin-chosen target `companyId`, for the models in `modelsWithCompany`
  above) or directly (for `Domain`/`Push`/`Channel`/`User`, which already take an explicit
  `companyId` param). **When you change the signature, return shape, or scoping behavior of any
  of those services, or add/rename a `Company` field, check `apps/admin` for breakage** — it
  won't show up in `apps/web`'s tests or typecheck since it's a separate package.
- **Hand-maintained frontend enum-label dictionaries** — some Prisma enums have a matching
  `Record<EnumValue, string>` label map in `apps/web` that is NOT generated from the schema
  (e.g. `BalanceTransactionType` → `TRANSACTION_LABEL` in
  `apps/web/src/app/(dashboard)/billing/page.tsx`). Adding a new enum value in
  `prisma/schema.prisma` silently produces an undefined/missing label in the UI unless you grep
  the frontend for that enum's existing dictionary and add the new case by hand.
- **"Is this `UserRole` elevated?" — 4 independent mechanisms, only one is a blacklist.**
  `apps/api/src/common/permissions/permission.constants.ts`'s `isElevatedRole()`
  (`RESTRICTED_ROLES` array) is a blacklist — a new role not added there is silently treated as
  elevated (full bypass of per-project permission checks). `projects.service.ts`'s
  `ELEVATED_ROLES` const + its own inline check in `assertAccess`, `audience.service.ts`, and
  `stories.service.ts` are all whitelists (`[OWNER, ADMIN, SUPER_ADMIN]`) — a new restricted role
  is correct there automatically. When adding a new `UserRole` value, `RESTRICTED_ROLES` is the
  one place you must touch deliberately; there's no compiler safety net for it.
- **`RolesGuard`'s `@Roles(...)` is a rank threshold (`Math.min(...requiredRoles)`), not a
  set-membership check.** Adding a lower-ranked role into an existing decorator's role list
  silently lowers the bar for every other role already at or above that rank, not just the new
  one. When a new role needs to reach a route gated this way but sits at the same rank as an
  existing restricted role (e.g. `BUYER`/`OPERATOR`/`OPERATOR_ADMIN`, all rank 1), don't edit the
  decorator list — remove the guard from that route and add an explicit role check inside the
  service instead (see `assertCanAssignRole`/`assertCanAccessTeamManagement` in
  `apps/api/src/modules/team/team-role.util.ts` for the established pattern).

## Stack quick reference

Backend: NestJS 10 + Prisma 5 + PostgreSQL 16 + Redis/BullMQ. Frontend: Next.js 14 App Router + TailwindCSS/shadcn + Zustand + React Query. Telegram via Grammy.js (webhook only, no polling). WhatsApp via 360dialog. Crypto payments: USDT TRC-20 (TronGrid) / ERC-20 (Infura/Alchemy) via HD wallet, one address per payment. Full list with versions in [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md).

## Doc maintenance

When a numbered doc (01-15) goes from stub to real content, also update [15_PHASES.md](15_PHASES.md) to reflect what phase actually shipped — it's the source of truth for "what's done vs planned," don't let it drift from reality.

`apps/admin` (Phase 4.3) is architecturally separate from the rest of the app and evolves
across many phases — 15_PHASES.md §4.3 is its authoritative status/plan doc; keep it current
phase-by-phase rather than writing it up only at the end.
