# TrafficCRM

Multi-tenant SaaS CRM for media buyers (traffic arbitrage), branded MWTRACK in the product UI. Spec docs (00-15) are complete; real implementation is underway following [15_PHASES.md](15_PHASES.md) phase by phase. As of 2026-07-02, Phase 1 is done including the first real production deploy (162.213.248.131, mw-track.com), Phase 2 steps 2.1-2.9 (2.7 Analytics/2.8 Lookalike Export confirmed done 2026-07-15, 2.9 = Team/roles Phase 1), 2.11 (audience overlap + Telegram client enrichment), and 2.12 (landing tracking links: pixel + Facebook/TikTok ad macros, customizable query-param names, ad/campaign breakdown) are done; Phase 3 steps 3.1 (Drip Campaigns/automation flows: trigger→delay→push→condition, linear step-list UI instead of drag-and-drop), 3.2 (A/B/n testing for landing pages: group N existing landings with per-landing traffic weights, card-click multi-select UI, no-cookie random split per request, comparison stats; extended 2026-07-17 so a domain/path binds to either one landing directly or the whole test group, not implicitly via landing membership), 3.4 (Smart Push Timing: CTR-by-hour analysis, first click-tracking in the project via a public push-button redirect endpoint) are done as of 2026-07-15, and 3.6 (Team Analytics: buyer attribution via a hidden query-param on the tracking link rather than landing ownership, "БЕЗ БАЕРА" bucket for unattributed clients, company-wide project comparison) done 2026-07-17 (3.3 paused, 3.5 cancelled, 3.7 obsolete per user scope decisions — Phase 3 otherwise complete); and Phase 4 step 4.1 (Telegram MTProto personal account, client dialogue tracking) shipped ahead of schedule on explicit user request; on 2026-07-22, bot scenarios were merged conceptually with 3.1's automation flows (shared step-chain idea — DELAY/SEND_MESSAGE/CONDITION, new `BotScenarioStep`/`BotScenarioRun` — but deliberately separate tables/engine, since scenario runs must be able to re-fire per client while automation enrollments are one-per-client-forever) and gained a step-chain editor + ≤10-item media gallery with video-note support at `/projects/[id]/scenarios`, while automation flows (3.1) were temporarily hidden from the UI (code/data untouched) per explicit user request; on 2026-07-25, the welcome message moved out of channel settings into scenarios entirely (new `SUBSCRIBE` trigger, replacing `Channel.tgWelcomeMessage*`/`sendWelcomeMessage`), the video-note pipeline was rewritten to always re-encode via ffmpeg's own runtime filter expressions instead of a skip-if-already-square fast path (the real cause of frequent kruzhok failures across formats/resolutions), and the scenario editor was redesigned around "elements" (one content type + optional button each, delay embedded per-element instead of a separate step type, condition/standalone-delay hidden from the UI for now); also on 2026-07-25, A/B testing shipped for bot scenarios (mirrors landing A/B groups — `ScenarioAbTestGroup`, weighted random variant pick per trigger firing with no client stickiness, 6 client-snapshot conversion metrics, synthetic `command` values to sidestep `BotScenario`'s unique constraint), including a same-day `endAbTest` lifecycle bug fix (member scenarios' `abTestGroupId` wasn't cleared, blocking a second test) plus a retroactive backfill for the one real production test already corrupted by it; and on 2026-07-27, per-event-type switches were added to gate whether tracking events get forwarded to Facebook/TikTok (`Project.disabledTrackingEvents`, gated in `TrackingService.recordEvent` alongside the existing ad-attribution check — the CRM's own `TrackingEvent` write is never affected), with a visible on/off indicator row on the project overview page and a new "События" settings tab; narrowed the same day to just the 3 event types the backend fires on its own initiative (Subscribe/Dialogue/Purchase — PageView/Lead/InitiateCheckout/Click are explicit SDK calls the tenant already controls in their own landing code, so a CRM switch for those is redundant), and made the switch gate only the automatic firing path via a new `RecordEventDto.forceSend` escape hatch — manual staff actions (the "Зарегистрировать диалог" button, the "Добавить покупку" form) keep forwarding to ad platforms even while the switch is off; and the same day, message personalization placeholders (`{first_name}`/`{last_name}`/`{full_name}`/`{username}`) shipped for both pushes and bot scenarios via one shared `renderMessagePlaceholders` helper, wired into `ChannelsService.sendMessage` (pushes + automations) and `BotScenarioEngineService.sendStepMessage` (scenarios, with its own Client lookup since scenarios must fire for cold contacts with no Client row); and on 2026-07-28, the Team/roles granular-permissions system (Phase 2) was redesigned from flat/global-per-user to per-project — a Buyer/Operator can now hold different `Permission` grants on different projects (`UserPermission` gained a `projectId` column, `@RequirePermission`/`PermissionsGuard` were retired in favor of a 5th `requiredPermissions?` param on `ProjectsService.assertAccess`, DB-backed for an immediate effect instead of waiting on JWT refresh), except `DOMAINS_*` which stays company-wide by explicit user decision (`Domain.projectId` doesn't meaningfully scope a domain to one project); a new `PROJECTS_EDIT` permission gates `PATCH /projects/:id` (archive/token-regeneration stayed `@Roles(OWNER, ADMIN)` instead of a permission, per the same decision) — see 15_PHASES.md checkboxes for current status. On 2026-07-30, after reaching full page-for-page parity, the "Studio" redesign (`apps/web/src/app/dashboard/studio/*`) was promoted to the primary UI for every role at `mw-track.com`'s own default paths (`/`, `/projects`, etc.) — the old UI (still fully intact, unchanged) was split off onto a separate subdomain, `old.mw-track.com`, reachable via a "Старый дизайн" link in Studio's sidebar. Both domains are served by the same Next.js process; the split happens via `apps/web/src/middleware.ts`, which reads the `Host` header and, on the main domain, transparently rewrites every path (except the shared `/login`/`/register`/`/invite` pages) to Studio's physical files under `dashboard/studio/*` — the browser's address bar stays clean since it's a server-side rewrite, not a redirect. Studio's own internal links are root-relative (`/projects/:id`, not `/dashboard/studio/projects/:id`) for exactly this reason. Also on 2026-07-30: push limits were converted from monthly to daily (`Company.maxPushesPerDay`/`pushesToday`, rolling-window reset), then the plan ladder's `maxProjects` was revised (TRIAL 1/STARTER 5/GROWTH 10/SCALE 20/ENTERPRISE 50, the last no longer an unbounded sentinel) with push limits recomputed via a per-project rate except ENTERPRISE (kept flat); NOWPayments was connected as a second `PaymentGatewayProvider` alongside the still-inactive Heleket, using `/v1/payment` (direct address) instead of a hosted checkout so the existing `PaymentModal` needed no changes; and a new project-scoped `OPERATOR_ADMIN` role was added — it manages only `OPERATOR`-role team members within its own `ProjectAccess` projects (not company-wide), enforced via a new `assertCanAccessTeamManagement`/`assertOperatorAdminScope` pair in `team-role.util.ts` rather than the `@Roles()` decorator (whose rank-threshold model would have silently admitted BUYER/OPERATOR too) — alongside this, `OPERATOR`'s own UI was hard-restricted to two pages (`/my-clients`, `/my-stats`, the latter backed by a new `Purchase.registeredBy`-based endpoint) and the client detail card now hides ad-attribution fields from `OPERATOR` via a server-computed `canViewTrafficSource` flag; and the same day, the project client list gained an "also in other projects" badge (`tgUserId` match, same identity concept as the existing audience-overlap feature) gated by a new `Permission.CLIENTS_VIEW_CROSS_PROJECT` — granted on the viewer's OWN project, not the other one it reveals — with elevated roles or grant-holders seeing full detail (which project, joined-when, dialogue-when) and everyone else seeing only the bare fact plus a dialogue date, no project name; and the same day, a full re-audit of the standalone `apps/admin` platform panel found and fixed two real gaps left over from Phase 4.3D's explicitly-deferred scope — automation flows/bot scenarios now check `checkSubscriptionLimit` before sending (a suspended/blocked company could previously still have automated messages go out) — plus added invoice visibility per company, pagination/company-filter on the Errors/Actions/Companies list pages (Errors had been silently truncating to 50 of 135 real rows), and a `COMPANY_VIEWED` audit-log entry so cross-tenant reads (not just mutations) leave a trace; the SUPER_ADMIN password was also rotated; a same-day regression this surfaced (`TEAM_MANAGER_ROLES`, part of the `OPERATOR_ADMIN` work above, had forgotten `SUPER_ADMIN`, breaking the admin panel's "Команда" tab) was found and fixed. Also on 2026-07-30, `apps/admin` moved off the path-based `mw-track.com/adminmw` onto its own subdomain, `admin.mw-track.com` (DNS was already pointed there) — `next.config.mjs`'s `basePath` was dropped, the existing multi-SAN `mw-track.com` certificate was expanded via `certbot --nginx --expand` to cover it, and the old `/adminmw` path now 301-redirects to the new domain rather than being removed outright. On 2026-07-31, a full how-to-use-every-feature documentation page (8 sections, ported from an earlier standalone Claude Artifact) shipped as a real in-app page at `/docs` in both trees, linked from the sidebar — a single shared `DocumentationContent` component (`apps/web/src/components/docs/`) used identically by both page wrappers, with 12 real product screenshots as static files under `apps/web/public/docs/*.png` (the app's first use of `public/`-hosted static assets); this surfaced a real middleware bug — the Studio-domain rewrite in `apps/web/src/middleware.ts` was catching every path including static files, resolving `/docs/overview.png` to a nonexistent `/dashboard/studio/docs/overview.png` — fixed by excluding paths with a file extension from the rewrite. Also on 2026-07-31, Operator project access was decoupled from participant creation (the user found the old create-time project+permission form inconvenient since operator rosters on a project change often): creating/inviting an Operator no longer takes `projectIds` at all (the sole exception to the "Buyer/OPERATOR_ADMIN must pick a project immediately" rule) and starts with zero `ProjectAccess`; access is granted afterward either from the project side (new "Операторы" tab in project settings, `GET/POST/DELETE /projects/:id/operators` + a company-wide `.../candidates` list so a brand-new zero-project operator is discoverable — confirmed via `AskUserQuestion` that Operator-admins see all company operators here, not just ones already overlapping their own projects) or from the team side (new "Операторы" tab on `/team`, splitting Operator rows out of the main roster, with a per-operator "assign projects" dialog that reuses the existing `PATCH /team/:userId` with only `projectIds` — zero backend changes needed for that direction). Operator permissions are no longer manually configurable at all — always the fixed `DEFAULT_ROLE_PERMISSIONS.OPERATOR`, applied via the pre-existing `seedDefaultsIfEmpty`. Also fixed a real bug found in the same request: the project-picker `<SelectValue>` on `/my-clients`/`/my-stats` had no children render function, so Base UI displayed the raw project id instead of its name (the same recurring pattern documented earlier in this file); both pages also gained an always-visible "available projects" chip row. On 2026-07-31, both sidebars (`Sidebar`/`StudioSidebar`) gained a collapse-to-icons toggle (new shared `useUiStore`, zustand+persist) with `Tooltip`-on-hover nav links in collapsed state. On 2026-08-02, two archived-project bugs were fixed: `ProjectsService.archive()` now also deletes `ProjectAccess`/`UserPermission` for every user on the archived project (previously these dangled and blocked all future `/team` edits, since the tenant-scoping middleware only forces `deletedAt: null` on top-level `Project` queries, not nested includes); and `ProjectsService.assertAccess` gained an `allowArchived` option (via `$queryRaw`, the only way to bypass that same middleware) so landing view/edit/delete keeps working against an archived project's landings instead of 404ing. On 2026-08-03, landings and tracking pixels both gained creator tracking (`createdById`, backfilled to the company's Owner for pre-existing rows, shown in the UI as "Создал: ...") following the `Purchase.registeredBy` pattern; and Buyers/Operator-admins gained a new per-user `User.landingsVisibilityScope` setting (`OWN_LANDINGS`/`PROJECT_LANDINGS`/`ALL_LANDINGS`, default `PROJECT_LANDINGS` = prior behavior) governing what the company-wide `/landings` page shows them, configurable from all 6 team create/invite/edit pages in both trees — a user-reported bug that Buyers could see every company landing regardless of project access could not be reproduced on clean or real production data during investigation. The same day, a follow-up request fixed the landing detail page missing its `createdBy` display (a separate Prisma query path, `LandingsService.findOne`, hadn't been given the relation) and three grey-in-Studio-dark-theme cards (`LandingContentCard`, `StatsCard`/`DualStatsCard`, `LandingCard` — all had hardcoded shadcn `Card`, fixed with the same optional `containerClassName` pattern each time), plus a 7th recurrence of the `<SelectValue>`-without-children id-display bug (`GetLinkDialog`'s pixel picker). Also on 2026-08-03, the client filters gained real multi-select for "Баер"/"Пиксель" (new `components/ui/multi-select.tsx` on top of `DropdownMenu`/`DropdownMenuCheckboxItem`), Buyers gained a per-user `User.clientsVisibilityScope` toggle (`ALL_CLIENTS` default / `OWN_CLIENTS`) that scopes the client list and every project-page stats endpoint (`getProjectStats`, `getConversionFunnel`, `getAdBreakdown`, `getLeaderboards`) down to only their own attributed clients — this required adding a real `TrackingEvent.buyerId` column and wiring the SDK's already-flowing `buyerRef` link parameter into `TrackingService.resolveAttribution`, since PageView/Lead events happen before a `Client` row exists and previously carried no buyer attribution at all (not an architectural limit, just unwired plumbing, per user pushback that correctly identified this); and the team edit page gained full editing of a member's email/name/password (`UpdateTeamMemberDto` extended, one-time password reveal on change, same UX as the create-member flow). On 2026-08-04, three more fixes shipped: the project page's ad breakdown now groups by campaign only (`ProjectsService.getAdBreakdown` dropped its `adId` dimension entirely — one row per campaign, not per campaign+ad pair); the scenario-album and push-media uploaders (`components/scenarios/element-media-editor.tsx`, `components/pushes/push-content-step.tsx`, both shared across trees) gained real multi-file selection (`<input multiple>`, sequential upload to respect the existing 10-item cap and avoid a stale-closure item-loss bug that concurrent uploads would have caused); and Studio's `/my-clients` page gained the same lead search (name/username/user_id/etc.) that the classic tree's version already had but Studio's had been missing entirely; the same day, a layout bug reported on the Operator account's `/my-clients` (the search input visually floating toward the center instead of sitting next to the project dropdown) was root-caused to `flex justify-between` distributing gaps evenly across 3 direct children rather than hugging the two right-side controls together — fixed in both trees by wrapping the search input and project dropdown in one shared flex sub-container, the same fix later confirmed to be needed nowhere else once checked. Also on 2026-08-04, the pushes/broadcasts frontend was redesigned end-to-end: the 3-step creation wizard (Контент → Аудитория → Подтверждение, with a "Далее" click and an intermediate draft-`Push`-row PATCH cycle between each) was replaced by a single-page `PushComposer` (`apps/web/src/components/pushes/push-composer.tsx` + Studio's `apps/web/src/app/dashboard/studio/push-composer.tsx`) reusing the existing `PushContentStep`/`PushAudienceStep`/`ScheduleCalendar` sub-components unchanged — no draft `Push` row is created until the final submit, audience preview instead hits a new stateless `POST /pushes/preview-audience`; the sidebar's "Календарь рассылок" entry was renamed to "Рассылки" with a `Send` icon (was `CalendarDays`) at the same `/pushes-calendar` URL, which now shows the existing calendar unchanged plus a new detailed table of every company push below it (`GET /pushes`, company-wide, same `getAccessibleProjectIds` visibility as the pre-existing calendar endpoints) and a blue "Создать рассылку" button; and — since `Push.projectId` is a hard single-value schema column with no precedent for multi-project fan-out anywhere in the codebase — a genuinely new capability shipped: the composer can select and send to several projects at once, via a new `POST /pushes` (`CreateMultiPushDto{projectIds, sendNow, ...CreatePushDto}` → `PushesService.createForProjects`, a plain loop calling the existing single-project `create()`/`send()` per id, not a transaction, since `send()`'s BullMQ side effects can't be rolled back — a partial failure, e.g. hitting the daily push limit partway through, leaves earlier projects' pushes created/sent and reports the rest with a `sendError` per project rather than failing silently or all-or-nothing). The project-scoped `/projects/[id]/pushes/new` route and the new company-wide `/pushes/new` route both render the same composer, differing only in `initialProjectIds` (pre-checked vs. empty) — satisfying the request that arriving from a project's own pushes page preselects that project without limiting the picker to it alone. A same-day follow-up request refined both halves further: the "Рассылки" list below the calendar now defaults to pending pushes only (`GET /pushes?scope=pending`, `PushStatus` DRAFT/SCHEDULED) with a second "Уже были" tab (`scope=history` — SENDING/SENT/FAILED) that only fires its query when clicked, so a visit to the page never pulls full send history up front; and the composer's project picker was pulled out of its own `Card`/`STUDIO_CARD` wrapper into a plain vertical list (checkbox + `ChannelAvatar` + name + a live "Доступно: N" reachable-audience count per row), while Контент/Аудитория/Отправка collapsed from three separate cards into one — the per-project audience preview call (`POST /pushes/preview-audience`) now always covers every visible project, not just the checked ones, so the picker can show every row's number before it's selected; the Audience section's own total is derived client-side by summing that same response over just the checked ids, and its old per-project breakdown list was removed since the picker already shows it. A same-day bug-fix round covered 4 more reports: the `TelegramMessagePreview` chat-bubble mockup (shared by pushes/scenarios/welcome-message) was hardcoded light-only (`bg-[#e7f3ff]`/`bg-white`, no `dark:` variants) and looked like a glaring bright box in Studio's dark theme — given real Telegram dark-mode colors (`#0e1621` chat bg, `#182533` bubble) rather than just Studio's own palette, since the component's whole purpose is a faithful "how it'll actually render in Telegram" preview; `PushAudienceStep`'s summary card (По фильтру/Доступны/Недоступны) gained the established `containerClassName` override prop (same pattern as `StatsCard`/`LandingContentCard`/`LandingCard`) since a bare shadcn `Card` inside Studio's dark theme paints from the placeholder `--card` CSS var, not Studio's real `#171F2B` — Studio's composer now passes `STUDIO_CARD`; the "Отправка" section's mode toggle (two tab buttons that revealed/hid the calendar) was replaced with an always-visible calendar plus a small "Отправить сейчас" checkbox beside it, since the reveal/hide was pushing the submit button down every time schedule mode was picked; and a real backend bug was found in `ClientsService.buildPushFilterWhere` — the "principal audience" condition (`subscribedAt`/`botActivatedAt` not null, i.e. excluding cold external contacts who never subscribed or messaged the bot) had been bundled inside the same `if (reachableOnly !== false)` branch as the `isBotActive` check, so `countAudienceTotal` ("По фильтру", called with `reachableOnly:false`) skipped it entirely and counted every `Client` row for the project including pure-external ones — the two conditions are now independent, with the origin/audience-membership check applied unconditionally and `isBotActive` staying gated to the reachable-only count. The same day, the composer got a full layout redesign per explicit user sketch: the top half is the content editor (`PushContentStep`'s own internal grid already put fields left / `TelegramMessagePreview` right, so no new layout code was needed there, just giving it the full-width top row to itself); the bottom half splits into two cards — left holds a compact scrollable "Проекты" list (`max-h-64 overflow-y-auto`, same avatar+checkbox+"Доступно: N" rows as before) plus a "Расписание" card, right holds "Фильтры получателей" (`PushAudienceStep`); the submit button sits alone beneath both, full page width. Since content editing is now the *first* thing on the page — above project selection, not gated behind it — a project auto-selects on load whenever `initialProjectIds` was empty (`GET /pushes/new` company-wide entry), so `PushContentStep`'s media-upload auth always has a valid `projectId` from the first render instead of starting in a broken disabled state. A genuinely new capability shipped alongside this: an explicit send-timezone picker in the schedule card (defaults to the browser's own IANA zone via `Intl.DateTimeFormat().resolvedOptions().timeZone` — "чей часовой пояс сидит в CRM", not `Project.timezone`, a different concept), reusing the existing `TimezoneInput` component (`components/timezone-input.tsx`, previously project-settings-only) after giving it optional `label`/`hint` overrides; converting the picked wall-clock date+time in that zone to the correct UTC instant needed a small new dependency-free helper (`lib/timezone.ts`'s `zonedTimeToUtcIso`, the standard `Intl.DateTimeFormat`-diffing trick — no date-fns-tz/luxon added) since the old code just fed the raw string into `new Date()` and implicitly relied on the browser's own local zone. The redesign surfaced one real regression caught before shipping: `PushAudienceStep`'s internal `lg:grid-cols-2` (fields | totals side-by-side) was sized for the old near-full-width placement — once nested in the new half-width "Фильтры получателей" card, the viewport-based `lg:` breakpoint still fired (it doesn't know its parent shrank) and the two columns overflowed the card; fixed by dropping the internal grid to always-stacked, which is also just the better layout at that width. On 2026-08-05, media attachment was redesigned too: the old "pick a type from a dropdown, then upload" flow (`PushContentStep`) became one drag-and-drop-or-click container that classifies each file by its own MIME type (`image/*`→photo, `video/*`→video, `audio/*`→voice — auto-detected, no more manual type-select) — multiple photos/videos become an album automatically, same as before. Video note ("kruzhok") is no longer chosen before upload; instead, once a video is the sole item in the album, a checkbox appears right on its thumbnail to convert it after the fact via a new `POST .../pushes/media/:key/to-video-note` endpoint (`PushesService.convertToVideoNote` streams the already-uploaded object back out of MinIO, runs the same `ensureSquareVideoNote` crop used at upload time, stores the result under a fresh key) — both the original and converted URLs are cached client-side (`PushContentStep`'s local `videoNoteCache`) so toggling back and forth doesn't re-hit the network or re-crop after the first conversion. Voice is a genuinely new push capability, not previously wired end-to-end: `ChannelProvider.sendMessage`/`TelegramProvider` already supported a `'voice'` mediaType (used by bot scenarios), but `PushMediaDto.type` only allowed `photo`/`video`/`video_note` — extended to include `voice`, with `PushesService.assertValidMedia` treating it as the same kind of "solo, no-album" type as video_note (Bot API's `sendMediaGroup` supports neither). The upload-time `key` needed to support the after-the-fact conversion doesn't belong in the wire payload (`PushMediaDto` only knows `type`/`url`, and the global `ValidationPipe`'s `forbidNonWhitelisted` would 400 the whole request if it leaked through) — both composers now `.map(({url,type}) => ...)` before submit to strip it. On 2026-08-05, 3 more composer bugs were fixed: the "Расписание" calendar disappeared the moment a 2nd project got checked (was gated on `selectedIds.length === 1`; now shows the first checked project's calendar regardless of total count, since it was always inherently single-project anyway); the calendar itself stayed stuck at a fixed compact width even though its card had grown much wider in the new layout (`CalendarGrid`'s `compact` mode switched from a fixed `w-[19rem]` to `w-full`, and `ScheduleCalendar` switched from a side-by-side flex row with `DayScheduleList` to stacking them vertically, since side-by-side made a full-width calendar pointless); and a one-frame "Нет проектов с правом на создание рассылок" flash on every page load (the empty-state check ran against `projects` before the `GET /projects` query had actually resolved — fixed by also gating on the query's own `isLoading`). On 2026-08-04, a genuinely new parallel feature shipped: broadcasts sent through a project's personal MTProto account (not the bot) rather than another `Push` mode — audience/send-mechanism/content/pacing were different enough to warrant separate tables/engine, the same call already made for scenarios-vs-automations. New `PersonalBroadcast`/`PersonalBroadcastLog` (not in `modelsWithCompany`, manual `projectId` scoping like `Push`) and `Client.lastPurchaseAt` (deposit recency — `Purchase` has no dedicated "deposit" entity, denormalized in `PurchasesService.create` next to the existing `totalSpent`/`purchasesCount`); audience always starts from `dialogueSource: 'PERSONAL_ACCOUNT'` (the one unambiguous "real dialogue with this specific account" signal, set only by `TelegramPersonalService.handleIncomingMessage`), plus a live `client.getInputEntity(tgUserId)` recheck immediately before each individual send. New `TelegramPersonalService.sendDirectMessage`/`getDialogFilters`/`getFolderTgUserIds` follow the pre-existing `sendStory` "never throw, return `{success,error}`" pattern; Telegram-folder filtering only resolves explicit `includePeers` membership (GramJS has no API to enumerate who matches a rule-based folder), a disclosed v1 scope cut. Because one MTProto socket per channel can't take concurrent `invoke()`s (the same constraint `StoriesCron` already documents), a whole broadcast runs as ONE long-lived BullMQ job looping with a random `delayMinSeconds`–`delayMaxSeconds` sleep between sends (anti-ban pacing) rather than one job per recipient; `FloodWaitError`/`SlowModeWaitError` put the recipient back to `pending` and sleep the server-requested duration instead of counting it as a failure. Content "variants" (deliberately manual now, AI-generated later) are assigned round-robin, not randomly, for even/debuggable distribution. New `Permission.PERSONAL_BROADCASTS_VIEW/CREATE/SEND/DELETE` is explicit-grant-only (no default role gets it, like `CLIENTS_VIEW_CROSS_PROJECT`), and surfaces as a project-page button (not a sidebar item, unlike Push) since — unlike bot pushes — the feature has no company-wide/multi-project concept: each project's personal account is its own persona. Live verification used an isolated throwaway test company/project/clients (never real connected production accounts, same restriction the user set for the Stories module) driven directly through `PersonalBroadcastsService` — all 10 audience-filter-dimension checks and round-robin variant assignment came back correct, and it surfaced one real bug fixed the same day: `getDialogFilters`/`getFolderTgUserIds` weren't wrapped in try/catch, so a stale/unreachable MTProto session threw uncaught and 500'd the whole audience preview instead of degrading to an empty folder list like every other method in that service already does. Also on 2026-08-04, a public marketing homepage shipped for logged-out visitors: previously an unauthenticated hit on `/` (either domain) showed a loading skeleton and immediately redirected to `/login`, with no product-description entry point at all. New shared `MarketingHome` component (`apps/web/src/components/marketing/marketing-home.tsx`, Studio-styled with full dark-mode support) is wired into both root-level auth gates — `apps/web/src/app/dashboard/layout.tsx` (Studio/mw-track.com) and `apps/web/src/app/(dashboard)/layout.tsx` (classic/old.mw-track.com) — purely additively: the already-authenticated branch is untouched, a new branch only fires for `!accessToken && usePathname() === '/'` (confirmed via the same reasoning `StudioSidebar` already relies on for its cross-domain `classicHref` — `usePathname()` reflects the clean public URL, not the Studio-rewrite destination), every other unauthenticated route still redirects to `/login` as before. Verified live by driving a locally-cached headless Chromium against the deployed build with the `Host` header swapped between both domains — renders identically on both, no console errors, "Войти" correctly navigates to `/login`, and desktop/mobile/dark-theme screenshots all confirmed clean layout. The same day, a follow-up request pointed out that the personal-broadcast composer's media upload ("не такая как в обычной рассылке") still used the pre-redesign button-plus-file-dialog interaction; `PersonalBroadcastVariantEditor` (`apps/web/src/components/personal-broadcasts/variant-editor.tsx`) was brought in line with `PushContentStep`'s 2026-08-05 drag-and-drop redesign — same dropzone visuals/interaction, MIME-based auto-classification — while deliberately keeping the pre-existing single-file (not album) constraint, since that's an intentional property of "variants" as the content-diversity mechanism, unrelated to the upload-UX complaint. Verified live end-to-end: an isolated test company/user/project/channel, a real login through the actual `/login` form via headless Chromium, then a real file upload through the composer — thumbnail and remove control rendered correctly, no console errors, test data deleted by exact id afterward. The same day, the user called the marketing page's first version "слишком нейроночно" (too AI-generated-looking) — the sparkle-badge-over-hero + symmetric 3×2 icon-card grid was indeed the generic-SaaS-landing template. Redesigned around real product screenshots instead of abstract icons: cropped (via `sharp`, no Pillow/ImageMagick available in this environment) the same PNGs already used by `/docs` (`apps/web/public/docs/*.png`) down to `public/marketing/*.png` — trimmed empty whitespace and the test project's name out of frame — and wrapped them in a small `BrowserFrame` component (traffic-light dots + mono address bar) instead of bare `<img>`s. Structure changed from a symmetric feature grid to alternating text/screenshot sections (clients list → client detail with real UTM/fbclid attribution → team/buyer-revenue stats), a hero with a real funnel screenshot given a slight `rotate` and a soft color blob behind it rather than a perfectly rectangular layout, and every other feature folded into one compact tag row instead of forcing icon-cards onto features with no good screenshot. Deliberately did NOT use the `billing.png` screenshot for pricing — its numbers could be stale, and presenting them as current on a public page would risk being misleading. Verified live the same way as the initial ship (headless Chromium against the deployed build, `Host` header swapped, desktop/mobile/dark screenshots) — no console errors, real screenshots render crisply in both themes. On 2026-08-05, a follow-up request covered `/login`/`/register`/`/invite`: "убери эту карточку для формы, добавь что-то красивое в наших цветах, форму можно в бок для красоты и антисимметрии" — the shared `(auth)/layout.tsx` (previously one centered white card on a grey background, identical across all three pages) was rebuilt around a brand panel (`AuthBrandPanel`, blue gradient + the same funnel screenshot crop as the homepage, tilted and bleeding off its edge) occupying ~44% width on `lg+`, with the form itself rendered card-less directly on the page background. The panel's side alternates by `usePathname()` — left on `/login`, right on `/register`/`/invite` — that alternation between two related pages is the "antisymmetry" asked for. One real bug caught before shipping: the first version absolutely-positioned the screenshot inside a `justify-between` flex column, which made it overlap the tagline text at ~900px viewport height; fixed by moving it into normal flex flow (logo → centered text block → image with negative-margin bleed) instead of `absolute`. Verified live the same way as the homepage. Also worth noting: mid-session, the Read/Edit tools hit a transient host-side hook timeout while Bash kept working — the remaining doc edits for this round were applied via a Python script through Bash instead of the Edit tool. Separately on 2026-08-05, a user-reported revenue/leaderboard mismatch on the project page (main revenue card correct, but a buyer's expanded leaderboard row showing a wildly different, larger number than the summary line right next to it) turned out to be two real backend bugs, not a caching artifact: `ProjectsService.getBuyersFunnel` (the funnel that expands under a buyer's leaderboard row) filtered its raw SQL only by `buyerId`, with no `projectId` at all — a buyer working across multiple company projects got their revenue/purchase-count silently summed across ALL of those projects on every single project's page (confirmed: $701/11 purchases was exactly their total across two projects, $572+$129 and 10+1, while the real per-project number was $129/1); `getEventColumnFunnel` (shared by the pixels/campaigns leaderboard categories) had the identical gap for `campaignId` (an external ad-platform ID not guaranteed unique across a company's projects) — fixed both by threading `projectId` through and filtering on it, even though only the buyers path had a confirmed live example. Live-verified with an isolated two-project/one-shared-buyer test company (reproduced the exact cross-project leak pre-fix, confirmed it gone post-fix), then re-confirmed against the real Igor Conel/Diego Herrera case from the bug report. The same day, a follow-up request ("в главной статистике не учитывай покупки внешних клиентах") found the main project stats (`ClientsRepository.getProjectStats`'s revenue/FD/RD cards and charts, plus `getConversionFunnel`'s deposit stages) counted every `Purchase` by `projectId` alone, with no check that the purchasing `Client` was actually "ours" (`subscribedAt IS NOT NULL` — the same `OURS_ONLY` criterion every other client metric in that file already used) rather than an external/cold contact who never came through the funnel. The existing buyer-scoping subquery only applied that check indirectly and only when a specific buyer was selected; made "is this client ours" an unconditional part of the same subquery instead of something that only ever showed up alongside an optional `buyerId`. `ClientsRepository.getMyStats` (Operator's personal stats, scoped by `Purchase.registeredBy` rather than client identity) was deliberately left alone as out of scope. Verified live with an isolated test project (one real client with a $100 purchase, one external contact with a $50 purchase) called directly through the service, confirming the external purchase no longer appears in any of the affected fields. Later the same day, investigating a real user-reported bug — a bot's join requests were being approved (people got into the channel fine) but nothing registered in the CRM and no welcome message went out — traced to Telegram silently withholding webhook delivery for that one specific bot for ~20 hours (confirmed via nginx access logs: this bot's webhook path went completely quiet while every other connected bot on the same server kept receiving updates normally the whole time; likely Telegram-side anti-spam throttling, given the bot's welcome message was aggressive/ALL-CAPS and had a high block-rate). Since there was no monitoring for "webhook went silent," this went unnoticed until traffic/revenue had already been lost. Built a permanent safeguard: new `Channel.lastWebhookAt` column, stamped in `TelegramProvider.handleWebhook` (the one common entry point before grammY dispatches by update type) on every single incoming webhook regardless of type, fire-and-forget so it never adds latency to the Telegram-facing response. "Is this webhook stale" is computed lazily on read in `ProjectsService.sanitizeChannel` (same pattern as the existing `tgPersonalConnected` computed field, not a stored/cron-maintained boolean like `isActive`) — `lastWebhookAt` set and older than a deliberately generous 6-hour threshold, chosen because real per-channel webhook volume in this system spans two orders of magnitude (~15 to ~800+/day) and a tighter threshold would false-positive on quiet channels. Surfaced as a new "Нет вебхуков" badge alongside the existing bot-blocked indicator, in all 4 places that indicator already appears (project card + project page, both classic and Studio trees) — two independent signals, since a bot can be fully unblocked and healthy while Telegram simply isn't sending it anything. Confirmed via `AskUserQuestion` with the user beforehand: deliberately did NOT attempt retroactively reconciling the channel's ~12,600 real members against the ~296 CRM-registered ones to backfill/welcome-message the gap — cold-messaging thousands of people who never contacted the bot is a strong ban-risk that outweighs recovering the lost registrations; and the indicator itself is a CRM-only badge, not a separate alerting/admin-panel system. Verified live: an isolated test project with `lastWebhookAt` set 10 hours in the past rendered the badge correctly in all 4 spots (real login, both domains, headless Chromium, no console errors), and separately confirmed in production that `lastWebhookAt` is now actually being stamped in real time off real incoming Telegram webhooks. The very next day, that same monitoring effort led straight into a second, more concrete real incident for the same bot: a customer reported a client going through the correct tracked landing link, getting auto-accepted into the channel, but never registering or receiving a welcome message — this time the webhook wasn't silent at all (traffic was flowing), which pointed away from the previous root cause. Investigation found the same bot token (`@Diego_Herrera_Bot`) had been connected to three different projects over time (two now archived, one active) — Telegram only allows a bot ONE registered webhook URL at a time, and `getWebhookInfo` confirmed it was currently pointed at an **archived** project's channel, not the active one, because whichever channel's `initialize()` last called `setWebhook()` wins regardless of how many `Channel` rows in our own DB reference that same token. Real traffic was landing on the archived channel, which happened to be in `BOT_DIRECT` mode rather than `PRIVATE_CHANNEL_REQUEST` — so the guard that normally prevents `handleChatMemberUpdate` from creating a client on an untracked join didn't apply, and 80 real clients had silently accumulated in the archived, invisible project over ~40 minutes. Fixed immediately (via `NestFactory.createApplicationContext`, bypassing HTTP): re-ran `ChannelsService.reactivate()` on the correct channel to reclaim the webhook, deactivated the archived channel so it can't steal it back, and migrated all 80 misrouted clients into the active project — merging into an existing record (adopting `subscribedAt`/`isSubscribed`, filling missing name fields, soft-deleting the duplicate) for the 33 people who already existed there via the personal-account dialogue path, plain `projectId` reassignment for the other 47. Deliberately did not retroactively fire the welcome scenario for the moved clients as part of this — that's a separate, not-yet-made decision. Then built the actual prevention the user asked for: `ProjectsService.archive()` now cascades to the project's channel — `TelegramPersonalService.disconnect()` (logs out the MTProto session, clears the session columns) if a personal account was connected, then `ChannelsService.deactivate()` (calls `deleteWebhook()`, flips `isActive` false) — the exact same methods already used for manual disconnection from settings, just now automatic on archive, wrapped in try/catch so a Telegram-side failure can't block the archive itself. Required adding `TelegramPersonalService` to `ProjectsService`'s constructor; no new circular-dependency risk since `ChannelsModule` already flows into `ProjectsModule` via the existing `forwardRef` and already exports that service. Verified via an isolated fake-session test project (confirmed the cascade correctly clears `isActive`/session fields) and confirmed in production that the webhook stayed correctly pointed at the active channel even through a subsequent API restart (which re-initializes every `isActive` channel on boot). Later the same day, a different project ("Isabella Ramirez") surfaced a related-but-distinct revenue-mismatch report: the main stats card showed $99 while the "ТОП" leaderboard drill-downs summed to only $94 — same symptom family as the cross-project buyer-funnel leak fixed earlier that day, but this time the missing filter was `deletedAt`, not `projectId`. `getProjectStats`'s two `purchase.aggregate()` calls and its `purchaseBuyerFilterSql`, `getConversionFunnel`'s own separate `purchaseBuyerFilterSql`, and `getLandingsFunnel`/`getEventColumnFunnel`'s raw-SQL purchase queries all checked `Client.subscribedAt IS NOT NULL` but not `deletedAt IS NULL` — so a soft-deleted client (e.g. a duplicate merged away during that same day's Diego Herrera migration) still had its historical purchases counted in some stats but not others, while `getLeaderboards` (top-level) already filtered `deletedAt` correctly — hence the two numbers disagreeing instead of both being wrong. `getBuyersFunnel`'s purchase query got the same fix, on top of the `projectId` fix it had already received earlier that day for the unrelated cross-project leak. Separately confirmed the user's other complaint on the same project (buyer-leaderboard client/dialogue counts not summing to the page total) is not a bug: buyer rows only include clients with `buyerId IS NOT NULL`, so unattributed clients are correctly excluded — the landings breakdown, which has no such gate, summed to the total exactly. Verified live by re-running the real service methods against the actual project before/after the fix: `totalRevenue` dropped from 99 to 94, the affected buyer's funnel revenue from 15 to 10, the affected landing's funnel revenue from 99 to 94 — all now match `getLeaderboards` exactly. Later the same day, three requested pushes/broadcasts features shipped: editing an existing DRAFT/SCHEDULED push, duplicating any push, and inline cancel/edit/duplicate actions directly on the company-wide "Рассылки" (`/pushes-calendar`) list rather than only on a project's own pushes page. The backend edit (`PATCH .../pushes/:id`) and cancel (`DELETE .../pushes/:id`, sets `FAILED` — `PushStatus` has no separate `CANCELLED` value) endpoints already existed from the original build but had no UI wired to them at all; only `PushesService.duplicate` (clones content/filter into a fresh `DRAFT` via the existing `create()`, deliberately dropping `scheduledAt`/status/counters — a copy is a new send, not a resumed one) and its `POST .../pushes/:id/duplicate` route were new. `PushComposer`/`StudioPushComposer` gained optional `pushId`/`editProjectId` props that switch them into edit mode: content/filter/schedule prefill from `GET .../pushes/:id` (a new `lib/timezone.ts` `utcIsoToZonedParts` reverses the existing `zonedTimeToUtcIso`, since `Push.scheduledAt` only stores the UTC instant — the zone it was picked in is never persisted, so editing always redisplays in the browser's current zone), the project picker becomes a fixed read-only row (`Push.projectId` is single-value, multi-project doesn't apply to edit), and submit calls `PATCH` instead of `POST /pushes`. The "Отправить сейчас" checkbox is reused for edit mode but re-labeled "Без расписания" and re-wired to mean `scheduledAt: null` (revert to `DRAFT`) rather than an immediate send, since `PATCH` never sends. New edit routes `.../pushes/:id/edit` (project-scoped, both trees) render the same composer with those props; the company-wide list's rows already carried `push.project.id` from `findAllForCompany`, so no new company-wide backend route was needed — "Изменить"/"Копировать"/"Отменить" there call the same project-scoped endpoints directly. Live-verified via an isolated throwaway company/project (no real channel) driven straight through `PushesService`: create→DRAFT, edit with a date→`SCHEDULED`, edit with `scheduledAt:null`→back to `DRAFT`, duplicate→new independent `DRAFT` named "(копия)", and confirmed editing an already-cancelled (`FAILED`) push is correctly rejected by the pre-existing `assertEditable` guard — all 5 checks passed, test data deleted by exact id afterward.

## Before doing anything

Read [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md) first, then the numbered doc matching the area you're touching (01-15, listed in 00's file map). Each numbered doc beyond 00 is currently a TODO stub with section headers only — fill them in as that part of the system gets built, don't let them go stale.

## Hard invariants (do not violate when writing code here)

- Every DB query that touches tenant data must be scoped by `company_id`. This is enforced at the middleware level, not by convention — if you add a new table or query path, it must go through that scoping.
- Channels (Telegram, WhatsApp, Instagram, Viber, Email) implement one `ChannelProvider` interface. Adding a channel = new class implementing that interface, never an if/else branch in business logic.
- Tracking events (PageView, Lead, Purchase) are written to an `events` table first, then pushed to Facebook/TikTok asynchronously via queue. Never call Facebook CAPI / TikTok Events API synchronously from the request path.
- All pushes go through BullMQ queues with rate limiting (30 msg/sec for Telegram). Never send a push directly.
- Every external/idempotent-sensitive request carries an `idempotency_key`.
- No hard deletes — every delete is `deleted_at` soft delete. Exceptions (documented deviations, not oversights): `Channel` has no `deletedAt` column; its "delete" is `isActive: false` (bot/webhook torn down, row kept). `Domain` also has no `deletedAt` column; its "delete" is a genuine hard delete (`domain` hostname is globally `@unique` — soft-deleting would permanently block re-adding the same hostname later). `TeamInvite` also has no `deletedAt` column; it's a transient token/credential (like `RefreshToken`, also hard-deleted on logout), not tenant data — revoking one is a genuine hard delete. All three are excluded from the `PrismaService` tenant-scoping middleware's `modelsWithCompany` list for the same reason — that middleware auto-injects `deletedAt: null` into every read, which would throw `Unknown argument` against any of them.

## Shared choke points — check before changing

These are places where multiple independent consumers rely on one piece of code staying
consistent. If you're editing one, grep for the others before you ship.

- **`modelsWithCompany` array** (`apps/api/src/prisma/prisma.service.ts`) — the tenant-scoping
  middleware only auto-injects `company_id`/`deletedAt` filters for models explicitly listed
  here (`Project`, `Landing`, `Client`, `BotScenario`, `AutomationFlow`, `AbTestGroup`,
  `StoryPost`). A new table needing tenant scoping does NOT get it automatically from having a
  `companyId` column — it must be added to this array, or every read against it is an unscoped
  cross-tenant leak.
- **`checkSubscriptionLimit`** (`apps/api/src/common/guards/subscription.util.ts`) — the single
  shared function deciding whether a company is blocked from sending pushes (plan expired,
  monthly cap, and — once Phase 4.3D ships — manually blocked/suspended by an admin). Consumed
  by both `SubscriptionGuard` (manual HTTP send) and `PushesCron.fireScheduledPushes`
  (automated). Any new "is this company allowed to push" condition belongs inside this one
  function, not as a second, divergent check bolted onto only one of its two callers.
- **`apps/admin`** (standalone Next.js app, separate from the CRM's `apps/web` — planned/being
  built per [15_PHASES.md](15_PHASES.md) §4.3, not yet fully live) is a second, cross-tenant
  consumer of core CRM services: `ProjectsService`, `ClientsService`, `PushesService`,
  `LandingsService`, `DomainsService`, `TeamService`, and the `Company` model itself. It reaches
  them either via a `runAsCompany()` helper (re-enters the tenant-scoping AsyncLocalStorage
  context with an admin-chosen target `companyId`, for the models in `modelsWithCompany`
  above) or directly (for `Domain`/`Push`/`Channel`/`User`, which already take an explicit
  `companyId` param). **When you change the signature, return shape, or scoping behavior of any
  of those services, or add/rename a `Company` field, check `apps/admin` for breakage** — it
  won't show up in `apps/web`'s tests or typecheck since it's a separate package.
- **Hand-maintained frontend enum-label dictionaries** — some Prisma enums have a matching
  `Record<EnumValue, string>` label map in `apps/web` that is NOT generated from the schema
  (e.g. `BalanceTransactionType` → `TRANSACTION_LABEL` in
  `apps/web/src/app/(dashboard)/billing/page.tsx`). Adding a new enum value in
  `prisma/schema.prisma` silently produces an undefined/missing label in the UI unless you grep
  the frontend for that enum's existing dictionary and add the new case by hand.
- **"Is this `UserRole` elevated?" — 4 independent mechanisms, only one is a blacklist.**
  `apps/api/src/common/permissions/permission.constants.ts`'s `isElevatedRole()`
  (`RESTRICTED_ROLES` array) is a blacklist — a new role not added there is silently treated as
  elevated (full bypass of per-project permission checks). `projects.service.ts`'s
  `ELEVATED_ROLES` const + its own inline check in `assertAccess`, `audience.service.ts`, and
  `stories.service.ts` are all whitelists (`[OWNER, ADMIN, SUPER_ADMIN]`) — a new restricted role
  is correct there automatically. When adding a new `UserRole` value, `RESTRICTED_ROLES` is the
  one place you must touch deliberately; there's no compiler safety net for it.
- **`RolesGuard`'s `@Roles(...)` is a rank threshold (`Math.min(...requiredRoles)`), not a
  set-membership check.** Adding a lower-ranked role into an existing decorator's role list
  silently lowers the bar for every other role already at or above that rank, not just the new
  one. When a new role needs to reach a route gated this way but sits at the same rank as an
  existing restricted role (e.g. `BUYER`/`OPERATOR`/`OPERATOR_ADMIN`, all rank 1), don't edit the
  decorator list — remove the guard from that route and add an explicit role check inside the
  service instead (see `assertCanAssignRole`/`assertCanAccessTeamManagement` in
  `apps/api/src/modules/team/team-role.util.ts` for the established pattern).

## Stack quick reference

Backend: NestJS 10 + Prisma 5 + PostgreSQL 16 + Redis/BullMQ. Frontend: Next.js 14 App Router + TailwindCSS/shadcn + Zustand + React Query. Telegram via Grammy.js (webhook only, no polling). WhatsApp via 360dialog. Crypto payments: USDT TRC-20 (TronGrid) / ERC-20 (Infura/Alchemy) via HD wallet, one address per payment. Full list with versions in [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md).

## Doc maintenance

When a numbered doc (01-15) goes from stub to real content, also update [15_PHASES.md](15_PHASES.md) to reflect what phase actually shipped — it's the source of truth for "what's done vs planned," don't let it drift from reality.

`apps/admin` (Phase 4.3) is architecturally separate from the rest of the app and evolves
across many phases — 15_PHASES.md §4.3 is its authoritative status/plan doc; keep it current
phase-by-phase rather than writing it up only at the end.
