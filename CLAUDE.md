# TrafficCRM

Multi-tenant SaaS CRM for media buyers (traffic arbitrage), branded MWTRACK in the product UI. Spec docs (00-15) are complete; real implementation is underway following [15_PHASES.md](15_PHASES.md) phase by phase. As of 2026-07-02, Phase 1 is done including the first real production deploy (162.213.248.131, mw-track.com), Phase 2 steps 2.1-2.9 (2.7 Analytics/2.8 Lookalike Export confirmed done 2026-07-15, 2.9 = Team/roles Phase 1), 2.11 (audience overlap + Telegram client enrichment), and 2.12 (landing tracking links: pixel + Facebook/TikTok ad macros, customizable query-param names, ad/campaign breakdown) are done; Phase 3 steps 3.1 (Drip Campaigns/automation flows: trigger→delay→push→condition, linear step-list UI instead of drag-and-drop), 3.2 (A/B/n testing for landing pages: group N existing landings with per-landing traffic weights, card-click multi-select UI, no-cookie random split per request, comparison stats; extended 2026-07-17 so a domain/path binds to either one landing directly or the whole test group, not implicitly via landing membership), 3.4 (Smart Push Timing: CTR-by-hour analysis, first click-tracking in the project via a public push-button redirect endpoint) are done as of 2026-07-15, and 3.6 (Team Analytics: buyer attribution via a hidden query-param on the tracking link rather than landing ownership, "БЕЗ БАЕРА" bucket for unattributed clients, company-wide project comparison) done 2026-07-17 (3.3 paused, 3.5 cancelled, 3.7 obsolete per user scope decisions — Phase 3 otherwise complete); and Phase 4 step 4.1 (Telegram MTProto personal account, client dialogue tracking) shipped ahead of schedule on explicit user request; on 2026-07-22, bot scenarios were merged conceptually with 3.1's automation flows (shared step-chain idea — DELAY/SEND_MESSAGE/CONDITION, new `BotScenarioStep`/`BotScenarioRun` — but deliberately separate tables/engine, since scenario runs must be able to re-fire per client while automation enrollments are one-per-client-forever) and gained a step-chain editor + ≤10-item media gallery with video-note support at `/projects/[id]/scenarios`, while automation flows (3.1) were temporarily hidden from the UI (code/data untouched) per explicit user request; on 2026-07-25, the welcome message moved out of channel settings into scenarios entirely (new `SUBSCRIBE` trigger, replacing `Channel.tgWelcomeMessage*`/`sendWelcomeMessage`), the video-note pipeline was rewritten to always re-encode via ffmpeg's own runtime filter expressions instead of a skip-if-already-square fast path (the real cause of frequent kruzhok failures across formats/resolutions), and the scenario editor was redesigned around "elements" (one content type + optional button each, delay embedded per-element instead of a separate step type, condition/standalone-delay hidden from the UI for now); also on 2026-07-25, A/B testing shipped for bot scenarios (mirrors landing A/B groups — `ScenarioAbTestGroup`, weighted random variant pick per trigger firing with no client stickiness, 6 client-snapshot conversion metrics, synthetic `command` values to sidestep `BotScenario`'s unique constraint), including a same-day `endAbTest` lifecycle bug fix (member scenarios' `abTestGroupId` wasn't cleared, blocking a second test) plus a retroactive backfill for the one real production test already corrupted by it; and on 2026-07-27, per-event-type switches were added to gate whether tracking events get forwarded to Facebook/TikTok (`Project.disabledTrackingEvents`, gated in `TrackingService.recordEvent` alongside the existing ad-attribution check — the CRM's own `TrackingEvent` write is never affected), with a visible on/off indicator row on the project overview page and a new "События" settings tab; narrowed the same day to just the 3 event types the backend fires on its own initiative (Subscribe/Dialogue/Purchase — PageView/Lead/InitiateCheckout/Click are explicit SDK calls the tenant already controls in their own landing code, so a CRM switch for those is redundant), and made the switch gate only the automatic firing path via a new `RecordEventDto.forceSend` escape hatch — manual staff actions (the "Зарегистрировать диалог" button, the "Добавить покупку" form) keep forwarding to ad platforms even while the switch is off; and the same day, message personalization placeholders (`{first_name}`/`{last_name}`/`{full_name}`/`{username}`) shipped for both pushes and bot scenarios via one shared `renderMessagePlaceholders` helper, wired into `ChannelsService.sendMessage` (pushes + automations) and `BotScenarioEngineService.sendStepMessage` (scenarios, with its own Client lookup since scenarios must fire for cold contacts with no Client row); and on 2026-07-28, the Team/roles granular-permissions system (Phase 2) was redesigned from flat/global-per-user to per-project — a Buyer/Operator can now hold different `Permission` grants on different projects (`UserPermission` gained a `projectId` column, `@RequirePermission`/`PermissionsGuard` were retired in favor of a 5th `requiredPermissions?` param on `ProjectsService.assertAccess`, DB-backed for an immediate effect instead of waiting on JWT refresh), except `DOMAINS_*` which stays company-wide by explicit user decision (`Domain.projectId` doesn't meaningfully scope a domain to one project); a new `PROJECTS_EDIT` permission gates `PATCH /projects/:id` (archive/token-regeneration stayed `@Roles(OWNER, ADMIN)` instead of a permission, per the same decision) — see 15_PHASES.md checkboxes for current status. On 2026-07-30, after reaching full page-for-page parity, the "Studio" redesign (`apps/web/src/app/dashboard/studio/*`) was promoted to the primary UI for every role at `mw-track.com`'s own default paths (`/`, `/projects`, etc.) — the old UI (still fully intact, unchanged) was split off onto a separate subdomain, `old.mw-track.com`, reachable via a "Старый дизайн" link in Studio's sidebar. Both domains are served by the same Next.js process; the split happens via `apps/web/src/middleware.ts`, which reads the `Host` header and, on the main domain, transparently rewrites every path (except the shared `/login`/`/register`/`/invite` pages) to Studio's physical files under `dashboard/studio/*` — the browser's address bar stays clean since it's a server-side rewrite, not a redirect. Studio's own internal links are root-relative (`/projects/:id`, not `/dashboard/studio/projects/:id`) for exactly this reason. Also on 2026-07-30: push limits were converted from monthly to daily (`Company.maxPushesPerDay`/`pushesToday`, rolling-window reset), then the plan ladder's `maxProjects` was revised (TRIAL 1/STARTER 5/GROWTH 10/SCALE 20/ENTERPRISE 50, the last no longer an unbounded sentinel) with push limits recomputed via a per-project rate except ENTERPRISE (kept flat); NOWPayments was connected as a second `PaymentGatewayProvider` alongside the still-inactive Heleket, using `/v1/payment` (direct address) instead of a hosted checkout so the existing `PaymentModal` needed no changes; and a new project-scoped `OPERATOR_ADMIN` role was added — it manages only `OPERATOR`-role team members within its own `ProjectAccess` projects (not company-wide), enforced via a new `assertCanAccessTeamManagement`/`assertOperatorAdminScope` pair in `team-role.util.ts` rather than the `@Roles()` decorator (whose rank-threshold model would have silently admitted BUYER/OPERATOR too) — alongside this, `OPERATOR`'s own UI was hard-restricted to two pages (`/my-clients`, `/my-stats`, the latter backed by a new `Purchase.registeredBy`-based endpoint) and the client detail card now hides ad-attribution fields from `OPERATOR` via a server-computed `canViewTrafficSource` flag; and the same day, the project client list gained an "also in other projects" badge (`tgUserId` match, same identity concept as the existing audience-overlap feature) gated by a new `Permission.CLIENTS_VIEW_CROSS_PROJECT` — granted on the viewer's OWN project, not the other one it reveals — with elevated roles or grant-holders seeing full detail (which project, joined-when, dialogue-when) and everyone else seeing only the bare fact plus a dialogue date, no project name; and the same day, a full re-audit of the standalone `apps/admin` platform panel found and fixed two real gaps left over from Phase 4.3D's explicitly-deferred scope — automation flows/bot scenarios now check `checkSubscriptionLimit` before sending (a suspended/blocked company could previously still have automated messages go out) — plus added invoice visibility per company, pagination/company-filter on the Errors/Actions/Companies list pages (Errors had been silently truncating to 50 of 135 real rows), and a `COMPANY_VIEWED` audit-log entry so cross-tenant reads (not just mutations) leave a trace; the SUPER_ADMIN password was also rotated; a same-day regression this surfaced (`TEAM_MANAGER_ROLES`, part of the `OPERATOR_ADMIN` work above, had forgotten `SUPER_ADMIN`, breaking the admin panel's "Команда" tab) was found and fixed. Also on 2026-07-30, `apps/admin` moved off the path-based `mw-track.com/adminmw` onto its own subdomain, `admin.mw-track.com` (DNS was already pointed there) — `next.config.mjs`'s `basePath` was dropped, the existing multi-SAN `mw-track.com` certificate was expanded via `certbot --nginx --expand` to cover it, and the old `/adminmw` path now 301-redirects to the new domain rather than being removed outright. On 2026-07-31, a full how-to-use-every-feature documentation page (8 sections, ported from an earlier standalone Claude Artifact) shipped as a real in-app page at `/docs` in both trees, linked from the sidebar — a single shared `DocumentationContent` component (`apps/web/src/components/docs/`) used identically by both page wrappers, with 12 real product screenshots as static files under `apps/web/public/docs/*.png` (the app's first use of `public/`-hosted static assets); this surfaced a real middleware bug — the Studio-domain rewrite in `apps/web/src/middleware.ts` was catching every path including static files, resolving `/docs/overview.png` to a nonexistent `/dashboard/studio/docs/overview.png` — fixed by excluding paths with a file extension from the rewrite. Also on 2026-07-31, Operator project access was decoupled from participant creation (the user found the old create-time project+permission form inconvenient since operator rosters on a project change often): creating/inviting an Operator no longer takes `projectIds` at all (the sole exception to the "Buyer/OPERATOR_ADMIN must pick a project immediately" rule) and starts with zero `ProjectAccess`; access is granted afterward either from the project side (new "Операторы" tab in project settings, `GET/POST/DELETE /projects/:id/operators` + a company-wide `.../candidates` list so a brand-new zero-project operator is discoverable — confirmed via `AskUserQuestion` that Operator-admins see all company operators here, not just ones already overlapping their own projects) or from the team side (new "Операторы" tab on `/team`, splitting Operator rows out of the main roster, with a per-operator "assign projects" dialog that reuses the existing `PATCH /team/:userId` with only `projectIds` — zero backend changes needed for that direction). Operator permissions are no longer manually configurable at all — always the fixed `DEFAULT_ROLE_PERMISSIONS.OPERATOR`, applied via the pre-existing `seedDefaultsIfEmpty`. Also fixed a real bug found in the same request: the project-picker `<SelectValue>` on `/my-clients`/`/my-stats` had no children render function, so Base UI displayed the raw project id instead of its name (the same recurring pattern documented earlier in this file); both pages also gained an always-visible "available projects" chip row. On 2026-07-31, both sidebars (`Sidebar`/`StudioSidebar`) gained a collapse-to-icons toggle (new shared `useUiStore`, zustand+persist) with `Tooltip`-on-hover nav links in collapsed state. On 2026-08-02, two archived-project bugs were fixed: `ProjectsService.archive()` now also deletes `ProjectAccess`/`UserPermission` for every user on the archived project (previously these dangled and blocked all future `/team` edits, since the tenant-scoping middleware only forces `deletedAt: null` on top-level `Project` queries, not nested includes); and `ProjectsService.assertAccess` gained an `allowArchived` option (via `$queryRaw`, the only way to bypass that same middleware) so landing view/edit/delete keeps working against an archived project's landings instead of 404ing. On 2026-08-03, landings and tracking pixels both gained creator tracking (`createdById`, backfilled to the company's Owner for pre-existing rows, shown in the UI as "Создал: ...") following the `Purchase.registeredBy` pattern; and Buyers/Operator-admins gained a new per-user `User.landingsVisibilityScope` setting (`OWN_LANDINGS`/`PROJECT_LANDINGS`/`ALL_LANDINGS`, default `PROJECT_LANDINGS` = prior behavior) governing what the company-wide `/landings` page shows them, configurable from all 6 team create/invite/edit pages in both trees — a user-reported bug that Buyers could see every company landing regardless of project access could not be reproduced on clean or real production data during investigation. The same day, a follow-up request fixed the landing detail page missing its `createdBy` display (a separate Prisma query path, `LandingsService.findOne`, hadn't been given the relation) and three grey-in-Studio-dark-theme cards (`LandingContentCard`, `StatsCard`/`DualStatsCard`, `LandingCard` — all had hardcoded shadcn `Card`, fixed with the same optional `containerClassName` pattern each time), plus a 7th recurrence of the `<SelectValue>`-without-children id-display bug (`GetLinkDialog`'s pixel picker). Also on 2026-08-03, the client filters gained real multi-select for "Баер"/"Пиксель" (new `components/ui/multi-select.tsx` on top of `DropdownMenu`/`DropdownMenuCheckboxItem`), Buyers gained a per-user `User.clientsVisibilityScope` toggle (`ALL_CLIENTS` default / `OWN_CLIENTS`) that scopes the client list and every project-page stats endpoint (`getProjectStats`, `getConversionFunnel`, `getAdBreakdown`, `getLeaderboards`) down to only their own attributed clients — this required adding a real `TrackingEvent.buyerId` column and wiring the SDK's already-flowing `buyerRef` link parameter into `TrackingService.resolveAttribution`, since PageView/Lead events happen before a `Client` row exists and previously carried no buyer attribution at all (not an architectural limit, just unwired plumbing, per user pushback that correctly identified this); and the team edit page gained full editing of a member's email/name/password (`UpdateTeamMemberDto` extended, one-time password reveal on change, same UX as the create-member flow). On 2026-08-04, three more fixes shipped: the project page's ad breakdown now groups by campaign only (`ProjectsService.getAdBreakdown` dropped its `adId` dimension entirely — one row per campaign, not per campaign+ad pair); the scenario-album and push-media uploaders (`components/scenarios/element-media-editor.tsx`, `components/pushes/push-content-step.tsx`, both shared across trees) gained real multi-file selection (`<input multiple>`, sequential upload to respect the existing 10-item cap and avoid a stale-closure item-loss bug that concurrent uploads would have caused); and Studio's `/my-clients` page gained the same lead search (name/username/user_id/etc.) that the classic tree's version already had but Studio's had been missing entirely; the same day, a layout bug reported on the Operator account's `/my-clients` (the search input visually floating toward the center instead of sitting next to the project dropdown) was root-caused to `flex justify-between` distributing gaps evenly across 3 direct children rather than hugging the two right-side controls together — fixed in both trees by wrapping the search input and project dropdown in one shared flex sub-container, the same fix later confirmed to be needed nowhere else once checked. Also on 2026-08-04, the pushes/broadcasts frontend was redesigned end-to-end: the 3-step creation wizard (Контент → Аудитория → Подтверждение, with a "Далее" click and an intermediate draft-`Push`-row PATCH cycle between each) was replaced by a single-page `PushComposer` (`apps/web/src/components/pushes/push-composer.tsx` + Studio's `apps/web/src/app/dashboard/studio/push-composer.tsx`) reusing the existing `PushContentStep`/`PushAudienceStep`/`ScheduleCalendar` sub-components unchanged — no draft `Push` row is created until the final submit, audience preview instead hits a new stateless `POST /pushes/preview-audience`; the sidebar's "Календарь рассылок" entry was renamed to "Рассылки" with a `Send` icon (was `CalendarDays`) at the same `/pushes-calendar` URL, which now shows the existing calendar unchanged plus a new detailed table of every company push below it (`GET /pushes`, company-wide, same `getAccessibleProjectIds` visibility as the pre-existing calendar endpoints) and a blue "Создать рассылку" button; and — since `Push.projectId` is a hard single-value schema column with no precedent for multi-project fan-out anywhere in the codebase — a genuinely new capability shipped: the composer can select and send to several projects at once, via a new `POST /pushes` (`CreateMultiPushDto{projectIds, sendNow, ...CreatePushDto}` → `PushesService.createForProjects`, a plain loop calling the existing single-project `create()`/`send()` per id, not a transaction, since `send()`'s BullMQ side effects can't be rolled back — a partial failure, e.g. hitting the daily push limit partway through, leaves earlier projects' pushes created/sent and reports the rest with a `sendError` per project rather than failing silently or all-or-nothing). The project-scoped `/projects/[id]/pushes/new` route and the new company-wide `/pushes/new` route both render the same composer, differing only in `initialProjectIds` (pre-checked vs. empty) — satisfying the request that arriving from a project's own pushes page preselects that project without limiting the picker to it alone. A same-day follow-up request refined both halves further: the "Рассылки" list below the calendar now defaults to pending pushes only (`GET /pushes?scope=pending`, `PushStatus` DRAFT/SCHEDULED) with a second "Уже были" tab (`scope=history` — SENDING/SENT/FAILED) that only fires its query when clicked, so a visit to the page never pulls full send history up front; and the composer's project picker was pulled out of its own `Card`/`STUDIO_CARD` wrapper into a plain vertical list (checkbox + `ChannelAvatar` + name + a live "Доступно: N" reachable-audience count per row), while Контент/Аудитория/Отправка collapsed from three separate cards into one — the per-project audience preview call (`POST /pushes/preview-audience`) now always covers every visible project, not just the checked ones, so the picker can show every row's number before it's selected; the Audience section's own total is derived client-side by summing that same response over just the checked ids, and its old per-project breakdown list was removed since the picker already shows it. A same-day bug-fix round covered 4 more reports: the `TelegramMessagePreview` chat-bubble mockup (shared by pushes/scenarios/welcome-message) was hardcoded light-only (`bg-[#e7f3ff]`/`bg-white`, no `dark:` variants) and looked like a glaring bright box in Studio's dark theme — given real Telegram dark-mode colors (`#0e1621` chat bg, `#182533` bubble) rather than just Studio's own palette, since the component's whole purpose is a faithful "how it'll actually render in Telegram" preview; `PushAudienceStep`'s summary card (По фильтру/Доступны/Недоступны) gained the established `containerClassName` override prop (same pattern as `StatsCard`/`LandingContentCard`/`LandingCard`) since a bare shadcn `Card` inside Studio's dark theme paints from the placeholder `--card` CSS var, not Studio's real `#171F2B` — Studio's composer now passes `STUDIO_CARD`; the "Отправка" section's mode toggle (two tab buttons that revealed/hid the calendar) was replaced with an always-visible calendar plus a small "Отправить сейчас" checkbox beside it, since the reveal/hide was pushing the submit button down every time schedule mode was picked; and a real backend bug was found in `ClientsService.buildPushFilterWhere` — the "principal audience" condition (`subscribedAt`/`botActivatedAt` not null, i.e. excluding cold external contacts who never subscribed or messaged the bot) had been bundled inside the same `if (reachableOnly !== false)` branch as the `isBotActive` check, so `countAudienceTotal` ("По фильтру", called with `reachableOnly:false`) skipped it entirely and counted every `Client` row for the project including pure-external ones — the two conditions are now independent, with the origin/audience-membership check applied unconditionally and `isBotActive` staying gated to the reachable-only count. The same day, the composer got a full layout redesign per explicit user sketch: the top half is the content editor (`PushContentStep`'s own internal grid already put fields left / `TelegramMessagePreview` right, so no new layout code was needed there, just giving it the full-width top row to itself); the bottom half splits into two cards — left holds a compact scrollable "Проекты" list (`max-h-64 overflow-y-auto`, same avatar+checkbox+"Доступно: N" rows as before) plus a "Расписание" card, right holds "Фильтры получателей" (`PushAudienceStep`); the submit button sits alone beneath both, full page width. Since content editing is now the *first* thing on the page — above project selection, not gated behind it — a project auto-selects on load whenever `initialProjectIds` was empty (`GET /pushes/new` company-wide entry), so `PushContentStep`'s media-upload auth always has a valid `projectId` from the first render instead of starting in a broken disabled state. A genuinely new capability shipped alongside this: an explicit send-timezone picker in the schedule card (defaults to the browser's own IANA zone via `Intl.DateTimeFormat().resolvedOptions().timeZone` — "чей часовой пояс сидит в CRM", not `Project.timezone`, a different concept), reusing the existing `TimezoneInput` component (`components/timezone-input.tsx`, previously project-settings-only) after giving it optional `label`/`hint` overrides; converting the picked wall-clock date+time in that zone to the correct UTC instant needed a small new dependency-free helper (`lib/timezone.ts`'s `zonedTimeToUtcIso`, the standard `Intl.DateTimeFormat`-diffing trick — no date-fns-tz/luxon added) since the old code just fed the raw string into `new Date()` and implicitly relied on the browser's own local zone. The redesign surfaced one real regression caught before shipping: `PushAudienceStep`'s internal `lg:grid-cols-2` (fields | totals side-by-side) was sized for the old near-full-width placement — once nested in the new half-width "Фильтры получателей" card, the viewport-based `lg:` breakpoint still fired (it doesn't know its parent shrank) and the two columns overflowed the card; fixed by dropping the internal grid to always-stacked, which is also just the better layout at that width. On 2026-08-05, media attachment was redesigned too: the old "pick a type from a dropdown, then upload" flow (`PushContentStep`) became one drag-and-drop-or-click container that classifies each file by its own MIME type (`image/*`→photo, `video/*`→video, `audio/*`→voice — auto-detected, no more manual type-select) — multiple photos/videos become an album automatically, same as before. Video note ("kruzhok") is no longer chosen before upload; instead, once a video is the sole item in the album, a checkbox appears right on its thumbnail to convert it after the fact via a new `POST .../pushes/media/:key/to-video-note` endpoint (`PushesService.convertToVideoNote` streams the already-uploaded object back out of MinIO, runs the same `ensureSquareVideoNote` crop used at upload time, stores the result under a fresh key) — both the original and converted URLs are cached client-side (`PushContentStep`'s local `videoNoteCache`) so toggling back and forth doesn't re-hit the network or re-crop after the first conversion. Voice is a genuinely new push capability, not previously wired end-to-end: `ChannelProvider.sendMessage`/`TelegramProvider` already supported a `'voice'` mediaType (used by bot scenarios), but `PushMediaDto.type` only allowed `photo`/`video`/`video_note` — extended to include `voice`, with `PushesService.assertValidMedia` treating it as the same kind of "solo, no-album" type as video_note (Bot API's `sendMediaGroup` supports neither). The upload-time `key` needed to support the after-the-fact conversion doesn't belong in the wire payload (`PushMediaDto` only knows `type`/`url`, and the global `ValidationPipe`'s `forbidNonWhitelisted` would 400 the whole request if it leaked through) — both composers now `.map(({url,type}) => ...)` before submit to strip it. On 2026-08-05, 3 more composer bugs were fixed: the "Расписание" calendar disappeared the moment a 2nd project got checked (was gated on `selectedIds.length === 1`; now shows the first checked project's calendar regardless of total count, since it was always inherently single-project anyway); the calendar itself stayed stuck at a fixed compact width even though its card had grown much wider in the new layout (`CalendarGrid`'s `compact` mode switched from a fixed `w-[19rem]` to `w-full`, and `ScheduleCalendar` switched from a side-by-side flex row with `DayScheduleList` to stacking them vertically, since side-by-side made a full-width calendar pointless); and a one-frame "Нет проектов с правом на создание рассылок" flash on every page load (the empty-state check ran against `projects` before the `GET /projects` query had actually resolved — fixed by also gating on the query's own `isLoading`). On 2026-08-04, a genuinely new parallel feature shipped: broadcasts sent through a project's personal MTProto account (not the bot) rather than another `Push` mode — audience/send-mechanism/content/pacing were different enough to warrant separate tables/engine, the same call already made for scenarios-vs-automations. New `PersonalBroadcast`/`PersonalBroadcastLog` (not in `modelsWithCompany`, manual `projectId` scoping like `Push`) and `Client.lastPurchaseAt` (deposit recency — `Purchase` has no dedicated "deposit" entity, denormalized in `PurchasesService.create` next to the existing `totalSpent`/`purchasesCount`); audience always starts from `dialogueSource: 'PERSONAL_ACCOUNT'` (the one unambiguous "real dialogue with this specific account" signal, set only by `TelegramPersonalService.handleIncomingMessage`), plus a live `client.getInputEntity(tgUserId)` recheck immediately before each individual send. New `TelegramPersonalService.sendDirectMessage`/`getDialogFilters`/`getFolderTgUserIds` follow the pre-existing `sendStory` "never throw, return `{success,error}`" pattern; Telegram-folder filtering only resolves explicit `includePeers` membership (GramJS has no API to enumerate who matches a rule-based folder), a disclosed v1 scope cut. Because one MTProto socket per channel can't take concurrent `invoke()`s (the same constraint `StoriesCron` already documents), a whole broadcast runs as ONE long-lived BullMQ job looping with a random `delayMinSeconds`–`delayMaxSeconds` sleep between sends (anti-ban pacing) rather than one job per recipient; `FloodWaitError`/`SlowModeWaitError` put the recipient back to `pending` and sleep the server-requested duration instead of counting it as a failure. Content "variants" (deliberately manual now, AI-generated later) are assigned round-robin, not randomly, for even/debuggable distribution. New `Permission.PERSONAL_BROADCASTS_VIEW/CREATE/SEND/DELETE` is explicit-grant-only (no default role gets it, like `CLIENTS_VIEW_CROSS_PROJECT`), and surfaces as a project-page button (not a sidebar item, unlike Push) since — unlike bot pushes — the feature has no company-wide/multi-project concept: each project's personal account is its own persona. Live verification used an isolated throwaway test company/project/clients (never real connected production accounts, same restriction the user set for the Stories module) driven directly through `PersonalBroadcastsService` — all 10 audience-filter-dimension checks and round-robin variant assignment came back correct, and it surfaced one real bug fixed the same day: `getDialogFilters`/`getFolderTgUserIds` weren't wrapped in try/catch, so a stale/unreachable MTProto session threw uncaught and 500'd the whole audience preview instead of degrading to an empty folder list like every other method in that service already does. Also on 2026-08-04, a public marketing homepage shipped for logged-out visitors: previously an unauthenticated hit on `/` (either domain) showed a loading skeleton and immediately redirected to `/login`, with no product-description entry point at all. New shared `MarketingHome` component (`apps/web/src/components/marketing/marketing-home.tsx`, Studio-styled with full dark-mode support) is wired into both root-level auth gates — `apps/web/src/app/dashboard/layout.tsx` (Studio/mw-track.com) and `apps/web/src/app/(dashboard)/layout.tsx` (classic/old.mw-track.com) — purely additively: the already-authenticated branch is untouched, a new branch only fires for `!accessToken && usePathname() === '/'` (confirmed via the same reasoning `StudioSidebar` already relies on for its cross-domain `classicHref` — `usePathname()` reflects the clean public URL, not the Studio-rewrite destination), every other unauthenticated route still redirects to `/login` as before. Verified live by driving a locally-cached headless Chromium against the deployed build with the `Host` header swapped between both domains — renders identically on both, no console errors, "Войти" correctly navigates to `/login`, and desktop/mobile/dark-theme screenshots all confirmed clean layout. The same day, a follow-up request pointed out that the personal-broadcast composer's media upload ("не такая как в обычной рассылке") still used the pre-redesign button-plus-file-dialog interaction; `PersonalBroadcastVariantEditor` (`apps/web/src/components/personal-broadcasts/variant-editor.tsx`) was brought in line with `PushContentStep`'s 2026-08-05 drag-and-drop redesign — same dropzone visuals/interaction, MIME-based auto-classification — while deliberately keeping the pre-existing single-file (not album) constraint, since that's an intentional property of "variants" as the content-diversity mechanism, unrelated to the upload-UX complaint. Verified live end-to-end: an isolated test company/user/project/channel, a real login through the actual `/login` form via headless Chromium, then a real file upload through the composer — thumbnail and remove control rendered correctly, no console errors, test data deleted by exact id afterward. The same day, the user called the marketing page's first version "слишком нейроночно" (too AI-generated-looking) — the sparkle-badge-over-hero + symmetric 3×2 icon-card grid was indeed the generic-SaaS-landing template. Redesigned around real product screenshots instead of abstract icons: cropped (via `sharp`, no Pillow/ImageMagick available in this environment) the same PNGs already used by `/docs` (`apps/web/public/docs/*.png`) down to `public/marketing/*.png` — trimmed empty whitespace and the test project's name out of frame — and wrapped them in a small `BrowserFrame` component (traffic-light dots + mono address bar) instead of bare `<img>`s. Structure changed from a symmetric feature grid to alternating text/screenshot sections (clients list → client detail with real UTM/fbclid attribution → team/buyer-revenue stats), a hero with a real funnel screenshot given a slight `rotate` and a soft color blob behind it rather than a perfectly rectangular layout, and every other feature folded into one compact tag row instead of forcing icon-cards onto features with no good screenshot. Deliberately did NOT use the `billing.png` screenshot for pricing — its numbers could be stale, and presenting them as current on a public page would risk being misleading. Verified live the same way as the initial ship (headless Chromium against the deployed build, `Host` header swapped, desktop/mobile/dark screenshots) — no console errors, real screenshots render crisply in both themes. On 2026-08-05, a follow-up request covered `/login`/`/register`/`/invite`: "убери эту карточку для формы, добавь что-то красивое в наших цветах, форму можно в бок для красоты и антисимметрии" — the shared `(auth)/layout.tsx` (previously one centered white card on a grey background, identical across all three pages) was rebuilt around a brand panel (`AuthBrandPanel`, blue gradient + the same funnel screenshot crop as the homepage, tilted and bleeding off its edge) occupying ~44% width on `lg+`, with the form itself rendered card-less directly on the page background. The panel's side alternates by `usePathname()` — left on `/login`, right on `/register`/`/invite` — that alternation between two related pages is the "antisymmetry" asked for. One real bug caught before shipping: the first version absolutely-positioned the screenshot inside a `justify-between` flex column, which made it overlap the tagline text at ~900px viewport height; fixed by moving it into normal flex flow (logo → centered text block → image with negative-margin bleed) instead of `absolute`. Verified live the same way as the homepage. Also worth noting: mid-session, the Read/Edit tools hit a transient host-side hook timeout while Bash kept working — the remaining doc edits for this round were applied via a Python script through Bash instead of the Edit tool. Separately on 2026-08-05, a user-reported revenue/leaderboard mismatch on the project page (main revenue card correct, but a buyer's expanded leaderboard row showing a wildly different, larger number than the summary line right next to it) turned out to be two real backend bugs, not a caching artifact: `ProjectsService.getBuyersFunnel` (the funnel that expands under a buyer's leaderboard row) filtered its raw SQL only by `buyerId`, with no `projectId` at all — a buyer working across multiple company projects got their revenue/purchase-count silently summed across ALL of those projects on every single project's page (confirmed: $701/11 purchases was exactly their total across two projects, $572+$129 and 10+1, while the real per-project number was $129/1); `getEventColumnFunnel` (shared by the pixels/campaigns leaderboard categories) had the identical gap for `campaignId` (an external ad-platform ID not guaranteed unique across a company's projects) — fixed both by threading `projectId` through and filtering on it, even though only the buyers path had a confirmed live example. Live-verified with an isolated two-project/one-shared-buyer test company (reproduced the exact cross-project leak pre-fix, confirmed it gone post-fix), then re-confirmed against the real Igor Conel/Diego Herrera case from the bug report. The same day, a follow-up request ("в главной статистике не учитывай покупки внешних клиентах") found the main project stats (`ClientsRepository.getProjectStats`'s revenue/FD/RD cards and charts, plus `getConversionFunnel`'s deposit stages) counted every `Purchase` by `projectId` alone, with no check that the purchasing `Client` was actually "ours" (`subscribedAt IS NOT NULL` — the same `OURS_ONLY` criterion every other client metric in that file already used) rather than an external/cold contact who never came through the funnel. The existing buyer-scoping subquery only applied that check indirectly and only when a specific buyer was selected; made "is this client ours" an unconditional part of the same subquery instead of something that only ever showed up alongside an optional `buyerId`. `ClientsRepository.getMyStats` (Operator's personal stats, scoped by `Purchase.registeredBy` rather than client identity) was deliberately left alone as out of scope. Verified live with an isolated test project (one real client with a $100 purchase, one external contact with a $50 purchase) called directly through the service, confirming the external purchase no longer appears in any of the affected fields. Later the same day, investigating a real user-reported bug — a bot's join requests were being approved (people got into the channel fine) but nothing registered in the CRM and no welcome message went out — traced to Telegram silently withholding webhook delivery for that one specific bot for ~20 hours (confirmed via nginx access logs: this bot's webhook path went completely quiet while every other connected bot on the same server kept receiving updates normally the whole time; likely Telegram-side anti-spam throttling, given the bot's welcome message was aggressive/ALL-CAPS and had a high block-rate). Since there was no monitoring for "webhook went silent," this went unnoticed until traffic/revenue had already been lost. Built a permanent safeguard: new `Channel.lastWebhookAt` column, stamped in `TelegramProvider.handleWebhook` (the one common entry point before grammY dispatches by update type) on every single incoming webhook regardless of type, fire-and-forget so it never adds latency to the Telegram-facing response. "Is this webhook stale" is computed lazily on read in `ProjectsService.sanitizeChannel` (same pattern as the existing `tgPersonalConnected` computed field, not a stored/cron-maintained boolean like `isActive`) — `lastWebhookAt` set and older than a deliberately generous 6-hour threshold, chosen because real per-channel webhook volume in this system spans two orders of magnitude (~15 to ~800+/day) and a tighter threshold would false-positive on quiet channels. Surfaced as a new "Нет вебхуков" badge alongside the existing bot-blocked indicator, in all 4 places that indicator already appears (project card + project page, both classic and Studio trees) — two independent signals, since a bot can be fully unblocked and healthy while Telegram simply isn't sending it anything. Confirmed via `AskUserQuestion` with the user beforehand: deliberately did NOT attempt retroactively reconciling the channel's ~12,600 real members against the ~296 CRM-registered ones to backfill/welcome-message the gap — cold-messaging thousands of people who never contacted the bot is a strong ban-risk that outweighs recovering the lost registrations; and the indicator itself is a CRM-only badge, not a separate alerting/admin-panel system. Verified live: an isolated test project with `lastWebhookAt` set 10 hours in the past rendered the badge correctly in all 4 spots (real login, both domains, headless Chromium, no console errors), and separately confirmed in production that `lastWebhookAt` is now actually being stamped in real time off real incoming Telegram webhooks. The very next day, that same monitoring effort led straight into a second, more concrete real incident for the same bot: a customer reported a client going through the correct tracked landing link, getting auto-accepted into the channel, but never registering or receiving a welcome message — this time the webhook wasn't silent at all (traffic was flowing), which pointed away from the previous root cause. Investigation found the same bot token (`@Diego_Herrera_Bot`) had been connected to three different projects over time (two now archived, one active) — Telegram only allows a bot ONE registered webhook URL at a time, and `getWebhookInfo` confirmed it was currently pointed at an **archived** project's channel, not the active one, because whichever channel's `initialize()` last called `setWebhook()` wins regardless of how many `Channel` rows in our own DB reference that same token. Real traffic was landing on the archived channel, which happened to be in `BOT_DIRECT` mode rather than `PRIVATE_CHANNEL_REQUEST` — so the guard that normally prevents `handleChatMemberUpdate` from creating a client on an untracked join didn't apply, and 80 real clients had silently accumulated in the archived, invisible project over ~40 minutes. Fixed immediately (via `NestFactory.createApplicationContext`, bypassing HTTP): re-ran `ChannelsService.reactivate()` on the correct channel to reclaim the webhook, deactivated the archived channel so it can't steal it back, and migrated all 80 misrouted clients into the active project — merging into an existing record (adopting `subscribedAt`/`isSubscribed`, filling missing name fields, soft-deleting the duplicate) for the 33 people who already existed there via the personal-account dialogue path, plain `projectId` reassignment for the other 47. Deliberately did not retroactively fire the welcome scenario for the moved clients as part of this — that's a separate, not-yet-made decision. Then built the actual prevention the user asked for: `ProjectsService.archive()` now cascades to the project's channel — `TelegramPersonalService.disconnect()` (logs out the MTProto session, clears the session columns) if a personal account was connected, then `ChannelsService.deactivate()` (calls `deleteWebhook()`, flips `isActive` false) — the exact same methods already used for manual disconnection from settings, just now automatic on archive, wrapped in try/catch so a Telegram-side failure can't block the archive itself. Required adding `TelegramPersonalService` to `ProjectsService`'s constructor; no new circular-dependency risk since `ChannelsModule` already flows into `ProjectsModule` via the existing `forwardRef` and already exports that service. Verified via an isolated fake-session test project (confirmed the cascade correctly clears `isActive`/session fields) and confirmed in production that the webhook stayed correctly pointed at the active channel even through a subsequent API restart (which re-initializes every `isActive` channel on boot). Later the same day, a different project ("Isabella Ramirez") surfaced a related-but-distinct revenue-mismatch report: the main stats card showed $99 while the "ТОП" leaderboard drill-downs summed to only $94 — same symptom family as the cross-project buyer-funnel leak fixed earlier that day, but this time the missing filter was `deletedAt`, not `projectId`. `getProjectStats`'s two `purchase.aggregate()` calls and its `purchaseBuyerFilterSql`, `getConversionFunnel`'s own separate `purchaseBuyerFilterSql`, and `getLandingsFunnel`/`getEventColumnFunnel`'s raw-SQL purchase queries all checked `Client.subscribedAt IS NOT NULL` but not `deletedAt IS NULL` — so a soft-deleted client (e.g. a duplicate merged away during that same day's Diego Herrera migration) still had its historical purchases counted in some stats but not others, while `getLeaderboards` (top-level) already filtered `deletedAt` correctly — hence the two numbers disagreeing instead of both being wrong. `getBuyersFunnel`'s purchase query got the same fix, on top of the `projectId` fix it had already received earlier that day for the unrelated cross-project leak. Separately confirmed the user's other complaint on the same project (buyer-leaderboard client/dialogue counts not summing to the page total) is not a bug: buyer rows only include clients with `buyerId IS NOT NULL`, so unattributed clients are correctly excluded — the landings breakdown, which has no such gate, summed to the total exactly. Verified live by re-running the real service methods against the actual project before/after the fix: `totalRevenue` dropped from 99 to 94, the affected buyer's funnel revenue from 15 to 10, the affected landing's funnel revenue from 99 to 94 — all now match `getLeaderboards` exactly. Later the same day, three requested pushes/broadcasts features shipped: editing an existing DRAFT/SCHEDULED push, duplicating any push, and inline cancel/edit/duplicate actions directly on the company-wide "Рассылки" (`/pushes-calendar`) list rather than only on a project's own pushes page. The backend edit (`PATCH .../pushes/:id`) and cancel (`DELETE .../pushes/:id`, sets `FAILED` — `PushStatus` has no separate `CANCELLED` value) endpoints already existed from the original build but had no UI wired to them at all; only `PushesService.duplicate` (clones content/filter into a fresh `DRAFT` via the existing `create()`, deliberately dropping `scheduledAt`/status/counters — a copy is a new send, not a resumed one) and its `POST .../pushes/:id/duplicate` route were new. `PushComposer`/`StudioPushComposer` gained optional `pushId`/`editProjectId` props that switch them into edit mode: content/filter/schedule prefill from `GET .../pushes/:id` (a new `lib/timezone.ts` `utcIsoToZonedParts` reverses the existing `zonedTimeToUtcIso`, since `Push.scheduledAt` only stores the UTC instant — the zone it was picked in is never persisted, so editing always redisplays in the browser's current zone), the project picker becomes a fixed read-only row (`Push.projectId` is single-value, multi-project doesn't apply to edit), and submit calls `PATCH` instead of `POST /pushes`. The "Отправить сейчас" checkbox is reused for edit mode but re-labeled "Без расписания" and re-wired to mean `scheduledAt: null` (revert to `DRAFT`) rather than an immediate send, since `PATCH` never sends. New edit routes `.../pushes/:id/edit` (project-scoped, both trees) render the same composer with those props; the company-wide list's rows already carried `push.project.id` from `findAllForCompany`, so no new company-wide backend route was needed — "Изменить"/"Копировать"/"Отменить" there call the same project-scoped endpoints directly. Live-verified via an isolated throwaway company/project (no real channel) driven straight through `PushesService`: create→DRAFT, edit with a date→`SCHEDULED`, edit with `scheduledAt:null`→back to `DRAFT`, duplicate→new independent `DRAFT` named "(копия)", and confirmed editing an already-cancelled (`FAILED`) push is correctly rejected by the pre-existing `assertEditable` guard — all 5 checks passed, test data deleted by exact id afterward. On 2026-08-06, the home page (both trees) gained real company-wide statistics — revenue, clients, FD/RD — plus a today/yesterday/7d/30d/custom period selector, neither of which existed there before (previously just 4 all-time counts reduced client-side from `GET /projects`). New `ProjectsService.getCompanyStats`/`GET /projects/company-stats` lives in `ProjectsService` itself rather than the more natural-looking `ClientsRepository`, specifically to dodge a circular import (`ClientsModule` already imports `ProjectsModule`); resolves period boundaries once against `'UTC'` (via the existing `resolveStatsPeriod`) rather than per-project-timezone-correct like the single-project `getProjectStats` does, since 7d/30d don't care about timezone at all and today/yesterday/custom's skew is an accepted tradeoff for a summary widget — documented inline as a deliberate simplification, not an oversight. Revenue is scoped to a narrower project set (`STATS_VIEW_REVENUE`) than client counts (`STATS_VIEW`), mirroring the existing per-project "zero money fields, keep counts" convention from `ClientsController.stats`, via the projectId-less `hasAnyPermission`/`getAccessibleProjectIds(..., permission)` pattern already established for company-wide checks. Frontend ported the classic project page's URL-persisted-period pattern to both home pages for the first time (Studio's own project page still doesn't have it) — classic reuses the shared `PeriodSelector`/`StatsCard`/`DualStatsCard`, Studio hand-rolls the same pill UI inline like its project page already does. Live-verified by calling the new service method directly against the real internal single-project demo company and cross-checking against the existing single-project stats endpoint — exact match on every field. The same day, the personal-broadcasts (MTProto) module got a significant audience-model change: "чтобы можно было пушить не только клиентов пришедших через нашу срм, но и всех остальных, даже внешних, все абсолютно чаты должны пушиться" — the audience source flipped from `Client` rows with `dialogueSource='PERSONAL_ACCOUNT'` (structurally invisible to anyone the CRM never created a row for) to a new `TelegramPersonalService.getAllDialogs` (live GramJS `client.getDialogs()`, 1:1 non-bot non-self chats), with CRM data merged in as an optional per-`tgUserId` overlay — a dialog with no matching `Client` is treated as zero/null CRM data (so `hasPurchase:true` correctly excludes it, `hasPurchase:false` correctly includes it, no special-casing). `PersonalBroadcastLog.clientId` had to become optional (new `tgUserId`/`tgFirstName`/`tgUsername` snapshot columns, since a clientId-less row can't join through `client` for delivery) — a real schema migration on a feature that had shipped literally the same day. The user also reported a real bug on the "Maria Lopez" project: the folder picker showed no folders despite folders existing in the real Telegram app. Root cause turned out to be two layers: `Api.messages.GetDialogFilters()`'s `DialogFilterChatlist` (shared/imported folder-link folders) was being silently dropped by a `className==='DialogFilter'`-only filter (fixed, both types share the same `title`/`includePeers` shape) — but the actual live incident was `401 AUTH_KEY_UNREGISTERED` (the MTProto session had been revoked on Telegram's side), completely invisible before this fix because every one of `getDialogFilters`/`getFolderTgUserIds` had a bare `catch { return [] }` with zero logging, indistinguishable from "this account genuinely has no folders." Added logging plus a new `handleDeadSession` pattern — regex-matches auth-dead error codes and calls the existing `disconnect()` method (same one the manual settings button uses) so the already-existing "not connected" UI badge picks it up automatically, no new UI needed. Live-verified the real diagnosis against the actual Maria Lopez channel (read-only calls only) and confirmed the session got correctly cleared in the DB; separately verified the new audience-merge logic via an isolated test project with monkey-patched `getAllDialogs`/`getFolderTgUserIds` (no real MTProto call) driving the real service end-to-end — all filter combinations and the clientId-null-vs-set split in created `PersonalBroadcastLog` rows came back exactly as expected. Later the same day, per the user's request to "проверять раз в некоторое время" whether a personal account is still connected, a `TelegramPersonalHealthCron` (`@Cron('*/30 * * * *')`, in `ChannelsModule`) was added — pings every channel with `tgSessionEncrypted` set via `TelegramPersonalService.checkSessionHealth` (GramJS `client.getMe()`, the cheapest authenticated call), routing any failure through the same dead-session detection already built for the folder bug. A new `Channel.tgPersonalLastError` column distinguishes "Telegram revoked the session" (set) from "never connected" or "manually disconnected" (both null) — `disconnect()` gained an optional `reason` param, passed only from the automatic dead-session path, so the existing `tgPersonalConnected` UI badge and a new line in Studio's notification bell ("Личный аккаунт проекта «X» отключён") both light up automatically with zero new plumbing beyond the one field. On 2026-08-06/07, while verifying this exact cron, a real incident occurred: the verification script called `app.get(TelegramPersonalService)` — the real singleton, not a mock — and overrode its `getLiveClient` method globally to simulate a dead session, then ran `checkAllPersonalSessions()`, which by design iterates every connected personal account in the whole database, not just the intended test one. Every real channel hit the faked error and was disconnected for real (a genuine `Api.auth.LogOut()` invoke against Telegram), because `disconnect()` reads `this.liveClients` directly (bypassing the mocked method), and that map had already been populated with real live connections by `ChannelsService.onModuleInit()`, which runs automatically on every `NestFactory.createApplicationContext(AppModule)` boot — including throwaway verification scripts. 5 real company projects lost their personal-account connection (Diego Herrera, Ricardo Ramirez, Santiago Vargas, Isabella Ramirez, Rolando Acuna). Root lesson, explained to the user in full: `NestFactory.createApplicationContext(AppModule)` is not an isolated test sandbox for this app — it boots the real lifecycle, including real parallel MTProto connections to every live session, and every Prisma write through it hits the real production database exactly like the real server would. Two forward-looking fixes shipped as a direct result of the incident: (1) a system-wide uniqueness check in `finalizeConnection` — the same Telegram account (`tgPersonalUserId`) can no longer be connected on two different `Channel` rows at once, anywhere in the system, not even across companies (same bug class as the earlier bot-token webhook hijack, just for MTProto sessions instead of webhooks — `id: {not: channel.id}` still allows reconnecting a channel to its own account); (2) an audit of every catch block in `telegram-personal.service.ts` found and fixed several that silently swallowed Telegram errors — `submitPassword`'s catch didn't even capture the error variable, `startConnect`/`submitCode` threw a user-visible exception but never logged server-side, `sendStory` returned failure silently, and `sendStory`/`sendDirectMessage` both called `getLiveClient()` *outside* their own try/catch (a connection failure there propagated fully uncaught, invisible to both the log and to dead-session handling) — every method now unconditionally logs before any conditional handling, and both calls moved inside their try blocks. `TelegramProvider` (bot side) was audited too and found already fully logged on every catch, no changes needed there. This round's verification deliberately avoided `NestFactory.createApplicationContext` entirely (the exact mechanism that caused the incident) — code review, `tsc`/`nest build` only, plus a read-only `PrismaClient` query (no AppModule) confirming every personal-account session in the company had already been cleared by the incident itself, so the final `pm2 restart trafficcrm-api` carried zero additional risk. Later the same day, a follow-up request ("везде где есть список клиентов... время час минута") found that Studio's client table (`apps/web/src/app/dashboard/studio/clients-table.tsx`, a separate re-implementation, not shared with classic's `ClientsTable`) formatted subscribed/unsubscribed dates via `toLocaleDateString('ru-RU')` — no `date-fns`, no time at all, unlike classic's equivalent columns which already had `HH:mm` from earlier work — plus a few other date-only spots (classic `ClientsTable`'s cross-project-overlap tooltip, `client-detail-drawer.tsx`'s Регистрация/Активность/purchase-history fields, both audience cross-project comparison pages). No shared date-format constant exists anywhere in `apps/web` — every `format()` call is an inline literal string — fixed each spot individually to `'d MMM yyyy, HH:mm'` rather than introducing one. Studio's external-subscribed/unsubscribed status pills also gained the `title` tooltip classic's already had (via a plain `<span title=...>` wrapper, since `StudioPill` itself has no `title` prop). On 2026-08-09, a user-reported project-page bug ("104 клиента за сегодня, а в топ баеров только 27, где остальные?" / pixel funnel showing 77 vs the leaderboard's 75 for the same pixel) turned out to be two separate things. The 77-vs-75 mismatch was a real bug: `ProjectsService.getEventColumnFunnel` (shared by the "Топ пикселей"/"Топ кампаний" funnel drill-downs) computed Subscribe/Dialogue from raw `TrackingEvent` rows instead of deduplicated `Client.subscribedAt`/`firstDialogueAt` — live-traced to a duplicate Subscribe event on one client plus a second client whose event write lagged the actual subscription moment by 3.5 hours, landing it in a different day-window than the leaderboard's own Client-based count. Fixed by switching to the same Client-table approach `getLandingsFunnel`/`getBuyersFunnel` already used correctly; `getAdBreakdown`'s `purchases` field was on the same latent pattern (TrackingEvent instead of the real `Purchase` table) and was fixed too for consistency, even though it didn't actually disagree for this project on this day. The "27 of 104" question was confirmed NOT a bug via live query — the missing clients have no buyer/pixel/campaign attribution at all (organic/unlabeled traffic), correctly excluded from a ranking that only makes sense between real entities — but the project page had no equivalent of the "БЕЗ БАЕРА" bucket that `TeamService.getBuyerAnalytics` already shows company-wide, so those clients just silently vanished from view. Added `buyersUnattributed`/`pixelsUnattributed`/`campaignsUnattributed` to `getLeaderboards`'s response (one combined query) and a matching non-ranked footer row in both trees' leaderboard card components, shown only when non-zero, gated by the same `STATS_VIEW_TEAM_LEADERBOARDS`/`STATS_VIEW_REVENUE` rules the rest of the leaderboard already uses. Live-verified via read-only queries reproducing the exact reported gap, then via direct (unmocked) service calls confirming the pixel funnel and leaderboard now agree exactly and that buyers/pixels sums plus their unattributed buckets both equal the independently-queried "today" total. On 2026-08-13, a routine "restart everything" request surfaced that `trafficcrm-admin` had gone missing from `pm2 list` after a server reboot (not crash-looping — just silently absent, because `pm2 save` had never been run after that app was added to `ecosystem.config.js`); fixed via delete+start-fresh+`pm2 save`, and a new `README.md` was added at the repo root with a practical setup-from-scratch + day-to-day restart/deploy runbook for this specific server (as opposed to `14_INFRA_AND_DEPLOY.md`'s fuller architecture spec). The same day, a user request to verify all connected personal Telegram accounts were confirmed healthy via a real, unmocked `checkSessionHealth` call per channel (deliberately avoiding any mocking of `TelegramPersonalService`, per the discipline established after the personal-session incident above) — all 6 tracked accounts, including the 5 disconnected by that incident and Maria Lopez, came back healthy, confirming they'd been manually reconnected since. That same verification then surfaced a new real bug: the personal-broadcast audience preview on the ROLANDO ACUNA project loaded for a long time and showed "—". Root cause, confirmed live: that account has 7553 Telegram dialogs, and `client.getDialogs({})` (added in the 2026-08-06 "all dialogs" redesign) with no limit hits heavy Telegram-side flood-wait for an account this size (97-133s across test runs) — well past `api.mw-track.com`'s 60s nginx `proxy_read_timeout`, and the frontend's debounced preview effect had no `.catch()`, so a failed request just left `audienceTotal` at its initial `null`, rendered identically to a genuine zero. Fixed with three changes: `TelegramPersonalService.getAllDialogs` gained an in-memory TTL cache (5 min) with in-flight-call deduplication, plus a non-blocking `prewarmDialogsCache` fired from `PersonalBroadcastsService.getFolders` (already hit on page load) so a large account's cold fetch is usually already running by the time the user reaches filters; a dedicated nginx `location` regex-matched to just `.../personal-broadcasts/(preview-audience|folders)` got a 200s timeout, leaving the global 60s untouched for every other route; and the frontend gained a distinct `calcError` state so a failed calculation no longer looks identical to a real zero-match result. Verified live (real, unmocked calls): prewarm + an explicit call correctly deduped into one live fetch (not two), a second call was an instant 0ms cache hit, and the account's session stayed fully healthy through the flood-wait-heavy testing. On 2026-08-18, a new landing template shipped from a user-supplied archive (`new_landing.7z`) — `age-gate-invite` ("Проверка возраста + приглашение"), the first template with two popups instead of one screen: popup 1 asks "18+?" (Sì/No), popup 1's "No" and popup 2's own button both always go to the real `TG_REDIRECT_URL`, popup 1's "Sì" only reveals popup 2 client-side (no URL) — all 7 texts/button-labels across both popups are independently editable `templateData` fields, wired through the same manual per-field whitelist every existing field already uses (`LandingsService.createFromTemplate`/`.update()`, plus the `TEMPLATE_IDS` `@IsIn` whitelist gotcha from 2026-07-15). Popup 1's default text is the archive's own Italian copy verbatim (explicit user instruction, "как сейчас в шаблоне" — the `lang="es"`-vs-Italian-content mismatch already in the source archive was left alone, out of scope); popup 2's default was deliberately changed away from the archive's own unrelated "earn money watching videos" copy to a generic "Подпишись на наш канал!" default matching every other template's `JOIN_BUTTON_TEXT` convention. New SDK capability (not landing-specific) — `data-auto-redirect-defer="true"` on the tracking script tag: instead of firing the existing `data-auto-redirect-url` immediately on load like every other template, the SDK exposes `window.tcrm.triggerAutoRedirect` for the template's own inline JS to call whenever it actually wants the redirect to fire (`template.html`'s popup-2-reveal handler calls it) — required moving `window.tcrm`'s object-literal assignment earlier in `browser.ts` so it could be mutated rather than clobbered by the later unconditional reassignment. Deploy had three parts, not the usual two: `nest build`/`next build` as always, plus a separate manual SDK publish (`apps/sdk`'s own `npm run build && node scripts/publish-cdn.js`, per the established CDN runbook) — confirmed `cf-cache-status: MISS` with a fresh `last-modified` on the live `cdn.mw-track.com/track.js` afterward. Live-verified via real (unmocked) `NestFactory` + direct `LandingRendererService.renderAndServe` calls against a fake Express req/res and an isolated throwaway Company/Project/Channel/Landing (15 checks: both popups' defaults render, zero leftover unsubstituted `{{VAR}}` placeholders, `TG_REDIRECT_URL` resolves identically into exactly 2 places, the defer attribute only appears when auto-redirect is actually enabled on the landing, editing via `update()` patches only the touched field) — one false-positive caught in the test script itself (an inline HTML comment in the new template happened to contain the literal attribute name as documentation text, tripping a naive substring check) fixed before re-confirming all-green; all test rows, including 2 genuinely orphaned ones from earlier failed attempts (missing required `Company.slug`/`Project.secretKey`/`Channel.name` on the first tries), deleted by exact id afterward. On 2026-08-18, the project clients page gained a traffic-source filter (Facebook/TikTok, derived from `Client.fbclid`/`ttclid` presence rather than pixel assignment — a click-id is present on every client who genuinely arrived via that platform's ad click, while a pixel isn't always assigned) and a period selector matching the project page's (today/yesterday/7d/30d/custom, filtering `Client.createdAt` — deliberately not reusing the existing-but-dead `subscribedFrom`/`subscribedTo` DTO fields, which no frontend had ever called and which would have wrongly bypassed the ours/external origin filter). The accompanying request to re-audit period handling everywhere surfaced a real, previously-unnoticed inconsistency: `resolveStatsPeriod`'s `7d`/`30d` branch was a raw non-timezone-aware rolling window (`now() - N*24h`) while `today`/`yesterday`/`custom` were all calendar-day-aligned in the project's own timezone — fixed by giving `7d`/`30d` the same `date_trunc`-based alignment, a genuine behavior change (7d/30d numbers now shift up to ~24h depending on time-of-day at request time) called out explicitly rather than silently shipped. Separately, URL-persisted period turned out to already exist independently on 3 pages (project page, both home pages) via 3 copies of the identical ~25-line block, plus 2 more pages missing it entirely (my-stats ×2) and Studio's project page missing it too — consolidated into one `apps/web/src/lib/use-period-query-state.ts` hook and rolled out to all 6 period-bearing pages, deleting every local duplicate. Live-verified via real `ClientsService.findMany`/`resolveStatsPeriod` calls against an isolated test project with clients carrying distinct `createdAt`/`fbclid`/`ttclid` — calendar alignment, period-window filtering, and traffic-source filtering all confirmed correct (one false-positive caught and diagnosed in the test script itself, not the product).

The same day, a follow-up round covered 4 more requests on the clients page. Full URL persistence (page/origin/search/all 16 filter fields, not just period) — rather than a second independent `router.replace` racing against `usePeriodQueryState`'s own, the clients page moved off the shared hook onto one combined `useEffect` serializing everything together; new `readClientsFiltersFromSearchParams`/`writeClientsFiltersToSearchParams` helpers in `clients-filter.tsx` share the 16-field mapping between both trees. A traffic-source column (abbreviated "FB"/"TT") was added to all 8 real `<ClientsTable>`/`<StudioClientsTable>` render sites (both dedicated clients pages, both `/my-clients`, both landing-detail pages, both project-overview "recent clients" mini-tables), gated by `user.role !== 'OPERATOR'` — implementing it surfaced a real pre-existing gap: `ClientsService.findMany`'s unrestricted Prisma query already returned `fbclid`/`ttclid` raw to every role including OPERATOR (who's explicitly denied the same data on the single-client detail endpoint via `canViewTrafficSource`), invisible only because no list column had ever read those fields before — fixed by threading the same `canViewTrafficSource` check into the list endpoint and nulling both fields server-side when false, deliberately not also touching the wider (and still-open) buyerId/pixelId/campaign/utm exposure since nothing displays those in list form today. A one-line Studio-only bug was found and fixed — the dialogue-column `<td>` in `dashboard/studio/clients-table.tsx` was missing `whitespace-nowrap` (every sibling date cell already had it; classic's table was unaffected since its shared `TableCell` primitive hardcodes the class), causing rows to grow tall instead of wide when the dialogue text was long. Finally, period URLs now always carry concrete `from`/`to` dates for every preset, not just `custom` — new `computePeriodDates` (browser-local approximation, calendar-aligned to match the prior round's backend fix) makes bookmarked period links stable across days, while the actual API calls still send the `period` enum for the backend's real timezone-correct resolution.

The same day, a design-only follow-up covered 5 more reports on the clients pages, all confirmed live via a real headless-Chromium login+screenshot pass (found a cached Playwright install left from an earlier session, reused it rather than installing anything new). Root cause: Studio's clients page had reused the plain shared `PeriodSelector`/`ClientsFilter`/`Input` components as-is — styled for the classic tree (`border-border`/`bg-blue-600`/`bg-card`/`bg-transparent`), the exact same "bare shadcn component paints wrong in Studio's dark theme" class of bug already fixed repeatedly elsewhere (`LandingContentCard`/`StatsCard`/`PushAudienceStep`). Fixed with a new `StudioClientsPeriodPicker` (`app/dashboard/studio/ui.tsx`, copies the already-correct pill markup from Studio's project page rather than risking a shared-component refactor of an already-working page), `ClientsFilter` gaining the standard `containerClassName` override, and the search `Input` given explicit Studio classes plus a height fix (`h-9` correctly overrides the base `h-8` since `cn()` uses `tailwind-merge`). Origin toggle and period picker moved onto one row with period right-aligned (both trees, pure layout). The period default changed from `'today'` to an explicit "all time" state — rather than adding a 6th nullable state to the existing `PeriodValue` type (which 6 other pages already use non-nullably), built two new `PeriodValue | null`-aware pickers with an explicit "Все" option representing "no date filter at all." Checking "apply this to `/my-clients` too" surfaced that page had never received the *entire* prior round's feature, not just today's polish — no period picker at all, and its query params only forwarded 6 of 16 `ClientsFilterState` fields even though the shared filter UI already rendered all of them (a real silent gap: visible filters that never actually filtered). Brought to full parity in the same pass, including URL persistence extended to cover `projectId` (since this page picks its project from a dropdown, not a route param). Verified end-to-end with an isolated throwaway company/user/project/3 clients created via `AuthService.register` (real password hashing), a real login through the actual `/login` form, dark mode toggled via the real UI control, and screenshots of both `/projects/:id/clients` and `/my-clients` confirming all 5 fixes — zero console errors, test data deleted by exact id afterward.

The same day, a third round covered 3 more unrelated reports. Project creation's "Способ вступления" (tgMode) dropdown defaulted to `BOT_DIRECT`, changed to `PRIVATE_CHANNEL_REQUEST` (`EMPTY_CHANNEL_FORM` in `channel-fields-editor.tsx`, confirmed used only by the two creation pages, never by channel-editing) — and its long option labels were clipped by the default `w-(--anchor-width)` `SelectContent`, the same recurring bug already fixed ~4 times elsewhere in this codebase (`create-landing-dialog.tsx`/`landing-content-card.tsx`/`get-link-dialog.tsx`/`multi-select.tsx`) but never applied to this one Select. A new "Топ источников" (Facebook/TikTok) leaderboard category was added to `ProjectsService.getLeaderboards`/`getLeaderboardFunnel` and both project pages' tab lists — the first leaderboard category grouped by a derived SQL expression (`CASE WHEN fbclid...THEN 'FACEBOOK'...END`) rather than a real column or entity table, which surfaced a genuine, previously-untested gap: `GROUP BY <alias>` referencing that CASE expression threw a real Postgres `42803` error at runtime (invisible to `tsc`), caught by the live verification pass and fixed by repeating the full expression in `GROUP BY` in all 3 query occurrences instead of the alias. `getSourcesFunnel` mirrors `getBuyersFunnel`'s simpler shape (no pageViews/leads — `TrackingEvent` has no `fbclid`/`ttclid` column) and is revenue-gated like pixels/campaigns, not buyer-identity-gated. Finally, the clients-page search input height fix from two rounds prior (`h-9`) was still off by 2px in the user's own re-measurement (36 vs 38) — root cause: `StudioLinkButton` has no fixed height at all, it's whatever `text-sm px-4 py-2`(`+dark:border`) naturally computes to, so pinning the input to a guessed number could never track it exactly; fixed by giving the input the identical box-model recipe (`h-auto` + same padding/border) instead of another guessed pixel value, verified via real `getBoundingClientRect()` measurement (36px = 36px) rather than eyeballing a screenshot, applied to both Studio clients pages including `/my-clients` again per the same "don't forget the operator page" reminder as the prior two rounds.

On 2026-08-14, the OPERATOR role gained access to personal-account (MTProto) broadcasts, project-scoped as with every other Operator-visible resource. Investigation first established that no route-level guard exists anywhere blocking OPERATOR from `/projects/[id]/personal-broadcasts` directly — the two sidebars' hardcoded 2-item `OPERATOR_NAV_ITEMS` is a pure navigation-link restriction, not an access boundary; the real gaps were (1) `DEFAULT_ROLE_PERMISSIONS.OPERATOR` never included any `PERSONAL_BROADCASTS_*` permission, and (2) no entry point existed that kept Operators inside their deliberately-sandboxed few-page UI rather than sending them into the full project page. Added `PERSONAL_BROADCASTS_VIEW/CREATE/DELETE` to the OPERATOR default template (no `SEND` — nothing in the UI has a standalone send button, sending is always via `create()`'s `sendNow`) plus a one-time live backfill (`userPermission.createMany` + `skipDuplicates`) for the 11 already-existing (Operator, Project) pairs, since `seedDefaultsIfEmpty` only seeds a pair with zero existing rows and does not retroactively apply new defaults to already-provisioned operators. Frontend: extracted the project-scoped list and composer into `projectId`-parameterized shared components (`components/personal-broadcasts/broadcast-list.tsx`/`broadcast-composer.tsx` classic, `dashboard/studio/personal-broadcasts-list.tsx`/`-composer.tsx` Studio — same `initialProjectIds`-prop precedent already used by `PushComposer`), reused by both the original project-scoped routes (now thin wrappers) and new company-wide `/my-personal-broadcasts` + `/my-personal-broadcasts/new` pages, which mirror the existing `/my-clients`/`/my-stats` picker+chip-row pattern exactly (including the established `<SelectValue>` children-render-function fix) and reuse the same unfiltered `GET /projects` those pages already call — no new backend endpoint needed since "has ProjectAccess" and "has PERSONAL_BROADCASTS_VIEW" are now the same set for every Operator by design. Added a 3rd `OPERATOR_NAV_ITEMS` sidebar entry in both trees. Live-verified: real backfill against production data, real unmocked `assertAccess` positive+negative control against a real backfilled operator, and a full personal-account health re-check (all 6 real connected accounts) after the verification's `NestFactory.createApplicationContext` boot, per the standing caution established after the earlier personal-session incident. Mid-deploy, a second bare `npm run build` (run only to grep its own warnings, not intending an actual redeploy) silently overwrote the correctly-built `apps/web` output with one missing the sourced prod env, baking `localhost:3001` back into the bundle — caught via the routine pre-restart grep before it reached users, fixed with a third correctly-sourced build.

On 2026-08-18, a follow-up request asked to fix "все попапы и дропдауны чёрно-серого цвета" — a design-consistency sweep the user scoped explicitly beyond the ~13 named locations (landing-creation popup, landing-settings popups, scenario steps/stats pages, the scenario-stats status dropdown, the domain-path popup, team-page popups, the billing popup, the docs table of contents, the client card, the client card on the audience-overlap page) to "and all others too." Rather than continuing the established per-component `containerClassName` override pattern (already used ~6 times this session — `LandingContentCard`/`StatsCard`/`PushAudienceStep`/`ClientsFilter`/etc. — but too labor-intensive to scale to dozens of popups), this round fixed the whole class at once: every shared shadcn/base-ui primitive (`Dialog`, `Select`, `DropdownMenu`, `Drawer`, `Card`, `Accordion`, `Sheet`) already styles itself purely through semantic Tailwind tokens (`bg-popover`/`bg-card`/`bg-muted`/`bg-primary`/etc.), and `globals.css`'s `.dark` block backing those tokens was a neutral achromatic `oklch` grey that had simply never been tuned to Studio's real palette (`#0F1620` page / `#171F2B` card / `#7BA9EE` accent, established in `dashboard/studio/ui.tsx`'s `STUDIO_CARD`/`StudioLinkButton`). Added one new `.studio.dark` CSS-variable override block (`--background`/`--card`/`--popover`/`--primary`/`--secondary`/`--muted`/`--accent`/`--border`/`--input`/`--ring`, all mapped onto the real Studio hex values) that every current and future component on those tokens picks up automatically, with zero further per-file edits needed. The one non-obvious wrinkle: `Dialog`/`Select`/`DropdownMenu`/`Drawer` content all render via `*.Primitive.Portal` straight to `document.body`, outside Studio's own layout subtree — so the scoping class (`studio`) has to live on `<html>` itself, added via a `useEffect` in `dashboard/studio/layout.tsx` (`document.documentElement.classList.add('studio')` on mount, removed on unmount) rather than anywhere in the nested JSX, since `<html>` is shared between both domains/trees and can't be branded server-side by a nested layout. Separately, the one genuinely-broken (not just miscolored) popup named in the request — the landing-creation dialog's project picker (`components/create-landing-dialog.tsx`) — got the same established `w-auto min-w-(--anchor-width)` `SelectContent` fix already applied ~4 times elsewhere in the codebase but missed on this one Select. Live-verified via real headless-Chromium screenshots (an isolated test company/project/client, real login, dark mode forced via `localStorage`) confirming `<html class="studio dark">` correctly cascades through Portal-rendered content: the client-detail drawer, the landing-creation dialog, the billing top-up dialog, the "add domain" dialog, and the team role `DropdownMenu` (including its hover/selected-row highlight) all render Studio's real navy instead of the old grey-black. `tsc --noEmit` and `next build` both clean, no `localhost:3001` leak, test data (company/project/client) deleted by exact id afterward.

On 2026-08-18, two small requests shipped together. First, a content-preview popup for bot pushes ("показывать мини превью рассылки если её открыть... можно смотреть любые — новые, старые, отменённые, в драфте, запланированные") — new shared `PushPreviewDialog` (`components/pushes/push-preview-dialog.tsx`, one component for all 4 lists: project-scoped + company-wide, both trees) reuses the existing `TelegramMessagePreview` component against `GET /projects/:id/pushes/:id` (already returned full `messageText`/`messageMedia`/`buttons`, just had no UI reading it), plus an audience/sent/errors/date summary row and the send-log table below. This replaces the old click-to-open behavior on the two project-scoped pages, which previously opened a logs-only `PushLogsDialog` (now deleted, duplicated per tree) — the new dialog is a strict superset, working identically for a never-sent DRAFT (logs table just shows "Логов пока нет") through SENT/CANCELLED. The two company-wide "Рассылки" list pages (`/pushes-calendar`, both trees) had no row-click at all before this — added the same dialog there too, with `e.stopPropagation()` on the existing project-name link and action buttons so those keep working independently of the new row click. Second, a temporary favicon ("хотя бы букву М, чтобы хоть что-то было") — the site had shipped with Next.js's untouched default `favicon.ico` since Phase 1 with nobody having replaced it. Used the App Router `icon.tsx` metadata-file convention (`next/og`'s `ImageResponse`, a blue `#1F4E9C` rounded square with a white "M", matching the sidebar logo's brand blue) instead of hand-crafting a raw `.ico` (sharp, the only image library available in this environment, can't encode ICO), and deleted the stale `favicon.ico` so the two don't conflict. This surfaced a real, exact repeat of the 2026-07-31 `/docs/*.png` middleware bug: `icon.tsx` compiles to the route `/icon` — no file extension — so `middleware.ts`'s Studio-domain rewrite (which already excludes paths with a dot via regex) didn't catch it, and silently rewrote it to a nonexistent `/dashboard/studio/icon`, 404ing the favicon on the main domain (old.mw-track.com was unaffected, it has no rewrite at all). Fixed by adding an explicit `METADATA_PATHS` exclusion list (`/icon`, plus `/apple-icon`/`/opengraph-image`/`/twitter-image` preemptively, for whenever those get added) alongside the existing extension-regex check — worth remembering that any *future* Next.js special metadata file (these all compile to extension-less routes) will hit this exact gap again unless added to that list. Live-verified: real headless-Chromium login against an isolated test company/project/DRAFT-push (content, buttons, audience/date fields, empty-logs state all correct, including in dark mode on the company-wide list), plus `curl -I` confirming `/icon` returns `200 image/png` on both domains post-fix (was `404` with `x-middleware-rewrite: /dashboard/studio/icon` before). `tsc --noEmit`/`next build` clean, no `localhost:3001` leak, test company deleted by exact id afterward.

The same day, a follow-up request compacted the day-schedule list next to the calendar on the company-wide "Рассылки" page (`/pushes-calendar`, both trees) — user-provided sketch: "проект (время1) (время2) (время3), в ряд, и когда нажимаешь на время открывается превью". `DayScheduleList` (`components/pushes/day-schedule-list.tsx`, shared by both `pushes-calendar` pages via `showProject=true` and the push composer's own schedule calendar via `showProject=false`) previously rendered one full row per push (name + project link + time), which grew tall fast on a day with several pushes in the same project. Redesigned to group items by `projectId` first: one flex-wrap row per project, project name on the left, every push's time as a small clickable pill to its right — the push's name is no longer shown inline at all, since clicking a time pill now opens the same `PushPreviewDialog` from earlier today, which already shows the name/content/stats. For the composer's single-project usage (`showProject=false`) grouping collapses to one implicit group, so it just renders a flat pill row with no project label. Live-verified with an isolated test company (2 projects, 5+2 scheduled pushes on the same day) — confirmed the exact `Project (10:00) (12:00) (14:00)...` layout on both domains, and that clicking a time pill opens the correct push's preview (content, scheduled time, empty logs) without triggering the project-name link's own click handler.

The same day, a bug report on this new preview popup — "первые секунды показывает содержимое старого пуша, потом обновляется" — traced to the global `QueryClient` default (`app/providers.tsx`, `staleTime: 30_000`, tuned for list/table pages) making `PushPreviewDialog` serve a previously-cached push's content instantly on open before a background refetch quietly corrected it, since nothing invalidated that query key on edit and 30s of staleness is invisible on a list but reads as a real bug on a rarely-opened single-entity modal. Fixed by overriding `staleTime: 0, gcTime: 0` locally on both of the dialog's queries (content + logs) so every open is a guaranteed-clean fetch, plus `key={pushId}` at all 5 render sites as a defensive belt-and-suspenders against any prop-only-update path. Live-verified by sampling the DOM at 0–900ms after switching between two pushes with distinct marker text — the wrong push's content never appeared at any sampled instant.

On 2026-08-19, a user-reported pixel-numbers mismatch ("60 логов на подписку, но 87 клиентов в топе пикселей... до фейсбука дошло только 50... у части клиентов баер БЕЗ БАЕРА или чужой, хотя пиксель использовал только один баер") turned out to be two completely separate, both-confirmed-real issues on the same project/landing. First, not a bug at all: the gap between 94 real Subscribe `TrackingEvent`s and only 60 `TrackingEventDelivery` rows was a clean, exact-timestamp-aligned cutoff at `project.updatedAt` (2026-08-18 16:45 UTC) — someone had switched OFF the "Подписка" toggle in this project's Settings → События tab; every Subscribe event before that instant was delivered, every one after was correctly, silently skipped by design (`TrackingService.recordEvent`'s `isEventTypeDisabled` gate) — re-enabling the switch is the fix, not a code change. Second, a real, now-fixed bug: several clients on the same landing had `buyerId: null` or a wrong buyer, and a couple had literal unresolved `{{campaign.id}}`-style Facebook macros — traced to `landing-visit-attribution:<landingId>` (`LandingRendererService.injectTrackingScripts` / `TelegramProvider.getCachedLandingAttribution`, the PRIVATE_CHANNEL_REQUEST-only attribution bridge since that flow has no per-visitor `start:<code>` deep link) being a single Redis key written with plain `SET` and read with `GET` — a "last write wins" design that the code's own 2026-07-24 comment had already flagged as a known, accepted risk ("если на лендинг почти одновременно зайдёт два разных человека, может подставиться не тот — согласовано") for the low-stakes country-guess field, but the SAME mechanism was extended in 2026-07-23 to carry the full buyer/campaign/pixel attribution block too — turning an accepted approximation into real, money-relevant misattribution once a landing gets simultaneous traffic from more than one buyer or ad. Fixed by turning the single key into a Redis LIST (`RPUSH`/`LTRIM`/`EXPIRE` on write, `LPOP` on read) — each visit queues instead of overwriting, each join request consumes the oldest unmatched visit (FIFO) instead of reading one shared value, so two visitors to the same landing no longer contaminate each other's attribution; the one remaining approximation is visit/join *order* within a pair, not full data loss. The fbp-patch-after-render path (`TrackingController.tgRedirect`, needs the browser-set `_fbp` cookie added after the fact) was updated in tandem to patch the list's *tail* (`LINDEX`/`LSET` at -1) instead of GET/SET. Deploying this surfaced a real, self-inflicted transitional risk caught by live testing before it could hit production traffic: leftover pre-deploy Redis keys were still the old STRING type (up to their remaining ~30min TTL), and `RPUSH`/`LPOP`/`LINDEX` against a STRING key throw `WRONGTYPE` — uncaught, that would have crashed the hottest real-traffic paths in the whole tracking system (every ad-click redirect, every Telegram join request) for up to half an hour post-deploy. Fixed with self-healing try/catch-and-delete-then-retry at all three call sites, and the 7 real leftover legacy keys found in production Redis were deleted proactively before re-deploying. Separately confirmed live: the one client attributed to a specific named wrong-seeming buyer was NOT conclusively an instance of this bug (a single Redis write can't merge two different visitors' fields — pixelId and buyerId in that row belonged together to one real click) — more likely that buyer genuinely generated their own tracking link for the same landing using the same shared, project-level (not buyer-exclusive) pixel, which the system has always allowed; flagged to the user as something to verify on their side rather than claimed as fixed. The literal unresolved `{{campaign.id}}`/`{{ad.id}}` macros seen on a couple of clients are a Facebook-side ad-configuration issue (that ad's destination URL isn't wired for FB's dynamic URL parameters), not a CRM bug. Verified live end-to-end against the real deployed code (not a mock): `LandingRendererService.renderAndServe` called twice with distinct fake visitor data confirmed both entries queue in order with correct per-visitor fields, cleaned up by exact list-entry match afterward (never touching real backlog via `LPOP`).

The same day, two direct follow-ups shipped. First, the user asked to actually send the missed Facebook events for the window the "Подписка" toggle was accidentally off — found the exact re-enable timestamp (`project.updatedAt` moved to 2026-08-18T23:25:23Z / 02:25 MSK, matching the user's own report almost to the second) and backfilled the 75 real Subscribe events (project-wide, all with genuine ad attribution) created during the true 16:45–23:25 UTC disabled window by re-enqueuing the exact same `send-to-platforms` BullMQ job real-time events use — not a bespoke sender, so `event_time` stayed the true original moment and dedup/routing behaved identically; all 75 delivered successfully, zero errors. Second, even after the attribution race-condition explanation above, the user asked to slow the landing auto-redirect down to a flat 2 seconds regardless — `apps/sdk/src/browser.ts`'s `fireAutoRedirect()` (previously redirected immediately once the `_fbp` cookie appeared, or polled up to 3s otherwise) was simplified to one unconditional `setTimeout(..., 2000)`, rebuilt and republished to the CDN (the publish script hit a transient MinIO `AccessDenied` on the bucket-exists check on the first attempt — a bare retry succeeded, the bucket already existed).

On 2026-08-19, three bundled requests shipped for the project page's "Топ" leaderboard section. First, each of the 5 categories (buyers/pixels/landings/campaigns/sources) was hard-capped at top-5 with no way to see more — new `GET /projects/:id/leaderboards/:category/full` (`ProjectsService.getLeaderboardFull`, same 5 query shapes as the top-5 endpoint minus `LIMIT 5`, capped at 200 as a safety net not a real limit) backs new `/projects/[id]/leaderboards/[category]/page.tsx` pages in both trees, with the same period selector as the project page itself and a "Сравнить все →" link added to each leaderboard card carrying the current period through. Second, a real bug: some buyers showed a literal "null" after their name everywhere they appeared — three independent backend call sites built `` `${firstName} ${lastName}`.trim() ``, and `.trim()` doesn't remove the literal string "null" that a null `lastName` produces inside a template literal (a common case, not an edge case) — fixed via a new shared `common/user-name.util.ts` `formatUserName()` used in `ProjectsService.getLeaderboards`, `TeamService.getBuyerAnalytics`, and `ClientsService.getClientDetail`. Third, confirmed-correct-but-confusing behavior: the project page's Топ баеров deliberately doesn't filter out deleted buyers (their historical revenue should stay visible), but gave no visual signal they were gone — added an `isDeleted` flag rendering the name in red with a "Удалённый пользователь" tooltip, both on the top-5 card and the new full-list page. Verified live on both domains with a real deleted buyer (via the actual `TeamService.remove()` path) and a real buyer with no `lastName` — no literal "null" text anywhere, correct red+tooltip rendering, full-list pages working with period persistence.

The same day, a follow-up caught a real gap in the new "Сравнить все" pages: they only showed rank/name/clients/revenue, missing the per-item conversion funnel (PageView→Lead→Subscribe→Dialogue→Purchase) that the project page's own top-5 ТОП cards already show. Fixed by raising the existing `/leaderboards/:category/funnel` endpoint's `ids` cap from 10 (tuned for top-5) to 200 (the funnel queries already batch `WHERE id IN (...ids)` per category, not N+1, so this is free), having both full-list pages fetch that same endpoint for every row, and switching both pages from a table layout to the same per-item block layout the ТОП cards use — a multi-stage funnel widget doesn't fit inside one table cell. Verified live: the full-list page's funnel-mini matched the project page's own ТОП section exactly for the same test data.

On 2026-08-20, a follow-up bug report on the leaderboard fixes above ("всё ещё показывается Удалённый пользователь") led to a real, still-active bug: the specific buyerId values in question weren't deleted users at all — they were truncated prefixes of a real, frequently-occurring buyerId, confirmed both on the reported project and company-wide on a second buyer, with timestamps showing the corruption still happening the day before the investigation. Root cause: `buildTrackedLink()` (`apps/web/src/lib/landings.ts`) placed `buyerRef` LAST in the tracking-link query string, after all the Facebook `{{macro}}` placeholders — once Facebook substitutes real (sometimes long, URL-encoded) campaign/ad names into those macros, any downstream URL-length limit truncates the tail, and buyerRef, being last, is the first and most-truncated casualty, by a different amount on every click depending on how long that click's macros happened to be. Fixed by moving `buyerRef`/`pixelId` to the FRONT of the parameter list — fixes new links going forward, but buyers with an already-affected running link need to regenerate it. Separately, the "buyer not found" fallback label was renamed from the misleading "Удалённый пользователь" to "Некорректный ID баера", since that branch can now only be reached by genuinely corrupted data (real deleted users are found and marked red per the prior fix). The same request bundled four more items: landing names in both the ТОП section and the new "Сравнить все" pages now link to the landing's own detail page, with the same preview eye-icon added to the comparison pages; DRAFT landings are now marked red instead of grey everywhere (list dots, badges, detail-page pills), and both landing detail pages gained the same publish/unpublish toggle the list card already had; and new landings now auto-publish immediately on creation (`LandingsService.createFromTemplate`/`createCustom` call the existing `publish()` method right after creating, rather than a bare status flip, so the PRIVATE_CHANNEL_REQUEST invite-link-creation side effect still fires correctly). Verified live end-to-end on an isolated test project plus a direct unmocked call against the real reported project.

On 2026-08-20, a user question about a real-world implausible A/B test group (one landing variant showing 6316 views vs the other's 144) led to a confirmed bug: `LandingsService.computeLandingStats` had zero date filtering, so a landing added to an A/B group with pre-existing traffic carried its full lifetime history into the group comparison — a landing joining a test 10 days after it was created showed 10 days of history alongside the other variant's few hours. The codebase already had a correctly-implemented sibling, `computeAbTestMemberSnapshot` (used only when a test ends), whose own comment named this exact limitation of `computeLandingStats`. Fixed by giving `computeLandingStats` an optional `since?: Date` param (conditional `Prisma.sql`/`Prisma.empty` fragments for raw queries, `{gte: since}` for Prisma queries), applied only in `getStats`'s member loop and `getGroupStats` — passing `group.createdAt` — while a landing's own standalone detail-page stats deliberately keep full lifetime history (no `since`), to avoid regressing that view. The same request also added an A/B test groups list to the company-wide `/landings` page (previously absent entirely) and replaced the project-scoped pages' always-visible groups section with a landings/groups pill-toggle on all 4 pages (classic/Studio × project-scoped/company-wide), with an empty-state "Создать тест" button. Company-wide test creation required extending the shared `AbTestGroupDialog` (`apps/web/src/components/landing-card.tsx`) to accept `projectId: null` and show an internal project `<Select>` — reusing the exact precedent `CreateLandingFromTemplateDialog`'s optional `fixedProjectId` already established. New `GET /ab-test-groups` route (`LandingsService.findAllGroupsForCompany`, scoped via `getAccessibleProjectIds(..., Permission.AB_TESTS_VIEW)`). Live verification (real headless-Chromium screenshots) caught the new project `<Select>` showing the raw project id instead of its name — the same recurring `<SelectValue>`-without-children-render-function Base UI bug documented ~7 times earlier in this file — fixed with the same established pattern before shipping. Verified via a direct unmocked `getGroupStats()` call with deliberately backdated test data confirming pageViews dropped from 23 (pre-fix, counting pre-group history) to the correct 3 (post-group-creation only); toggle/groups-list/create-test flow confirmed via real screenshots. Test data and scratch scripts deleted after.

The same day, a follow-up request pointed out the `since`-based fix above still didn't solve the real problem: "не нужно в ней учитывать статистику каждого лэндинга по отдельности, даже если эти лэндинги проливаются отдельно... группа лэндингов как отдельная сущность со своей статой разделенной" — group stats must count only traffic that came through the group's own split link, not any/all traffic to a member landing (which can also run its own separate ads via a direct link to the same landing). Fixed with an explicit `abTestGroupId` tag stamped only at the true group-routing point (`LandingRendererService.renderByDomain`, when `resolved.abTestGroupId` is set — i.e. the DomainPath binds to the GROUP, not to one landing directly) — threaded through `renderAndServe`/`injectTrackingScripts` into a new `data-ab-test-group-id` SDK script attribute (`apps/sdk/src/browser.ts` sends it on every tracked event, stored in `TrackingEvent.payload.abTestGroupId`, same JSON-path convention as the existing `landingId`) and into a new real column `Client.abTestGroupId` (indexed, no FK — same soft-reference convention as `buyerId`/`pixelId`), propagated through the `start:<code>` Redis bridge and the PRIVATE_CHANNEL_REQUEST `landing-visit-attribution` queue exactly like `landingId` already was. New `LandingsService.computeAbTestGroupMemberStats` replaces the `since`-based approach entirely (for both live stats and `stopAbTestGroup`'s frozen snapshot) — filters strictly on `abTestGroupId = groupId`, no time window needed since the tag itself only ever exists on genuinely group-routed traffic; `computeLandingStats`'s now-unused `since` param was removed. Not retroactive — pre-deploy traffic (including on already-running tests) never got the tag, disclosed explicitly to the user rather than silently shipped. The same request also locked A/B test membership: "после создания группы лэндингов для тестирования, уже нельзя будет их менять, так как статистика будет неверной" — `UpsertAbTestGroupDto` split into `CreateAbTestGroupDto` (name+members, create-only) and `UpdateAbTestGroupDto` (name only), `updateAbTestGroup`/`PATCH /ab-test-groups/:groupId` can no longer touch membership/weights at all (enforced by the global `ValidationPipe`'s `forbidNonWhitelisted`, not just UI convention); the shared `AbTestGroupDialog` now shows a read-only member+weight list with an explanatory note when editing an existing test, versus the full interactive picker when creating a new one. Live verification of this dialog change caught a real pre-existing bug: the member-prefill effect always re-equalized weights (`equalize(initialIds)`) regardless of create-vs-edit, invisible before because editing let you retype the wrong percentages before saving — now permanently mis-displayed once read-only — fixed by sourcing edit-mode weights from the real `Landing.abTestWeight` per member instead. Verified live: unmocked `renderAndServe`+`recordEvent` calls proved 3 group-routed PageViews vs. 5 separately-driven direct PageViews to the same landing correctly report as 3 via `getGroupStats`; real screenshots confirmed the locked read-only dialog (correct 60%/40%, not 50%/50%) and a working rename-only save. Test data and scratch scripts deleted by exact id afterward.

The same day, a user question ("неужели тик ток так же {{...}} использует?") after receiving a real TikTok pixel link back with literal unsubstituted `{{ad.id}}`/`{{campaign.id}}` text led to a confirmed bug — this had previously been assumed elsewhere in this same file (2026-08-19 entry) to be a Facebook-side ad-configuration issue, but that assessment was only ever checked against Facebook-driven examples. `buildTrackedLink()` (`apps/web/src/lib/landings.ts`) unconditionally emitted Facebook's `{{...}}` macro syntax for every generated tracking link regardless of platform — TikTok Ads Manager doesn't recognize that syntax at all and silently passes it through as literal text. Verified TikTok's actual macro format against its own docs before writing any code (WebFetch, cross-checked against an independent practitioner source since TikTok's macro names are notoriously mislabeled — `__AID__` is the ad GROUP id not the ad id, `__CID__` is the creative/ad id not the campaign id): `__CID__`/`__CID_NAME__` (ad), `__AID__`/`__AID_NAME__` (ad group = Facebook's adset), `__CAMPAIGN_ID__`/`__CAMPAIGN_NAME__`, `__PLACEMENT__` — no TikTok equivalent exists for `site_source_name`, so that param is simply omitted from TikTok links rather than filled with a placeholder that resolves to nothing. Fixed by branching `buildTrackedLink()` on `pixel?.platform === 'TIKTOK'`; the `pixel === null` ("all active pixels of the project") case keeps the prior Facebook-macro behavior since the platform is genuinely unknown there, a disclosed pre-existing ambiguity rather than a new regression. Verified live: a real headless-Chromium session generating a link for a TikTok pixel vs. a Facebook pixel on the same landing confirmed the TikTok link now carries `ad_id=__CID__&adset_id=__AID__&campaign_id=__CAMPAIGN_ID__...` while the Facebook link is unchanged. Existing already-running TikTok campaigns still have the old broken `{{...}}` links baked into their destination URLs and need to be regenerated from the CRM and re-pasted into TikTok Ads Manager — this fix only affects newly-generated links going forward.

The same day, a follow-up request asked to re-verify TikTok is genuinely ready for real ad spend ("перепроверь все еще раз, все ли готово к запуску тиктока"). Audited live production data (not just code) on the real account already running TikTok traffic: 15 real event deliveries in the preceding ~10 minutes all confirmed `code:0 "OK"` from TikTok's live Events API, schema re-verified against the 2026-07-29 fix with no regression, and the base `ttq.load(...)` pixel script confirmed actually injected on landings with an active TikTok pixel. Found a second real gap while investigating: `ttclid` had never been captured for this account — 0 of 14,787 real clients, 0 of the most recent real PageViews on the account's TikTok pixel — despite TikTok's claim that ttclid auto-appends to click URLs since April 2024; verified against TikTok's own docs (cross-checked a second source after the first WebFetch mis-rendered the macro's double underscores as markdown bold) that the documented fallback is appending `&ttclid=__CLICKID__` manually, added unconditionally to the TikTok branch of `buildTrackedLink()`. Also surfaced an urgent, user-actionable finding: the account's two real TikTok ads were, at the moment of the audit, actively receiving a burst of real click traffic through the OLD broken `{{...}}` destination URL from before today's macro fix — ad-platform destination URLs don't auto-update from the CRM, so real spend was running with broken ad-level attribution until the user manually regenerates the link and re-pastes it into TikTok Ads Manager. Also confirmed (not fixed, flagged as optional, non-blocking): TikTok's `_ttp` match-quality cookie is set by the base pixel script but never read back by the SDK, the same class of gap `_fbp` had for Facebook before an earlier fix — doesn't stop events from sending, just leaves match quality lower for browser-side events specifically.

The same day, a user question ("неужели тик ток так же...") turned into a broader re-check when the user separately reported real leads still landing without buyer attribution despite the buyer genuinely using a link with `z=`. Confirmed this was NOT a new bug but the exact 2026-08-20 buyerRef-truncation issue still firing for real, currently-running Facebook ads that hadn't been updated to the new link format yet: quantified at 1918 of 10,255 PageViews (≈18.7%) missing buyer attribution over 5 days on one real pixel, spread across 51 distinct ad variants under 2 campaigns — found a sample URL where Facebook's own destination-URL length limit had truncated `campaign_name` itself mid-percent-encoding sequence, dropping `placement`/`site_source_name`/`z` entirely. Since all 51 affected ads share the identical corruption pattern, they very likely share one campaign-level "website URL parameters" template in Facebook Ads Manager rather than 51 independently-typed destination URLs — flagged to the user as the probable single fix point. No code change needed — the existing fix is correct and already verified working for freshly-generated links; this is purely a not-yet-migrated old-link situation, the same class of issue as the TikTok destination-URL finding earlier the same day.

The same day, a direct follow-up question ("разве сейчас ссылка не обрезается из-за чего теряются последние параметры?") led to a genuine correction of scope: the truncation reorder fix repositions WHICH fields get lost when the destination URL is too long, it does not eliminate the underlying length limit itself (that's Facebook's own constraint, outside our control). Investigating a real card the user showed then surfaced a *different*, previously-unseen failure mode on the same buyer's traffic: `ad_id`/`ad_name` (earlier in the URL) and `placement`/`site_source_name` (later in the URL) all substituted correctly in the same click, but `adset_id`/`adset_name`/`campaign_id`/`campaign_name` in between came through as literal unsubstituted `{{...}}` text — and the broken URL was actually *shorter* than working examples of the same ad, ruling out length-based truncation entirely. Concluded this is Facebook's own ad→adset→campaign hierarchy lookup sporadically failing server-side on their end, not fixable by reordering or shortening our link. What was fixable: `decodeAdMacro()` (`apps/api/src/modules/landings/landing-renderer.service.ts`, mirrored in `apps/sdk/src/browser.ts`) previously stored a literal unsubstituted macro string as if it were real data, polluting the client card and campaign/ad leaderboards with fake `{{campaign.id}}`-shaped entries instead of showing absence of data. Added a shared `UNSUBSTITUTED_MACRO_PATTERN` regex (matches both Facebook's `{{...}}` and TikTok's `__...__` syntax) that nulls out any value still matching a raw macro placeholder after decoding, on both the backend and the SDK. Verified live by reproducing the exact real partial-substitution pattern through `LandingRendererService.renderAndServe` and reading back the resulting Redis payload — the genuinely-substituted fields passed through unchanged, the unsubstituted ones correctly became `null` instead of literal garbage text; SDK rebuilt and republished to the CDN.

Immediately after, the user caught a real gap in that fix by pointing back at their own original screenshot: `Источник показа: {{site_source_name` with no closing `}}` at all — a genuinely truncated macro, cut off mid-token by real URL-length truncation, which is a different failure mode from Facebook's complete-but-unsubstituted `{{campaign.id}}` and didn't match the previous round's full-match regex (`^\{\{.*\}\}$`). Fixed by switching `UNSUBSTITUTED_MACRO_PATTERN` from a full match to a substring test (`/\{\{|\}\}|^__[A-Z]/`) — any occurrence of `{{`/`}}` anywhere in the value, or a value starting with a TikTok-style `__UPPERCASE` prefix, now nulls it regardless of whether the macro token is complete or was cut off partway through. Verified live by reproducing the exact reported pattern through `LandingRendererService.renderAndServe` — both the complete and the truncated macro now resolve to `null`. Mirrored in `apps/sdk/src/browser.ts`, rebuilt and republished to the CDN.

The same day, a follow-up request asked to shorten the `z=`/`pixel=` tracking-link VALUES specifically (not the param names) — currently full 25-character cuids — while making OLD buyer links (already baked into running Facebook/TikTok ads) keep working, explicitly repeated as mandatory. Implemented `User.buyerShortCode`/`TrackingPixel.shortCode` (`String? @unique`, 6-character `nanoid()`, 64^6 ≈ 68.7 billion capacity, comfortably covering the requested "millions of buyers"/"hundreds of millions of pixels"). Backward compatibility is purely length-based — real ids are always exactly 25 characters, a 6-character code can never collide with that length, so parsing just checks length to pick direct-id-lookup (old links, zero DB cost) vs short-code-lookup (new links) — no migration of existing links needed. Short codes generate eagerly at creation (`AuthService.register`/`TeamService.create`/`TeamInvitesService.accept` for buyers, `PixelsService.create` for pixels) via a new `common/short-code.util.ts`; the one transactional creation site (`TeamInvitesService.accept`, inside `$transaction`) needed a separate pre-check variant since Postgres aborts the whole transaction on the first error, making a catch-and-retry-on-P2002 pattern unsafe there. Resolution back to real ids happens at exactly two independent entry points that never share code — `LandingRendererService.injectTrackingScripts` (feeds the Telegram-side Redis caches, resolved once so every downstream consumer gets an already-resolved value) and `TrackingService.recordEvent` (the browser SDK's direct POST path, which bypasses `injectTrackingScripts` entirely). Verified live via direct unmocked calls exercising both the Telegram-cache path and the SDK-direct path with both old-format and new-format values — all 4 combinations resolved to the exact same real ids; confirmed the new logic is present in the deployed frontend bundle. Test data and scratch scripts deleted by exact id afterward.

The same day, a user question ("почему они остаются пустыми... данные не приходят для баера Игорь") — comparing a real client with empty campaign/ad/UTM data against another buyer's client with full data, suspecting the just-shipped short-code feature — led to a real fix, but not the one suspected: direct investigation of production data confirmed pixelId/buyerId short-code resolution was working correctly (both resolved right on the affected client). The actual cause was the PRIVATE_CHANNEL_REQUEST `landing-visit-attribution` FIFO queue (see the 2026-08-19 entry) hitting its known approximation under real load — 15 real visits landed on the same landing within ~15 minutes, and at least one had Facebook itself fail to substitute the ad macros (the literal-`{{...}}` failure mode from the entry above, not a bot/length issue — a real human visit can hit it), which a strict `LPOP` had no way to avoid handing to an unrelated real registration even when better-attributed visits sat right next to it in the same queue. Fixed by having `TelegramProvider.getCachedLandingAttribution` `LRANGE` a bounded lookahead window (10 entries) and prefer the first one with a non-null `campaignId`, removing that specific entry via `LREM` while leaving the skipped, poorer entries in the queue for the next join request rather than discarding them — falls back to plain oldest-first if nothing in the window qualifies. Verified live against a synthetic Redis queue driven through the real `TelegramProvider` instance via reflection on the private method: a good entry buried behind 3 bad ones was correctly found and consumed while the bad ones stayed queued, and a second call correctly fell back to strict FIFO once no good candidate remained.

## Before doing anything

Read [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md) first, then the numbered doc matching the area you're touching (01-15, listed in 00's file map). Each numbered doc beyond 00 is currently a TODO stub with section headers only — fill them in as that part of the system gets built, don't let them go stale.

## Hard invariants (do not violate when writing code here)

- Every DB query that touches tenant data must be scoped by `company_id`. This is enforced at the middleware level, not by convention — if you add a new table or query path, it must go through that scoping.
- Channels (Telegram, WhatsApp, Instagram, Viber, Email) implement one `ChannelProvider` interface. Adding a channel = new class implementing that interface, never an if/else branch in business logic.
- Tracking events (PageView, Lead, Purchase) are written to an `events` table first, then pushed to Facebook/TikTok asynchronously via queue. Never call Facebook CAPI / TikTok Events API synchronously from the request path.
- All pushes go through BullMQ queues with rate limiting (30 msg/sec for Telegram). Never send a push directly.
- Every external/idempotent-sensitive request carries an `idempotency_key`.
- No hard deletes — every delete is `deleted_at` soft delete. Exceptions (documented deviations, not oversights): `Channel` has no `deletedAt` column; its "delete" is `isActive: false` (bot/webhook torn down, row kept). `Domain` also has no `deletedAt` column; its "delete" is a genuine hard delete (`domain` hostname is globally `@unique` — soft-deleting would permanently block re-adding the same hostname later). `TeamInvite` also has no `deletedAt` column; it's a transient token/credential (like `RefreshToken`, also hard-deleted on logout), not tenant data — revoking one is a genuine hard delete. All three are excluded from the `PrismaService` tenant-scoping middleware's `modelsWithCompany` list for the same reason — that middleware auto-injects `deletedAt: null` into every read, which would throw `Unknown argument` against any of them.

## Shared choke points — check before changing

These are places where multiple independent consumers rely on one piece of code staying
consistent. If you're editing one, grep for the others before you ship.

- **`modelsWithCompany` array** (`apps/api/src/prisma/prisma.service.ts`) — the tenant-scoping
  middleware only auto-injects `company_id`/`deletedAt` filters for models explicitly listed
  here (`Project`, `Landing`, `Client`, `BotScenario`, `AutomationFlow`, `AbTestGroup`,
  `StoryPost`). A new table needing tenant scoping does NOT get it automatically from having a
  `companyId` column — it must be added to this array, or every read against it is an unscoped
  cross-tenant leak.
- **`checkSubscriptionLimit`** (`apps/api/src/common/guards/subscription.util.ts`) — the single
  shared function deciding whether a company is blocked from sending pushes (plan expired,
  monthly cap, and — once Phase 4.3D ships — manually blocked/suspended by an admin). Consumed
  by both `SubscriptionGuard` (manual HTTP send) and `PushesCron.fireScheduledPushes`
  (automated). Any new "is this company allowed to push" condition belongs inside this one
  function, not as a second, divergent check bolted onto only one of its two callers.
- **`apps/admin`** (standalone Next.js app, separate from the CRM's `apps/web` — planned/being
  built per [15_PHASES.md](15_PHASES.md) §4.3, not yet fully live) is a second, cross-tenant
  consumer of core CRM services: `ProjectsService`, `ClientsService`, `PushesService`,
  `LandingsService`, `DomainsService`, `TeamService`, and the `Company` model itself. It reaches
  them either via a `runAsCompany()` helper (re-enters the tenant-scoping AsyncLocalStorage
  context with an admin-chosen target `companyId`, for the models in `modelsWithCompany`
  above) or directly (for `Domain`/`Push`/`Channel`/`User`, which already take an explicit
  `companyId` param). **When you change the signature, return shape, or scoping behavior of any
  of those services, or add/rename a `Company` field, check `apps/admin` for breakage** — it
  won't show up in `apps/web`'s tests or typecheck since it's a separate package.
- **Hand-maintained frontend enum-label dictionaries** — some Prisma enums have a matching
  `Record<EnumValue, string>` label map in `apps/web` that is NOT generated from the schema
  (e.g. `BalanceTransactionType` → `TRANSACTION_LABEL` in
  `apps/web/src/app/(dashboard)/billing/page.tsx`). Adding a new enum value in
  `prisma/schema.prisma` silently produces an undefined/missing label in the UI unless you grep
  the frontend for that enum's existing dictionary and add the new case by hand.
- **"Is this `UserRole` elevated?" — 4 independent mechanisms, only one is a blacklist.**
  `apps/api/src/common/permissions/permission.constants.ts`'s `isElevatedRole()`
  (`RESTRICTED_ROLES` array) is a blacklist — a new role not added there is silently treated as
  elevated (full bypass of per-project permission checks). `projects.service.ts`'s
  `ELEVATED_ROLES` const + its own inline check in `assertAccess`, `audience.service.ts`, and
  `stories.service.ts` are all whitelists (`[OWNER, ADMIN, SUPER_ADMIN]`) — a new restricted role
  is correct there automatically. When adding a new `UserRole` value, `RESTRICTED_ROLES` is the
  one place you must touch deliberately; there's no compiler safety net for it.
- **`RolesGuard`'s `@Roles(...)` is a rank threshold (`Math.min(...requiredRoles)`), not a
  set-membership check.** Adding a lower-ranked role into an existing decorator's role list
  silently lowers the bar for every other role already at or above that rank, not just the new
  one. When a new role needs to reach a route gated this way but sits at the same rank as an
  existing restricted role (e.g. `BUYER`/`OPERATOR`/`OPERATOR_ADMIN`, all rank 1), don't edit the
  decorator list — remove the guard from that route and add an explicit role check inside the
  service instead (see `assertCanAssignRole`/`assertCanAccessTeamManagement` in
  `apps/api/src/modules/team/team-role.util.ts` for the established pattern).

## Stack quick reference

Backend: NestJS 10 + Prisma 5 + PostgreSQL 16 + Redis/BullMQ. Frontend: Next.js 14 App Router + TailwindCSS/shadcn + Zustand + React Query. Telegram via Grammy.js (webhook only, no polling). WhatsApp via 360dialog. Crypto payments: USDT TRC-20 (TronGrid) / ERC-20 (Infura/Alchemy) via HD wallet, one address per payment. Full list with versions in [00_MASTER_OVERVIEW.md](00_MASTER_OVERVIEW.md).

## Doc maintenance

When a numbered doc (01-15) goes from stub to real content, also update [15_PHASES.md](15_PHASES.md) to reflect what phase actually shipped — it's the source of truth for "what's done vs planned," don't let it drift from reality.

`apps/admin` (Phase 4.3) is architecturally separate from the rest of the app and evolves
across many phases — 15_PHASES.md §4.3 is its authoritative status/plan doc; keep it current
phase-by-phase rather than writing it up only at the end.
