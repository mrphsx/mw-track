-- Project:Channel переходит с 1:N на строгий 1:1 — проект сам становится каналом,
-- выбирается один раз при создании (см. ProjectsService.create()), не добавляется
-- отдельным шагом позже. На проде уже 1:1 фактически (проверено вручную перед
-- применением), поэтому бэкофилла данных не требуется — только constraint.
CREATE UNIQUE INDEX "Channel_projectId_key" ON "Channel"("projectId");
