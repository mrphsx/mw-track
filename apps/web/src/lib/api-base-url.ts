// Вынесено из api.ts отдельным файлом БЕЗ единого другого импорта (запрос пользователя
// 2026-07-30: "всё ещё просит логин при переходе на old" — расследование упёрлось в реальный
// циклический импорт auth.store.ts <-> lib/api.ts: store импортировал API_BASE_URL/api из
// api.ts, а api.ts импортирует useAuthStore обратно из store — тот же класс бага, что уже не
// раз ловили в NestJS-модулях этого проекта (см. память про circular-DI/ModuleRef), только на
// фронтенде. Раньше это было безопасно, поскольку код стора, реально трогающий эти импорты
// (refreshAccessToken), вызывался лениво в ответ на 401 — задолго после того, как оба модуля уже
// точно доинициализировались. Новый bootstrapFromSharedCookie (кросс-доменный SSO-мостик)
// выполняется СИНХРОННО прямо при гидратации стора, на самом раннем этапе загрузки страницы —
// именно тогда цикл и стрельнул реальным TypeError ("Cannot access 's' before initialization",
// классический symptom temporal-dead-zone у циклических ES-модулей). Фикс — не чинить порядок
// импортов, а устранить сам цикл: auth.store.ts берёт API_BASE_URL отсюда, а не из api.ts.
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api/v1';
